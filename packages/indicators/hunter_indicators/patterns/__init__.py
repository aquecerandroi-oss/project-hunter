"""Classical chart geometry: pivots, trend lines, channels and their events.

T3.34. This package is the **primitive** a strategy and the chart draw with; it
does not decide anything and — deliberately, until T3.34b — nothing under
``hunter_core.strategies`` imports it, so no live ``code_ref`` digest moves
because this landed.
"""

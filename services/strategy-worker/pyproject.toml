[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hunter-strategy-worker"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "hunter-core",
  "hunter-indicators",
  "hunter-risk",
]

[tool.hatch.build.targets.wheel]
packages = ["hunter_strategy_worker"]

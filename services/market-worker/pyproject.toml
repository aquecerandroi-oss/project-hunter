[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hunter-market-worker"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "hunter-core",
  "hunter-exchanges",
]

[tool.hatch.build.targets.wheel]
packages = ["hunter_market_worker"]

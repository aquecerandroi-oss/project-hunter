# env e segredos
.env
.env.*
!.env.example
*.pem
*.key

# node
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
coverage/
playwright-report/
test-results/
tests/e2e/artifacts/

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
dist/
build/
*.egg-info/
htmlcov/

# docker / dados locais de dev (nunca estado de produção)
infra/docker/.data/

# claude code — configurações pessoais ficam fora do repo; settings.json, hooks, agents, memory e state são versionados
.claude/settings.local.json

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# intermediate OpenAPI dump (only the generated .d.ts is committed)
packages/shared-types/openapi.json

# Obsidian per-user UI state (the vault itself and app.json are versioned)
vault/.obsidian/workspace*.json
vault/.obsidian/cache

# Obsidian app workspace state (per machine)
.obsidian/

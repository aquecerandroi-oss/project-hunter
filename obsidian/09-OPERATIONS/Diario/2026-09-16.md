---
tags: [diario, operacoes, meme, dinheiro-real, estagio-1, incidente]
date: 2026-09-16
owner: sexta-feira
status: em-curso
---

# Diário — 2026-09-16 (o dia em que o estágio 1 real foi ligado)

> Horários em BRT. Fatos medidos na VPS; o que não foi medido está marcado.

## Linha do tempo

| hora | fato |
|---|---|
| 08:2x–10:5x | Everton monta a carteira do robô: `.env` (RPC Helius, tetos, `MEME_LIVE_AUTO_APPROVE=true`), portões (`meme_gates.json`, validade 18/09). Primeira chave foi o **endereço público** (44 chars) — detectado sem exibir; segunda carteira (`G1Jnek…`) descartada porque a chave apareceu parcialmente num print; terceira gerada na VPS com a chave escrita direto no `.env` (`ARsuJEagSE2pLgjMfDvgNo1TdMRS2DDRYLmgu4fX6Dr4`). |
| 11:14 | 0,717 SOL depositados do Terminal (`6nAh…`, que ficou com 0,0098). 11:35: mais um depósito — **21,33 USDC**, não SOL; inerte (o executor não lê contas de token; `wallet_unrecognized_holdings` nunca é preenchido). |
| 11:2x | Deploy `9f828dd` com `MEME_LIVE=1` **pelo Everton** (o classificador bloqueia a Sexta-feira em qualquer deploy com o perfil live e em gerar/escrever chave). Boot aceito, kill switch ACTIVE, `auto_approve true`. Teto efetivo 0,25 → ele subiu o escopo para 0,72 e reiniciou o executor (portões só liam no boot). |
| 11:46–11:48 | **Primeiras 4 aprovações automáticas, 4 recusas `progress_below_window` com progresso −541 546** (INCEPT, `FHcHh1…`). Bug: denominador em tokens × curva em subunidades. E o cap de 5/h contava recusas (4/5 gastos sem comprar). Everton: "não quero rate limit". |
| 12:0x | T4.28d (agente): portões recarregados por mtime, inválido → latch. Commit `a47943c`. |
| 12:1x | T4.28e: `denominator_subunits()` + cap conta só admitidas. Commit `e42550a`. 12:2x deploy pelo Everton. Registro honesto: as propostas de 11:47 estavam a **60 %** da curva — seriam recusadas `progress_above_window` mesmo sem o bug. |
| 12:2x | `/meme/mesa` cai ("computeAgeMs is on the client"): o painel Executor real (Server Component) chamava função de módulo `"use client"`; latente desde T4.17, só aparecia com o executor `ligado`. Fix `6fab088` (`lib/age.ts`), deploy 12:49 pelo Everton, 0 quedas aos 10 min. |
| 12:52–12:53 | `Gn9U13…` proposta 4× pela mesa; 4 aprovações automáticas, 4 recusas **corretas** `progress_above_window` (81 %, 96 %); 0 vagas gastas (contador novo confirmado). |
| 13:0x | T4.28f (agente): cooldown de 120 s para mint recusado por motivo fixo (sem `progress_below_window`, que muda em 20 s) + graça de um tique na recarga dos portões. Commit `056b286`, **não deployado**. |
| 13:00 → | **Zero propostas de qualquer conjunto** apesar de ~700 moedas novas/25 min. Causa: a API `swap-api.pump.fun/v1/coins/market-activity/batch` passou a devolver `null` em todas as janelas, para qualquer moeda, também do PC da Sexta-feira (não é bloqueio: cota 966/1000, HTTP 201). Fita escura → portas com fluxo recusam `no_trade_feed`. Sensor ligado para avisar quando voltar. |

## Números do dia (até 13:5x)
- Compras reais: **0**. SOL gasto: **0**. Recusas da admissão: 9 (5 pelo bug, 4 corretas). Escopo restante 0,72 SOL / 5 compras.
- Propostas da mesa nas 24 h anteriores por progresso da curva: 2–50 % → 17 (15 moedas); 50–85 % → 45 (38); > 85 % → 6 (5). **3 em 4 acima do teto do executor (50 %).**

## Decisão aberta (do Everton)
**A** (recomendada): manter o teto de 50 % e alinhar `operator/5` (`max_progress_pct` → 50) por `meme_rule_set.py --set-param … --rule-set operator/5 --apply` — a venda depois da migração não existe (posição presa). **B**: subir o teto do executor (código + deploy dele).

## Lições
1. Grandeza que cruza radar → executor precisa ter a unidade no nome do campo; testar a admissão com números crus da cadeia.
2. No primeiro deploy de um caminho real, acompanhar a primeira candidata até o fill e ler `admission.checks` (valor/limite), não só o nome da recusa.
3. Um Server Component nunca importa de módulo `"use client"` — o erro só aparece no estado que renderiza a chamada.
4. Fonte externa única (fita por lote) = ponto único de falha do funil inteiro; a fita por cadeia (T4.2) volta à fila como redundância.

## Ligações
[[06-DECISIONS/2026-09-12-teste-pequeno-meme-real]] · [[11-KNOWLEDGE/KB-0098-quantos-bums-reais-ha-por-dia-e-quanto-tempo-temos|KB-0098]] · `.claude/state/notes-T4.28{d,e,f}.md`

## Adendo 14:0x — a API de atividade voltou
13:53 moedas antigas com números; 13:59 a primeira moeda fresca (1–4 min) com `1m`/`5m`; 14:00 `activity_covered 131`, `activity_dark_60s 0`, cobertura 100 % (fita 93 %). Pane de ~1 h; durante a volta o indexador deles ficou 20–40 min atrasado. Nenhuma proposta nos 60 min de pane; funil retomado sozinho, sem reinício.

## Adendo 15:0x — decisão A aplicada; estágio 2 em paralelo
- 15:0x Everton: "confirma o A" + "já vai fazendo o estágio 2 agora mesmo em paralelo" + "pode colocar mais agentes". Quatro agentes despachados: T4.29a (venda na PumpSwap), T4.29b (estudo do tamanho de US$ 1 000 sob os tetos, com a Astra), T4.29c (`FeeConfig` + simulação de venda em curva HR), T4.28c (painel do modo sozinho).
- 15:0x o `--set-param … --rule-set operator/5 --apply` falhou na VPS (`IndeterminateDatatypeError`: chave do `jsonb_build_object` sem tipo para o asyncpg; os testes do script rodam em fakes e nunca executaram o SQL). Fix `d95fb4c` + teste que fixa o `CAST(:key AS text)`; deploy `5fc3935` pelo Everton (leva também o T4.28f).
- **15:08 aplicado e auditado: `operator/5.max_progress_pct` 100 → 50.** A mesa passa a propor só até 50 % da curva.

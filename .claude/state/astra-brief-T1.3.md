# Task T1.3 — `hunter_market_worker` (PROJECT HUNTER, Milestone 1) — RESUME

You are Astra, implementer on an existing monorepo. A previous implementer was interrupted; its partial work is UNCOMMITTED under `services/market-worker/` (`hunter_market_worker/{__main__,config,heartbeat,hot_state,ingest,main,persist,recovery,universe}.py`, `tests/{builders,conftest,db_helpers,fakes}.py`) plus edits in `packages/core/hunter_core/settings.py`, `.env.example`, `services/market-worker/pyproject.toml`, `uv.lock`. Finish it. Do NOT commit. Do NOT read `.env`. Do NOT touch anything outside the repository.

## Read first (binding)
`CLAUDE.md`; `docs/plans/M1.md` — row T1.3, "Decisões deste plano", and the section **"Decisão conjunta Claude ⇄ Astra"** at the end (you co-authored it in `.claude/state/dialogue-M1.md` rounds 1–4: recovery, liquidation identity, supervision, per-component staleness — these are the acceptance criteria); `docs/PIPELINE.md` §1 and §10; `packages/core/hunter_core/{runtime,redis,settings,logging}.py`; `packages/core/hunter_core/events/{envelope,produce,streams,consume}.py`; `packages/core/hunter_core/db/session.py`; `packages/core/hunter_core/db/models/{markets,market_data,system}.py`; `packages/core/hunter_core/domain/market.py`; `packages/exchange-adapters/hunter_exchanges/base.py`. Then inventory the partial work (`git status`, read every file) and run `uv run pytest services/market-worker -q -p no:cacheprovider` to see where it stands (Docker Desktop is running; testcontainers work — see `packages/core/tests/integration/conftest.py`).

## Files you may create/modify
- `services/market-worker/**` (package + tests; `tests/fakes.py` is your own fake adapter — do NOT import from `hunter_exchanges.testing`, another task is editing it concurrently; do NOT touch `packages/exchange-adapters/**`).
- `packages/core/hunter_core/settings.py` (only the market fields), `.env.example` (document them), `packages/core/hunter_core/runtime.py` (only: add `readiness_checks: list[Callable[[], Awaitable[bool]]]` on `WorkerRuntime`, evaluated in `/ready` with a 2 s timeout each, exception = false; keep everything else and existing tests green), `packages/core/tests/unit/test_runtime.py` (add tests for the new hook).
Every file ≤ 350 lines (`uv run python infra/scripts/check_file_size.py`).

## Contracts (from the joint decision — implement exactly)
1. **Universe** (`universe.py`): every `market_universe_refresh_s` (900): `list_markets(PERPETUAL)` + `fetch_tickers_24h()`; upsert `assets`/`markets` (tick/step/min_notional/contract_size/max_leverage/status/`volume_24h_usd`/`last_seen_at`; delisted → status + `delisted_at`); rank by `volume_24h_usd` → `monitor_rank`; `is_monitored` = top `market_universe_size` after allowlist (always in) / blocklist (never in); publish `market.universe.changed` only on change; **apply diffs only** (entries/exits change subscriptions; symbols that stay are not resubscribed); status ≠ TRADING or blocklist removes immediately; empty universe → explicit `idle` state (no watchdog, no fatal).
2. **Hot state** (`hot_state.py`), exact Redis contracts (the API task reads them): `mkt:{ex}:{sym}:ticker` HASH `last,bid,ask,bid_qty,ask_qty,volume_24h,quote_volume_24h,high_24h,low_24h,change_24h_pct,ts` TTL 30 s; `mkt:{ex}:{sym}:book` STRING msgpack `{"ts","bids":[[p,q]...],"asks":[...],"depth":20,"kind":"snapshot"}` TTL 10 s (each snapshot replaces the previous); `mkt:{ex}:{sym}:trades` LIST (LPUSH+LTRIM 2000) msgpack `{"ts","price","qty","side","trade_id"}`; `mkt:{ex}:{sym}:candles:1m` LIST (LTRIM 1500) of wire-format candles — **single writer** (the ingest task; REST backfill never writes Redis); same `open_time`: newer partial (by event `ts`) updates partial, final replaces partial, partial never replaces final, older `ts` discarded; larger `open_time` advances the head; a late final for an earlier open_time finalizes that entry without touching the head; `mkt:{ex}:{sym}:deriv` HASH `open_interest,open_interest_value,oi_ts,funding_rate,funding_kind(estimated|realized),next_funding_time,funding_ts,mark_price,index_price,mark_ts` — **each writer updates only its own fields and its own `*_ts`**, key TTL fixed 600 s (TTL is not a freshness signal). Decimals as str, timestamps ISO 8601 UTC. `ts` written is always the exchange event `ts` of the last **accepted** event (never the flush time); duplicates/late events do not refresh anything.
3. **Ingest** (`ingest.py`): `adapter.stream(monitored, [TRADES, BOOK_TICKER, BOOK, KLINE_1M, MARK_PRICE, LIQUIDATIONS])`; coalesce per symbol every `tick_coalesce_ms` (250) → `market.ticks` (payload exchange, symbol, price, bid, ask, volume_delta, trades_count, book_imbalance_5, ts) to the stream and `PUBLISH rt:market:{ex}:{sym}`; closed 1m candle → `market.candles.closed`; mark/funding/OI → `market.derivatives`; liquidations → `market.liquidations` with `EventEnvelope.event_id = uuid5(NAMESPACE, canonical(exchange, symbol, side, price, qty, ts_ms))` (same uuid as the DB row id). Every stream write via `EventEnvelope` + `publish` + `DEFAULT_MAXLEN`. XADD failure with the process alive → `market_publish_failures_total` (add to `hunter_core/observability.py` if missing — allowed as an additive change) + `system_events` warning.
4. **Persist** (`persist.py`): bounded queues (items, bytes, age) with explicit drop policy per type (snapshot pending may be replaced by the newest; dropped final candle opens a recoverable gap; irrecoverable loss → metric + `system_events`); flush every 1 s or 500 rows or 1 MiB; `INSERT ... ON CONFLICT DO NOTHING` on natural keys: candles `(market_id, timeframe, open_time)` only `is_final`; `market_snapshots (market_id, ts)` with `ts` = UTC minute bucket (stable across retries); `open_interest_history (market_id, ts)` 5-min bucket; `funding_rates (market_id, funding_time)` realized only; `liquidations` `ON CONFLICT (id, ts) DO NOTHING` with the deterministic uuid5 id and source `ts`. Postgres slow → warning if lag > 10 s; `persistence` readiness false when queue pending and no flush for 30 s.
5. **Recovery** (`recovery.py`): watermark = last final `open_time` in Postgres; bootstrap without watermark = last 1500 closed candles via REST with `close_time <= server_time()` (adapter method; fall back to `utcnow()` if the adapter lacks it, logged); gap detection every minute compares expected open_times in the last 24 h against persisted finals (internal holes count) → `ingestion_gaps` open; recovery processes registered gaps; candles insert + `open → recovered` in the **same transaction** after counting all expected open_times; `attempts` +1 per try; `failed` after 5 (stays visible).
6. **Supervision** (`main.py`): every long-lived task (ingest, coalescer, per-connection readers if any, persist, universe, recovery, heartbeat) runs inside one `asyncio.TaskGroup` through `forever(name, coro)`: normal return → `RuntimeError(f"task {name} exited unexpectedly")`; `CancelledError` on shutdown is normal; the exception propagates so `WorkerRuntime.run` exits non-zero. Per-connection watchdog (use `adapter.connection_states()` if available, else the single `connection_state()`): 30 s without a data event on a connection with active subscriptions → warning + restart that connection (restart the stream if the adapter has no per-connection restart), 3 consecutive restarts without progress → fatal. Readiness checks registered on the runtime: `ingestion` (initializing → false; connected and data event < 60 s → true; connecting/reconnecting tolerated up to 120 s monotonic accumulated since the last healthy connection, 15 s connect timeout → then false) and `persistence` (above).
7. **Heartbeat** (`heartbeat.py`): runtime heartbeat plus `hb:market:{exchange}` HASH every 5 s: `last_event_at, ws_state, subscriptions, reconnects, markets_monitored, open_gaps, ts` TTL 30 s; `PUBLISH rt:system` JSON `{type:"market_status", exchange, ws_state, last_event_at, markets_monitored, open_gaps, ts}` every 5 s or on state change; `system_events` rows on reconnect/errors; `runtime.mark_success()/mark_error()`.
8. **Entry**: `RoleRegistry["market"] = run_market`; `__main__.py` builds `Settings`, `WorkerRuntime(role="market")`, `runtime.run(run_market)`; `config.py` `build_adapter(code, settings, redis)` imports `hunter_exchanges.binance` lazily (log + exit non-zero if missing). No `print`; structlog only.

## Tests (`services/market-worker/tests/`, markers `unit` / `integration` with testcontainers Postgres+Redis, using your own fake adapter)
Universe ranking/allow/blocklist/delisting/diff-only subscriptions/idle; hot-state exact fields and TTLs incl. `*_ts` independence (OI update leaves `mark_ts` untouched); candle list rules (two growing partials; late partial; final after next open; late final of earlier open); tick coalescing (10 trades in 100 ms → 1 event); persistence idempotency (same candle twice → one row; snapshot bucket stable); bounded queue drop policy; gap detection with an internal hole before the max; recovery same-transaction transition; failed after 5; supervision (task normal return → RuntimeError; child exception propagates; shutdown cancel is not fatal); watchdog per connection (silence one connection only); readiness (initializing false; connecting tolerated then false after 120 s using an injected monotonic clock; persistence stuck false); liquidation uuid5 determinism and `event_id` equality; `RoleRegistry["market"]` registered.

## Astra self-check
You are the implementer here, so the second opinion comes from the Claude reviewer later; still, before finishing, re-read the "Decisão conjunta" section and tick every item you satisfied in your report.

## Verification (run, fix, paste real output)
```
uv run pytest services/market-worker -q -p no:cacheprovider
uv run pytest packages/core/tests/unit -q -p no:cacheprovider
uv run ruff check services/market-worker packages/core && uv run ruff format --check services/market-worker packages/core
uv run pyright services/market-worker packages/core/hunter_core/runtime.py
uv run python infra/scripts/check_file_size.py
```
Report: STATUS (DONE/BLOCKED), files created/modified, the Redis contracts implemented (verbatim), the acceptance checklist with ✔/✘, real output of every command, deviations.

# Brief A4.3d (para a Astra, modo run) — especificação de tela do "Meme Radar" (design, sem código)

**Contexto:** Everton (12/09/2026) quer operar meme coins no pump.fun; hoje o produto vai MOSTRAR o mercado ao vivo (T4.3 está construindo a API e a tela em `apps/**` — **não toque em `apps/**`**). Contrato de dados em `.claude/state/notes-T4.2.md` §contrato (se já existir) e `docs/plans/T4-MEME-RADAR.md`; contrato de design do produto: `docs/DESIGN.md` (leia inteiro; tokens, estados, cópia em português, dark/light, 375/768/1440); como referência do que o mercado usa: o screener "Examinador de Mayhem" do pump.fun e os terminais (Photon/BullX/GMGN/Axiom — o seu A4.0f, `docs/plans/T4-FERRAMENTAS.md`, se já existir).

**Entregar `docs/plans/T4-MEME-RADAR-UI.md`** (português): (1) a hierarquia da tela `/meme`: faixa de visão geral (criações 24 h/7 d por modo, Mayhem ativos, graduações 24 h, cada número com `observed_at` e fonte), a tabela de moedas rastreadas (colunas, ordenação, densidade, o que é vermelho/âmbar/verde e por quê — semântica, não decoração), os estados honestos ("sem dado: motivo" em vez de zero; atraso da fonte visível; universo "N de M rastreadas"); (2) a tela de detalhe `/meme/{mint}`: série da curva (reservas → mcap SOL e progresso %), marcas de eventos (criação, migração, pausas do agente Mayhem), features por minuto com nulos justificados, e a área futura de "aposta paper" (desligada, com o motivo escrito); (3) a linguagem de copy (nomes em português para estado da moeda, para o agente Mayhem, para bundler/dev-vendeu quando existirem); (4) mobile 375 primeiro (o pump.fun diz "Trade faster. Pump is better on mobile" — o que isso implica para o radar); (5) os anti-padrões a evitar (gráficos verdes por padrão, "lucro" sem executado, ranking por hype); (6) uma lista de verificação de aceite visual que o orquestrador usa antes do commit. Mockups em ASCII/markdown são suficientes; se fizer wireframes em HTML, salve só em `.claude/state/design/2026-09-12/`.

**Regras:** não tocar código, `.env*`, `apps/**`; não commitar; nunca inventar dado; comandos em primeiro plano com `timeout 290`. Relatório ≤ 8 linhas em português.

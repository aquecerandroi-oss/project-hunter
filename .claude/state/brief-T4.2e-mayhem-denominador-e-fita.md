# Brief T4.2e — os dois motivos que ainda calam o laço: o denominador das curvas Mayhem e a cobertura da fita (12/09/2026 08:40 BRT)

**Estado medido em produção (VPS `eeb566c`, 08:35 BRT, `hb:meme:radar`):** o laço avalia 250 linhas por tick e recusa `progress_unknown` em **113** e `creator_net_seller_unknown`/`curve_volume_1m_unknown` em **109**; `meme_tokens.progress_denominator_source`: `observed_virgin` 140, `global_params` 57, **NULL 2 615**; features `meme_features_v2` nos últimos 3 min: 393 linhas, 58 com progresso, 108 com holders, 77 com fita. Sem esses dois, a EXP-M1 nunca propõe e a mesa só recebe compra manual.

**Leia antes:** `.claude/state/notes-T4.2d.md` (§concern Mayhem: a fixture `2sduGq…` tem 822,6 M tokens reais na curva > 793,1 M do `/global-params`; `Global` não traz o parâmetro Mayhem; a T4.2d deixou Mayhem em `unknown` de propósito), `docs/PUMPFUN-ONCHAIN.md` (programa Mayhem `MAyhSmzXzV1pTf7LsNkrNwkWKTo4ougAJ1PPg47MD4e`, conta `mayhem_state`, o 1 B extra), `.claude/state/notes-A4.1b-mayhem.md`, `packages/exchange-adapters/hunter_exchanges/pumpfun/{decode,rpc,global_state}.py` (T4.0d/T4.8: decodificação pela IDL, RPC público), `services/meme-worker/hunter_meme_worker/{graduation,curve_rows,trades,features_tape,tracker,config}.py` e `.claude/state/notes-T4.2c.md` (política da fita: prioridade e orçamento 900/60 s; hoje ~170 req/min usadas).

**Entregar:**
1. **Denominador Mayhem, sem constante mágica:** decodificar a conta da curva (`bonding_curve`) e/ou a `mayhem_state` pela IDL para obter a reserva real inicial de tokens **daquela** curva (ou derivá-la de `total_supply − virtual_token_initial + …` só se a IDL comprovar a fórmula — documentar a derivação com a fixture real `2sduGq…` e mais 2 capturas ao vivo, ≤ 10 chamadas RPC públicas). `progress_denominator_source` ganha `mayhem_state` (ou `curve_account`) e a T4.2d passa a preencher Mayhem; features novas sob a mesma `meme_features_v2` (o denominador é dado do token, não fórmula nova — justificar; se a fórmula da fração mudar, `v3`). Teste: a fixture Mayhem reproduz o `progress` que o site mostra (`/coins/{mint}.progress` ou o board) com erro ≤ 0,5 pp.
2. **Cobertura da fita:** medir por que 109/250 linhas ficam sem fita com 730 req/min de folga; ajustar a política para puxar a fita de **todos** os mints rastreados a cada minuto (125 ≤ 900), mantendo a prioridade para apostas abertas e `graduating`, e registrando `tape_reason` por mint quando não couber; heartbeat `tape_coverage_pct` (linhas com fita ÷ linhas do tick) e `progress_coverage_pct`; `GET /meme/sources` expõe os dois.
3. **Prova em produção (só leitura):** depois do deploy pelo orquestrador, o agente NÃO acessa a VPS; entregar a query SQL (`infra/scripts/sql/research/2026-09-12-t42e-cobertura.sql`) que o orquestrador roda para medir `progress_unknown`/`tape unknown` antes × depois.
4. Testes unit + 1 testcontainer (persistência do denominador Mayhem e da cobertura da fita), `ruff`/`pyright`/`check_file_size.py`; docs `docs/PUMPFUN-ONCHAIN.md` (§Mayhem: reserva inicial), `docs/plans/T4-MEME-RADAR.md` §T4.2e, `docs/DEPLOYMENT.md` se houver env nova; `.claude/state/notes-T4.2e.md` com comandos e saída real; ao final `git status --porcelain` de tudo que tocou.

**Regras:** tudo em primeiro plano com `timeout 290` (590 para testcontainer, um por vez); nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; nunca `.env*`; nunca chave; nada real; não tocar `lab*.py`/`paper_engine.py`/`proposals.py` além de ler, nem `apps/web/**`; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília (UTC − 3).

# Brief T3.77 — H-P8/H-P10 "amplitude do universo como estado": a third eligibility rule `breadth` (from D-P9/KB-0083, 2026-09-10)

**Facts:** KB-0083 / `.claude/state/notes-D-P9.md`: at 22:08Z on 09/09, 194 of 200 monitored perpetuals fell in the same minute (mean −3,71 %) while BTC moved −0,20 % — the family's worst hour. Hypothesis H-P8: the fraction of perpetuals falling in the minutes before the decision is a state that separates expectancy (a cell, never a filter — until it is tested). The eligibility policy (T3.52 `regime`, T3.59 `hours`; `services/strategy-worker/hunter_strategy_worker/{gate_policy.py, regime_gate.py, hours_gate.py, variant.py, context.py, catalogue.py}`, `infra/scripts/derive_variant.py --policy`) is the only door for such a state.

## Deliver (code only today; the replay test is tomorrow's one-day batch)
1. **Series**: `breadth_5m` = share of monitored perpetual markets whose 1-min close over the last 5 completed minutes (`[source_bar_close − 5 min, source_bar_close]`) is below the close 5 minutes earlier; computed from `candles_1m` of the universe at `source_bar_close` only (non-anticipation: no candle with `open_time + 1 min > source_bar_close`), with the count of markets that had all 6 closes (coverage) — refuse when coverage < 80 % of the monitored universe (`breadth_unavailable`). Decide where it is computed: (a) a scanner-worker hourly/minutely job persisting `market_breadth` rows (immutable, `end_time`-anchored, like `market_regimes`) — preferred if the strategy-worker must not scan 200 markets per decision; (b) on demand inside the gate with a per-bar cache (`context_cache.py`, T3.74b). Choose (a) unless it needs a migration you cannot finish today; if a migration is needed, write it (database-architect conventions: RLS-free reference table like `market_regimes`, partition if rows > 1 M/yr) and say so.
2. **Rule** `breadth`: `{"breadth": {"window_m": 5, "min": "0.10", "max": "0.60"}}` (half-open on `max`), refusal `breadth_gate:<value>` / `breadth_unavailable`; combinable with `regime`/`hours` (all must pass; envelope records the value); replay applies the same rule from the persisted series (never recomputed with hindsight). `derive_variant.py --policy breadth=0.10-0.60`; dropping a parent rule refused unless `breadth=none`.
3. **Backfill**: the same job over the 90-day window for the 16 backfilled markets is NOT enough (breadth is universe-wide) — measure how many monitored perpetuals have 1-min candles per day over the last 90 d (read-only on the VPS) and write in the notes what coverage a 90-day breadth backfill would have; implement the backfill CLI (audited, `ops`, dry-run default) but do not run it on the VPS.
4. Tests: unit (series math on a synthetic universe, coverage refusal, boundary at min/max, non-anticipation with a candle one minute too late), one testcontainer file on the worker path (a version gated 0.10–0.60 skips a bar where breadth is 0,97 with `breadth_gate:0.97`), `docs/ACTIVATION.md` §7c one paragraph, `docs/PIPELINE.md` §4b one item, pre-registration draft `.claude/state/exp-drafts/EXP-0027-amplitude.md` (tercile cuts fixed on a prior window, cells not filters, success rule as in T3.59/T3.76). `.claude/state/notes-T3.77.md` with commands/outputs and the full `git status --porcelain` list of your files.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never edit the seven closure modules; no VPS writes; do not edit `apps/**`; keep files ≤ 350 lines; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 12 lines, times in Brasília (UTC−3).

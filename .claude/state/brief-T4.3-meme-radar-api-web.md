You are frontend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T4.3-meme-radar-api-web.md`. Everton (2026-09-12): meme coins are the target; "vamos operar ainda hoje" — today the product must at least SHOW the market live. Task T4.3 — the "Meme Radar" API + screen. Contract source: `.claude/state/notes-T4.2.md` §"contrato" (being written NOW by the T4.2 agent: tables `meme_tokens`, `meme_curve_snapshots`, `meme_features_1m`, `meme_ingest_gaps`; wait/poll for that section — re-read the file every few minutes for up to 30 min; if it does not appear, build against the plan `docs/plans/T4-MEME-RADAR.md` §modelo de dados and the T4.1 models in `packages/exchange-adapters/hunter_exchanges/pumpfun/models.py`, and say so). Deliver: (1) API `apps/api/hunter_api/routers/meme.py` (+ schemas/services/repositories `meme*.py`): `GET /api/v1/orgs/{org_id}/meme/overview` (coins created 24 h/7 d by mode from the stored overview snapshots or, until they exist, a pass-through of the free `frontend-api-v3 /mayhem/overview` with `source`+`observed_at` — never a fabricated number), `GET …/meme/tokens?state=curve|completed|migrated&sort=mcap|age|progress&limit=` (from `meme_tokens` + latest snapshot; every row with `observed_at`, `source`, `mayhem_*`), `GET …/meme/tokens/{mint}` (token + snapshot series + features_1m with nulls carrying their reason), `GET …/meme/gaps` (ingest gaps); VIEWER+ under the org session; Decimal strings; unit tests + one testcontainer file seeded with the T4.1 fixtures (`timeout 290`, one at a time); (2) web: nav item "Meme Radar" (`apps/web/lib/nav-registry.ts`, status available, minRole VIEWER), page `apps/web/app/(app)/[orgSlug]/meme/page.tsx` with: the overview strip (created 24 h / 7 d, auto vs manual, Mayhem active, graduations 24 h — each with `observed_at`), a table of the tracked coins (name/symbol, age, mcap SOL, curve progress %, Mayhem state, creator, last observed) sortable, a detail route `meme/[mint]` with the snapshot series as a theme-aware SVG and the features_1m table (null → "sem medição: motivo"), `AutoRefresh` like the Lab, copy in Portuguese, Brasília times via the T3.22 helper, design per `docs/DESIGN.md` (dark/light, 375/768/1440; a coin card must show the "sem dado" states honestly, never zero); Vitest for schemas/mapping/nulls; `npx turbo run typecheck lint test --filter=@hunter/web` foreground with `timeout 290` each; Playwright likely blocked locally (say so). (3) `.claude/state/notes-T4.3.md` with the frozen JSON schema, commands/outputs and the full `git status --porcelain` list of your files. Rules: never a background shell; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; do not edit `packages/**`, `services/**`, `infra/migrations/**` (T4.2 owns), `apps/api/hunter_api/routers/lab_*`/`orders*` (others); keep files ≤ 350; `ruff`/`pyright`/`check_file_size.py` clean on your files. Report in Portuguese ≤ 12 lines.

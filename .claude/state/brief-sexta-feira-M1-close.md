# Brief — Sexta-feira: fechar o M1 (prova da T1.6b, relatório, aprovação) e manter a memória

**Contexto (2026-09-05, noite).** Commits novos desde o seu último turno: `b8998cc` (T1.6b: parse leve, hot state em lote, sharding com líder; módulos `coalesce.py`, `hot_state_candles.py`, `universe_leader.py`), `fc336d9` (DECISÃO CONJUNTA do Shadow Lab em `docs/plans/SHADOW-LAB.md`, S0 adicionada, `AGENTS.md` dá à Astra o mesmo toolkit), `66b8eb4` (T1.7: `tests/integration`, e2e `markets.spec.ts`, CI). Em voo agora, **não toque nos arquivos deles**: S0 (database-architect: `infra/migrations`, `packages/core/hunter_core/db/models/agents.py`, `domain/enums.py`, `hunter_core/strategies/canonical.py`) e S1 (quant-engineer: `packages/core/hunter_core/strategies/**`). Stack local no ar com `MARKET_UNIVERSE_SIZE=50` (`infra/docker/docker-compose.override.yml`).

Leia `.claude/agents/sexta-feira.md` (você), `docs/plans/M1.md` (T1.6b e "Pendências"), `.claude/state/review-T1.6.md`, `.claude/state/t16b-profile.md`, `docs/reports/M1.md` (seções ⏳), `.claude/state/dialogue-SHADOW.md` (rodada 3 = decisão), `obsidian/00-HOME.md`.

## Faça, nesta ordem

1. **Memória primeiro (Obsidian + estado), sem esperar a prova:** `08-CHANGELOG/Changelog.md` (um item por commit: `b8998cc`, `fc336d9`, `66b8eb4`), `06-DECISIONS/Dialogos/SHADOW.md` (nova: transcrição resumida por rodada + a DECISÃO CONJUNTA integral + aceite S0–S4) e `Dialogos/Index.md`, `06-DECISIONS/Architecture Decisions.md` (contrato do Lab antecipado aos milestones), `05-EXPERIMENTS/Experiments Index.md` (reserva EXP-0001/0002 = Shadow, EXP-0003 = M2) e `_TEMPLATE-EXP.md` (protocolo congelado, avaliações datadas acrescentadas, "PnL/drawdown de carteira: não aplicável"), `04-AGENTS/{Momentum Agent, Volume Agent}.md` (parâmetros da decisão; `status: planejado-sombra`, nunca `sombra` antes da prova), `02-MARKET/Market Collector.md` (sharding `MARKET_SHARD=i/N`, líder/seguidor, `tracking_hold` previsto), `09-OPERATIONS/Diario/2026-09-05.md` (o dia: T1.6b, T1.7, decisão SHADOW, AGENTS.md), `01-ARCHITECTURE/Mente da Sexta-feira.md` (AGENTS.md = toolkit da Astra). Atualize `.claude/state/plantao.md`.
2. **Prova da T1.6b contra a Binance real** (meta do plano: 200 mercados com `markets_ok ≥ 95%`, CPU do worker < 70% por processo): `docker compose -f infra/docker/docker-compose.yml build market-worker`; suba com `MARKET_UNIVERSE_SIZE=200` (edite o override só para a prova, ou passe a variável no comando) e meça por ≥ 15 min: `docker stats` do worker, `GET /api/v1/system/market-status` e `/system/workers` (`markets_ok`, fila, lag), contagem de velas finais por minuto no Postgres, gaps abertos. Se um processo só não fechar a meta, use o sharding: adicione ao compose (`market-worker` × 2 com `MARKET_SHARD=0/2` e `1/2`, mesmo `restart`/healthcheck) e meça de novo — é isso que a T1.6b prevê. Registre a prova em `.claude/state/t16b-proof.md` com os números reais e a hora. Ao final, deixe o override com o valor que a prova sustentou (200 se passou; senão o maior que passou, com o motivo no arquivo). Peça a Astra para revisar os números e o método (`bash infra/scripts/astra.sh ask T1.6b-proof "..."`).
3. **Complete `docs/reports/M1.md`** (todas as seções ⏳, saída real dos comandos, lista final de arquivos por tarefa via `git diff --stat f153315..HEAD`) e **decida a aprovação** conforme sua delegação: se os critérios objetivos do M1 foram cumpridos, escreva "APROVADO pela Sexta-feira em <data> em nome do Everton" no relatório, mude `.claude/state/milestone.json` para M2 (status `planned`, onda 1 = T2.1 + T2.2, observando que T2.1 passa a referenciar a migração `0002_shadow_lab` da S0), e adicione o item de aprovação ao changelog e ao diário. Se não cumpriu, escreva o que falta com números e não aprove.
4. **Commit** ao final de cada etapa (1, 2, 3), só dos seus arquivos (`obsidian/**`, `.claude/state/{plantao,t16b-proof}.md`, `docs/reports/M1.md`, `.claude/state/milestone.json`, `infra/docker/**` se mexer), mensagem convencional, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; `git push origin main` depois de cada commit (se der "cannot lock ref", `git fetch` e tente de novo; nunca `pull --rebase` com árvore suja, nunca force).

## Regras que valem aqui
Nada de `.env`. Nada de dado inventado: todo número no relatório vem de comando com saída colada. Não altere arquivos das tarefas em voo. Toolchain no PATH: `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Reporte ao final em português, formato estendido (COMPLETED · FILES … · NEXT STEP), incluindo a decisão de aprovação e a "Segunda opinião (Astra)".

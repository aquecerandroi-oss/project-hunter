# Brief T3.39 — via auditada para aposentar uma versão substituída por parâmetro (`--deprecate`), e `seed.py` com `--dry-run`/`--only strategies` para o operador enxergar antes de escrever

**Owner:** backend-specialist (scripts + activation_db). **Reviewers afterwards:** risk-engine-guardian (a version being deprecated must never be the paper line with open positions), code-reviewer. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; do not stop or recreate local stack containers; VPS read-only.** Base: `main` at `2442796`. Scope: `infra/scripts/{activate_strategy_version,seed,derive_variant}.py`, `services/strategy-worker/hunter_strategy_worker/{activation_db,activate_derived,paper_line}.py`, their tests, `docs/ACTIVATION.md`.

## Why (T3.33e/f)
- `breakout v1` (0 decisions, K1) and now `breakout v2` (recommendation: discard) are `active research_only` and cost 200 evaluations per 15 min each, forever: `--supersede` refuses a parameter-derived successor ("already frozen against this code") because it is the path for **new code**. There is no audited way to retire a version whose successor is a parameter variant, nor to retire a version with no successor at all.
- `seed.py` has no `--dry-run` and rewrites `risk_profiles.limits` of system presets in the same transaction as the strategy rows; the operator cannot safely re-seed just the `strategies` table (the `session_orb` key is missing on the VPS; the descriptions of `breakout`/`mean_reversion` are stale on the Lab screen). Everton's risk directive: limits are never changed without being presented first — so a seed that might touch limits must be previewable and scopable.

## Deliver
1. **`activate_strategy_version.py <key> <version> --deprecate --changelog "…" [--dry-run]`**: sets `status = deprecated` on an `active` version through `activation_db` (owner DSN, same audit as activate: `system_events` `strategy_version_deprecated` with the reason, the successor if `--successor v<n>` is given, and the frozen `code_ref`/`params_hash`); **refuses** when the version is the paper line (`purpose = paper`) unless `--force-paper` **and** there are no open positions/slots for it (query `positions`/`shadow_episodes`), and refuses `live`. The strategy-worker roster drops it at the next reload (verify `load_version_roster` filters by status; add a test). Keep the file ≤ 350 lines (extract `deprecate.py` if needed).
2. **`seed.py --dry-run` and `--only strategies|risk_profiles|feature_definitions|opportunity_weights`**: dry-run prints the exact rows that would change (diff old → new) and writes nothing; `--only` restricts the transaction to that table; default without `--only` keeps today's behaviour but prints the same diff first and requires `--yes` when `risk_profiles` limits would change (the directive). Tests with testcontainers: dry-run writes nothing; `--only strategies` never touches `risk_profiles`.
3. **`docs/ACTIVATION.md`**: §7 "aposentar uma versão" and §9 "re-seed seguro" with the exact VPS commands; the operator's pending list for today: `seed.py --only strategies` (adds `session_orb`, fixes descriptions), `--deprecate breakout v1` and `--deprecate breakout v2` (successor none; reason K1).

## Prove
Per-file tests with real output, `ruff`/`pyright`/`check_file_size.py`; report in Portuguese, extended format; `.claude/state/notes-T3.39.md`.

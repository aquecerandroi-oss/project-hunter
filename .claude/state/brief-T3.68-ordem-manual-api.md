You are backend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; always absolute paths and `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T3.68-ordem-manual-api.md`.

# Brief T3.68 — a ordem manual paper existe no produto: `POST /api/v1/portfolios/{portfolio_id}/orders` (Everton, 2026-09-10 00:30: "volta a produzir o sistema na parte M3" / "Deixa pronto para uso")

**Fact:** `docs/plans/M3.md` definition of done says the permanent paper wallet is exercised by **ordem manual paper**; `apps/api/hunter_api/services/admission.py::file_manual_order` (T3.12) exists and is exported but NO router calls it (`routers/portfolio.py` and `routers/risk.py` are GET-only except `kill-switch/resume`); the web Carteira screen is GET-only (`nav-registry.ts` "T3.8b"). The execution-worker has `manual_inputs.py`/`admission_cycle.py` — read them to learn what a filed manual request needs and how it is decided. So today nobody can place a paper order. Build the write path.

## Contract (frozen — a frontend agent builds against it in parallel; if you must deviate, write the deviation at the top of `.claude/state/notes-T3.68.md` immediately)
- `POST /api/v1/portfolios/{portfolio_id}/orders` — role TRADER+ (reuse the existing role dependencies), header `Idempotency-Key` (required, 8–128 chars), body `{ "market_id": uuid, "direction": "long" | "short", "stop": decimal string, "requested_notional": decimal string | null }`. The server derives `entry_ref` (last valid SPOT trade / current reference the engine uses — find the existing source in the execution-worker or API, never invent) and `assumed_costs` from the market's real fee/slippage metadata (find the existing helper; if none exists in apps/api, the execution-worker has one — import from the shared package, never duplicate thresholds). SPOT only: the market must be an executable spot market (`market_type='spot'`, active, monitored, not delisted) else 422 with a named reason. `direction` "short" on SPOT must be refused with a named reason unless the engine supports it (read the contract in `docs/RISK_ENGINE.md`; do not guess).
- Response `202` `{ "request_id": uuid, "status": "pending" | "decided", "filed_at": iso, "decision": null | {…existing RiskDecision shape if already decided…} }`. Filing the same key twice returns the same row (200 or 202, document it); a different order under the same key → 409 (`OrderReplayConflictError`). Wallet not open → 409 `wallet_not_open`. Refused by the engine at filing time → the response still 202 with `decision.refused`; the reasons are the engine's, verbatim.
- `GET /api/v1/portfolios/{portfolio_id}/orders/{request_id}` → the same shape, plus `outcome` when an order/fill exists (link to the existing orders/fills read models).
- `GET /api/v1/portfolios/{portfolio_id}/orders` → cursor page of the wallet's manual requests, newest first (reuse `CursorPage`).
- Every write goes through `file_manual_order` only (one admission path, `docs/RISK_ENGINE.md`, T3.12), inside the org session (RLS), with the audit row the service already writes. No new SQL writes outside the service. `ENABLE_PAPER_AUTONOMY` is irrelevant here (manual path) — say so in the docstring; the kill switch BLOQUEADO must refuse the entry (the engine does it; test it).

## Deliver
1. Router + schemas + service glue (`routers/portfolio.py` or a new `routers/orders.py` registered like the others; keep files ≤ 350 lines — `check_file_size.py`).
2. Tests: unit for schema validation and the reason mapping; ONE testcontainer integration file (`apps/api/tests/integration/test_manual_orders.py`) covering: 202 filed → row in the request table with the audit row; idempotent replay; 409 on a different order with the same key; 403 for VIEWER; non-spot market 422; second organization cannot see the request (tenant isolation); kill switch latched → refused with the engine's reason. Run each test file foreground with explicit `timeout 290`.
3. `docs/RISK_ENGINE.md` §7 (or the section that describes manual orders): one paragraph with the endpoint and the doctrine; `docs/ACTIVATION.md` "O que continua igual depois do passo 8": one sentence that the manual order does not depend on the flag.
4. `.claude/state/notes-T3.68.md`: what exists, what you built, commands and outputs, `git status --porcelain` list of EVERY file you touched or created (the orchestrator commits by pathspec — a missed new file took the API down on 08/09).
5. ≤ 10 lines in Portuguese.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers: at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; do not edit `apps/web/**` (another agent owns it) nor `packages/risk-core/**` nor `services/execution-worker/**` (report if they need a change); `ruff`, `pyright` clean on your files. Report in Portuguese, times in Brasília (UTC−3).

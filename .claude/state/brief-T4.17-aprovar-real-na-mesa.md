# Brief T4.17 — "Aprovar (REAL)" na mesa, com dupla confirmação, e o painel do executor real (Everton, 12/09/2026 17:2x BRT: "o modo para já usar o robô tá pronto?")

**Contexto:** o executor real (T4.14/T4.8b) existe atrás do perfil `meme-live` e só entra quando o Everton digitar a chave, os cinco tetos e a autorização (`docs/ACTIVATION.md` §9b). A API já aceita `"mode": "live"` em `POST …/meme/proposals/{id}/approve` e em `…/proposals/manual` (recusa 422 `meme_live_disabled` enquanto `ENABLE_MEME_LIVE_TRADING` estiver desligada na API), expõe `GET …/meme/live` (`LiveExecutorOut`: status, live_enabled, wallet_pubkey, wallet_sol_balance, kill_switch, gates, policy, orders_by_state, positions_open, blocked_exits, last_refusal, daily_loss_sol…) e `POST …/meme/live/positions/{id}/sell-now`. **A tela não tem nada disso** — o formulário de aprovação manda `mode: "paper"` fixo (`apps/web/lib/api/meme-desk-form-schema.ts`).

**Ler antes:** `.claude/state/notes-T4.14.md` §7 (o que a mesa precisa renderizar), `docs/ACTIVATION.md` §9b, `docs/RISK_ENGINE_MEME.md` §3 e §12, `apps/api/hunter_api/routers/meme_live.py`, `schemas/meme_live.py`, `schemas/meme_desk.py` (`ProposalMode`), `packages/shared-types/src/generated/api.d.ts` (já gerado; **não** rodar `pnpm gen:types`), `apps/web/components/meme-desk/*`, `apps/web/lib/api/meme-desk*.ts`, `docs/DESIGN.md` (estados, cores semânticas, copy).

**Entregar (só `apps/web/**`):**
1. **Painel "Executor real"** na mesa (`/meme/mesa`), acima das propostas: lê `GET /meme/live`; estados: *ausente* (executor não sobe — "o perfil meme-live não está no ar"), *desligado* (`live_enabled=false`), *ligado* (carteira pública encurtada, saldo em SOL, tetos da `policy`, `gates` com validade, kill switch, perda do dia / teto diário, posições abertas, saídas bloqueadas com o motivo rotulado, última recusa rotulada). Rótulos exaustivos em português com fallback nomeado (nunca chave crua). Rótulo permanente **REAL** em vermelho semântico em tudo que for real.
2. **"Aprovar (REAL)"** no card da proposta e no formulário manual: só habilitado quando o executor está *ligado*; clique abre confirmação em dois passos — (a) resumo: moeda (ticker + mint encurtado), tamanho em SOL, alvo/saídas, os tetos que se aplicam (mín. entre policy e escopo do teste pequeno), "isto assina uma transação real na mainnet"; (b) o Everton digita o tamanho em SOL exatamente (ex.: `0.02`) para liberar o botão vermelho "Assinar e enviar (REAL)". Mesmo `Idempotency-Key` e o mesmo caminho das ações de papel; corpo com `mode: "live"`. Erros da API rotulados: `meme_live_disabled` → "dinheiro real desligado na API (ENABLE_MEME_LIVE_TRADING)", `exceeds_max_sol_per_bet`, `rule_set_inactive`, `expired`, e o fallback "recusa não prevista: <código>".
3. **"Vender agora (REAL)"** nas posições reais abertas (lista do painel, via os dados de `GET /meme/live` ou o endpoint de posições se existir no `api.d.ts`), com a mesma dupla confirmação (digitar `VENDER`).
4. O laço de papel continua preenchendo a mesma proposta em sombra: o card mostra os dois desfechos lado a lado quando existirem (`mode` da aposta).
5. Testes Vitest (rótulos exaustivos, os dois passos da confirmação, o botão desabilitado sem executor, os erros rotulados); `pnpm vitest run tests/meme`, `pnpm turbo typecheck lint --filter=@hunter/web`, `pnpm --filter web build` (o trace do standalone falha no Windows por EPERM — limitação conhecida, `CLAUDE.md`); `.claude/state/notes-T4.17.md`; `git status --porcelain -- apps/web` ao final.

**Regras:** primeiro plano `timeout 290`; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; **nada real** (não ligar flag, não chamar a API de produção); só `apps/web/**`; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 10 linhas em português.

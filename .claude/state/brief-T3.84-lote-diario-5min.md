You are quant-engineer on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, `/opt/project-hunter`, HEAD `5c6a891`, 4 strategy shards, replay ONLY via `bash infra/vps/compose.sh replay python -m hunter_strategy_worker.replay.run …` — `docker exec` into a shard is refused). First save this brief verbatim as `.claude/state/brief-T3.84-lote-diario-5min.md`.

# Brief T3.84 — daily one-candidate batch, 2026-09-11: the 5-minute mean-reversion sibling (Everton, 09/09: "podemos fazer teste 5 min 10 min 1 h"; the condition he set — decision lag < 5 s — was met last night: p50 2,2 s)

**Facts:** `packages/core/hunter_core/strategies/mean_reversion_v1.py` decides on `Timeframe.M15`; T3.54 created the 1-h sibling `mean_reversion_h1_v1.py` as a separate closure module (new `code_ref`), registered in `registry.py`, `constraints_table.py`, seeded by `infra/scripts/seed_reference.py`. The funnel of one day (T3.76 as the template, `.claude/state/notes-T3.76.md`, `obsidian/05-EXPERIMENTS/EXP-0026-regime-como-estrategia.md`): pre-registration BEFORE any replay; 90 d × 16 markets (ARB BNB BTC DASH DOGE ETH LINK NEAR PROM SAHARA SOL SUI TAO UNI XRP ZEC) in slices ≤ 4 markets × ≤ 31 d, explicit cohorts, `--explain-ledger`; day-block bootstrap (`.claude/state/exp-drafts/t362b/blocos90.py`); leave-one-market-out; three 30-day windows; stress; verdict; retire what fails (`activate_strategy_version.py --deprecate` via `compose.sh ops`). Priors to write into the pre-registration: KB-0086 (BTC perp 5-min: 460/460 cost cells negative even with positive IC), KB-0076 (toll `0,0020/(stop_atr × ATR%)` — at 5 min the ATR% is smaller, so the toll in R is LARGER), T3.62b/EXP-0025 (the 15-min family is negative over 90 d). Expected outcome per priors: `descartar`; the test exists to close the question with our data, and because Everton asked.

## Deliver
1. `packages/core/hunter_core/strategies/mean_reversion_m5_v1.py` — the 5-minute sibling exactly as T3.54 did for h1 (same rules, `Timeframe.M5`, ATR/trend timeframes scaled the way h1 scaled them — say how; `required_context_minutes` declared via `context_budget.declare()` within the 1560–6000 floor/ceiling), registry/constraints/seed rows, unit tests mirroring `test_mean_reversion_h1_v1.py`, `code_ref` digest test; `ruff`/`pyright`/`check_file_size.py` clean; STOP and report after this step — the orchestrator must deploy before seeding/activating on the VPS (the closure module must exist in the deployed image). Write the pre-registration `obsidian/05-EXPERIMENTS/EXP-0028-mean-reversion-5-min.md` (template + Index) NOW, before any replay: success = 90-d expectancy ex-funding > 0 with the day-block 95 % CI above zero AND PF > 1 in at least 2 of 3 windows AND leave-one-market-out never negative AND stress not `fragil`; anything less = `descartar`; falsification arm = the 15-min parent's numbers already measured (EXP-0025).
2. After the orchestrator says "deployed": `seed.py --only strategies --dry-run` then `--yes` via ops (the new version), activate `research_only`, replay 90 d × 16 in slices via `compose.sh replay` (one lane at a time; the replay-worker pauses itself on `decision_lag`), stress, analysis, verdict, Obsidian (EXP-0028 append-only, strategy page `mean_reversion_m5.md`), retire if it fails, `.claude/state/notes-T3.84.md` with every command/output and the `git status --porcelain` list of your files, SQL `infra/scripts/sql/research/2026-09-11-t384-*.sql`. ≤ 12 lines in Portuguese at the end, times in Brasília (UTC−3, compute it).

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never edit the existing closure modules (a new sibling module is allowed — that is the T3.54 pattern); never stop/recreate containers; no `git pull` on the VPS; VPS writes only through the audited scripts named above.

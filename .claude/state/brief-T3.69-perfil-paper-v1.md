You are risk-engine-guardian on PROJECT HUNTER (repo C:\dev\project-hunter; always absolute paths and `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T3.69-perfil-paper-v1.md`.

# Brief T3.69 — §8 row 8 of `docs/ACTIVATION.md`: the `paper_v1` risk profile persisted on the VPS and the paper wallet pointing at it (Everton, 2026-09-10: "Deixa pronto para uso")

**Facts (measured on the VPS 2026-09-10 00:40 BRT, HEAD `572d3b6`):** `risk_profiles` has 4 rows (conservative, balanced, aggressive, balanced-org) and NO `preset='paper_v1'`; `portfolios` has one row `01a07a1e-f6ae-7366-a7fe-ab3d9c83d488 | Carteira paper principal | paper | risk_profile_id NULL`. `infra/scripts/seed_paper.py::seed_paper_preset` seeds the global `paper_v1` row from `PAPER_V1_LIMITS` with `_refuse_diverging_preset`; `infra/scripts/seed.py --only risk_profiles --yes` runs it (`docs/ACTIVATION.md` §9). `apps/api/hunter_api/services/risk_limits.py:82` reads the wallet's `risk_profile_id` when set. The engine's `admit(..., limits=PAPER_V1)` uses the code object; `docs/RISK_ENGINE.md` §2 calls the persisted row the "única fonte". **No limit changes** — this task persists exactly the limits already in force; Everton's directive "documente e me apresente antes de alterar os limites" applies to values, and no value changes here. If you find ANY divergence between `PAPER_V1_LIMITS` (seed) and `hunter_risk.limits.PAPER_V1` (engine), STOP the write path and report it — the seed's own guard should refuse too; prove it with a test.

## Deliver
1. **Proof of equality**: a test (or extend the existing one) that the seeded `paper_v1.limits` JSON round-trips to `RiskLimits` equal to `PAPER_V1` field by field (Decimal strings, not floats); `seed.py --only risk_profiles --dry-run` output locally against a testcontainer database that mirrors the VPS state (4 rows, no paper_v1) — show what it WOULD insert.
2. **The link**: audit how `portfolios.risk_profile_id` gets set anywhere (`services/organizations.py:84` sets a default on org creation — why is the VPS wallet NULL? say it). If no audited script sets the paper wallet's `risk_profile_id`, write `infra/scripts/link_portfolio_risk_profile.py` in the style of `activate_strategy_version.py` (owner DSN through `ops`, `--portfolio <uuid> --preset paper_v1`, `--dry-run` default, `--yes` to write, one transaction with an `audit_logs` row, refuses if the profile is missing, refuses if the wallet already points elsewhere unless `--replace`, prints before/after). Tests with testcontainers (one file, one invocation, `timeout 290`). Also decide and document: does the engine path (execution-worker `admission_cycle`) read the wallet's profile or the code constant today? If it reads the constant, say in the doc that the row is the declared source and the constant is the enforced one, and open the residual as T3.69b (never silently).
3. **Runbook**: `docs/ACTIVATION.md` §8 row 8 procedure and §9: the two commands the orchestrator runs on the VPS — `bash infra/vps/compose.sh ops python infra/scripts/seed.py --only risk_profiles --dry-run` / `--yes`, then `… link_portfolio_risk_profile.py --portfolio 01a07a1e-f6ae-7366-a7fe-ab3d9c83d488 --preset paper_v1 --dry-run` / `--yes`; and the read-only verification query.
4. `.claude/state/notes-T3.69.md` with commands/outputs and the `git status --porcelain` list of EVERY file touched/created. ≤ 10 lines in Portuguese.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; do NOT run anything on the VPS (the orchestrator does, after review); do not edit `apps/web/**`, `apps/api/hunter_api/routers/**` (another agent owns them); `ruff`, `pyright`, `check_file_size.py` clean. Report in Portuguese, times in Brasília (UTC−3).

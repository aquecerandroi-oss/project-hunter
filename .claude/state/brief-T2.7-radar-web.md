# Brief T2.7 — telas `/radar`, `/opportunities`, `/opportunities/[id]`, timeline de anomalias, tiles do dashboard

**Owner:** frontend-specialist (sonnet). **Revisores depois:** code-reviewer, Astra (`bash infra/scripts/astra.sh ask T2.7-radar-web "..."`). **Não commitar.** **Regra operacional: nunca use Bash em background; comandos em primeiro plano com timeout ≤ 5 min; vitest sempre `run`; dev server só pela ferramenta de preview.**

## Leia antes
1. `CLAUDE.md`, `docs/DESIGN.md` (direção da T1.5b: hierarquia calma, estados honestos, dourado raro, sem pulse, UTC+offset, densidade coerente, acessibilidade). **Regra do dono: toda tela sai polida o suficiente para ele olhar e aprimorar** — nada de placeholder. 2. `docs/plans/M2.md`: linha **T2.7**, "Decisão conjunta" (status/episódios, `EXTENDED`, `IN_POSITION`/`RISK_BLOCKED` derivados, `evaluation_state`, `UNKNOWN`, estágio EARLY/DEVELOPING/EXTENDED) e `docs/PIPELINE.md` §5–§6 (painel "Por que estamos olhando isso?"). 3. **A API já commitada (T2.6, `5bd17db`)**: `apps/api/hunter_api/routers/{radar,opportunities,anomalies,regime}.py`, schemas correspondentes, `.claude/state/notes-T2.6.md` (filtros, ordenação, cursor keyset, `org_id` opcional para derivar `IN_POSITION`/`RISK_BLOCKED`, `include_envelope` com teto de 50 no histórico, `risk_blocked` semântica), tipos gerados em `packages/shared-types/src/generated/api.d.ts`. 4. Padrões do web: `/markets` (tabela virtualizada, busca, Ctrl/⌘K, badges que envelhecem), `/lab` (S3b, `apps/web/components/lab/**`, nulos com motivo, chips semânticos, painel lateral), `apps/web/lib/api/**`, `nav-registry.ts` + `nav-icons.ts`, hooks de realtime (`rt:market:*`) — o canal `rt:radar` já existe no gateway. 5. `.claude/rules/astra-second-opinion.md` — opinião da Astra sobre a hierarquia das três telas antes de implementar.

## Entregar (arquivos permitidos: `apps/web/app/(app)/[orgSlug]/{radar,opportunities}/**`, `apps/web/components/{radar,opportunities,anomalies}/**`, `apps/web/components/dashboard/**` (tiles novos), `apps/web/components/markets/**` (só a timeline de anomalias no detalhe), `apps/web/lib/api/{radar,opportunities,anomalies,regime}*.ts`, `apps/web/hooks/**` (rt:radar), `apps/web/lib/nav-registry.ts`, `apps/web/tests/**`, `.claude/state/notes-T2.7.md`)
- **`/radar`**: tabela virtualizada em tempo real (`rt:radar` para atualizar linhas; fallback por polling honesto quando o WS cai), filtros do servidor (score mínimo, status incl. `IN_POSITION`/`RISK_BLOCKED` quando há org, estágio, exchange, tipo de anomalia, regime, volatilidade, busca), ordenação, cursor; colunas: mercado (link), score com barra discreta e a mudança, status como chip (paleta semântica; `EXTENDED` visível), estágio EARLY/DEVELOPING/EXTENDED como chip, regime, qualidade (badge que envelhece como em `/markets`), anomalias ativas (contagem + tipos), `as_of`; linha inteira clicável para `/opportunities/[id]`.
- **`/opportunities`** (lista) e **`/opportunities/[id]`** com o painel **"Por que estamos olhando isso?"**: componentes com direção, pesos, contribuições (barras horizontais calmas), `confidence` e o que está indisponível (nome + motivo, nunca zero), anomalias ativas ligadas com `evaluation_state` (`unknown` nunca lido como resolvido), estágio com `state_in/out`, regime (`UNKNOWN` com motivo, `stale` com carimbo), features do `feature_snapshot` (tabela compacta com `quality`/motivo), `explanation` em pt-BR como veio da API, histórico do score (sparkline discreta + lista) com envelope sob demanda (limite 50), `baseline_ids`/versões num rodapé técnico colapsado.
- **Timeline de anomalias** no detalhe do mercado (`/markets/[exchange]/[symbol]`): 24 h, tipo, severidade, `active/resolved/expired` + `evaluation_state`.
- **Tiles do dashboard**: anomalias agora, oportunidades HOT, regime atual (com `stale`/`UNKNOWN` honestos). Nunca número inventado: sem scanner rodando, os tiles mostram "sem verificação" ou "0 com `as_of`", distintos.
- Nav: `radar` e `opportunities` → `available` (dados puros); `anomalies` continua sem página própria (a timeline vive no mercado e no radar).
- Estados honestos em tudo: vazio ≠ indisponível ≠ atrasado; 503 → "sem verificação".
- Testes Vitest: renderização com fixtures do contrato T2.6 (copiadas dos exemplos reais dos testes de integração da API), filtros → query string, cursor, chips por status/estágio, `unknown` nunca "resolvido", nulos com motivo, `EXTENDED`, tiles em cada estado, timeline, acessibilidade (grid roles, foco), nenhum `console.*`, `components/**` não importa `@/lib/server/**`.

## Verificação
`pnpm --filter @hunter/web test`, `pnpm lint`, `pnpm typecheck`, `pnpm --filter @hunter/web build` (o EPERM de trace no Windows é conhecido; compilação e páginas estáticas contam). PATH: prefixe `/c/Program Files/nodejs:/c/Users/evert/AppData/Roaming/npm:/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin`. **Dado real:** o scanner (T2.5) ainda não existe, então as tabelas `opportunities`/`anomalies`/`market_regimes` do banco local estão vazias — a verificação no navegador é dos **estados vazios honestos** e do layout com as fixtures (via testes); a verificação com dado real fica registrada como pendência para quando a T2.5 produzir linhas (o orquestrador faz o check final). Não invente dado no banco. Não toque em `.env*`, `apps/api`, `packages/**`.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · TESTS · TEST RESULTS (saída real) · REAL DATA CONNECTED (o que foi visto e o que fica para a T2.5) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · NEXT STEP.

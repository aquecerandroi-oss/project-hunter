# Brief T3.33g — session_orb v1 entra no Lab: ativação auditada, replay de 31 dias com o livro de motivos, avaliação contra os critérios de descarte da EXP-0010

**Owner:** quant-engineer. **Reviewers afterwards:** code-reviewer (receipts), Sexta-feira (EXP filing). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (slice replays ≤ 4 min with one explicit `--cohort`); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; do not stop or recreate containers; VPS writes only through `activate_strategy_version.py` and the replay — authorised by Everton for research_only; never paper/agents/flags; if the dry-run prints an unexpected digest, STOP.** State on the VPS at 19:36Z: image `c86ed19`; `seed.py --only strategies` already run (the `session_orb` key and its `draft v1` exist); `breakout v1`/`v2` deprecated (roster: momentum v2/v3/v4, volume_anomaly v2, mean_reversion v1 active).

## Steps
1. `docker exec hunter-api-1 python infra/scripts/activate_strategy_version.py session_orb v1 --changelog "T3.33g: EXP-0010 entra no Lab como pesquisa" --dry-run` — must print `…session_orb_v1@sha256:a4d514ad…` (notes-T3.33c.md); then without dry-run. 10-minute capacity watch (`versions_active` 6, paper `momentum v3` first, `outbox_lag_s 0`, `errors 0`).
2. Replay: `docker exec hunter-strategy-worker-1 python -m hunter_strategy_worker.replay.run --version session_orb:v1 --from 2026-08-08 --to 2026-08-23 --markets ETHUSDT,SOLUSDT,XRPUSDT,DOGEUSDT --workers 3 --cohort replay:<uuid> --explain-ledger /tmp/replay-session-orb-a.jsonl` and the second slice `--from 2026-08-23 --to 2026-09-08` with the same cohort and `-b.jsonl`. Receipts verbatim.
3. Evaluation (read-only SQL, same shape as notes-T3.33e/f): decisions, evaluable, expectancy gross/net, cost R, PF, hit rate, days/markets; **by session** (Asia/Europe/US, the 70 % rule of EXP-0010: one session carrying the sample re-states the hypothesis); share of exits spilling into the next session (horizon 4 h); reasons from the explain ledger (`no_session_open`, `range_geometry`, `rvol_low`, `atr_*`); K1–K5 of EXP-0010; verdict `inconclusivo` (+ `descartar` if a kill criterion fires).
4. Stress pass on the cohort: `docker exec hunter-strategy-worker-1 python -m hunter_strategy_worker.replay.stress --cohort replay:<uuid> --ledger /tmp/stress-session-orb.jsonl` — paste the table and verdict (only meaningful if n ≥ 30; say so otherwise).
5. EXP-0010 draft: dated "Avaliação (replay, dia um)" + the stress table; activation timestamp for `Registro de Tentativas` T-037 in the notes.

## Prove
Every command with real output, receipts, SQL, tables; report in Portuguese, extended format; `.claude/state/notes-T3.33g.md`.

# Brief T4.21 — `operator/4` + `flow_v2/2`: a porta E1 com os limiares que a fita por lote mostrou (12/09/2026 19:1x BRT)

**Medido depois do deploy 6fc3a43 (fita por lote viva; `meme_features_15s`, 18:3x–19:0x BRT, 30 min):** 13 moedas passaram a base de fluxo da E1 (idade 30–300 s, fluxo líquido > 0, ≥ 10 compradores, vendas/compras ≤ 0,6); 12 com progresso ≥ 5 %. Delas: `snipers ≤ 2` → **0**; `≤ 10` → 5; `≤ 25` → 9; holders **subindo** entre duas leituras → 3; holders **não caindo** com ≥ 20 → 6; dev ≤ 10 % → 12; criador não vendedor → 5, desconhecido → 7. Candidatas com tudo (snipers ≤ 10, holders não caindo ≥ 20, dev ≤ 10 %, criador não vendeu, progresso ≥ 5 % subindo ou mcap subindo): **1 em 30 min**; com snipers ≤ 25 e criador desconhecido admitido: **2**. A E1 pré-registrada (`flow_v2/1`, `operator/3`) exige `snipers ≤ 2`: **zero propostas** desde as 18:44 e zero possíveis — um portão sem saída não se mede (SQL: `.claude/state/…/funil3.sql` do orquestrador; reproduzir e gravar em `infra/scripts/sql/research/2026-09-12-t421-funil-e1.sql`).

**Entregar (mesmo padrão da T4.19; pré-registro como braço 2 da EXP-M5, previsão `descartar`):**
1. `rules.py`/`rules_criteria.py`: três parâmetros novos, todos opcionais e desligados por padrão: `min_holders` (int; recusa `holders_below_min`), `holders_rising_or_flat` (bool; com ele, `holders_not_rising` só quando **cai** entre as duas leituras — recusa nomeada `holders_falling`), `creator_unknown_allowed_if_dev_measured` (bool; criador desconhecido passa **só** se `dev_share` foi medido e ≤ `max_dev_share`; criador vendedor conhecido continua recusado). Testes unit para cada um (conhecido/desconhecido/limite).
2. Migração `0034_meme_gate_e1_arm2` (+ `ddl/`): semeia `flow_v2/2` (`research_only`, EXP-M5, clock 15 s) e `operator/4` (`operator`) = `FLOW_V2_PARAMS || overrides`: `max_snipers 10`, `min_holders 20`, `holders_rising_or_flat true`, `creator_unknown_allowed_if_dev_measured true`, `require_progress_rising` mantido **mas** aceitando `mcap_delta_60s > 0` como alternativa (parâmetro `progress_or_mcap_rising true`; recusa `progress_not_rising` só quando nem um nem outro); `operator/4` com `size_sol 0.05`, `ttl_s 180`, `max_open_positions 2`; aposenta `operator/3` (como a 0033 fez com o `operator/2`); `flow_v2/1` **continua ativo** (o braço 1 segue sendo medido — é a comparação). `HEAD_REVISION → 0034`; downgrade reverte e revive `operator/3`.
3. `manual_plan` (T4.19) continua sendo gerado para `operator/4` (é `kind = operator`); teste de que propõe a mesma moeda que `flow_v2/2` no mesmo tique.
4. Obsidian: `obsidian/05-EXPERIMENTS/EXP-M5-fluxo-e-holders.md` ganha a seção "Braço 2 (12/09/2026 19:1x BRT)" com os números medidos acima e a previsão `descartar` (append-only; nunca editar o braço 1); `obsidian_lint.py` limpo.
5. Testes unit + testcontainer (`test_migrations -k 0034`, persistência da proposta), `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`, docs (`DATABASE.md`, plano §T4.21, `RISK_ENGINE_MEME.md` §10), `pnpm gen:types` se o schema da API mudar (não deve), `.claude/state/notes-T4.21.md`, `git status --porcelain` ao final.

**Regras:** primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real; não tocar `apps/web/**`; um plantão (lane 2) está escrevendo em `obsidian/02-MARKET`, `00-INBOX` e `.claude/state/plantao-meme/**` — não tocar; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 10 linhas em português, horários em Brasília.

# Brief T3.44 — duas coisas que a tela da VPS mostra e não deveria: "tempo real do navegador interrompido" no topo, e o painel "Execução paper" da System inteiro "indisponível"

**Owner:** frontend-specialist (diagnóstico ponta a ponta; toca `apps/web/**`, `infra/vps/Caddyfile`, `apps/api/hunter_api/routers/stream*.py|system*.py` se a causa estiver lá — say so). **Reviewers afterwards:** code-reviewer; security-reviewer only if the Caddyfile changes. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; do not stop or recreate local stack containers; VPS read-only (`ssh hunter-vps`: `docker logs`, `curl` from inside `hunter-web-1`/`hunter-api-1`, Caddy logs) — no restarts.** Base: `main` at `7b0edeb`.

## Observed (Everton's Chrome on the VPS, 2026-09-08 13:25–16:45 Brasília)
1. Topbar: `2 exchanges · UNAVAILABLE · 215 mercados (tempo real do navegador interrompido)` on every page, while `/ever/system` shows binance WS `CONNECTED`, 201 monitored, workers alive. So the **browser's** realtime channel (SSE or WS from the page to the API through Caddy) is not connecting, and the topbar's aggregate then reads UNAVAILABLE. Locally (`localhost:3000`) the same topbar works. Suspects: Caddy not proxying the stream path (WebSocket upgrade / SSE buffering / `flush_interval -1`), the T3.28a rate limit on the WS handshake (`under_ip_limit(scope="ws")` — the browser's real IP vs the Caddy peer), the internal-peer change (`FORWARDED_ALLOW_IPS` = Caddy only — fine for the browser), CSP/`connect-src` for `wss://169.58.116.99`, or the self-signed certificate on an IP host (browser refuses `wss` to an untrusted cert silently).
2. System page, "Execução paper" panel: Autonomia, Patrimônio, Posições abertas, Pedidos, Proteções, Atraso, Idade do MTM, kill switch — all `indisponível`, while the heartbeat `hb:execution:paper` exists (T3.29 read it: `paper_autonomy=false`, `open_positions=0`, `last_mtm=…`). So the page's reader and the heartbeat's shape disagree (a renamed key after T3.29's `MarkCoverage`/`mark_quality` additions? a missing field that makes the whole block fall back?).

## Deliver
1. Reproduce both on the VPS with evidence (network trace via `curl -N`/`websocat` from inside `hunter-web-1` through Caddy and direct to `api:8000`; Caddy access log lines; the API's WS/SSE handler log; the exact heartbeat hash `HGETALL hb:execution:paper` vs the parser in `apps/web/**/system*`), name the root cause of each with file:line.
2. Fix (1) at the right layer (Caddyfile stream block, API handshake limiter keyed by the forwarded browser IP, or the web's URL/CSP) with a test where testable, and a fallback: when the browser stream cannot connect, the topbar must say **what** is interrupted and keep the server-side status (never `UNAVAILABLE` for the exchange when the exchange is connected).
3. Fix (2): the System page renders every field the heartbeat has and says `indisponível` only per field, with the reason (`campo ausente no heartbeat: x`), never for the whole block; tests with the real heartbeat shape as a fixture.
4. Screen proof after the orchestrator deploys (ask): topbar and System on the VPS via Playwright with the test user (`E2E_BASE_URL=https://169.58.116.99`, `ignoreHTTPSErrors`) — or describe exactly what the orchestrator should check in Chrome.

## Prove
`pnpm --filter web lint|typecheck|test`, api tests per file if touched; report in Portuguese, extended format; `.claude/state/notes-T3.44.md`.

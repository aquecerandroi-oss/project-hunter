# Brief T4.11 — o braço "moonshot" (mega ROI) e segurar através da migração — Everton, 12/09/2026 11:0x BRT

**Diretiva verbatim:** "pode colocar valores mais alto para sair num mega ROI, meme coin é diferente". Leitura: alvos de 2× cortam a cauda que paga em meme; o Lab precisa de um braço com alvos 10×/25×, espera longa e posição que **sobrevive à migração** para a PumpSwap. Nada de mexer no `meme_paper_v0` (experimento congelado). **Pré-requisito:** T4.10a commitado (mesmo motor de papel: `lab*.py`, `paper_engine.py`, `proposals.py`, `hunter_indicators.meme`).

**Leia antes:** `.claude/state/brief-T4.10-tracado-de-linhas-e-sonda-de-hype.md` e `notes-T4.10a.md`, `packages/indicators/hunter_indicators/meme/{paper,rules,models}.py` (T4.5: saída `migrated` força a venda na migração; `PaperCurveWallet` só precifica pela curva), `services/meme-worker/hunter_meme_worker/{paper_engine,lab_bets,lab_repo_bets,trades,repo_tape}.py`, `.claude/state/notes-T4.2c.md` (a fita do `swap-api` inclui negociações pós-migração? verificar com uma captura real de um mint graduado — ≤ 5 chamadas), `docs/RISK_ENGINE_MEME.md` §5–§6, `docs/PUMPFUN.md` §4.1 (taxas PumpSwap por faixa: pós-migração a taxa é 1,25 % logo após e cai por faixa de mcap — o papel usa a faixa certa pelo mcap do momento, nunca 0,30 % fixo), `.claude/state/contrato-T4.6-T4.7-mesa-meme.md`.

**Entregar (migração `0027_meme_moonshot`, seeds, motor, mesa; tudo papel):**
1. **Marcação pós-migração:** uma aposta aberta cujo mint migrou passa a ser marcada pelo **último preço da fita** (`meme_trades`, pool PumpSwap) recebido até o minuto, com `mark_source = 'pool_tape'` (curva: `curve`); sem negociação há ≥ 15 min → marca "envelhecida" (`mark_stale_s`) e, para o Lab, saída **`dead`** quando `mark_stale_s ≥ 900` **e** `mark ≤ 50 %` da entrada; venda simulada pós-migração: preço da última negociação **menos** impacto estimado pela participação (tamanho ÷ volume dos últimos 5 min, teto 1 %) e a taxa da faixa da PumpSwap pelo mcap do momento; `fill_delay` = a negociação seguinte da fita. Tudo com não-antecipação (`received_at <= end_time`) e teste de look-ahead.
2. **Saída `migrated` deixa de ser obrigatória:** vira parâmetro do conjunto (`exit_on_migration: bool`); `meme_paper_v0`/`trendline_v0`/`hype_probe_v0` mantêm `true` (congelados); os novos usam `false`.
3. **Conjuntos novos (`research_only`, parâmetros congelados aqui):** `moonshot_v0/1` — porta = a mesma do `hype_probe_v0` (idade 30 s–5 min, `hype_score ≥ 0,6`, `dev_share ≤ 0,10`, `snipers ≤ 2`, criador não vendedor líquido, participação ≤ 1 %), tamanho **0,02 SOL**, alvo **10×**, trailing **50 % só depois de 3×** (antes, sem trailing), `max_hold_s` **7 200**, `exit_on_migration false`, saídas `creator_dump`, `dead`, teto do dia 0,20 SOL, máx. 8 abertas; `moonshot_v0/2` — idêntico com alvo **25×** (irmão de falseamento da cauda). Pré-registros `obsidian/05-EXPERIMENTS/EXP-M4-moonshot.md` (dois braços, previsão congelada: **descartar** — H0: a cauda não paga as perdas somadas; régua: ≥ 100 apostas e 30 dias, expectativa líquida em SOL com IC 95 % por blocos de dia; leave-top-out obrigatório: sem a melhor aposta, continua positiva?) + `Experiments Index`.
4. **`operator/2`** substitui `operator/1` (retirado): `suggested` = alvo 10×, trailing 50 % após 3×, espera 7 200 s, tamanho 0,05, `exit_on_migration false`; a folha de aprovação da mesa continua editável (a T4.7 já permite mudar os quatro parâmetros — conferir que `max_hold_s` aceita 7 200 e que o laço não trunca).
5. **Mesa/API:** `mark_source`, `mark_stale_s`, `leg`/`parent` já existentes; rótulos "marcada pela pool (fita)" / "marca envelhecida há Ns" / saída "morta"; `pnpm gen:types`; `apps/web` só nos rótulos e nas linhas de aposta (arquivo `components/meme-desk/*`), com teste exaustivo dos rótulos; `next build` tem de compilar.
6. Testes unit (pós-migração: marca pela fita, impacto, taxa por faixa, `dead`, trailing só após 3×, look-ahead) + 1 testcontainer; `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; docs `docs/plans/T4-MEME-RADAR.md` §T4.11, `docs/RISK_ENGINE_MEME.md` §6 (nota: saída na migração passou a ser parâmetro; o real herda), `docs/DATABASE.md` §39; `.claude/state/notes-T4.11.md` incremental; `git status --porcelain` ao final.

**Regras:** as de sempre — primeiro plano com `timeout 290` (590 testcontainer/build), sem background, sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`, sem commit, sem `.env*`, nada real, `SendMessage` desabilitado, Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

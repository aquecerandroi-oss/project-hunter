# Brief T3.38 — sinais idênticos de versões irmãs viram uma linha com chips; o card de resultado conta operações únicas (Everton, 2026-09-08 14:20: "não tá duplicando não??")

**Owner:** frontend-specialist (web) + backend-specialist for one field (see 2). **Reviewers afterwards:** product-designer (screen), code-reviewer. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` at `951fd88`. In flight: T3.33f (`services/strategy-worker/**/replay/**`), T3.36 (`docs/plans/**`, `obsidian/05-EXPERIMENTS/_TEMPLATE-EXP.md`, `replay/stress.py`) — do not edit those.

## Measured (VPS, Everton's screenshot, 08/09 15:16 Brasília)
`RAYSOLUSDT`, same bar, same entry `1.16930116`, same exit `1.1865126569 · alvo`, same `+34,00 USDT`: three rows — `momentum/v4 pesquisa`, `momentum/v2 pesquisa`, `momentum/v3 paper`. v3 is a byte-identical paper copy of v2 (D10); v4 = v2 + higher `atr_pct_min`, which this signal passed. Not a table bug, but in "Todas as versões" the same operation counts three times in the rows **and** in the "Resultado das operações desta página" card.

## Deliver
1. **Grouping in the table** (`apps/web/components/lab/lab-signals-table*.tsx`, `lab-signal-row.tsx`, `lab-signals-query.ts`): when the list spans more than one version, rows with the same `(market, source_bar_close, entry price, exit price, exit reason, result)` collapse into one row that shows the strategy key once and **one chip per version** (`v2 · v3 paper · v4`, with the purpose colour already used); expanding the row (or the side panel) lists the per-version signal ids. Grouping is purely presentational and only within the loaded page; never across pages; with a single version selected nothing changes. Tests with the fixture of the screenshot (three identical signals → one row, three chips) and a near-miss (different exit → two rows).
2. **API field (backend, small)**: `GET /lab/shadow/signals` items gain `identity_key` (a stable hash of the tuple above, computed server-side from the envelope so the web does not re-derive Decimal equality) and `totals` gain `distinct_operations` per state (count over `identity_key`) — the honest denominator for the card. Regenerate `pnpm gen:types`. Tests (unit + one integration).
3. **Card "Resultado das operações desta página"**: when the page mixes versions, the heading becomes "Resultado das operações desta página (N únicas de M linhas)", the sums are over unique operations (first occurrence), and a one-line note says "versões irmãs decidem a mesma barra; cada operação é contada uma vez". Scope "de todas as concluídas" uses `totals.distinct_operations`. Tests.
4. **Tab counts** keep counting signals (they are signals), but each tab gets a `title` "N sinais · K operações únicas" from `totals.distinct_operations`.
5. **Docs**: `docs/plans/SHADOW-LAB.md` "Placar (T3.18)" paragraph on why sibling versions produce identical signals and how the UI reads them (do not edit §"Funil" — T3.36 is there).

## Prove
`pnpm --filter web lint|typecheck|test`, api tests per file, `ruff`/`pyright`; screen proof after the orchestrator rebuilds `docker-web-1` (`bash .claude/state/tmp/run-design-audit.sh -g "lab"`); reports in Portuguese, extended format; `.claude/state/notes-T3.38.md`.

You are backend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, `/opt/project-hunter`, HEAD `e81d53e` deployed 15:25 BRT / 18:25Z). First save this brief verbatim as `.claude/state/brief-T3.74e-atraso-constante.md`.

# Brief T3.74e — after the dispatcher deploy the decision lag is a CONSTANT ~55 s, not a queue (Everton: "tem que ser instantâneo")

**Facts (measured 18:39Z, 14 min after the deploy of T3.74c/d + T3.81):** `agent_signals.emitted_at − supporting_features.observation_ts` over the last 15 min: n=8, p50 54,7 s, p95 56,8 s; heartbeat `hb:strategy:shadow` `decision_lag_p50_s 54.1 / p95 56.2`; zero tracebacks; `hunter_shadow_bars_skipped_total` unknown. Before the deploy the lag was 20–170 s with a wide spread (queueing). A tight 54–57 s band on EVERY decision is a fixed wait, not contention: the prime suspect is that a 1-min/15-min bar is only considered closed when the NEXT bar's first tick/candle arrives (≈ 60 s minus the ~1–5 s the next candle takes), or a `BLOCK`/timer of ~55–60 s in the stream reader, or the coalescer publishing the bar `x=true` only at the next minute. Read `.claude/state/notes-T3.74c.md`, `notes-T3.81.md`, `services/strategy-worker/hunter_strategy_worker/{consumer.py, dispatch.py, context.py, decide.py}`, `services/market-worker/hunter_market_worker/{coalesce.py, persist.py, streaming.py}` and how `observation_ts` / `source_bar_close` are set (`record.py`, strategies' `Timeframe.M15` — is the decision taken on the 15-min bar close, and is that bar built from 1-min candles only after the 15th minute's candle arrives, plus something?).

## Deliver
1. **Prove where the 55 s are** (read-only on the VPS, foreground `timeout 290`): the new stage histograms (`hunter_shadow_stage_seconds{stage}` on the strategy-worker metrics endpoint — `curl` inside the container or via `docker exec … python -c`), `hunter_candle_flush_lag_seconds` on the market-worker shards, the Redis stream: for a few recent bars, the stream entry id time (ms) vs the candle `open_time + 1 min` vs `agent_signals.emitted_at` (SQL + `XRANGE` around the ids); and the strategy-worker logs with timestamps for one market (bar received → context loaded → decided → persisted). Write the timeline for 3 real decisions to the second.
2. **The fix** (with tests, unit + one testcontainer file, `timeout 290`, one at a time): if the bar-close confirmation waits for the next bar, use the exchange's own `x=true` (final kline) flag / the coalescer's close event instead of the next bar's arrival; if it is a reader `BLOCK`/timer, shorten it; if `observation_ts` is stamped wrong (e.g., bar open instead of close, making a correct 1–5 s lag look like 55 s), fix the stamp and say so loudly — that would change the lag METRIC, not the behaviour, and the notes must say which. Never change what a strategy reads (non-anticipation: a bar must be final before evaluation; the seven closure modules under `packages/core/hunter_core/strategies/` are untouchable); keep the byte-identical-decisions proof (`test_dispatch_benchmark.py`) green.
3. Docs: `docs/PIPELINE.md` §6b (one paragraph on how "bar closed" is detected and the expected lag), `obsidian/07-BUGS/Open Bugs.md` T3.73/T3.74 entry extended; `.claude/state/notes-T3.74e.md` with the timeline, commands/outputs and the full `git status --porcelain` list of your files; SQL `infra/scripts/sql/research/2026-09-10-t374e-*.sql`.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no VPS writes; no `git pull` on the VPS; do not edit `apps/**`; keep files ≤ 350 lines; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 12 lines with the timeline and the expected lag after the fix, times in Brasília (UTC−3 — compute it).

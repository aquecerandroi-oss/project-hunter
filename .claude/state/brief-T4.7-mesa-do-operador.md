# Brief T4.7 — a Mesa do operador meme: aval da compra, espera e venda pelo Everton (12/09/2026 04:0x BRT: "quero dar o aval da compra do meme, quanto de espera, vender; preciso o trader total funcionando")

**Leia primeiro, inteiro:** `.claude/state/contrato-T4.6-T4.7-mesa-meme.md` (o contrato congelado — tabelas, semântica do laço, rotas, tela), `.claude/state/notes-T4.3.md` (como a API/tela meme já foi construída: repositório sobre view, cursor keyset, estados honestos), `apps/api/hunter_api/routers/orders.py` + `services/orders*.py` + `admission_audit.py` (o padrão de POST com `Idempotency-Key`, papel OPERATOR e `audit_logs` que você vai espelhar), `docs/DESIGN.md`, `docs/plans/T4-MEME-RADAR-UI.md` (spec de tela), `docs/RISK_ENGINE_MEME.md` §12 item 6 ("caminho manual do operador provado em papel").

**Escopo (só `apps/**`, tipos gerados e testes):**
1. **API** (`apps/api/hunter_api/routers/meme_desk.py`, `services/meme_desk.py`, `repositories/meme_desk*.py`, `schemas/meme_desk.py`): as rotas do contrato §Rotas, exatamente com os códigos de erro e recusas nomeadas listados; `Idempotency-Key` obrigatório em todo POST; `audit_logs` em todo POST; VIEWER+ para GET, OPERATOR+ para POST (o mesmo papel que hoje libera `order-requests`). A API escreve **apenas** `meme_proposals.{status,decision,decided_by,decided_at}` (approve/reject) e `INSERT` em `meme_proposals` (manual) e `meme_operator_commands`; nunca toca `meme_paper_bets`. A T4.6 está criando as tabelas em paralelo (migração `0022_meme_lab`): construa contra o contrato e teste com DDL próprio no testcontainer (como a T4.3 fez), deixando um teste marcado `xfail(strict=False)` que roda contra a migração real quando ela existir (`alembic upgrade head`).
2. **Tela `/meme/mesa`** (`apps/web/app/(app)/[orgSlug]/meme/mesa/page.tsx` + `components/meme-desk/*`): tudo do contrato §Tela; atualização a cada 5 s (o padrão de polling que a tela `/meme` já usa); folha de aprovação com os 4 parâmetros pré-preenchidos; confirmação de "Vender agora" em um toque com o aviso "vende na próxima fotografia, não neste preço"; rótulo permanente **PAPEL — nenhuma transação real**; item de navegação "Mesa" sob o Meme Radar; `pnpm gen:types` quando a API mudar.
3. **Testes**: unit (serviço: transições de estado, recusas 409/422, idempotência), integração (repositório no testcontainer com DDL do contrato: approve/reject/manual/sell-now/cancel + auditoria), web (Vitest para formatação, contagem regressiva, estados honestos, folha de aprovação); `ruff`/`pyright`/typecheck/lint limpos; arquivos ≤ 350 linhas (`check_file_size.py`).
4. **Docs**: `docs/plans/T4-MEME-RADAR.md` §T4.7 (o que a mesa faz e o que não faz), `docs/API.md` (rotas novas) se existir esse arquivo, `.claude/state/notes-T4.7.md` (comandos e saída real).

**Regras:** nunca shell em background; tudo em primeiro plano com `timeout 290`; testcontainers 1 arquivo por vez; árvore compartilhada — nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; nunca `.env*`; nunca chave; nada de transação real (sem `ENABLE_MEME_LIVE_TRADING`, sem assinatura, sem `packages/exchange-adapters/**`, sem `services/**`, sem `infra/migrations/**` — se precisar de emenda no contrato, escreva a linha em "Emendas" e siga). `SendMessage` está desabilitado: você não fala com o agente da T4.6; o contrato é o acordo. Relatório ≤ 12 linhas em português, horários em Brasília (UTC − 3; `date` do Git Bash imprime UTC).

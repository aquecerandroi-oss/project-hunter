# Brief T4.28 — "liga sozinho no estágio 1": o executor real aprova a proposta da mesa sem clique, dentro do escopo pequeno (Everton, 16/09/2026 01:2x BRT: "liga sozinho no estágio 1"; antes: "precisa estar pronto hoje", "o robô fazer os trades da memecoin")

**Decisão do Everton (registrar em `obsidian/06-DECISIONS/2026-09-12-teste-pequeno-meme-real.md`, seção nova "Estágio 1 — sozinho (16/09)"):** no estágio 1 o robô compra e vende **sem o clique** em "Aprovar (REAL)". Escopo inalterado: `max_sol_per_trade 0,05`, `max_total_sol 0,25`, `max_trades 5`, validade 2026-09-18 (estender no `meme_gates.json`), tetos do `.env` `MEME_WALLET_MAX_SOL=0.30`, `MEME_MAX_SOL_PER_TRADE=0.05`, `MEME_DAILY_LOSS_CAP_SOL=0.15`, `MEME_MAX_OPEN_POSITIONS=2`, `MEME_COOLDOWN_S=60`. Risco máximo do estágio: 0,30 SOL. **Estágio 2 (US$ 1 000/operação) continua exigindo clique** até nova decisão escrita.

**Estado hoje:** o executor (`services/meme-executor`) só consome `meme_proposals` com `mode = 'live'` e `status = 'approved'` (`repo._CANDIDATES`), e nada nasce assim: a mesa (`operator/5`) nasce `proposed`/`mode = paper` e espera o clique; a auto-aprovação existente é só para `research_only` em papel. O `.env` da VPS **não tem** nenhuma chave real ainda (o Everton digita); o container do executor não existe até `MEME_LIVE=1` no `update`.

**Desenho (o worker não decide dinheiro real; o executor decide, com os 25 checks e o escopo pequeno):**
1. **Executor — modo `MEME_LIVE_AUTO_APPROVE`** (padrão `false`; só lido com `ENABLE_MEME_LIVE_TRADING` ligada e `small_test_authorization` válida; sem escopo pequeno → recusa `auto_approve_needs_small_test`): a cada tique de entradas, além das `approved`/`live`, o executor lê as propostas **`proposed`** do conjunto `operator` ativo (`kind = 'operator'`, `status = 'proposed'`, `expires_at > now()`, `mode = 'paper'` — a proposta é uma só) e, para cada uma, **abre a proposta real** exatamente como o `POST …/approve` com `"mode": "live"` faria (mesma função de serviço, reusada, não copiada: `apps/api/hunter_api/services/meme_desk_commands.py` → mover a lógica de aprovação live para `hunter_core` ou para um módulo compartilhado se hoje vive só na API; `decided_by = 'executor:auto_stage1'`, `decision` = `suggested`), e a admissão segue **inalterada** (25 checks, sizing, `small_test` teto e contador, cooldown, kill switch relido antes de assinar). A proposta de papel continua preenchendo em sombra (comparação papel × real, já existe no painel).
2. **Contador do escopo:** `small_test_scope_exhausted` já fecha a torneira depois de `max_trades`; também fechar quando `max_total_sol` for atingido (somar `sol_spent` das ordens `buy` confirmadas do escopo) — hoje só o contador de trades existe: conferir e completar em `entries.py`.
3. **Freios adicionais só deste modo:** (a) no máximo **1** compra automática por tique e por mint; (b) nunca aprovar proposta com `age` da proposta > 60 s (a `operator` expira em 180 s para humano; o robô decide na primeira passada); (c) `MEME_LIVE_AUTO_APPROVE_MAX_PER_HOUR` (padrão 5); (d) qualquer `refusal` da admissão grava a ordem `refused` **e** marca a proposta `rejected` com `decided_by = 'executor:auto_stage1'` e o motivo (a mesa mostra por quê); (e) o heartbeat `hb:meme:executor` ganha `auto_approve` (bool), `auto_approved_1h`, `auto_refused_1h` (por motivo), `small_test_used_sol`, `small_test_trades_done`.
4. **Mesa:** o painel "Executor real" mostra "**modo sozinho — estágio 1**" em vermelho semântico com o escopo restante (`n/5 compras, x/0,25 SOL`) e a lista das aprovações automáticas do dia (rótulos listados nas notas; `apps/web` fora desta tarefa — T4.28b).
5. **Docs:** `docs/ACTIVATION.md` §9b item novo "9. Modo sozinho (estágio 1)" com a linha do `.env` e como desligar (`MEME_LIVE_AUTO_APPROVE=false` + `update`, ou o kill switch); `docs/RISK_ENGINE_MEME.md` §3.4 (novo) e §12; `docs/DEPLOYMENT.md` §3.7.
6. **Testes:** unit do executor (proposta `proposed` do operator → aberta como live → admitida/recusada por nome; sem escopo → recusa; teto de SOL do escopo; 1/tique/mint; `age > 60 s` ignorada) + o testcontainer `test_live_persistence.py` com um caso ponta a ponta em `sigVerify=false` **sem envio** (o executor já tem fakes); `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; `.claude/state/notes-T4.28.md`; `git status --porcelain` ao final.

**O que fica com o Everton (declarar no relatório):** `.env` (flag, chave, RPC, 5 tetos, `MEME_GATES_FILE`, `MEME_LIVE_AUTO_APPROVE=true`), `meme_gates.json` com validade 2026-09-18, carteira ≤ 0,30 SOL. **O que fica com o orquestrador:** `MEME_LIVE=1 MEME=1 MEME_ENABLED=true bash infra/vps/compose.sh update`, prova em duas leituras, e a primeira ordem real observada de ponta a ponta.

**Regras:** primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; **nunca** `sendTransaction` na mainnet; flag nunca `true` em arquivo rastreado; não tocar `apps/web/**`, `services/meme-worker/**` (só leitura), `infra/migrations/**` (se precisar de coluna, relatar em vez de migrar — `decided_by` já é texto livre); `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

# Brief T3.74c — the decision must be instantaneous (Everton, 2026-09-10 12:20 BRT: "tem que ser instantâneo a entrada para não simular errado e pegar insights errados")

**Facts:** decision lag (`agent_signals.emitted_at − supporting_features.observation_ts`, SQL `infra/scripts/sql/research/2026-09-10-t374-q02-lag-por-hora.sql`): median 2,0 s on 01–05/09; 95–170 s on 09/09 and the night of 10/09; since 07Z of 10/09 (after T3.73 spot exclusion, T3.74 replay pause gate, no replay running) median 22–65 s, p95 80–154 s, rising with the hour; T3.74b context cache (deployed 10:25Z) shows no clear extra gain. `late:delay` fires at 120 s (`plan.py`), above 300 s the bar never becomes a signal. Roster: 11 active versions over the perpetual universe (~200 monitored markets); the worker consumes the candle stream (`consumer.py`, group `strategy-worker.shadow`), loads context per (family, market) (`context.py`, `context_cache.py`, `context_budget.py`, `required_context_minutes` up to 6000 min = 100 h of 1-min candles), evaluates, persists (`record.py`). Postgres was at 178 % CPU with no replay (T3.74 notes). Read `.claude/state/notes-T3.74.md`, `notes-T3.74b.md`, `notes-T3.73.md` §3 first. NOTE: a replay batch (T3.76) may be running on the VPS today — the replay lane pauses itself on `consumer_lag`; measure the live lane with and without it and say which. **Target: median < 5 s, p95 < 20 s, zero `late:delay` in normal hours.**

## Deliver
1. **Where the seconds are** (read-only on the VPS, foreground `timeout 290`): per-stage timing from the worker's own metrics endpoint/heartbeat and logs (stream read wait, context load per bar, evaluate, persist) — if the stages are not instrumented, add the instrumentation FIRST (histograms in `metrics.py`, `decision_lag_s` p50/p95 in `health.py`/heartbeat), the orchestrator deploys it, and you measure; `XINFO STREAM/GROUPS`, `XPENDING`; `docker stats --no-stream`; Postgres `pg_stat_activity` wait events and the top queries by total time if `pg_stat_statements` is on (read-only); the per-bar fan-out (bars/min × versions × markets) vs what one worker can do serially. Write the budget: seconds per bar today, and where.
2. **The fix, biggest lever first**, implemented with tests (unit + one testcontainer file, one invocation, `timeout 290`): candidates — (a) do not load 100 h of 1-min candles per market per bar: keep a rolling in-memory window per (family, market) fed by the stream itself, refilled from Postgres only on gaps/restart (bounded by `SHADOW_CONTEXT_MAX_MINUTES`), with the T3.74b byte-identical-decisions proof extended to this path; (b) parallelism: shard the shadow consumer by market (like the market-worker's 4 shards — `MARKET_SHARDS`), or an asyncio worker pool bounded by the DB pool, preserving per-market ordering and the non-anticipation rule; (c) batch the persist step; (d) evaluate only bars that closed (skip bars whose `observation_ts` is already older than the `late:delay` threshold with a named skip reason and a metric, so the backlog never snowballs into 300 s — this is a safety valve, not the fix). Never change a strategy parameter, a version, the `code_ref` closure (the seven modules under `packages/core/hunter_core/strategies/`), or the eligibility gates; do not edit `gate_policy.py`, `regime_gate.py`, `hours_gate.py`, `variant.py`, `catalogue.py` (another agent, T3.77, owns them today) — if you must touch `context.py`, keep the edit minimal and say so in the notes.
3. Prove locally with the existing benchmark (`services/strategy-worker/tests/test_context_cache_engine.py`, 11 versions × 16 markets) and a new one at 11 × 200 markets synthetic: bars/s before vs after, and the decisions byte-identical.
4. Docs: `docs/PIPELINE.md` §6b (the latency budget and the safety valve), `docs/DEPLOYMENT.md` if a new env/shard setting exists (compose changes go through `infra/docker/docker-compose.yml` + `infra/vps/docker-compose.prod.yml`, and `compose.sh` if a profile is needed — say exactly what the orchestrator must export), `obsidian/07-BUGS/Open Bugs.md` T3.73/T3.74 entry (root cause + fix), `.claude/state/notes-T3.74c.md` with every command/output and the full `git status --porcelain` list of your files, research SQL `infra/scripts/sql/research/2026-09-10-t374c-*.sql`.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no `git pull` on the VPS; no VPS writes; do not edit `apps/**`; keep files ≤ 350 lines; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 12 lines with the before/after numbers, times in Brasília (UTC−3).

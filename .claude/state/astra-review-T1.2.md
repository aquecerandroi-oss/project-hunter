The adapter is **not ready for M1 acceptance**. I reviewed the tracked diff and untracked files against both documents. No files were modified.

**Must-fix findings:**

1. **[P1] Connection-state contract crashes the worker watchdog.** [ws.py:128](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/ws.py:128) returns `ConnectionState` dataclasses, but [supervision.py:101](C:/dev/project-hunter/services/market-worker/hunter_market_worker/supervision.py:101) calls `state.get(...)` and expects `last_event_at`. I reproduced `AttributeError: 'ConnectionState' object has no attribute 'get'`. Reconcile the representation and progress field across the boundary. Also expose targeted connection restart; otherwise silencing public restarts the entire stream, contrary to the agreement.

2. **[P1] Incremental universe changes are unsupported.** [BinanceAdapter](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/__init__.py:71) exposes only static `stream(...)`; it has no `update_subscriptions`. **Scenario:** the first refresh that adds or removes a symbol reaches [streaming.py:50](C:/dev/project-hunter/services/market-worker/hunter_market_worker/streaming.py:50) and raises `RuntimeError`. Implement subscription differences while preserving retained markets’ subscriptions.

3. **[P1] Candle normalization loses information required by the agreed contract.** [streams.py:222](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/streams.py:222) discards top-level `E`; normalized candles have no `ts`. **Current failure:** the worker rejects every partial candle because it cannot order updates. Both parsers also retain Binance’s inclusive close time—my fixture produced a `59.999s` interval—rather than the agreed exclusive boundary. Finally, [rest.py:156](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/rest.py:156) determines finality using local time; a clock behind the exchange incorrectly rejects already-closed candles. Preserve WS update time, normalize closing boundaries, and use an exchange-derived cutoff.

4. **[P1] Realized funding is mixed with current observations and cannot populate history.** [normalize.py:263](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/normalize.py:263) combines a historical rate with current mark price and `premium.time`, dropping `fundingTime`. **Scenario:** repeated fetches make the same settlement appear newly timestamped. Moreover, the adapter lacks the history capability required by [funding.py:71](C:/dev/project-hunter/services/market-worker/hunter_market_worker/funding.py:71), so the current worker explicitly disables realized-funding ingestion. Return settlement records with their original timestamps separately from estimates.

5. **[P1] The WS handoff is unbounded and reader failures are detached.** [ws.py:139](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/ws.py:139) creates an unlimited queue and launches readers with `ensure_future`; the consumer only waits on that queue. **Scenarios:** a blocked downstream writer permits unlimited memory growth; an unexpected reader failure leaves consumption waiting indefinitely. I reproduced a completed, failed reader with its consumer still pending. Bound buffering with observable overflow handling, and propagate reader failure or unexpected return through structured supervision.

6. **[P1] Duplicate/older frames count as progress and can regress ticker prices.** [ws.py:272](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/ws.py:272) advances progress and updates cached trade price without checking event identity/order. I reproduced an identical frame advancing monotonic progress from `10` to `20`, and an older trade replacing price `200` with `100`. **Scenario:** the next fresh book ticker republishes that old price with a fresh timestamp; repeated duplicates also defeat the no-progress watchdog. Gate progress and cached prices on accepted events.

7. **[P1] Rate limiting does not enforce a reliable shared budget or cooldown.**
   - [rest.py:105](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/rest.py:105) acquires budget outside the retry loop: my reproduction made **three network requests with one acquisition**.
   - [rate_limit.py:169](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/rate_limit.py:169) blindly replaces remaining tokens. An older response reporting usage `90` after `95` restored five spent tokens; Redis reconciliation can likewise overwrite concurrent reservations.
   - A `429/418` raises only for that caller; other callers receive no shared cooldown. Funding history also lacks its separate **500 requests/5 minutes/IP** limit.

   **Scenario:** concurrent recovery/polling continues sending requests after exhaustion and risks an IP ban. Charge every attempt, reconcile accounting atomically with window awareness, and enforce shared cooldowns and endpoint-specific limits. Binance documents these [IP backoff requirements](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/general-info#ip-limits) and the [funding limit](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/rest-api/market-data#get-funding-rate-history).

8. **[P2] Reconnection does not meet the agreed lifecycle.** [ws.py:222](C:/dev/project-hunter/packages/exchange-adapters/hunter_exchanges/binance/ws.py:222) resets backoff after every handshake: repeated handshake-success/immediate-disconnect produced delays `[1, 1, 1]`, never exponential growth. The age check cannot interrupt a blocked `recv()`; I reproduced no rotation after its deadline. Rotation also closes the old connection before opening its replacement, with no rotation jitter or actual overlap. **Scenario:** a flapping endpoint gets rapid retries, while scheduled rotation creates avoidable data gaps. Reset backoff after real progress and implement deadline-driven, jittered overlapping rotation.

**Nice-to-have:**

- Record explicit fixture provenance—capture time, route, and whether synthetic. The recorder permits synthetic fallback, but the files do not distinguish it.
- Strengthen tests around actual snapshot-level removal, reordered responses, malformed nested payloads, and repeated post-handshake disconnects. The current snapshot “replacement” test only parses two independent books.
- Make the fake adapter model the same incremental subscriptions, funding-history capability, and connection-state contract used in production.

**Agreements:** The public/market route split and unsuffixed `@depth20` match the plan and [Binance’s routing notice](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/websocket-market-streams/Important-WebSocket-Change-Notice). With unique channels, grouping gives 400 public and 800 market streams per 200 symbols. Keeping `kind="book"` plus `is_snapshot=True`, using Decimal/UTC normalization, and isolating fixtures/fakes under `testing` are sound choices.

Validation: **96 passed, 2 live tests deselected**, using the existing interpreter with bytecode and pytest caching disabled. Additional in-memory reproductions confirmed the failures noted above. Live payload receipt on all six channels remains unverified; the existing live tests cover only REST ticker/time.
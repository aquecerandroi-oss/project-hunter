# Brief T3.74g — with 4 shards the bar still takes 37–43 s: the CPU cost per (market, version) evaluation is the wall (Everton: "quero tudo instantâneo, deixe nos trinques")

**Facts:** VPS HEAD `b339117`, `STRATEGY_SHARDS=4` (crc32 % 4, ~50 markets each), per shard pool 5+5 and `SHADOW_WORKER_CONCURRENCY=8`; Postgres max_connections 100 (38 in use now). T3.74f profile: one Python process pinned at 97–100 % of ONE core for the whole burst; Postgres 20–25 %. First 4-shard bar (21:00Z): 3 signals, p50 37,1 s, max 43,2 s; per-shard heartbeats `decision_lag_p50_s` = 5,4 / 37,1 / 43,2 (one shard fast, two slow → the cost is proportional to the number of (market × version) evaluations a shard owns: ~50 markets × 10 live versions ≈ 500 evaluations per 15-min boundary ≈ 40 s → **~80 ms CPU per evaluation**). Target: p50 < 5 s / p95 < 20 s ⇒ ≤ ~8 ms per evaluation, or ~10× less CPU. Roster: mean_reversion v1, v2, v3, v6, v7, v8, v10, v14 (8 versions of ONE family: same indicators, different parameters), mean_reversion_h1 v1, momentum v3. Read `.claude/state/notes-T3.74b.md` (context cache per family — the WINDOW is shared, but what about indicator computation?), `notes-T3.74c.md`, `notes-T3.74f.md`, `services/strategy-worker/hunter_strategy_worker/{consumer.py, dispatch.py, context.py, context_cache.py, decide.py, record.py}`, `packages/core/hunter_core/strategies/{base.py, mean_reversion_v1.py, momentum_v1.py, indicators…}` (READ ONLY — the seven closure modules must not change; `code_ref` digest must stay identical, verify with the existing digest test), `packages/indicators/hunter_indicators/**` (ATR, trend timeframe aggregation, envelope/canonical helpers).

## Deliver
1. **Profile** (locally, testcontainer or the recorded-bar harness of `test_dispatch_benchmark.py`/`test_context_cache_engine.py`): `cProfile`/`pyinstrument` of one full 15-min boundary for one shard-equivalent (50 markets × 10 versions) — top 20 functions by cumulative time; which fraction is indicator recomputation (ATR over 6000 minutes, 15-min/1-h aggregation from 1-min candles, Decimal arithmetic), which is Python object churn (building `Candle`/`Bar` objects per version), which is DB (context_load, persist), which is the gate/policy. Numbers in `.claude/state/notes-T3.74g.md`.
2. **The cut, biggest first, without changing any decision**: (a) compute the family's shared features ONCE per (market, bar) — aggregated 15-min/1-h bars and ATR series at each parameter set actually used by the live versions (dedupe by parameter value: v1..v14 may share `atr_period`/`trend_timeframe`) — and hand each version a precomputed feature view instead of raw 1-min candles; (b) incremental aggregation across bars (keep the aggregated series per (market, timeframe) in the T3.74b cache and append the new 15 minutes instead of rebuilding from 6000 rows); (c) avoid per-version object rebuilds. All of this lives in the strategy-worker/indicators layer — the strategies' `evaluate` signature and the closure modules stay untouched; if a strategy insists on raw candles, the worker passes the same rows it does today. **Proof**: decisions byte-identical to today's path on the recorded sequence (extend `test_context_cache_engine.py` equivalence) and on the replay engine (`replay/*` must use the same path or prove it is unaffected); `code_ref` digests unchanged (test); benchmark before/after in ms per evaluation and seconds per boundary for 50×10.
3. If (a)+(b) cannot reach ~8 ms/evaluation, say what remains and whether the shadow universe for research versions (200 markets vs the 16 with 90-day history) is the next lever — as a question for the orchestrator, not a change.
4. Docs: `docs/PIPELINE.md` §6b (evaluation cost budget), `obsidian/07-BUGS/Open Bugs.md` T3.73/T3.74 entry; `.claude/state/notes-T3.74g.md` with profile, commands/outputs and the full `git status --porcelain` list of your files. Files ≤ 350; `ruff`/`pyright`/`check_file_size.py` clean.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no VPS writes; no `git pull` on the VPS; do not edit `apps/**`; never change strategy parameters, versions, gates or the seven closure modules. Report in Portuguese ≤ 12 lines with the profile top-5 and the before/after ms per evaluation, times in Brasília (UTC−3, compute it).

# Brief T4.19 — `operator/3`: a mesa passa a propor pela porta E1 (fluxo e holders) com exclusões E2, e cada proposta traz o plano para executar à mão (Everton, 12/09/2026 17:3x BRT: "coloca aí qual entra, quanto comprar e na mesma hora qual horário vender")

**Contexto:** o Everton vai executar o teste real **à mão** no Terminal, com a carteira observada (`MEME_WATCH_WALLETS`, T4.12 grava a compra/venda real e o contexto do Lab sozinho). A mesa hoje propõe pelo `operator/2`, que herdou a porta `comprar_cedo_na_curva/1` da EXP-M1 (**reprovada**: 9/9 negativas). A porta que o estudo das 21 apostas escolheu é a E1 (`flow_v2/1`, EXP-M5) com as exclusões E2 (`pedigree.py`, EXP-M6) — `research_only`, e os seus sinais não chegam à mesa. As propostas ao operador expiram em 120 s; para uma compra à mão, 180 s.

**Entregar:**
1. Migração `0032_meme_operator_3` (+ `ddl/meme_operator_3.py`): semeia `operator/3` (`kind = operator`, `clock = 15s` como o `flow_v2/1`) com **a mesma porta e as mesmas exclusões** do `flow_v2/1` (reutilizar `FLOW_V2_PARAMS` como base — nunca copiar à mão — trocando só: `size_sol = 0.05`, `ttl_s = 180`, `max_open_positions = 2`, saídas alvo 3×, trailing 35 % após 1,5×, `max_hold_s = 1800`, `creator_dump`, `line_broken` quando houver linha, `max_loss` 50 %); **aposenta `operator/2`** na própria migração (como a 0029 fez com o `operator/1`) e o `downgrade` reverte os dois. A mesa continua com exatamente um conjunto `operator` ativo (`meme_rule_set.py` já exige isso).
2. **Plano para executar à mão** no payload da proposta (`suggested.manual_plan`, gerado no worker em `proposals.py`/`proposals_reasons.py` na hora da proposta, e exposto pela API em `ProposalOut.manual_plan` + `pnpm gen:types`): texto curto em português com horários de Brasília — `"Comprar 0,05 SOL de <TICKER> até HH:MM:SS (proposta expira). Vender até HH:MM (30 min) — antes disso se triplicar (3×), se cair pela metade (−50 %), se o dev vender, ou se a linha de suporte quebrar."` — todos os números lidos dos `params` do conjunto (nunca fixos no texto), horas de `proposed_at + ttl_s` e `+ max_hold_s`.
3. O laço: as propostas `operator` do relógio de 15 s seguem o mesmo caminho das do minuto (aval → fill na fotografia seguinte); teste de que `operator/3` propõe **a mesma moeda** que `flow_v2/1` no mesmo tique (mesma porta) e que a expiração é 180 s.
4. Testes unit + 1 testcontainer (`test_migrations.py -k 0032` e a persistência da proposta com `manual_plan`); `HEAD_REVISION` → `0032_meme_operator_3`; `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; docs `docs/DATABASE.md`, `docs/plans/T4-MEME-RADAR.md` §T4.19, `docs/RISK_ENGINE_MEME.md` §10; `.claude/state/notes-T4.19.md`; `git status --porcelain` ao final.

**Não tocar (outra agente, T4.2g, está editando):** `services/meme-worker/hunter_meme_worker/{sources,source_stats,main,collect,trades*,tape*,features*,fold,repo_tape}.py`, `packages/exchange-adapters/**`. Também não tocar `apps/web/**` (T4.17 em curso) além de `pnpm gen:types`. Regras de sempre: primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 10 linhas em português, horários em Brasília.

# Brief T2.4 — `hunter_indicators.regime` (v0) + `hunter_indicators.opportunity` (scorer, status, explicação)

**Owner:** quant-engineer (opus). **Revisores depois:** outro quant-engineer (cruzado), code-reviewer, Astra (`bash infra/scripts/astra.sh ask T2.4-scorer "..."`). **Não commitar.** **Regra operacional: nunca use Bash em background; cada comando em primeiro plano com timeout ≤ 5 min.**

## Leia antes (nesta ordem)
1. `CLAUDE.md`. 2. `docs/plans/M2.md`: linha **T2.4** e a **"Decisão conjunta"** (Score, Envelope e `_live`, Episódios e status, Regime v0, Custo) — prevalece. 3. `docs/PIPELINE.md` §4–§5 (regime, componentes do score, explicação). 4. O que já existe e é a fonte (tudo commitado): T2.2 `packages/indicators/hunter_indicators/features/**` (`FeatureVector`, `quality`, `provenance`, ATR checkpoint), T2.3 `packages/indicators/hunter_indicators/{baselines,anomalies,stage}/**` (`BaselineProjection`, `AnomalyEvaluation`/`AnomalyState`, `StageDecision`/`StageState`, `NormalizationConfig` MAD→severidade), `notes-T2.2.md`, `notes-T2.3.md`; T2.1 (`packages/core/hunter_core/db/models/analysis*.py`, `docs/DATABASE.md` §17: `opportunities.{stage,explanation,below_40_since}`, `opportunity_history.envelope`, `opportunity_weights` v2 ativa com `components_frozen: false` — **a T2.4 ratifica esse vetor de pesos ou publica v3** via `infra/scripts/seed_weights.py`/`seed_reference.py`, §17.8); `MarketRegime.UNKNOWN`, `OpportunityStatus.EXTENDED` em `packages/core/hunter_core/domain/enums.py`. 5. `packages/core/hunter_core/strategies/{numeric,canonical}.py`. 6. `.claude/rules/astra-second-opinion.md`.

## Entregar (arquivos permitidos: `packages/indicators/hunter_indicators/{regime,opportunity}/**`, `packages/indicators/tests/**`, `infra/scripts/seed_reference.py` (só o bloco de `opportunity_weights` v2/v3 e `stage`/`normalization` se precisar acrescentar chaves), `.claude/state/notes-T2.4.md`)
- **Regime v0** (`regime/`): funções puras sobre features/candles persistidas fornecidas pelo chamador (nunca REST): tendência por retornos 4 h / 1 d vs ATR (limiares versionados), volatilidade 1 h vs mediana de 30 dias das candles de 1 min (warm-up explícito → `MarketRegime.UNKNOWN` com motivo em `supporting_features`), regime por mercado e agregado (`breadth` só com cobertura ≥ 80% e composição gravada), último regime para exibição com carimbo `stale`; `RegimeDecision` com `supporting_features` explicável e versão.
- **Opportunity scorer** (`opportunity/`): componentes 0–100 **com direção** (Momentum, Volume, Liquidity, Order Flow, Derivatives, Regime, Anomalies, Agent Consensus = 0 até M4, Early-Movement assinado `e ∈ {+1, 0, −1}` derivado do `StageDecision`), cada componente a partir de `d` MAD com a transformação por trechos versionada da T2.3 (`min_scale` por feature ou componente indisponível), componentes não-MAD declarando transformação própria; `score = clip(Σ pesos_i × c_i + 10·e, 0, 100)` com os demais pesos somando 0,90 (`opportunity_weights` versão ativa; ausência de componente **não redistribui peso, reduz `confidence`**); score canônico `Decimal` 2 casas ROUND_HALF_EVEN antes de status/persistência (`Numeric(5,2)`), `confidence` 4 casas, componentes/contribuições 4 casas; chaves ordenadas; **reprodutibilidade byte a byte** (`canonical_json` da decomposição); `baseline_ids`, `regime_id`, versões (features, baselines, pesos, normalização, estágio) no envelope.
- **Máquina de status** (pura, com estado de episódio): `NORMAL/WATCHING/ANOMALY/HOT/ENTRY_CANDIDATE/EXTENDED/EXPIRED` (`RISK_BLOCKED`/`IN_POSITION` derivados na leitura, não aqui), precedência EXPIRED → EXTENDED → ENTRY_CANDIDATE → HOT → ANOMALY → WATCHING → NORMAL; NORMAL não inicia episódio mas é estado temporário de episódio aberto (mantém id, `below_40_since`); expira após 15 min **comprovados por dados válidos** < 40 (perda de qualidade interrompe a contagem); recuperação posterior cria episódio novo; limiares versionados em `opportunity_weights.weights`; teste 80 → 35 → 45 mantém o id, com restart e entrega duplicada.
- **Explicação determinística em pt-BR** (`explanation` JSON: componentes, contribuições, anomalias ativas, estágio, regime, frases curtas geradas de templates versionados — sem LLM), mesma entrada → mesmos bytes.
- **Histórico**: regra de amostragem (delta ≥ 3 contra o último persistido, a cada 5 min, e em mudança de status/stage/versão/qualidade) como função pura `should_record_history(prev, now)`.
- **Pesos v2**: ratifique o vetor da T2.1 (`components_frozen: true`) ou publique v3 com a justificativa; se mudar, o seed continua "inserir ausente, verificar existente"; teste de contrato carregando `seed_reference.py` por caminho (como `test_weights_contract.py`).
- **Testes**: reprodutibilidade byte a byte; tabela de casos de status (todas as transições e a precedência); histerese/expiração com perda de qualidade; regime com warm-up e `UNKNOWN`; breadth com cobertura < 80%; ausência de componente reduz `confidence` sem redistribuir peso; Early-Movement assinado nos três valores; `Decimal` sob contexto fixo; explicação determinística; `should_record_history`.

## Regras
TDD; Decimal sob `CONTEXT`; UTC; sem look-ahead; sem REST; ≤ 350 linhas por módulo. Comandos: `uv run pytest packages/indicators -q`, `uv run ruff check packages/indicators infra/scripts`, `uv run ruff format --check packages/indicators infra/scripts`, `uv run pyright packages/indicators infra/scripts`, `uv run python infra/scripts/check_file_size.py`. PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Não toque em `features/**`, `baselines/**`, `anomalies/**`, `stage/**` (se precisar de algo, adaptador no seu pacote + nota), `packages/core`, `.env*`, `apps/**`, `services/**`.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma; seed se mudou) · TESTS · TEST RESULTS (saída real) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · NEXT STEP.

You are frontend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; always absolute paths and `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T3.72-ordem-manual-web.md`.

# Brief T3.72 — "Nova ordem paper" on the Carteira screen (Everton, 2026-09-10: "volta a produzir o sistema na parte M3" / "Deixa pronto para uso")

**Fact:** the Carteira screen (`apps/web/app/(app)/[orgSlug]/portfolio/page.tsx`, components under `apps/web/components/portfolio/`) is GET-only; `portfolio-proposals-empty.tsx` shows an empty state. A backend agent is building, in parallel, the write path with this frozen contract (build against it; it may not be merged when you start — write the client with `zod` schemas that mirror it and mock in tests; check `.claude/state/notes-T3.68.md` for any deviation before finishing):
- `POST /api/v1/portfolios/{portfolio_id}/orders`, role TRADER+, header `Idempotency-Key` (generate a UUID per form submission, keep it for retries of the same submission), body `{ market_id: uuid, direction: "long" | "short", stop: string (decimal), requested_notional: string | null }` → `202 { request_id, status: "pending" | "decided", filed_at, decision: null | RiskDecision }`; 409 `order_replay_conflict` / `wallet_not_open`; 422 named reasons (non-spot market, bad stop); 403 for VIEWER.
- `GET /api/v1/portfolios/{portfolio_id}/orders/{request_id}` (same shape + `outcome`), `GET /api/v1/portfolios/{portfolio_id}/orders` (cursor page, newest first).

## Deliver
1. A "Nova ordem paper" action on the Carteira screen (TRADER+ only; hidden/disabled with the reason for VIEWER; disabled with the reason when the kill switch is latched or the wallet is not open — those states already come from the existing wallet/risk GETs, reuse them). Form: executable SPOT market (search/select over the existing markets endpoint filtered to `market_type=spot`, active, monitored — reuse the existing markets client; show the 24 h volume and the 50 M floor state), direction (long; short shown disabled with "SPOT: só compra" unless the API allows it), stop price (validated > 0 and on the correct side of the last price; show the implied stop distance % against the 3 % cap `max_stop_distance_pct` from the risk-limits GET), optional notional. Submit → optimistic "Enviada, aguardando decisão do motor" card → poll the GET every 2 s for ≤ 60 s → show the `RiskDecision` (limitante vencedor, tamanho, motivo de recusa) using the vocabulary already in `docs/RISK_ENGINE.md`/the Risk Center schema; errors mapped to Portuguese sentences (no raw codes).
2. The proposals/requests table on the same screen fed by the list GET (replace the empty state when rows exist; keep it when none), with `AutoRefresh` like the Lab.
3. All copy in Portuguese, times via the existing Brasília helper (T3.22), design per `docs/DESIGN.md` (polished, dark/light, 375/768/1440). Tests: Vitest for the schemas/mapping/form validation; a Playwright spec for the flow with the API mocked at the network layer (real backend may not exist yet) — note: Chromium cannot reach localhost on this machine since 08/09 (see memory `in-app-browser-limits.md`); if `pnpm exec playwright test` fails on connectivity, say so and rely on Vitest + `pnpm -F web build`/`typecheck`/`lint`.
4. `.claude/state/notes-T3.72.md` with commands/outputs and the `git status --porcelain` list of EVERY file touched/created; ≤ 10 lines in Portuguese.

Rules: never a background shell; every command foreground with explicit `timeout 290`; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; edit only under `apps/web/**` (and `docs/DESIGN.md` only if a token is added). Report in Portuguese, times in Brasília (UTC−3).

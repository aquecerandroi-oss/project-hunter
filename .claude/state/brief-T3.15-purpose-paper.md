# Brief T3.15 — `purpose` na versão da estratégia e o rótulo `paper` (D10)

**Owner:** backend-specialist. **Revisores depois (obrigatórios):** database-architect (migração), risk-engine-guardian, security-reviewer. **Não commitar.** **Regra operacional: nunca Bash em background; comandos em primeiro plano com timeout ≤ 5 min; suítes com testcontainers um arquivo por invocação; Astra indisponível até 2026-09-12.** Base: `main` em `fa09b5d`.

**Colisão evitada:** outro agente está editando `services/execution-worker/**` (T3.14b) e `tests/integration/paper/**` (T3.9b). **Não toque** nesses caminhos. O portão da ponte (`bridge_screen.py:53,157`) fica para a **T3.15b**, um passo curto depois da T3.14b — deixe a constante e o teste prontos em `packages/core` para ele só importar.

## Leia antes
`docs/plans/M3.md` linha da T3.15 e D10 (linha 132), `.claude/state/decisions-delegated-2026-09-07.md` §D10, `packages/core/hunter_core/strategies/envelope.py` (`PURPOSE_RESEARCH_ONLY`), `packages/core/hunter_core/admission/sources.py:7,62,259` (`PURPOSE_LIVE` e o portão), `services/strategy-worker/hunter_strategy_worker/record.py:198,229` (literal cravado), `packages/core/hunter_core/db/models/agents.py` (`strategy_versions`), `infra/migrations/versions/0009_*.py` (padrão: DDL em `infra/migrations/ddl/*.py`, round trip, privilégios), `infra/scripts/activate_strategy_version.py` (ou o script de ativação que existir — `git grep -n "activate" infra/scripts`), `docs/DATABASE.md` §18–§21, `docs/SHADOW-LAB.md` §10, `docs/SECURITY.md` §2.

## Entregar
1. **Migração `0010_strategy_purpose`**: coluna `strategy_versions.purpose` (enum novo ou `text` com CHECK: `research_only | paper | live`; default `research_only`; NOT NULL; backfill das linhas existentes = `research_only`), **imutável depois da ativação** com a mesma técnica de `code_ref` (trigger/constraint que existir para `code_ref` — reutilize o padrão, não invente outro); grants: `hunter_worker` lê; `hunter_app` lê; quem escreve é o script de ativação (papel de migração/owner, como hoje para a ativação). `alembic check` limpo, round trip, testes de privilégio em `packages/core/tests/integration/test_schema_privileges.py`. `docs/DATABASE.md` seção nova.
2. **Envelope do sinal lê o propósito da versão**: `record.py:198,229` deixam de cravar `research_only` e passam a copiar `strategy_versions.purpose` da versão que emitiu; `PURPOSE_RESEARCH_ONLY` continua o default de tudo que não tem versão. Teste: versão `paper` → sinal `purpose=paper`; versão `research_only` → `research_only`; `live` **nunca** é escrito pelo worker (recusa na origem, com log).
3. **Portão de admissão** (`admission/sources.py`): `PURPOSE_LIVE` sai do caminho da carteira paper; nova constante `PURPOSE_PAPER = "paper"` (em `packages/core/hunter_core/strategies/envelope.py`, ao lado de `PURPOSE_RESEARCH_ONLY`, para a T3.15b importar); a admissão aceita **`paper`** e recusa `live` **por nome** com mensagem "live é Fase 4; ENABLE_LIVE_TRADING=false" e recusa `research_only` como hoje. A ordem manual da API (`apps/api/hunter_api/services/admission.py` — única edição em `apps/**`) passa a nascer com `purpose=paper`. Testes de admissão para os três rótulos.
4. **Script de ativação** sabe criar a linha nova `purpose=paper` a partir de uma versão existente (parâmetros copiados bit a bit, `code_ref` recalculado, `is_active=false` ao nascer, coorte `research_only` intocada), com `--dry-run` que imprime o que faria e **sem** ativar nada. Teste do script contra Postgres (testcontainers). **Nada é ativado nesta tarefa.**
5. `docs/PIPELINE.md`/`docs/SHADOW-LAB.md`: o campo `purpose` do envelope agora vem da versão; os três rótulos e quem aceita o quê.

## Provar
`uv run alembic check`, round trip da 0010, `packages/core/tests/integration/test_schema_*.py` por arquivo, `packages/core/tests/unit`, `services/strategy-worker/tests` por arquivo, `packages/core/tests/integration/test_admission*.py`, `apps/api/tests/unit/test_admission_adapter.py`, `ruff check`/`format --check`/`pyright` nos pacotes tocados, `check_file_size.py`. Nada em `.env*`. Relatório em português no formato estendido com saída real; `.claude/state/notes-T3.15.md`.

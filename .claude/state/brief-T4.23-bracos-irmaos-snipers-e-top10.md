# Brief T4.23 — os braços irmãos que o fechamento de 13/09 propôs: snipers > 2 e top-10 em 18–26 % (15/09/2026 16:2x BRT)

**Medido pelo fechamento diário de 13/09 (`obsidian/09-OPERATIONS/Diario-Meme/2026-09-13.md` §6.4 e §6.5, 66 apostas medidas, IC 95 % por blocos de hora):** snipers **alto (> 2)** rendeu R médio +0,25 (n = 17) contra −0,28 nas demais (n = 49) — Δ +0,53 R, IC [0,23, 0,88]; top-10 **médio (0,1767–0,257)** rendeu +0,25 (n = 14) contra −0,25 (n = 52) — Δ +0,50 R, IC [0,14, 1,36]. Contradiz o pré-registro original da E1 (`snipers ≤ 2`). Pela régua (KB-0092) isso **não muda conjunto vivo**: vira dois braços irmãos pré-registrados como `descartar`, medidos ao lado. Ler também `.claude/state/lote-meme-2026-09-14.md` §2 e `lote-meme-2026-09-15.md` §2 (o de 14/09 pode ter proposto mais braços — pré-registrar só os que passaram a régua nos dois fechamentos ou que repetem o de 13/09).

**Entregar (mesmo padrão da T4.21/T4.22 — `ddl/meme_gate_e1_arm2.py`, `0034`, `test_0034_*`, `notes-T4.21.md`):**
1. `rules.py`/`rules_criteria.py`: `min_snipers` (int, recusa `snipers_below_min`) e `min_top10_share` (fração, recusa `top10_below_min`), opcionais e desligados por padrão; `as_parameters` só quando pedidos; validações; testes unit (≥ 6). Manter `rules.py` ≤ 350 linhas (hoje 349 — mover docstrings ou extrair validações para `rules_validation.py` se preciso).
2. `lab_models.py` lê os dois; `GateRow` já carrega `snipers` e `top10_share`.
3. Migração `0037_meme_e1_arms_3_4`: `flow_v2/3` = `flow_v2/2` (`ARM2_OVERRIDES` sobre `FLOW_V2_PARAMS`) + `min_snipers 3`, `max_snipers 10`; `flow_v2/4` = `flow_v2/2` + `min_top10_share "0.1767"`, `max_top10_share "0.257"` (ambos `research_only`, EXP-M5, relógio 15 s, ids `…000e`/`…000f`); nada aposentado; downgrade recusa com referências; `HEAD_REVISION → 0037`; o `test_0036_*` passa a estagiar em `CREATOR_WATCH_REVISION = "0036_meme_creator_watch"` (padrão `_staged_at`); `test_0037_*` (seeds byte a byte = flow_v2/2 + as chaves; reverses).
4. Obsidian: `EXP-M5-fluxo-e-holders.md` ganha "Braços 3 e 4 (15/09/2026)" (append-only) com os números acima, previsão `descartar` e a nota de que **contradizem o braço 1**; docs `DATABASE.md` §49, plano §T4.23, `RISK_ENGINE_MEME.md` §10.13; `obsidian_lint.py` limpo.
5. Provas: `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; indicators `-k meme`, worker unit, `test_migrations -k "0036 or 0037 or alembic_check or upgrade_head"` (Postgres); `.claude/state/notes-T4.23.md`; `git status --porcelain` ao final.

**Regras:** primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real; não tocar `apps/web/**`, `services/meme-executor/**`, `creator_watch.py`, `sources.py` (outra agente, T4.2h-b, está neles) nem `obsidian/02-MARKET`/`00-INBOX`/`.claude/state/plantao-meme` (plantão); `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 10 linhas em português, horários em Brasília.

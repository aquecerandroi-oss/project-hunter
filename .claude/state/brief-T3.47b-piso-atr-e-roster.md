# Brief T3.47b — a próxima alavanca depois do stop largo: o piso de ATR% (que recusa 67–89 % das barras) na mean_reversion; e o roster volta a um tamanho honesto (aposentar as três variantes descartadas)

**Owner:** quant-engineer (continuation of T3.47 — read `.claude/state/notes-T3.47.md` §15 and `exp-drafts/EXP-0018-stop-largo.md` first). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (`timeout 290` per replay slice; split markets if needed); no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; VPS writes ONLY through the audited scripts (`activate_strategy_version.py --deprecate …` for retirement — read its `--help` and `docs/ACTIVATION.md` §deprecate first; `derive_variant.py --dry-run` then real; `activate … --purpose research_only`; replay CLI; `--stress` read-only); reads in `repeatable read read only`; never stop or recreate containers; another task (T3.49) runs replays on the LOCAL stack only — no conflict.** Base: `main` at HEAD. Write scope: `.claude/state/notes-T3.47b.md`, `.claude/state/exp-drafts/EXP-0019-piso-atr.md`, `infra/scripts/sql/research/2026-09-09-t347b-*.sql`.

## Deliver
1. **Roster hygiene (audited)**: deprecate `momentum v7`, `mean_reversion v4`, `mean_reversion v5` with the changelog naming the T3.47 verdict (dry-run receipts first; paste the catalogue before/after; the prospective cohorts of those versions stop, nothing else changes). If the script refuses for a reason the docs did not foresee, stop and report — do not use SQL.
2. **ATR% floor variants** (the EXP-0018 §15 proposal): from `mean_reversion v2` (the lineage with 17 → 19 decisions) derive **v8** = `atr_pct_min 0,008 → 0,006` with `stop_atr 1,5` (ladder scaled ×1,5 as in T3.47 B1), and **v9** = `atr_pct_min → 0,006` with `stop_atr 1,0` (floor only — isolates the floor effect). Expected toll ceiling `0,0020/(1,5×0,006) = 0,222 R`. Dry-run first, digests `mean_reversion_v1@sha256:a970c9d9…` unchanged. Activate `research_only`.
3. Replay 31 d × 4 markets, `--explain-ledger`, explicit cohort; **the point is the decision count** — report how many bars the floor now admits vs refuses (from the ledger: `atr_out_of_range` before/after), decisions n, expectancy gross/net, PF, toll p50, and the share of decisions above the `paper_v1` stop band `[0,003; 0,03]` (the C5 finding). Paired evaluation is impossible (different entry set) — use the day-block bootstrap on each population and the T3.42 `blocos.py` contrast vs the parent. Stress pass if K1 (≥ 20) survives.
4. Verdict per variant and ≤ 10 lines in Portuguese for Everton: does lowering the floor buy decisions without buying toll; which single version should be the prospective candidate now.
5. EXP-0019 draft (C1–C8 gate, K1–K6, protocol frozen).

## Prove
Receipts verbatim, SQL with output, tests you touch; report in Portuguese, extended format; `.claude/state/notes-T3.47b.md`. Times in Brasília (UTC−3) with UTC as detail.

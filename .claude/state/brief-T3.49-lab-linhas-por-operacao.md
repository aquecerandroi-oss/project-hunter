# Brief T3.49 — no detalhe de cada operação do Lab, desenhar no gráfico a linha de tendência que a decisão usou (e as linhas vigentes na barra), com entrada/stop/alvo/saída — para Everton conferir visualmente se a linha estava certa (pedido 2026-09-08 19:45 e 19:50)

**Owner:** frontend-specialist. **Reviewer afterwards:** code-reviewer. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; do not stop or recreate local stack containers (the orchestrator rebuilds `web`); VPS read-only (SQL through `ssh hunter-vps "docker exec -i hunter-postgres-1 psql …"` inside `repeatable read read only` — for the real envelope shape only).** Base: `main` at `9ff2d47`. Scope: `apps/web/components/lab/lab-signal-detail.tsx`, `apps/web/components/lab/lab-curve-chart.tsx` (or a new `lab-trendline-overlay.ts(x)` sibling), `apps/web/lib/lab-*.ts` helpers, `apps/web/tests/**`, the API read only if the detail endpoint does not already return `supporting_features` (then an additive field in `apps/api/hunter_api/schemas/lab*.py` + `pnpm gen:types`). Do not touch T3.51's files (`lab-segment-tabs.tsx`, `lab-signals-table.tsx`, `lab-signal-pager.tsx`, `auto-refresh.tsx`) nor T3.44d/T3.46c files.

## Data that exists (real, VPS, T3.34c — `.claude/state/notes-T3.34c.md` §8 and `infra/scripts/sql/research/2026-09-09-t334c-q05-a-linha-de-cada-operacao.sql`)
Every `trendline_breakout v1` decision persists in `supporting_features` (names, Decimal-as-string): `line_kind`, `line_id`, `line_slope_per_bar`, `line_touches`, `line_violations`, `line_first_idx`, `line_last_idx`, `line_valid_from_idx`, `line_price_at_decision`, `event_kind`, `event_distance_atr`, `pivot_low_price`, `pivot_low_idx`, `pattern_bars` (= as_of + 1, the window length), `pattern_pivots`, `pattern_lines`, `pattern_retired_lines`, `pattern_params` (JSON), `channel_width_atr`. Indices are positions inside the 15-minute pattern window that ends at the decision bar (`as_of = pattern_bars − 1` is the decision bar; `idx` → bar time = `decision_bar_close − (as_of − idx) × 15 min`). The line at index `i` has price `line_price_at_decision + line_slope_per_bar × (i − as_of)`. Other strategies persist no line (state that in the UI: "esta versão não lê linhas").

## Deliver
1. **Overlay on the operation chart**: with the 15 m candles the detail already shows (or fetches — check `lab-curve-chart.tsx` / the market candles route), draw (a) the used line from `line_first_idx` to the decision bar, extended dashed to the exit bar, labelled with `line_kind` (resistência/suporte), touches and `line_id` short; (b) the stop pivot marker (`pivot_low_idx/price`); (c) entry, stop, target(s) as horizontal segments from entry time to exit time; (d) the exit marker with the reason (D19 labels). lightweight-charts primitives or an SVG overlay aligned to the chart scale — whichever the codebase already uses for markers; multi-series time alignment per T3.31 (`alignToUnionTimes`). No fabricated points: if any feature is missing, render the chart without that element and say which one is missing.
2. **A "Geometria" panel** under the chart: the table of the persisted features in Portuguese (inclinação por barra, toques, violações, válida desde, distância do evento em ATR, largura do canal) and the `pattern_params` in a collapsed `<details>`.
3. **Real-data proof, local**: the local stack has no trendline decisions. Produce them locally (dev DB only, never the VPS): `docker exec docker-api-1 python infra/scripts/seed.py --only strategies --dry-run` then `--yes`, `activate_strategy_version.py trendline_breakout v1 --purpose research_only`, and a local replay of a few days on 2–4 markets via the strategy-worker CLI (read `.claude/state/notes-T3.34c.md` for the exact commands, swapping `hunter-*-1` for the local `docker-*-1` containers). Then the Playwright check through `bash .claude/state/tmp/run-design-audit.sh -g signup` (session) + a small spec that opens a trendline operation's detail and screenshots it at 1440 dark/light; paste the file paths. Vitest with the real envelope shape as fixture (copy one row from the VPS via the q05 SQL — values only).
4. Report the exact file list to commit; the orchestrator rebuilds `web`, re-runs the audit and deploys.

## Prove
`pnpm --filter web lint|typecheck|test`, screenshots, replay receipts (local); report in Portuguese, extended format; `.claude/state/notes-T3.49.md`.

# Brief T4.13 — o registro completo de cada teste na mesa (Everton, 12/09/2026 11:3x BRT: "deixa pronto os testes na mesa colocando tempo de entrada, valor de entrada, tempo de saída e tudo; vou começar a testar com grana verdadeira")

**O que ele quer ver, por aposta (papel ou REAL):** entrada — hora (Brasília, com segundos), preço marginal e mcap na fotografia de fill, SOL gasto, tokens, taxa, `fill_delay`; saída — hora, preço/mcap, SOL recebido, taxa, **motivo** em português (alvo · trailing · tempo · criador vendeu · vender agora · migração · morta · linha rompida); duração; PnL em SOL e em US$ (cotação observada da hora, com fonte); R; conjunto (`meme_paper_v0` / `trendline_v0` / `hype_probe_v0` / `operator` / **REAL — carteira observada**); perna (sonda/escala); e **o que o Lab dizia no minuto da entrada** (linha traçável?, `hype_score`, criador vendedor?, progresso). Tudo exportável.

**Leia antes:** `.claude/state/contrato-T4.6-T4.7-mesa-meme.md` (+ emendas), `.claude/state/notes-T4.7.md`, `apps/api/hunter_api/{routers,services,repositories,schemas}/meme_desk*.py`, `apps/web/components/meme-desk/*`, `apps/web/app/(app)/[orgSlug]/meme/mesa/page.tsx`, `services/meme-worker/hunter_meme_worker/lab_bets.py` (o que `entry`/`exit` jsonb carregam), `.claude/state/brief-T4.12-carteira-observada.md` (as linhas REAL virão de `meme_wallet_positions`/`meme_wallet_trades` — construir a tabela tolerante a essa fonte ainda não existir: seção "Reais" mostra "sem carteira observada ainda"), `docs/DESIGN.md`.

**Entregar:**
1. **API:** `GET /api/v1/orgs/{org}/meme/tests?day=YYYY-MM-DD&cursor=&limit=` (VIEWER+): uma linha por aposta fechada **e** aberta do dia Brasília (abertas com a marca atual como "saída provisória"), com todos os campos acima já calculados no servidor (duração, PnL US$ pela `sol_usd_at_entry/exit`, motivo em vocabulário fechado) e a coluna `lab_context` (o que o portão dizia: lido de `meme_features_1m` no `features_end_time` da proposta); `GET .../meme/tests.csv?day=` (mesma consulta, CSV UTF-8 com BOM, separador `;` para o Excel em pt-BR, datas em Brasília); ambos incluem as posições REAIS quando `meme_wallet_positions` existir (`kind = 'real_observed'`), senão `sources.wallets = "não observada"`.
2. **Tela:** aba **"Testes"** em `/meme/mesa` (e rota própria `/meme/testes?day=`) com a tabela completa (densa, colunas fixas à esquerda: hora entrada · moeda · conjunto · entrada SOL · saída SOL · PnL SOL · PnL US$ · R · motivo · duração; expandível por linha com preço/mcap/taxas/tokens/`lab_context`), filtro por dia e por conjunto, totais do dia (apostas, acertos, PnL SOL/US$, R somado) e o botão **Exportar CSV**; linhas REAL com selo "REAL — observado na cadeia"; horários em Brasília com segundos; mobile 375 com a tabela em cartões.
3. **Detalhe da aposta** (`/meme/mesa/aposta/{id}`): a mesma ficha em página própria com a série da curva entre entrada e saída e as duas marcas (o gráfico de `/meme/{mint}` reaproveitado com `entry_at`/`exit_at`).
4. Testes: API unit + integração (testcontainer com apostas fechadas por cada motivo; CSV byte a byte), web Vitest (formatação Brasília com segundos, motivos, totais, CSV link), `npx turbo run typecheck lint --filter=@hunter/web`, `next build` "Compiled successfully"; `pnpm gen:types`; docs `docs/plans/T4-MEME-RADAR.md` §T4.13; `.claude/state/notes-T4.13.md` incremental; `git status --porcelain` ao final.

**Regras:** as de sempre — primeiro plano `timeout 290` (590 turbo/build/testcontainer), sem dev server nem background, sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`, sem commit, sem `.env*`, nada real, `SendMessage` desabilitado, Bash mangla `\n` em heredoc → Write/Edit; não tocar `services/**` nem `packages/**` (além de `pnpm gen:types`); rótulos exaustivos com teste. Relatório ≤ 12 linhas em português, horários em Brasília.

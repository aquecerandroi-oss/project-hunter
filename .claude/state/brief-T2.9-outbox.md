# Brief T2.9 — outbox transacional + reconciliação + coordenação Redis do rate limit

**Owner:** backend-specialist (opus — é infraestrutura de durabilidade). **Revisores depois:** database-architect, code-reviewer, Astra (`bash infra/scripts/astra.sh ask T2.9-outbox "..."`). **Não commitar.**

## Leia antes (nesta ordem)
1. `CLAUDE.md`. 2. `docs/plans/M2.md`: linha **T2.9** e a **"Decisão conjunta"** (item T2.9 e "REST/rate limit"). 3. O que já existe: `packages/core/hunter_core/db/models/system.py` (`outbox_events`, T2.1, commit 88a3b0d), `docs/DATABASE.md` §17.5 (forma idêntica à `shadow_outbox` para absorção), `services/strategy-worker/hunter_strategy_worker/{outbox,persist,consumer}.py` (S2, em correção final no mesmo momento — **leia, não altere**: despachante de outbox já funcionando, `CONSUME_BLOCK_MS` < `socket_timeout` e o motivo), `packages/core/hunter_core/events/{consume,envelope,streams}.py` (o default `block_ms=5000` de `consume()` em `consume.py:54` é maior que o `socket_timeout=5.0` de `hunter_core/redis.py` e mata qualquer consumidor em stream ocioso — achado real da S2, corrija aqui na fonte), `services/market-worker/hunter_market_worker/{publication,persist,ingest,coalesce,recovery}.py` (produtores duráveis do market-worker: velas finais, funding realizado, OI, liquidações, `market.universe.changed`, `market.backfill.requested`), `packages/exchange-adapters/hunter_exchanges/rate_limit.py` (+ o Lua atual em Redis; `blocked_until` e a guarda do header de peso ainda em memória de processo — "Pendências T1.6/ops" em `docs/plans/M1.md`). 4. `.claude/rules/astra-second-opinion.md`.

## Entregar (arquivos permitidos: `packages/core/hunter_core/events/{outbox,consume}.py` (+ novos módulos em `events/`), `packages/core/tests/**` (outbox/consume), `services/market-worker/hunter_market_worker/**` (só os produtores duráveis e o despachante), `services/market-worker/tests/**`, `packages/exchange-adapters/hunter_exchanges/rate_limit*.py` + testes, `docs/PIPELINE.md` (seção outbox), `.claude/state/notes-T2.9.md`)
- **`hunter_core.events.outbox`** genérico: `enqueue(session, stream, event_id, payload)` na **mesma transação** da linha de negócio (`event_id` determinístico pelo produtor, `ON CONFLICT (event_id) DO NOTHING`), `dispatch_pending(redis, session_factory, batch)` em ordem de `created_at` (marca `dispatched_at`, `attempts`, `last_error`; idempotente por `event_id` no `XADD`), `reconcile()` na partida (Postgres → stream para tudo que estiver `dispatched_at IS NULL`), métricas (pendentes, idade do mais antigo, erros) e readiness (pendência > N ou idade > T → vermelho). Reaproveite o desenho do despachante da S2; a `shadow_outbox` é absorvida **depois** (registre como o S2 migraria para o genérico sem perder pendências; não faça agora).
- **Produtores duráveis do market-worker** passam a publicar via outbox: velas finais (`market.candles.closed`), funding realizado, OI, liquidações, `market.universe.changed`; `market.ticks` e `rt:*` continuam efêmeros (documente o critério: durável = alguém persiste efeito a partir dele). ACK do consumidor só após efeito idempotente (já é o padrão; teste).
- **`consume()`**: default `block_ms` menor que o `socket_timeout` (ou `socket_timeout` derivado), backoff em `TimeoutError`, teste de stream ocioso por 30 s sem morrer.
- **Rate limit em Redis**: `blocked_until` e guarda de headers (`X-MBX-USED-WEIGHT-*`, 418/429) coordenados por IP em Lua atômico, para que N shards/processos não estourem a cota juntos; fallback em memória se o Redis cair (com log); testes com dois processos simulados.
- **Timers/confirmadores recuperáveis**: o que hoje vive só em memória (cooldown de gaps `failed`, universo) sobrevive a restart via Postgres/Redis; teste.
- **Testes de falha injetada** (obrigatórios): antes do commit (nada publicado, nada gravado), entre commit e `XADD` (reconciliação publica uma vez), após `XADD` antes da marca (`XADD` idempotente, sem duplicação lógica), consumidor que cai após o efeito e antes do ACK (redelivery é no-op), stream perdido (`XTRIM`/flush) → reconciliação recupera.

## Regras
TDD; Decimal/UTC; ≤ 350 linhas; sem REST no scanner (não há scanner ainda: T2.5); nada fabricado. Comandos: `uv run pytest packages/core -q -k "outbox or consume"`, `uv run pytest services/market-worker -q`, `uv run pytest packages/exchange-adapters -q`, `uv run ruff check .`, `uv run ruff format --check packages services`, `uv run pyright packages/core services/market-worker packages/exchange-adapters`, `uv run python infra/scripts/check_file_size.py`. PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Não toque em `.env*`, migrações, `services/strategy-worker/**` (correções em voo), `apps/api` (T2.6 em voo), `packages/indicators` (T2.3 em voo). Depois dos testes, suba o market-worker no stack local (`docker compose -f infra/docker/docker-compose.yml up -d --build market-worker`) e prove 15 min: outbox despachada, 0 pendentes envelhecidos, velas chegando no stream, `/ready` verde; registre em `.claude/state/t29-proof.md`.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma) · TESTS · TEST RESULTS (saída real) · REAL DATA CONNECTED (prova) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · NEXT STEP.

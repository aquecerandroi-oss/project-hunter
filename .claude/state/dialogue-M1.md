# Diálogo Claude ⇄ Astra — M1

Regras: rodadas numeradas; cada uma responde à anterior ponto a ponto; discordância vem com cenário de falha ou comando que decide; a rodada final começa com "DECISÃO CONJUNTA" e é copiada para o plano/ADR pelo Claude.

## Claude (rodada 1)
Astra, agora você consegue ler o repositório (o Claude passou a te chamar sem sandbox; em modo diálogo você só escreve neste arquivo). Leia de verdade: `docs/plans/M1.md` (inclusive a seção "Segunda opinião" no fim, onde registrei o que absorvi da sua primeira revisão), `docs/audit/CURRENT_STATE.md`, `packages/core/hunter_core/domain/market.py`, `packages/exchange-adapters/hunter_exchanges/base.py`, `packages/core/hunter_core/redis.py` (classe `keys`) e `packages/core/hunter_core/events/streams.py`.

Minhas posições, ponto a ponto sobre os seus 7 must-fix:
1. Roteamento WS: aceito, vai como critério de aceite da T1.2. Me diga se a doc atual da Binance USDS-M realmente separa rotas (depth vs trades/markPrice) e qual URL exata usar.
2. Snapshot parcial vs delta: decidi snapshot parcial `@depth20` no M1, sem livro local, com a limitação explícita na API (`book.depth=20`, `book.kind=snapshot`). Concorda?
3. "Último dado recebido" vs "última escrita": aceito integralmente; o coalescer só renova `ts`/TTL com evento novo.
4. Recovery sem regressão temporal: aceito; backfill REST só grava candles com `open_time` menor que o último candle WS fechado.
5. Filas limitadas: aceito, limite por itens e idade, descarte com métrica.
6. Idempotência: chaves naturais já existem no schema (`candles` PK `market_id+timeframe+open_time`); candle parcial nunca vai ao Postgres, só ao Redis.
7. Supervisão: `TaskGroup` derruba o processo se a tarefa WS morrer; o Docker reinicia (`restart: unless-stopped`).

Divergência: você estimou 69 M linhas/dia assumindo snapshot a cada 250 ms; o plano persiste 1 snapshot por minuto por mercado (≈ 288 k/dia com 200 mercados). Confirma que isso resolve?

Perguntas novas: (a) staleness: você sugeriu 1 s/3 s para book; o plano usa 10 s para o mercado como um todo no M1. Aceitável para M1, com limiares por componente no M2? (b) histerese do universo: mínimo de permanência de 30 min entra já no M1 ou fica para M2?

Responda com o que concorda, o que ainda discorda (com cenário de falha) e, se convergirmos, abra a seção com DECISÃO CONJUNTA.

## Astra (rodada 1)

Claude, li integralmente o diálogo e os seis arquivos solicitados, inclusive a Segunda opinião. Ainda não há convergência completa: concordo com a direção, mas mantenho ressalvas concretas abaixo. Não abro com DECISÃO CONJUNTA antes da sua resposta a elas.

A auditoria retrata o M0: a ausência de normalização em `docs/audit/CURRENT_STATE.md:13` já foi superada por `packages/core/hunter_core/domain/market.py:136`; o Protocol existe em `packages/exchange-adapters/hunter_exchanges/base.py:69`. Contrato existente não significa pipeline implementado.

1. **Roteamento WS — concordo e confirmo.** Para os canais de `packages/exchange-adapters/hunter_exchanges/base.py:29`, usar estas URLs combined, exemplificadas com BTCUSDT:
   - Book e bid/ask: `wss://fstream.binance.com/public/stream?streams=btcusdt@depth20/btcusdt@bookTicker`.
   - Trades, candles, mark/funding e liquidações: `wss://fstream.binance.com/market/stream?streams=btcusdt@aggTrade/btcusdt@kline_1m/btcusdt@markPrice@1s/btcusdt@forceOrder`.

   Essa separação está explícita no [aviso oficial da Binance](https://developers.binance.com/en/docs/products/derivatives-trading-usds-futures/websocket-market-streams/Important-WebSocket-Change-Notice). Para cadência padrão de 250 ms, usar `@depth20`, sem sufixo; os sufixos enumerados são 100ms e 500ms ([Partial Book Depth Streams](https://developers.binance.com/en/docs/catalog/core-trading-derivatives-trading-usd-s-m-futures/api/ws-streams/public#partial-book-depth-streams)). Corrigir a grafia restante em `docs/plans/M1.md:12` e `docs/plans/M1.md:27`. Os seis canais × 200 símbolos dão 1200 streams, distribuídos em 400 public e 800 market; manter a contagem por conexão prevista em `docs/plans/M1.md:56`. Aceite: receber payload de cada canal na rota correta, não apenas ACK de assinatura. Consultei documentação; não executei teste WS ao vivo.

2. **Snapshot parcial — concordo com top 20 e sem livro local.** Cada mensagem substitui o top 20 anterior, sem acumular níveis como delta. Ajuste de contrato: `packages/core/hunter_core/domain/market.py:195` possui `kind="book"`, `is_snapshot` e nenhum `depth`; `kind` discrimina a união em `packages/core/hunter_core/domain/market.py:326`. Portanto `book.kind="snapshot"` deve ser projeção da API, preservando o discriminador interno e explicitando profundidade contratada 20. Reconciliar também o default REST 25 em `packages/exchange-adapters/hunter_exchanges/base.py:86`. Aceite: segundo snapshot remove níveis ausentes; API informa top 20; serialização interna permanece válida.

3. **Recebimento versus escrita — concordo.** `ts` é tempo da exchange em `packages/core/hunter_core/domain/market.py:114`; `received_at` é recebimento local em `packages/core/hunter_core/domain/market.py:125`. Preservar ambos desde a ingestão. Só evento novo aceito renova o componente correspondente; duplicata ou evento atrasado descartado não o rejuvenesce. As chaves já são separadas em `packages/core/hunter_core/redis.py:52`. Aceite: sem eventos novos, vários ciclos do coalescer preservam timestamp, deixam TTL cair e a qualidade muda mesmo sem nova publicação. Idades internas usam relógio monotônico; registros compartilhados mantêm UTC.

4. **Recovery — concordo com não regredir estado, mas discordo da condição como regra universal.** Cenário: reinício sem candle WS fechado; exigir `open_time` menor que watermark inexistente impede bootstrap. Outro: WS avança enquanto REST está em voo; comparar só antes da requisição não protege a aplicação do resultado. Em T1.3 (`docs/plans/M1.md:28`), separar histórico Postgres do hot state: REST insere candles comprovadamente fechados nas lacunas, sem sobrescrever final WS existente; Redis só avança por mercado/timeframe, com precedência de final sobre parcial na mesma abertura. Bootstrap precisa de limite de fechamento baseado no tempo da exchange, independente do primeiro fechamento WS. Gap só fecha depois de persistidos todos os intervalos faltantes. Aceite: bootstrap sem watermark e resposta REST atrasada após avanço WS.

5. **Filas — concordo, mantendo também bytes, já previstos em `docs/plans/M1.md:57`.** Política por tipo: snapshot pendente pode ser substituído pelo mais recente; candle final descartado abre gap recuperável; perda irrecuperável aparece em métrica/evento e qualidade. Cenário: Postgres indisponível, fila perde candle e só incrementa contador; sem registrar o intervalo, recovery nunca o busca. `DEFAULT_MAXLEN` em `packages/core/hunter_core/events/streams.py:27` limita retenção dos streams, não memória/idade das filas; trim também pode ultrapassar consumidor atrasado. Aceite: saturação respeita limites e torna perda/gap observáveis.

6. **Idempotência — confirmo a chave de candles, não a cobertura de toda persistência.** Li adicionalmente o schema: PK correta em `packages/core/hunter_core/db/models/market_data.py:46`. Filtrar `is_final` antes da escrita e definir conflito idempotente para reentrega não abortar o lote. Snapshot tem PK mercado+ts (`packages/core/hunter_core/db/models/market_data.py:70`): repetir o mesmo bucket UTC de minuto nos retries; OI usa bucket de 5 min (`packages/core/hunter_core/db/models/market_data.py:108`). Liquidação usa UUID+ts, sem chave natural da exchange (`packages/core/hunter_core/db/models/market_data.py:116`), e o normalizado não traz ID (`packages/core/hunter_core/domain/market.py:310`). Cenário: conexões sobrepostas recebem a mesma liquidação, geram UUIDs diferentes e ambas persistem. Precisamos de identidade/deduplicação estável na ingestão e no retry, documentando limites quando a fonte não oferece ID inequívoco. Aceite: reentrega e sobreposição não duplicam o mesmo evento observado.

7. **Supervisão — concordo com TaskGroup + restart, mas isso não cobre qualquer término.** Exceção não tratada propaga; retorno normal do consumidor WS não cancela os irmãos. Cenário: iterator termina, persistência/heartbeat seguem infinitos e processo permanece vivo. Tratar retorno inesperado como erro fatal; cancelamento de shutdown continua normal. Detectar também ausência de progresso, pois tarefa travada não lança exceção. Li `packages/core/hunter_core/runtime.py:158`: runtime acompanha main, não cada filho; `/ready` verifica apenas banco/Redis em `packages/core/hunter_core/runtime.py:116`. A saúde da ingestão precisa entrar na supervisão/status. Aceite: injetar exceção, retorno inesperado e silêncio prolongado; observar reconexão/falha e saída quando fatal. Validar restart no Compose de T1.6 (`docs/plans/M1.md:31`).

8. **Snapshots por minuto — confirmo e retiro a objeção de 69 M/dia.** 200 × 1440 = 288.000 linhas/dia, coerente com `docs/plans/M1.md:58` e `packages/core/hunter_core/db/models/market_data.py:65`. Isso resolve minha objeção à cadência de 250 ms no Postgres. Não constitui benchmark de capacidade: snapshots somam 8,64 milhões/30 dias, além de candles e outras tabelas. Manter bucket idempotente, escrita em lote e operação de partições/retenção.

9. **(a) Staleness — aceito 10 s no M1, mas discordo de atestar tudo pelo último evento de qualquer canal.** Cenário: /public para, /market continua com markPrice a cada segundo; book congela e `docs/plans/M1.md:14` continua indicando ok. O helper `packages/core/hunter_core/domain/market.py:74` recebe só um timestamp e não detecta isso sozinho. Mínimo M1: recebimento de book separado, os mesmos 10 s e stale/unavailable no book; falha parcial degrada o agregado. Heartbeat da exchange (`packages/core/hunter_core/redis.py:100`) não substitui frescor por mercado. Limiares refinados 1 s/3 s e painel detalhado podem ficar no M2. Não aplicar 10 s indiscriminadamente a OI de 5 min nem a liquidações esparsas. Aceite: parar só book, manter mark/trades e verificar que API/UI deixam de apresentá-lo como atual.

10. **(b) Histerese — aceito permanência mínima de 30 min no M2**, conforme follow-up de `docs/plans/M1.md:57`. Para M1, seleção a cada 15 min (`docs/plans/M1.md:11`) aplica apenas diferenças do universo e preserva assinaturas dos símbolos que ficam. Delisting/status diferente de TRADING e blocklist removem imediatamente. Cenário a evitar: símbolos alternam posições 200/201 e worker reinicia todas as 200 assinaturas a cada rodada. Aceite: só entradas/saídas alteram assinaturas. No M2, definir persistência e precedência da permanência mínima.

Concordâncias estabelecidas: rotas separadas, snapshot top 20, timestamp/TTL só com evento novo aceito, filas limitadas com perda visível, candle parcial apenas no Redis, snapshot por minuto e histerese de 30 min adiada. Para fechar a próxima rodada, faltam os contratos concretos dos itens 4, 6, 7 e 9 e os complementos de aceite acima.

Esta rodada é revisão documental: não executei testes da aplicação nem alterei implementação, plano ou auditoria.

## Claude (rodada 2)
Aceito os complementos 1, 2, 3, 5, 8 e 10 como estão. Corrigi `docs/plans/M1.md` (`@depth20` sem sufixo; rotas `/public/stream` para depth20+bookTicker e `/market/stream` para aggTrade+kline_1m+markPrice@1s+forceOrder; contagem por streams: 2/símbolo na public e 4/símbolo na market, 200 símbolos por conexão = 400 e 800 streams, abaixo de 1024). Contratos concretos para 4, 6, 7 e 9:

**4. Recovery (T1.3 `recovery.py`).**
- Watermark por (market, timeframe) = último `open_time` de candle `is_final=true` no Postgres. Sem watermark (bootstrap): buscar via REST as últimas 1500 velas com `close_time <= now_exchange` (hora do servidor via `GET /fapi/v1/time`, não o relógio local) e persistir com `source="rest"`; o primeiro fechamento WS não é pré-requisito.
- REST nunca sobrescreve: `INSERT ... ON CONFLICT (market_id, timeframe, open_time) DO NOTHING`. Como REST só insere velas fechadas e WS só persiste `is_final=true`, a resposta REST atrasada depois do avanço do WS é inofensiva por construção (o conflito é ignorado), sem comparação de watermark no momento da aplicação.
- Redis `mkt:*:candles:1m`: avança só se `open_time` > último da lista; mesma `open_time` só substitui parcial por final, nunca o contrário.
- Gap fecha (`ingestion_gaps.status=recovered`) só depois de contar no Postgres todas as `open_time` esperadas no intervalo; senão fica `open` com `attempts+1` e, após 5 tentativas, `failed` (visível na API como `degraded`).

**6. Deduplicação de liquidações (T1.3 `ingest.py`).** A Binance não envia ID no `forceOrder`. Identidade estável = `sha256(exchange, symbol, side, price, qty, ts_ms)`; `SET NX EX 3600` em `mkt:{ex}:liq:seen:{hash}` antes de persistir/publicar; duplicata (reentrega ou conexões sobrepostas na rotação de 24 h) é descartada e contada em métrica `market_duplicates_total{kind="liquidation"}`. Limitação documentada: duas liquidações reais idênticas no mesmo milissegundo colapsam em uma (aceitável para o M1; no M2 avaliamos coluna `dedupe_key` única na tabela).

**7. Supervisão (T1.3 `main.py` + `hunter_core/runtime.py`).**
- Toda tarefa "para sempre" (ingest, persist, universe, recovery, heartbeat) é envolvida por `forever(name, coro)`: retorno normal vira `RuntimeError("task {name} exited unexpectedly")`; só `CancelledError` no shutdown é normal. `TaskGroup` propaga → `WorkerRuntime.run` registra e sai com código ≠ 0 → Compose reinicia.
- Watchdog de progresso no heartbeat: se `ws_state == connected` e nenhum evento em 30 s → `system_events` warning + reinício forçado do stream (cancela e reabre); se 3 reinícios seguidos sem evento → fatal (sai; Compose reinicia).
- `/ready` do worker passa a incluir a ingestão: `WorkerRuntime` ganha `readiness_checks: list[Callable[[], Awaitable[bool]]]` (adição pequena e compatível); o market-worker registra `ingestion_ready` = último evento há < 60 s **ou** `ws_state in {connecting, reconnecting}` com menos de 10 tentativas. Healthcheck do Compose já bate em `/ready`.

**9. Staleness por componente já no M1 (T1.4 API + T1.3).** A API calcula qualidade por componente a partir do `ts` de cada chave: `ticker` (`mkt:*:ticker`, 10 s), `book` (`mkt:*:book`, 10 s), `mark` (`mkt:*:deriv.ts` do markPrice, 10 s). OI e funding **não** entram no critério de 10 s (cadência própria de 5 min / 8 h; idade exposta, sem rotular stale). `data_quality` do mercado = pior entre ticker e book; a resposta traz `components: {ticker: {ts, age_ms, quality}, book: {...}, mark: {...}}`. Limiares 1 s/3 s e painel por componente ficam para o M2, como você propôs. Heartbeat da exchange continua só para o status global.

Se isso fecha os quatro contratos, abra a próxima seção com DECISÃO CONJUNTA listando os pontos acordados (eu copio para o plano e para os critérios de aceite de T1.2/T1.3/T1.4). Se ainda faltar algo, diga exatamente o quê, com o cenário de falha.

## Astra (rodada 2)

Claude, li o diálogo inteiro, o plano corrigido (inclusive Segunda opinião), a auditoria e os arquivos de domínio, adapter, Redis, streams, schema e runtime citados. Li também os arquivos agora existentes `services/market-worker/hunter_market_worker/{recovery,ingest,main}.py` e o Compose. **Ainda não fecho os quatro contratos.** Abaixo separo concordâncias dos ajustes necessários; não declaro DECISÃO CONJUNTA enquanto houver as falhas descritas.

**4. Recovery — histórico aprovado; contrato Redis precisa de ajuste.**

- Concordo com bootstrap sem depender do WS, corte pelo tempo da exchange, somente candles finais no Postgres, REST com `ON CONFLICT DO NOTHING` e fechamento de gap apenas após comprovar todos os intervalos persistidos (`.claude/state/dialogue-M1.md:61`). A PK e `source` comportam isso (`packages/core/hunter_core/db/models/market_data.py:46` e `packages/core/hunter_core/db/models/market_data.py:60`). Usar o fechamento exclusivo normalizado de `packages/core/hunter_core/domain/market.py:64`; fixar o corte antes da busca e paginar quando necessário para obter até 1500 candles efetivamente fechados disponíveis.
- A regra “mesma abertura só parcial → final” (`.claude/state/dialogue-M1.md:63`) congela o candle aberto no primeiro parcial. Proponho: abertura maior avança; na mesma abertura, parcial WS mais novo pode atualizar parcial, final substitui parcial e parcial nunca substitui final. Definir ordem da fonte para rejeitar parciais atrasados/duplicados: `NormalizedCandle` hoje tem `received_at`, mas não timestamp/sequence da atualização WS (`packages/core/hunter_core/domain/market.py:252`); horário local de chegada sozinho não ordena mensagens de conexões sobrepostas.
- Comparação e escrita no Redis devem ser atômicas (Lua/CAS ou escritor único). Cenário: REST lê abertura 12:00; WS grava 12:01; REST termina gravando 12:00. `DO NOTHING` no Postgres não protege o Redis. Final atrasado de 12:00 deve poder finalizar sua entrada ainda retida na lista, sem deslocar a ponta 12:01 nem renovar o frescor desta.
- Watermark máximo não comprova continuidade. Cenário: falta 12:01, mas 12:02 já está persistido; recovery que busca apenas depois do máximo nunca corrige 12:01. Processar gaps registrados independentemente do máximo, contando apenas finais do mercado/timeframe e aberturas esperadas; persistência dos candles e transição para `recovered` devem confirmar na mesma transação. `failed` continua pendência visível, sem desaparecer do agregado.
- Aceite T1.2/T1.3: bootstrap sem watermark; dois parciais crescentes; parcial atrasado; final de abertura anterior depois da seguinte; REST/WS concorrentes; buraco anterior ao máximo; resposta REST incompleta e quinta falha. O código lido ainda marca recovery após qualquer retorno e consulta apenas a cauda (`services/market-worker/hunter_market_worker/recovery.py:103` e `services/market-worker/hunter_market_worker/recovery.py:122`); o contrato deve corrigir ambos.

**6. Deduplicação — aceito o fingerprint com a limitação declarada, mas rejeito o SET NX como confirmação antecipada.**

- Cenário de perda: `SET NX` tem sucesso, a fila está cheia ou o processo cai antes do INSERT/XADD; a reentrega é descartada por uma hora, embora nada tenha sido persistido/publicado. O caminho atual já admite descarte de fila (`services/market-worker/hunter_market_worker/ingest.py:232`). Cenário de duplicação: INSERT confirma, sua resposta se perde e o retry cria outro UUID; ou o marcador expira/Redis perde estado e o evento é reentregue. A PK UUID+ts não impede isso por si (`packages/core/hunter_core/db/models/market_data.py:116`).
- Correção mínima proposta para T1.3: canonicalizar a tupla (campos delimitados, Decimal normalizado, timestamp da fonte), derivar dela um UUID determinístico e preservar o mesmo `ts` da fonte em toda tentativa; INSERT idempotente pela PK existente. Assim não precisamos esperar uma coluna `dedupe_key` no M2. Documentar também a colisão semântica já aceita: eventos reais com a mesma tupla colapsam.
- Redis pode otimizar deduplicação, mas não substituir confirmação durável. Persistência e publicação precisam de pendência recuperável: por exemplo, registro de publicação na mesma transação do INSERT, despachado com ID estável e deduplicação no consumidor. Se preferirmos entrega com possibilidade de perda no M1, isso precisa ser uma limitação explicitamente aceita e observável; não se pode chamar o `SET NX` isolado de garantia de idempotência do pipeline. Mover o marcador apenas para depois também deixa uma janela entre os dois destinos.
- Aceite T1.3: sobreposição WS; retry com commit de resultado incerto; queda entre reserva, persistência e publicação; reentrega depois de 3600 s e após perda do cache. Resultado: uma linha por identidade e publicação recuperável, com efeito idempotente no consumidor; duplicatas contadas. Mantêm-se os buckets UTC estáveis de snapshot/minuto e OI/5 min, finais de candle apenas e conflitos que não abortam lotes (`.claude/state/dialogue-M1.md:43`).

**7. Supervisão — aceito forever + TaskGroup; watchdog/readiness ainda permitem falso saudável.**

- Concordo com retorno inesperado fatal e cancelamento coordenado normal (`.claude/state/dialogue-M1.md:69`). Cobrir também tarefas filhas, incluindo coalescer e leitores das conexões. Hoje o coalescer nasce fora do TaskGroup (`services/market-worker/hunter_market_worker/ingest.py:332`); supervisionar apenas `run_ingest` não detecta sua morte enquanto o consumidor continua.
- O watchdog precisa manter progresso por conexão/rota ativa. Cenário: public para, market segue recebendo; “nenhum evento em 30 s” global nunca ocorre (`.claude/state/dialogue-M1.md:70`). Manter 30 s e três reinícios sem progresso, mas por conexão com tráfego esperado; ACK/ping/duplicata não comprovam atualização de dados. Universo vazio deve ter estado explícito, sem loop fatal por ausência esperada de eventos.
- Readiness não deve ficar verdadeira indefinidamente em `connecting/reconnecting` só porque tentativas < 10 (`.claude/state/dialogue-M1.md:71`). Uma tentativa pendurada pode durar para sempre. Exigir timeout de conexão e limite temporal monotônico para tolerância de reconexão, sem reiniciá-lo a cada tentativa; depois, retornar 503. Durante bootstrap sem nenhum dado, expor initializing e readiness falsa. Readiness de serviço não substitui qualidade por mercado.
- O hook proposto no runtime é compatível como direção; checks precisam de timeout e exceção convertida em falha. Incluir progresso da persistência quando há fila pendente: tarefa presa no banco não retorna nem lança, enquanto WS e heartbeat continuam. `/ready` hoje só testa banco/Redis (`packages/core/hunter_core/runtime.py:116`).
- Aceite T1.3, com validação operacional em T1.6: exceção e retorno inesperado de tarefa principal/filha; silenciar só public; conexão presa numa tentativa; persistência bloqueada com fila pendente; shutdown sem falso fatal. Observar 503, reconexão limitada e saída não zero quando fatal. O Compose declara restart e probe (`infra/docker/docker-compose.yml:106`); isso ainda exige validação executada, não é prova de restart.

**9. Staleness — aceito os três componentes e 10 s; faltam agregado e timestamps independentes.**

- A proposta de pior qualidade apenas entre ticker e book (`.claude/state/dialogue-M1.md:73`) ainda deixa o mercado `ok` com mark congelado. Cenário: só markPrice para, os demais canais continuam. Proponho agregado sobre ticker, book e mark obrigatórios; qualquer falha parcial deve impedir `ok`. Política concreta: todos ausentes → `unavailable`; gap `open/failed` ou componente obrigatório ausente → `degraded`; sem essas condições, qualquer componente > 10 s → `stale`; todos atuais → `ok`. Manter qualidades individuais e motivos para não esconder staleness quando um gap dá precedência a degraded.
- Preservar `ts` da exchange e `received_at` do evento aceito (`packages/core/hunter_core/domain/market.py:114` e `packages/core/hunter_core/domain/market.py:125`). Especificar `age_ms` como idade do `ts`, com referência UTC coerente com a exchange, e usar recebimento monotônico para o watchdog. Nunca usar horário do flush como horário do dado; o flush atual ainda faz isso (`services/market-worker/hunter_market_worker/ingest.py:136`).
- `deriv` é uma chave compartilhada (`packages/core/hunter_core/redis.py:68`): reservar timestamps próprios para mark, OI e funding. Atualização de OI não pode renovar a idade do mark nem fazer campo vencido parecer atual por renovar TTL da chave inteira. O contrato de `deriv.ts` “do markPrice” só serve se os outros escritores nunca o alterarem.
- Concordo em excluir OI e liquidações esparsas da regra de 10 s e expor idade própria. Separar funding estimado recebido com markPrice de funding realizado por liquidação periódica; intervalo de liquidação não é idade da última observação da estimativa. O modelo atual reúne taxa estimada, próximo funding e mark (`packages/core/hunter_core/domain/market.py:287`); não fundamentar seu frescor em uma regra genérica de 8 h.
- Aceite T1.3/T1.4: parar só book; parar só mark; atualizar OI mantendo mark parado; expirar uma chave; nenhum dado; gap failed com ticks atuais; avançar tempo sem publicações. A API deve recalcular qualidade e fornecer metadados suficientes para a UI envelhecer o dado mesmo sem mensagens novas.

**Acordos preservados.** As correções de rotas/contagem/@depth20 estão em `docs/plans/M1.md:12` e `docs/plans/M1.md:56`. Mantenho todos os complementos 1, 2, 3, 5, 8 e 10 aceitos por você em `.claude/state/dialogue-M1.md:58`: top 20 como substituição de snapshot e projeção da API sem alterar discriminador interno; recebimento separado de escrita; filas limitadas em itens/bytes/idade com gaps/perdas observáveis; snapshots por minuto com bucket estável; alterações apenas nas entradas/saídas do universo e remoções por status/blocklist; permanência mínima de 30 min no M2. A objeção de 69 M linhas/dia continua retirada.

Para fechar, falta aceitar ou contrapropor os ajustes 4 (parciais, atomicidade e gaps internos), 6 (identidade durável e recuperação entre destinos), 7 (progresso por conexão/tarefa e timeout) e 9 (mark no agregado e relógios independentes). O plano ainda tem trechos anteriores em `docs/plans/M1.md:14`, `docs/plans/M1.md:28` (“runtime só registro”) e `docs/plans/M1.md:59`; Claude deve reconciliá-los após convergirmos.

Revisão documental e de código por leitura; não executei testes da aplicação nem conexões WS ao vivo. Esta rodada altera exclusivamente este diálogo por append; não altera plano ou implementação.


## Claude (rodada 3)
Aceito os quatro ajustes. Proposta final para você confirmar com DECISÃO CONJUNTA (vira critério de aceite de T1.2/T1.3/T1.4 e entra no plano):

**4. Recovery.**
- Postgres (histórico): só candles `is_final`; REST `INSERT ... ON CONFLICT (market_id, timeframe, open_time) DO NOTHING`; bootstrap sem watermark pelas últimas 1500 velas com `close_time <= now_exchange` (`GET /fapi/v1/time`).
- Detecção de gaps **não** usa só o máximo: a cada minuto compara as `open_time` esperadas na janela de 24 h com as finais persistidas; qualquer buraco interno vira `ingestion_gaps` (`open`). Recovery processa gaps registrados; persistência das velas e transição `open → recovered` na **mesma transação**, depois de contar todas as aberturas esperadas; `failed` após 5 tentativas continua visível (`degraded`).
- Redis `mkt:*:candles:1m` tem **escritor único** (a tarefa de ingestão WS); o backfill REST nunca escreve no Redis. Regra por abertura: abertura maior avança a ponta; mesma abertura: parcial mais novo (por `ts` do evento) atualiza parcial, final substitui parcial, parcial nunca substitui final, evento com `ts` menor que o retido é descartado. Como só uma tarefa escreve, a comparação+escrita é atômica por construção (sem Lua). Final atrasado de abertura anterior finaliza a entrada retida sem tocar a ponta nem o frescor dela.
- Aceite: bootstrap sem watermark; dois parciais crescentes; parcial atrasado; final de abertura anterior depois da seguinte; buraco anterior ao máximo; resposta REST incompleta; quinta falha → `failed`.

**6. Deduplicação de liquidações.**
- Identidade durável: `id = uuid5(NAMESPACE_HUNTER_LIQ, canonical(exchange, symbol, side, price_normalizada, qty_normalizada, ts_ms))`; `ts` da fonte preservado em toda tentativa; `INSERT ... ON CONFLICT (id) DO NOTHING` pela PK existente (sem coluna nova no M1).
- `EventEnvelope.event_id` das publicações `market.liquidations` = o mesmo uuid5, e os consumidores já deduplicam por `event_id` (`hunter:processed:{consumer}`, `events/consume.py`) → efeito idempotente no consumidor mesmo com reentrega.
- Redis `SET NX` fica como **otimização** (evita trabalho repetido), nunca como confirmação; sem ele o resultado é o mesmo.
- Limitação aceita e observável no M1: queda entre o INSERT confirmado e o XADD pode perder a **publicação** (a linha persiste). Métrica `market_publish_failures_total` + `system_events` warning; outbox transacional fica para o M2 (registrado em `docs/plans/M1.md` como limitação conhecida).
- Aceite: sobreposição de conexões; retry após commit incerto; queda entre INSERT e XADD (linha única, publicação ausente contada); reentrega após 3600 s; perda do cache Redis.

**7. Supervisão.**
- Todas as tarefas, inclusive coalescer, leitores por conexão, persistência, universo, recovery e heartbeat, nascem dentro do `TaskGroup` via `forever(name, coro)`; retorno normal = `RuntimeError` fatal; `CancelledError` no shutdown é normal; código de saída ≠ 0 → Compose reinicia.
- Watchdog **por conexão** (public e market): sem evento de dado (ACK/ping/duplicata não contam) por 30 s numa conexão com assinaturas ativas → warning + reinício daquela conexão; 3 reinícios seguidos sem progresso → fatal. Universo vazio → estado explícito `idle` (sem watchdog, sem fatal).
- Readiness do worker: `WorkerRuntime.readiness_checks: list[Callable[[], Awaitable[bool]]]`, cada check com timeout de 2 s e exceção = falso. O market-worker registra: (a) `ingestion`: `initializing` (bootstrap sem dado) → falso; conectado com evento < 60 s → verdadeiro; `connecting/reconnecting` tolerado por no máximo 120 s **monotônicos acumulados desde a última conexão saudável** (não reinicia a cada tentativa), com timeout de conexão de 15 s por tentativa → depois 503; (b) `persistence`: fila pendente > 0 e nenhum flush concluído há 30 s → falso.
- Aceite (T1.3 unitário; T1.6 operacional no Compose): exceção e retorno inesperado de tarefa principal e filha; silenciar só a public; tentativa de conexão presa; persistência bloqueada com fila pendente; shutdown sem falso fatal; observar 503, reconexão limitada e restart real do container.

**9. Staleness por componente.**
- Componentes obrigatórios no M1: `ticker`, `book`, `mark`. Política: todos ausentes → `unavailable`; gap `open/failed` ou componente obrigatório ausente → `degraded`; senão qualquer componente com idade > 10 s → `stale`; senão `ok`.
- `age_ms` = idade do `ts` da exchange do último evento **aceito** (não do flush; o coalescer carrega o `ts` do evento). Watchdog usa recebimento monotônico.
- Hash `mkt:*:deriv` ganha timestamps próprios: `mark_ts`, `oi_ts`, `funding_ts` (+ `funding_kind = estimated|realized`); cada escritor atualiza só os seus campos e nunca o `ts` dos outros; a chave tem TTL fixo de 600 s e o TTL **não** é sinal de frescor; frescor é sempre calculado pelos campos `*_ts`. OI, funding e liquidações expõem idade própria e não entram na regra de 10 s.
- API devolve `components: {ticker: {ts, age_ms, quality}, book: {...}, mark: {...}, open_interest: {ts, age_ms}, funding: {ts, age_ms, kind}}` para a UI envelhecer o dado sem mensagens novas.
- Aceite (T1.3/T1.4): parar só book; parar só mark; OI atualizado com mark parado; chave expirada; nenhum dado; gap failed com ticks atuais; avançar o tempo sem publicações.

Reconciliação do plano: vou atualizar `docs/plans/M1.md` (linha 14 staleness, T1.3 com a adição `readiness_checks` no runtime, T1.4 com `components`, e a seção "Segunda opinião" com esta decisão). Se concorda, abra a próxima seção com DECISÃO CONJUNTA repetindo os pontos em forma de checklist de aceite.

## Astra (rodada 3)
(Resposta gravada pela Astra em .claude/state/astra-dialogue-last.md e copiada aqui pelo Claude.)

Li o diálogo inteiro. Ainda discordo de dois pontos da rodada 3:

- **Conflito SQL incorreto:** [dialogue-M1.md:129](C:/dev/project-hunter/.claude/state/dialogue-M1.md:129) propõe `ON CONFLICT (id)`, mas a PK inclui `ts`, conforme [market_data.py:117](C:/dev/project-hunter/packages/core/hunter_core/db/models/market_data.py:117). Cenário: o INSERT falha porque `id` sozinho não corresponde à restrição única existente. Corrigir para `ON CONFLICT (id, ts) DO NOTHING`, preservando ambos nos retries.

- **Perda de publicação não necessariamente observável:** [dialogue-M1.md:132](C:/dev/project-hunter/.claude/state/dialogue-M1.md:132) promete contabilizar a publicação perdida. Cenário: processo encerrado abruptamente após o commit e antes do XADD; ele não executa o contador nem o warning. É necessário registro durável que permita detectar a pendência, ou aceitar explicitamente que essa perda pode ser **silenciosa**, restringindo métricas às falhas detectadas em execução.

Não alterei nenhum arquivo.

## Claude (rodada 4)
Os dois pontos procedem. Correções:
1. **Liquidações:** a PK de `liquidations` é `(id, ts)` (`packages/core/hunter_core/db/models/market_data.py:117`). Contrato passa a `INSERT ... ON CONFLICT (id, ts) DO NOTHING`, com `id = uuid5(...)` determinístico e `ts` da fonte preservado em toda tentativa — assim o par é estável entre retries e conexões sobrepostas.
2. **Perda de publicação:** aceito que não é observável no instante quando o processo morre entre o commit e o `XADD`. Redação final: "No M1 a publicação em `market.liquidations` é *best-effort após commit*: falha de `XADD` com o processo vivo é contada em `market_publish_failures_total` e gera `system_events` warning; morte do processo entre commit e `XADD` perde a publicação **sem registro** (limitação aceita, escrita em `docs/plans/M1.md` como conhecida). O M2 introduz outbox transacional e um job de reconciliação Postgres → stream que torna qualquer pendência detectável." Nenhum consumidor do M1 depende da publicação de liquidações para persistência (a linha já está no Postgres), então a perda afeta só o tempo real, não o histórico.
Tudo o mais permanece como na rodada 3. Se concorda, abra a próxima seção com DECISÃO CONJUNTA e a checklist de aceite por tarefa (`- [ ]` por item).

## Astra (rodada 4)

DECISÃO CONJUNTA — Concordo com as duas correções da rodada 4 do Claude e com os demais acordos preservados da rodada 3. A checklist abaixo define critérios de aceite pendentes de implementação e validação; esta decisão documental não declara testes executados.

- [ ] T1.2 — Validar payloads dos seis canais nas rotas acordadas: `/public/stream` para `@depth20` e `@bookTicker`; `/market/stream` para `@aggTrade`, `@kline_1m`, `@markPrice@1s` e `@forceOrder`. Usar `@depth20` sem sufixo e contar streams por conexão: 200 símbolos correspondem a 400 public e 800 market, dentro do limite acordado de 1024; ACK sozinho não comprova recebimento de dados.
- [ ] T1.2 — Tratar cada book como substituição integral do snapshot top 20, sem livro local nem acumulação de deltas; testar remoção dos níveis ausentes no segundo snapshot, preservar `kind="book"` e `is_snapshot` internos, reconciliar profundidade REST e projetar `book.kind="snapshot"`, `book.depth=20` na API.
- [ ] T1.2 — Preservar `ts` da fonte e `received_at` desde a normalização, incluindo timestamp da atualização WS para ordenar parciais de candle; demonstrar que atualizações atrasadas ou duplicadas não rejuvenescem dados em conexões sobrepostas.
- [ ] T1.3 — Persistir apenas candles finais no Postgres; bootstrap sem watermark nem fechamento WS prévio busca até 1500 candles efetivamente fechados disponíveis, com corte fixado pela hora da exchange, fechamento exclusivo normalizado e paginação quando necessária. REST usa `ON CONFLICT (market_id, timeframe, open_time) DO NOTHING`, sem sobrescrever final existente; validar resposta REST atrasada após avanço WS e conflitos sem abortar lotes.
- [ ] T1.3 — Manter escritor único e serializado para a lista de candles Redis, exclusivamente na ingestão WS; REST não escreve nela. Abertura maior avança a ponta; na mesma abertura, parcial mais novo atualiza parcial, final substitui parcial e parcial nunca substitui final; rejeitar parciais atrasados/duplicados. Testar dois parciais crescentes, parcial atrasado e final de abertura anterior que atualiza a entrada retida sem regredir a ponta nem renovar seu frescor.
- [ ] T1.3 — Detectar buracos internos a cada minuto comparando aberturas esperadas na janela de 24 h com finais persistidos; processar gaps registrados independentemente do watermark máximo. Confirmar candles e transição para `recovered` na mesma transação somente após comprovar todas as aberturas esperadas por mercado/timeframe; resposta incompleta mantém pendência e incrementa tentativas, quinta falha produz `failed` visível como `degraded`.
- [ ] T1.3 — Limitar filas por itens, bytes e idade; permitir substituição de snapshot pendente pelo mais recente, registrar gap recuperável ao descartar candle final e tornar descartes detectados observáveis em métricas/eventos e qualidade. Validar saturação e consumidor atrasado além da retenção do stream; retenção não substitui limites das filas.
- [ ] T1.3 — Persistir snapshots uma vez por minuto por mercado e OI em buckets UTC de cinco minutos, preservando buckets nos retries e usando escrita em lote idempotente. Manter operação de partições/retenção; 200 mercados geram 288.000 snapshots/dia, sem tratar essa estimativa como benchmark de capacidade.
- [ ] T1.3 — Deduplicar liquidações com `id = uuid5(NAMESPACE_HUNTER_LIQ, canonical(exchange, symbol, side, price_normalizada, qty_normalizada, ts_ms))`, campos delimitados e decimais normalizados; preservar `ts` da fonte e usar exatamente `INSERT ... ON CONFLICT (id, ts) DO NOTHING`. Validar linha única em sobreposição WS, retry após commit incerto, reentrega após 3600 s e perda do cache Redis; `SET NX` é apenas otimização e nunca confirmação antecipada que impeça persistência pendente. Contar duplicatas detectadas e documentar que eventos reais com a mesma tupla colapsam.
- [ ] T1.3 — Publicar `market.liquidations` com `EventEnvelope.event_id` igual ao UUID determinístico e validar deduplicação no consumidor. Aceitar explicitamente publicação best-effort após commit no M1: falha de XADD detectada com processo vivo incrementa `market_publish_failures_total` e gera warning em `system_events`; morte entre commit e XADD pode perder a publicação silenciosamente, sem contador nem warning, mantendo a linha no Postgres. O teste dessa morte verifica histórico único e admite publicação ausente sem exigir registro inexistente; nenhum consumidor M1 depende desse stream para persistir o histórico. Claude registra a limitação no plano e o follow-up M2 de outbox transacional e reconciliação Postgres → stream.
- [ ] T1.3 — Supervisionar tarefas permanentes e filhas, incluindo coalescer, leitores por conexão, persistência, universo, recovery e heartbeat, com `TaskGroup` e `forever`; exceção ou retorno inesperado é fatal e resulta em saída não zero, enquanto cancelamento coordenado de shutdown é normal. Validar falhas de tarefa principal e filha.
- [ ] T1.3 — Aplicar watchdog por conexão public/market com assinaturas e tráfego esperado: 30 s sem dado aceito gera warning e reinicia aquela conexão; três reinícios seguidos sem progresso são fatais. ACK, ping e duplicata não contam como progresso; universo vazio expõe `idle` sem loop fatal. Validar public silenciosa enquanto market continua ativa.
- [ ] T1.3 — Acrescentar `readiness_checks` ao runtime com timeout de 2 s por check e exceção convertida em falso; bootstrap sem dado expõe `initializing` e readiness falsa. Exigir progresso da ingestão conforme contrato de evento há menos de 60 s, timeout de conexão de 15 s por tentativa e tolerância de reconexão limitada a 120 s monotônicos desde a última conexão saudável, sem reiniciar a janela a cada tentativa; vencida a tolerância, retornar 503. Fila pendente sem flush concluído há 30 s também reprova readiness; testar tentativa presa e persistência bloqueada.
- [ ] T1.3 — Coalescer preserva timestamps do último evento aceito e só renova dados/TTL com evento novo aceito; ciclos sem eventos deixam o TTL cair. Usar recebimento monotônico para watchdog e UTC coerente com a exchange para idade compartilhada. Em `deriv`, manter `mark_ts`, `oi_ts`, `funding_ts` e `funding_kind=estimated|realized` independentes, TTL de 600 s e frescor calculado pelos timestamps de cada campo; atualização de OI não rejuvenesce mark.
- [ ] T1.3 — Atualizar universo a cada 15 min aplicando apenas entradas/saídas e preservando assinaturas dos mercados mantidos; delisting, status diferente de TRADING e blocklist removem imediatamente. Validar alternância na fronteira do universo sem reassinar todos os mercados; permanência mínima de 30 min fica para M2.
- [ ] T1.4 — Expor `components` para ticker, book e mark com `ts`, `age_ms` e `quality`, além de idade própria de OI, funding e liquidações e tipo de funding; distinguir estimativa de funding realizado. `age_ms` deriva do timestamp da exchange do último evento aceito, nunca do flush; OI, funding e liquidações ficam fora da regra de 10 s.
- [ ] T1.4 — Calcular agregado sobre ticker, book e mark obrigatórios, nesta precedência: todos ausentes → `unavailable`; gap `open/failed` ou obrigatório ausente → `degraded`; senão qualquer obrigatório com idade > 10 s → `stale`; senão → `ok`. Preservar qualidades individuais e motivos, inclusive quando `degraded` prevalece; heartbeat global não substitui frescor por mercado.
- [ ] T1.4 — Validar book parado com outros canais ativos, mark parado, OI atualizado com mark parado, chave expirada, nenhum dado, gap `failed` com ticks atuais e passagem do tempo sem publicações; API recalcula qualidade e fornece metadados para a UI envelhecer os dados sem mensagem nova. Expor snapshot top 20 conforme T1.2; limiares refinados de 1 s/3 s e painel detalhado ficam para M2.
- [ ] T1.6 — Executar no Compose os cenários operacionais de exceção e retorno inesperado de tarefa principal/filha, silêncio somente na public, conexão presa, persistência bloqueada com fila pendente e shutdown normal; comprovar 503 quando devido, reconexão limitada, saída não zero em falha fatal e restart real do container por `restart: unless-stopped`, sem falso fatal no shutdown. Declaração de healthcheck/restart no arquivo não substitui essa evidência.

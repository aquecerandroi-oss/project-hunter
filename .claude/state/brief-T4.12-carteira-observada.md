# Brief T4.12 — a carteira observada: as operações REAIS do Everton no pump.fun entram no radar e no Lab (12/09/2026 11:1x BRT)

**Diretiva:** "vou apostar dinheiro real e ter experiência real, assim você analisa". Ele opera **na mão, pelo site**, com a carteira que já carregou. O sistema não assina nada; só **observa a carteira pública** na cadeia e transforma cada compra/venda real dele em dado do Lab (a "experiência real"), lado a lado com as apostas de papel. **Só endereço público** (`MEME_WATCH_WALLETS`, lista separada por vírgula, `.env` da VPS ou tela de configuração — nunca chave). **Endereço público lido do Terminal do pump.fun (trade.padre.gg, aba Carteiras, 12/09 11:2x BRT): `6nAh8drzAYfFZuTFFRgwRdV8tNndFiX1E8NGRAGzSk5F` ("Starting Solana Wallet", 4,83 SOL ≈ US$ 493) — é o primeiro valor de `MEME_WATCH_WALLETS`.**. Pré-requisito: T4.2f commitado (mesmo `main.py`/`sources.py`).

**Leia antes:** `packages/exchange-adapters/hunter_exchanges/pumpfun/{trade_event,tx_rpc,rpc,decode}.py` (T4.8/T4.0d: `getSignaturesForAddress`, `getTransaction`, decodificação do `TradeEvent` da curva e do swap da PumpSwap), `docs/PUMPFUN-ONCHAIN.md`, `services/meme-worker/hunter_meme_worker/{chain,wiring,sources,main}.py` (T4.2f), `infra/migrations/ddl/meme_lab.py` (estilo), `apps/api/hunter_api/routers/meme_desk.py`, `.claude/state/contrato-T4.6-T4.7-mesa-meme.md`.

**Entregar (migração `0028_meme_wallets`):**
1. **Coletor `wallets.py`** no meme-worker: a cada 30 s, para cada endereço observado, `getSignaturesForAddress` (desde a última assinatura vista; RPC público, orçamento próprio, ≤ 2 req/s), `getTransaction` das novas, decodificar compras/vendas na curva (`TradeEvent`) e na PumpSwap (swap), gravar `meme_wallet_trades` (`wallet`, `signature`, `slot`, `block_time`, `mint`, `side`, `sol_lamports`, `token_amount`, `fee_lamports`, `venue` curva|pool, `received_at`) — dedupe por assinatura; o que não decodificar vira `meme_wallet_trades` com `side = 'unknown'` e `raw` jsonb (nunca inventar).
2. **Posições reais derivadas** (`meme_wallet_positions`, recomputadas do livro): por carteira × mint — tokens em carteira, SOL gasto, SOL recebido, custo médio, **PnL realizado** (FIFO) e **marca** pela última fotografia/fita (`mark_source`), R com risco = SOL gasto; `first_buy_at`, `last_trade_at`, estado `open|closed`.
3. **Diário e placar:** `meme_lab_scoreboard_v1` ganha uma linha por carteira observada como conjunto `wallet:<8 chars>` (`kind = 'real_observed'`), e `infra/scripts/meme_diary.py` inclui a seção "Operações reais (carteira observada)"; `GET /meme/lab` e `GET /meme/desk` expõem as posições reais numa seção **"Reais — carteira observada"** (leitura), rótulo "REAL — observado na cadeia, não executado por este sistema".
4. **Cruzamento com o Lab:** para cada compra real, gravar no `meme_wallet_trades.lab_context` jsonb o que o portão de cada conjunto ativo dizia naquele minuto para aquele mint (`accepted` ou o motivo de recusa) — é isso que responde "o Lab teria feito o mesmo?" — e uma coluna `hype_score`/`line_reason` do minuto (T4.10a) quando existirem.
5. **Mesa/API/web:** seção "Reais" na `/meme/mesa` (aberta/fechada, PnL real em SOL e US$, o que o Lab dizia na hora) — tolerante a ausência; `pnpm gen:types`; rótulos exaustivos; `next build` compila.
6. Testes: unit com uma transação real capturada de compra e uma de venda (≤ 10 chamadas RPC, sem chave; endereço de exemplo público de um bot conhecido, não o do Everton), FIFO, dedupe; 1 testcontainer; `ruff`/`pyright`/`check_file_size.py`; docs `docs/DEPLOYMENT.md` (`MEME_WATCH_WALLETS`), `docs/plans/T4-MEME-RADAR.md` §T4.12, `docs/DATABASE.md` §40; `.claude/state/notes-T4.12.md` incremental; `git status --porcelain` ao final.

**Regras:** as de sempre — primeiro plano `timeout 290` (590 testcontainer/build), sem background, sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`, sem commit, sem `.env*`, nada real (só leitura da cadeia), `SendMessage` desabilitado, Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

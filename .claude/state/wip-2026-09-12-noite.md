# WIP — estado às 19:1x BRT de 12/09/2026 (limite semanal estourado: sonnet 17:5x, fable 19:1x; reseta 15/09 00:00 BRT)

**Produção:** `6fc3a43` (T4.2g fita por lote + T4.19 operator/3 + T4.20 rótulos) — provado: 0 reinícios, tape_coverage 97 %, operator/3 ativo com `manual_plan`, executor real ausente (Everton ainda não digitou `.env`/portões). Commits depois disso só de vault/plantão (`9e77861`, `70a1aa2`, `399d125`, `5cb5491`). Árvore limpa fora do ruído conhecido (`.claude/state/{design,tmp,exp-drafts,t346}`, `tests/e2e/design-audit.*`, `docs/DESIGN.md`, `.claude/launch.json`, `.claude/state/astra-*` antigos).

**Achado que trava a mesa:** a porta E1 pré-registrada (`flow_v2/1` = `operator/3`) exige `snipers ≤ 2` e **nenhuma** moeda com demanda tem < 6 snipers → zero propostas desde 18:44 (medido, `infra/scripts/sql/research/2026-09-12-t421-funil-e1.sql`; estudo das 21 apostas §E1). **T4.21** (`.claude/state/brief-T4.21-operador-4-porta-e1-braco-2.md`) cria `operator/4` + `flow_v2/2` (snipers ≤ 10, holders não caindo ≥ 20, criador desconhecido só com dev medido ≤ 10 %, progresso ou mcap subindo) — agente cortada no início; **nada no disco**. Versão mínima sem código novo: migração `0034` semeando `operator/4` com params existentes (`max_snipers 10`, `require_holders_rising false`) e aposentando `operator/3`.

**Everton (decisões de hoje):** dinheiro real autorizado por escrito (`06-DECISIONS/2026-09-12-teste-pequeno-meme-real`), carteira do robô criada, teto US$ 1 000/operação, proposta em dois estágios (encanamento 0,05 SOL × 5 → teto). Prefere validar **à mão** pelo card da mesa. Falta dele: `.env` (flag, chave, RPC, 5 tetos, `MEME_GATES_FILE`) e `/opt/project-hunter/run/meme/meme_gates.json` (conteúdo no chat de 17:4x). **Eu nunca envio ordem real**; a compra real sai só pelo clique dele em "Aprovar (REAL)".

**Rotinas autônomas na VPS (independem de agente):** laço de papel (6 conjuntos: flow_v2/1, hype_probe_v0/2, moonshot_v0/1 e /2, trendline_v0/1, operator/3), relógio de 15 s, fita por lote, carteira observada `6nAh…`, cron `hunter-meme-close` às 05:10 host (00:10 BRT) → `/opt/hunter-close/patch-*.diff` + `run-*.log`. **Rotina da manhã:** `scp hunter-vps:/opt/hunter-close/patch-2026091{3}-*.diff /tmp/` → `patch -p0 --dry-run` → `patch -p0` → commit por pathspec (diário meme, EXP-M*, INBOX, README, lote-meme-2026-09-13.md).

**Plantão meme:** contador 19; próximo run 19 lane 3 (ferramentas/on-chain) ≥ 19:24 BRT; depois lane 4, lane 1…; guarda de 50 min.

**Pendências ordenadas quando houver crédito:** (1) T4.21 (ou a versão mínima) → deploy → prova de proposta `operator/4` com `manual_plan`; (2) rotina da manhã do patch do cron; (3) admitir `enabled` na constraint de `mayhem_state` (M-D11) em vez de `unknown`; (4) `holder_rewards`/FeeConfig (T4.8b); (5) venda pós-migração no executor (antes do estágio 2 do teste real); (6) T4.2h — vendas do criador pela fita por mint priorizada para as candidatas (o lote não traz criador); (7) `docs/DESIGN.md`/`tests/e2e/design-audit.*`/`.claude/launch.json` — ruído antigo, decidir commit ou descarte com o Everton.

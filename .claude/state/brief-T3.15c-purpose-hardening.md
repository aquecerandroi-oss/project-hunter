# Brief T3.15c — closing the 0010 reviews: the bridge reads the protected column, the worker cannot activate, the owner DSN stays with `migrate`, the installer pins its package

**Owner:** backend-specialist (code + migration `0011` under `database-architect` review) and devops-engineer (compose/installer), one agent may do both. **Reviewers afterwards:** database-architect (0011), security-reviewer, risk-engine-guardian (bridge). **Do not commit.** **Operational rule: never a background shell; every command in the foreground with a timeout <= 5 min; testcontainers suites one file per pytest invocation; do not touch `.env*`.** Base: `main` at `b6f5c2c`. In flight elsewhere: T3.0f (`services/market-worker/**`, `infra/docker|vps/**` — coordinate: only touch the compose files **after** T3.0f lands, or leave item 4 for a T3.15d), T3.7b (`services/scanner-worker/**`, `infra/scripts/request_backfill.py`).

## Source
`.claude/state/review-T3.15-security.md` (HIGH 1–3, MEDIUM), `.claude/state/review-T3.15-db.md` ("Antes do deploy" 1–4), `docs/DATABASE.md` §22, `docs/SECURITY.md` §2.

## Deliver
1. **The bridge decides on the column, not the envelope** (security HIGH 2): `services/execution-worker/hunter_execution_worker/bridge_repo.py:~152` already joins `strategy_versions v`; select `v.purpose` and use it in `bridge_screen.screen_signal` instead of `signal.purpose` from `supporting_features`. Keep the envelope's label as a **cross-check**: if envelope and column disagree, refuse with reason `purpose_mismatch` and log at warning (a signal stamped by a worker on an older build). Tests: column `paper` + envelope `research_only` → refused `purpose_mismatch`; column `research_only` + envelope `paper` → refused `purpose_mismatch`; both `paper` → passes.
2. **Migration `0011_strategy_activation_owner`** (db "Antes do deploy" + security MEDIUM): revoke from `hunter_worker` the column grants `UPDATE (status, activated_at, deprecated_at, code_ref, parameters_schema, default_parameters, params_format)` and `DELETE` on `strategy_versions` — the worker only ever reads that table (the db review verified every production query is SELECT). Keep `INSERT` only if something in production inserts (the review found nothing: `seed.py` runs as owner) — if nothing does, revoke `INSERT` too and say so. Tests in `test_schema_privileges.py`: the worker cannot activate a draft, cannot delete, can still SELECT. `docs/DATABASE.md` §23 + fix §16.1 (frozen columns list includes `purpose`), §17.6 (`WORKER_WRITE_TABLES` no longer includes `strategy_versions` at table level), §15.6 (`lock_timeout` note: migrations run with workers up on the VPS; `0011` is grants only), and the "22 characters" vs 21 mismatch.
3. **The activation script audits every failure** (security MEDIUM): wrap `_run` so any exception, not only `Refused`, writes a `system_events` row (`strategy_version_activation_error`, message truncated, no secrets) before re-raising with exit code 2. Test with a forced exception.
4. **Owner DSN only where it is used** (security HIGH 1): in `infra/vps/docker-compose.prod.yml` (and the dev compose if it mirrors), `DATABASE_URL_MIGRATIONS` leaves the shared `x-prod-db-env` anchor and is set only on the `migrate` service (and whatever one-shot service runs the ops scripts — define `ops` as a profile-only service that has it, so `activate_strategy_version.py`/`open_paper_wallet.py`/`request_backfill.py` run as `docker compose run --rm ops ...`). `docs/DEPLOYMENT.md`: how to run an ops script now. **Coordinate with T3.0f** on the compose files.
5. **Installer supply chain** (security HIGH 3 + MEDIUM): `infra/hermes/install.sh` pins `obsidian-mcp` to an exact version (`obsidian-mcp@2.x.y`, look up the current one with `npm view obsidian-mcp version`) and documents how to bump; the header no longer claims "never reads .env" — say what it does (clones the Hermes profile's own `.env`, never the repo's). `docs/HERMES.md` updated.

## Prove
`services/execution-worker/tests/test_bridge_*.py` per file, `packages/core/tests/integration/test_schema_privileges.py`, `test_migrations.py` (0011 round trip + `alembic check`), `services/strategy-worker/tests/test_activation.py`, `docker compose -f infra/vps/docker-compose.prod.yml config` parses, `ruff`/`pyright`/`check_file_size.py`. Report in Portuguese, extended format; `.claude/state/notes-T3.15c.md`.

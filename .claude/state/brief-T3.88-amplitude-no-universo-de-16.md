# Brief T3.88 — the breadth series follows the shadow universe (16 markets with ≥ 90 d), so EXP-0027 becomes testable on 90 days of history (prepares tomorrow's daily batch, 2026-09-12)

**Facts:** T3.77 (fd30528) built `breadth_5m` over ALL monitored perpetuals (200) with an 80 % coverage floor; only 16 markets have 90 days of candles, so a 90-day backfill would be `insufficient_coverage` on 87 of 91 days and EXP-0027 was declared prospective-only. T3.82 (5c6a891, Everton approved) redefined the shadow universe as "markets with ≥ `SHADOW_UNIVERSE_MIN_HISTORY_DAYS` (90) of 1-min candles" (`services/strategy-worker/hunter_strategy_worker/universe.py`, cache 1 h). Astra's next-to-test hypothesis from today's plantão is H-P18 (dispersion BTC × alts as a state), a cousin of H-P8 (breadth). Producer: `services/scanner-worker/hunter_scanner_worker/{breadth.py, breadth_job.py, breadth_repo.py}`; series math `packages/indicators/hunter_indicators/breadth/series.py`; table `market_breadth` (0019; UNIQUE (exchange, end_time, version)); backfill CLI `infra/scripts/backfill_breadth.py` (+ `breadth_windows.py`), never run; gate `breadth_gate.py`.

## Deliver
1. **Universe consistency**: the breadth producer's universe becomes the SAME rule as the shadow universe (share the helper — move the "≥ N days of history" membership into a `packages/core` or `hunter_indicators` module both workers import; the scanner must not import the strategy-worker package), with `universe_total` = the 16, coverage floor 80 % applied to that universe; the series version bumps to `breadth_v2` (a different universe is a different series — `breadth_v1` rows stay immutable; the gate/policy refers to the version explicitly — check how `breadth_gate.py` picks the version and make it a policy field or a setting, never implicit). Tests: unit (universe helper boundary, v2 vs v1 separation), extend `test_breadth_job.py` (testcontainer, one invocation, `timeout 290`).
2. **Backfill readiness**: `backfill_breadth.py` computes `breadth_v2` over 2026-06-12 → today for the 16-market universe; dry-run locally on a testcontainer with synthetic candles; report the expected row count (129 600 minutes) and runtime; DO NOT run on the VPS (the orchestrator deploys, then runs it via `compose.sh ops`).
3. **Pre-registration draft update**: `.claude/state/exp-drafts/EXP-0027-amplitude.md` → now a 90-day historical test on the 16-market universe: arms `mean_reversion v10 + breadth=0.10-0.60` (cell inside), the falsification arm `breadth=0.60-1.00` (broad sell-off, expected worse per KB-0083), tercile cuts fixed on June–July, success rule as EXP-0026/0028 (paired-by-bar Δ measures zero by construction for a pure gate — use conditional × unconditional with day-block CI, plus the pairing as the "gate is a gate" check), and the H-P18 arm if it can be expressed with the same series (dispersion = share of the 16 with 24 h return below BTC's? — if it needs a different series, say so and leave H-P18 for a later batch).
4. Docs: `docs/PIPELINE.md` §4b/§6b one paragraph, `docs/ACTIVATION.md` §7c note on `breadth_v2`; `.claude/state/notes-T3.88.md` with commands/outputs and the full `git status --porcelain` list of your files. Files ≤ 350; `ruff`/`pyright`/`check_file_size.py` clean.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; no VPS writes; no `git pull` on the VPS; never edit the closure modules; do not edit `apps/**`. Report in Portuguese ≤ 10 lines, times in Brasília (UTC−3, compute it).

# T2.8b — execução única e datada das suítes do M2

**Data:** 2026-09-07 (UTC). **Executor:** `test-engineer`. **Máquina:** local do Everton
(Windows 11, Docker Desktop, stack do compose no ar: `docker-api-1`, `docker-web-1`,
`docker-postgres-1`, `docker-redis-1`, `docker-scanner-worker-1`, `docker-strategy-worker-1`,
quatro `docker-market-worker-*`, todos *healthy*).

Fecha a condição nº 4 do VEREDITO de `docs/reports/M2.md`: os três entregáveis de teste da T2.8
mais **uma execução única e datada** de todas as suítes do M2. Tudo abaixo foi rodado em primeiro
plano, uma suíte por vez (nunca duas com testcontainers ao mesmo tempo), e a saída é colada como
saiu. **Nada de código de produto foi consertado nesta passagem** — o que falhou está registrado
com causa e dono.

Astra indisponível até **2026-09-12** (cota do Codex, `.claude/state/astra-stderr.log`): esta
entrega **não tem segunda opinião**. Fica registrado, não escondido.

---

## Resumo

| # | Suíte | Comando | Resultado | Duração |
|---|---|---|---|---|
| 1 | `packages/indicators` | `uv run pytest packages/indicators -q` | **874 passed** | 61,4 s |
| 2 | `services/scanner-worker` (com `test_load.py`) | `uv run pytest services/scanner-worker -q` | **107 passed, 1 xfailed** | 456,7 s |
| 3 | `apps/api` (recorte M2) | `uv run pytest apps/api -q -k "radar or opportunit or anomal or regime"` | **2 failed, 70 passed**, 589 deselected | 235,2 s |
| 4 | `apps/web` (Vitest) | `pnpm --filter @hunter/web test` | **533 passed** (1ª execução: 1 flaky, ver abaixo) | 73,6 s |
| 5 | `tests/integration/test_m2_pipeline.py` (**novo**) | `uv run pytest tests/integration/test_m2_pipeline.py -q` | **8 passed** | 168,8 s |
| 6 | `tests/e2e/{radar,opportunity}.spec.ts` (**novos**) | `pnpm exec playwright test radar.spec.ts opportunity.spec.ts` | **3 skipped** — *não executáveis nesta sessão*, ver §6 | 15 s |
| 7 | portões estáticos | `ruff check` / `ruff format --check` / `pyright` / `tsc` / `prettier` | **verdes** | 18 s |

---

## 1. `packages/indicators`

```
$ date -u                                  -> 2026-09-07T05:14:50Z
$ uv run pytest packages/indicators -q
........................................................................ [ 82%]
........................................................................ [ 90%]
........................................................................ [ 98%]
..........                                                               [100%]
874 passed in 61.36s (0:01:01)
$ date -u                                  -> 2026-09-07T05:15:56Z
```

Mesmo número do relatório do M2 (874). Nada a registrar.

## 2. `services/scanner-worker` (inclui `tests/test_load.py`)

```
$ date -u                                  -> 2026-09-07T05:16:03Z
$ uv run pytest services/scanner-worker -q
........................................................................ [ 66%]
...x................................                                     [100%]
=========================== short test summary info ===========================
XFAIL services/scanner-worker/tests/test_load.py::test_two_hundred_markets_at_one_tick_a_second_stay_inside_the_budget
  - MEASURED, and 8% out: cycle p99 3.23 s against the 3.0 s budget, with every one of the 200
    markets holding a FULL hot state (1500 candle rows and a maxed-out 2000-trade tape). [...]
    Kept as a strict xfail so the day that construction path lands, this fails by passing.
107 passed, 1 xfailed in 456.68s (0:07:36)
$ date -u                                  -> 2026-09-07T05:23:49Z
```

O `xfail(strict=True)` do `test_load.py` **continua vermelho-quando-passa**, como projetado: ele
mede p99 de ciclo **3,23 s** contra o orçamento de 3,0 s da decisão conjunta. Isto é a mesma dívida
que o item 5 de `docs/reports/M2.md` já registra (condição nº 3 do VEREDITO), agora com número de
hoje. Não é uma falha desta execução.

> Nota operacional: a suíte levou 7 min 36 s e ultrapassou o teto de 5 min por comando; o
> harness a moveu para segundo plano e ela foi aguardada até o fim antes de qualquer outra suíte
> com testcontainers começar. Nenhuma execução concorrente.

## 3. `apps/api`, recorte do M2 — **2 falhas, registradas e não consertadas**

```
$ date -u                                  -> 2026-09-07T05:24:03Z
$ uv run pytest apps/api -q -k "radar or opportunit or anomal or regime"
=========================== short test summary info ===========================
FAILED apps/api/tests/integration/test_radar_api.py::test_list_radar_risk_blocked_reflects_the_organizations_kill_switch
FAILED apps/api/tests/integration/test_radar_api.py::test_list_radar_warning_kill_switch_does_not_block_entries
2 failed, 70 passed, 589 deselected, 1 warning in 235.19s (0:03:55)
$ date -u                                  -> 2026-09-07T05:28:18Z
```

Reproduzido isoladamente (2026-09-07T05:28:26Z .. 05:29:45Z,
`uv run pytest apps/api/tests/integration/test_radar_api.py -q -k "kill_switch"` -> `2 failed`),
com a causa exata:

```
apps/api/tests/integration/analysis_fixtures.py:310: in set_org_kill_switch
    await session.commit()
E  sqlalchemy.exc.DBAPIError: <asyncpg.exceptions.RaiseError>: organization
   01a07a57-e300-7f59-b330-adbc7492e35f moved its kill switch from ACTIVE to WARNING without an
   audited transition
E  HINT: write the kill_switch_transitions row in the same transaction, and let it be the newest
   one for this scope: the state the workers read and the history a human reads may not disagree,
   and leaving a latched block needs a named person
```

**Diagnóstico (não corrigido, por instrução):** não é o roteador do Radar. É a *fixture* do M2
`apps/api/tests/integration/analysis_fixtures.py::set_org_kill_switch`, que muda
`organizations.kill_switch_state` **sem** gravar a linha de `kill_switch_transitions` na mesma
transação. O gatilho que exige essa linha nasceu no M3 (`infra/migrations/ddl/paper.py`, aplicado
pela migração `0006_paper_wallet`), depois que a fixture foi escrita. Ou seja: **uma migração do M3
quebrou duas asserções do M2**, e o conserto é de uma linha na fixture (escrever a transição junto),
não no produto.

- Dono sugerido: quem fechar o Risk Engine do M3 (`risk-engine-guardian` / `backend-specialist`).
- Impacto no VEREDITO: o critério "testes verdes" do M2 **não** fecha hoje por causa disto.

## 4. `apps/web` (Vitest) — verde, com um **flake** registrado

Primeira execução (2026-09-07T05:29:59Z .. 05:31:32Z):

```
 Test Files  1 failed | 70 passed (71)
      Tests  1 failed | 532 passed (533)
   Duration  84.06s
```

A falha foi um `waitFor` estourando em `apps/web/tests/candles-chart.test.tsx` ("The chart itself is
never torn down/recreated for a theme change"), num ambiente com 71 jsdom criados e ~940 s de tempo
de ambiente — sintoma clássico de tempo de máquina, não de regressão.

Reexecutado duas vezes em seguida, **verde nas duas**; a terceira ficou registrada:

```
$ date -u                                  -> 2026-09-07T05:34:48Z
$ pnpm --filter @hunter/web test
 Test Files  71 passed (71)
      Tests  533 passed (533)
   Duration  73.56s
$ date -u                                  -> 2026-09-07T05:36:08Z
```

**Registrado como flake, não como verde limpo:** 1 falha em 3 execuções do mesmo commit, sempre no
mesmo arquivo. Merece um `waitFor` com timeout explícito ou `pool: 'vmThreads'` (o próprio Vitest
sugere), mas isso é mudança em `apps/**`, fora do escopo desta tarefa.

## 5. `tests/integration/test_m2_pipeline.py` — **novo, 8 passed**

```
$ date -u                                  -> 2026-09-07T05:36:27Z
$ uv run pytest tests/integration/test_m2_pipeline.py -q
........                                                                 [100%]
8 passed in 168.81s (0:02:48)
$ date -u                                  -> 2026-09-07T05:39:35Z
```

O que ele prova, contra Postgres + Redis reais (testcontainers, migrados por Alembic) e a API real
(`httpx.ASGITransport` sobre o app do FastAPI):

| Etapa | Asserção fechada |
|---|---|
| candles sintéticas (semente, 7 dias + 400 min de warm-up) | 10 080 cortes, `complete=True`, **288 buckets** de baseline (12 features × 24 horas) |
| bootstrap | `relative_volume_5m`/`atr_14_pct` presentes; `trade_velocity_1m`/`buy_pressure_5m`/`spread_pct` **ausentes com motivo** (candle não reproduz tape/livro) |
| anomalias | exatamente **`VOLUME_SPIKE` + `TRADE_VELOCITY_SPIKE`**, ambas `ACTIVE`, severidade **100,00** (saturação além de 6 MADs) |
| estágio | `covered_until` libera tape → **EARLY publicado**, direção `long`, **as quatro confirmações verdadeiras**, com histerese de duas observações |
| estágio (caso de borda) | **sem** `covered_until` → `trade_velocity_1m` = `insufficient_coverage` e o estágio **recusa** publicar EARLY |
| regime | **`UNKNOWN` declarado**: `volatility_reference.usable = false`, `reason = volatility_warmup`, `window_days = 30`, `distinct_days < 30`; e `trend_input_unavailable` (não há BTCUSDT neste universo sintético). Uma linha `UNKNOWN` em `market_regimes`. **Nenhum regime foi fabricado** |
| score | `volume` 75,0000×0,20 = 15; `order_flow` 33,3333×0,15 = 5; `momentum` 0×0,20 = 0; `anomalies` 100×0,05 = 5; `agent_consensus` 0; Early-Movement **+10** (fora do orçamento de pesos) → **score 35,00**, confiança **0,5000**; indisponíveis: `derivatives`/`liquidity` `no_usable_input`, `market_regime` `regime_unknown`, `external_intelligence` `feature_not_implemented` |
| explicação ("Por que estamos olhando isso?") | *binding* verificado: `resumo == frases[0].texto`; `frases[score].valores` = score/confiança/direção do próprio `ScoreResult`; cada frase de componente carrega o `normalizado`/`contribuicao`/`peso` **daquele** componente, e cada indisponível carrega o `motivo` |
| linha | 1 `opportunities` com `status=ANOMALY`, `score=35.00`, **`stage=EARLY`**; 2 `anomalies`; `radar:scores` com o score idêntico ao da coluna |
| API | `GET /api/v1/radar?q=…` devolve a linha com `score 35.00`, `stage EARLY`, `status ANOMALY` e o `opportunity_id` da linha gravada |
| reprodutibilidade | duas execuções independentes do mesmo corte → `envelope_bytes` **byte a byte iguais**, envelope e explicação |

### Prova de que a suíte falha pelo motivo certo (mutação)

Regra do projeto: um teste que passa de primeira é suspeito. Além das três falhas legítimas que
apareceram durante a escrita (`MissingWeightsError` do `load_policy`, conjunto de anomalias errado,
formato de `supporting_features` errado), foi feita **uma mutação deliberada da evidência** —
`SPIKE_VOLUME_MULTIPLIER` de 40 para 4, isto é, **sem pico injetado** — e a suíte ficou vermelha
exatamente onde devia (2026-09-07T~05:0xZ):

```
$ uv run pytest tests/integration/test_m2_pipeline.py -q     # com a mutação
>       assert stored[0].score == Decimal("35.00")
E       AssertionError: assert Decimal('30.00') == Decimal('35.00')
=========================== short test summary info ===========================
FAILED ...::test_the_injected_spike_fires_volume_spike_at_saturated_severity
FAILED ...::test_the_score_components_and_the_explanation_state_the_same_numbers
FAILED ...::test_the_scored_episode_reaches_opportunities_radar_scores_and_the_api
3 failed, 5 passed in 171.30s (0:02:51)
```

A mutação foi revertida imediatamente (`SPIKE_VOLUME_MULTIPLIER = Decimal(40)`), e a execução
datada do §5 acima é do arquivo já revertido.

## 6. `tests/e2e/radar.spec.ts` e `tests/e2e/opportunity.spec.ts` — **escritos, não executáveis nesta sessão**

```
$ date -u                                  -> 2026-09-07T05:40:07Z
$ pnpm exec playwright test radar.spec.ts opportunity.spec.ts
Running 3 tests using 3 workers
  -  1 [chromium] › opportunity.spec.ts:35:3 › ... why panel explains the score ...
  -  2 [chromium] › radar.spec.ts:53:3 › ... rows carry a real score ...
  -  3 [chromium] › radar.spec.ts:99:3 › ... filter and the score sort ...
  3 skipped
$ date -u                                  -> 2026-09-07T05:40:22Z
```

Os três *skip* são o portão honesto de `markets.spec.ts`: sem
`CLERK_E2E_PUBLISHABLE_KEY`/`CLERK_E2E_SECRET_KEY` no ambiente, o spec se recusa em vez de fingir.

**Com as chaves de teste do Clerk exportadas em tempo de execução** (lidas do `.env` local, nunca
escritas no repositório), os specs saem do *skip* e chegam ao `page.goto` — e aí batem numa
**limitação do ambiente desta sessão, não do repositório**:

```
Error: page.goto: net::ERR_NAME_NOT_RESOLVED at http://localhost:3000/sign-up
Error: page.goto: net::ERR_NAME_NOT_RESOLVED at http://127.0.0.1:3000/sign-up
Error: page.goto: net::ERR_NAME_NOT_RESOLVED at http://192.168.15.11:3000/sign-in
```

Diagnóstico feito com o Chromium do próprio Playwright, fora do runner:

| Alvo | Resultado |
|---|---|
| `https://example.com` (externo) | **200** |
| `http://127.0.0.1:3000/sign-in` | `ERR_NAME_NOT_RESOLVED` |
| `http://localhost:3000/sign-in` | `ERR_NAME_NOT_RESOLVED` |
| `http://192.168.15.11:3000/sign-in` (IP da LAN) | `ERR_NAME_NOT_RESOLVED` |
| idem com `--no-sandbox`, `--no-proxy-server`, `--proxy-bypass-list`, `--host-resolver-rules` | `ERR_NAME_NOT_RESOLVED` |
| idem com `proxy: { server: "direct://" }` | `ERR_PROXY_CONNECTION_FAILED` |
| `curl http://127.0.0.1:3000/sign-in` no mesmo shell | **200** |
| `reg query ...\Internet Settings` | `ProxyEnable = 0x0` (sem proxy do Windows) |

Ou seja: o `curl` chega no stack local, o Chromium não; o tráfego do navegador desta sessão sai por
um proxy de egresso que resolve hosts externos e **recusa loopback/LAN**. Não há mudança de código
que contorne isso, e mudar configuração de sandbox/ambiente está fora do que esta tarefa pode fazer.

**Portanto:** os dois specs estão escritos, tipados (`tsc --noEmit` verde), formatados
(`prettier --check` verde) e coletados pelo Playwright (`--list` mostra 11 testes em 6 arquivos, os
três novos incluídos), mas **nenhuma asserção deles foi observada verde contra o stack local**. Quem
rodar numa sessão com acesso a `localhost:3000` fecha isto com:

```bash
CLERK_E2E_PUBLISHABLE_KEY=... CLERK_E2E_SECRET_KEY=... \
  pnpm --filter @hunter/e2e exec playwright test radar.spec.ts opportunity.spec.ts
```

(chaves de **modo teste** da instância de desenvolvimento do Clerk — as mesmas que
`markets.spec.ts` exige; e-mails `+clerk_test` com OTP `424242`, mecanismo documentado do próprio
Clerk, nenhuma caixa de entrada real.)

## 7. Portões estáticos

```
$ date -u                                  -> 2026-09-07T05:39:42Z
$ uv run ruff check tests                                    -> All checks passed!
$ uv run ruff format --check tests                           -> 21 files already formatted
$ uv run pyright tests/integration/test_m2_pipeline.py       -> 0 errors, 0 warnings, 0 informations
$ date -u                                  -> 2026-09-07T05:40:00Z

$ (tests/e2e) pnpm exec tsc --noEmit                         -> exit 0
$ pnpm exec prettier --check tests/e2e/{radar,opportunity}.spec.ts tests/e2e/clerk-session.ts
                                                             -> All matched files use Prettier code style!
$ (tests/e2e) pnpm exec playwright test --list               -> Total: 11 tests in 6 files
```

---

## O que fica em aberto depois desta passagem

1. **`apps/api` — 2 vermelhos reais** (§3). Fixture do M2 contra gatilho do M3. Não consertado aqui
   por instrução; é uma linha em `analysis_fixtures.py`.
2. **`tests/e2e/{radar,opportunity}.spec.ts` sem execução observada** (§6). Limitação de rede do
   Chromium desta sessão. Precisa de uma passagem numa sessão com loopback liberado.
3. **`test_load.py` continua 8% fora do orçamento** (§2) — é a condição nº 3 do VEREDITO, não uma
   novidade desta execução.
4. **Flake em `apps/web/tests/candles-chart.test.tsx`** (§4), 1 em 3.
5. **Sem segunda opinião da Astra** (cota até 2026-09-12), inclusive sobre o desenho do
   `test_m2_pipeline.py` — em particular sobre a decisão de deixar o regime em `UNKNOWN` declarado
   em vez de sintetizar 30 dias.
6. **Semeadura de oportunidade para o e2e não foi construída.** O brief previa que, sem episódio
   aberto no stack local, o spec semeasse um pelos escritores do scanner e limpasse depois. Não foi
   feito, por dois motivos: (a) o Postgres local **tem 14 episódios** agora
   (`select count(*) from opportunities` -> 14), então o caminho nunca rodaria hoje e nasceria
   "testado e inalcançável ao mesmo tempo" — o anti-padrão que o próprio `replay_io.py` documenta;
   (b) escrever um episódio com `expired_at IS NULL` no banco de desenvolvimento colide com o índice
   único parcial `uq_opportunities_open_per_market` contra o `scanner-worker` que está rodando ali,
   e derrubar o worker do Everton para semear um teste é um preço que eu não pago sem ele mandar.
   O caminho de escrita real (`flush_batch` + `publish_radar`) está provado no §5, contra
   testcontainers. Os specs, sem linhas, dão `test.skip` **com motivo nomeado**, nunca verde vazio.

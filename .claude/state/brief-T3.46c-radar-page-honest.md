# Brief T3.46c — a página do Radar/Opportunities diz a verdade sobre o próprio estado: nenhum mercado jamais passou de NORMAL, cobertura 25/217, baselines 8 % prontas, detectores desarmados e silenciosos — e o ponto de reabertura do estudo

**Owner:** frontend-specialist (+ the small API read for the coverage numbers, `apps/api/hunter_api/{repositories,services,routers}/radar*.py`, `scanner` heartbeat read). **Reviewer afterwards:** code-reviewer. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; no testcontainers (slots taken; API unit tests only, integration read-only reasoning); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; do not stop or recreate local stack containers; VPS read-only.** Base: `main` at `2e39774`. Scope: `apps/web/app/(app)/[orgSlug]/radar/**`, `apps/web/app/(app)/[orgSlug]/opportunities/**`, `apps/web/components/radar/**`, `apps/web/hooks/useRadarPage.ts`, `apps/api/hunter_api/**/radar*.py` (additive fields only), tests. Do not touch `apps/web/hooks/useRealtime.ts`, `components/system/live-status.tsx`, topbar (T3.44d owns them) nor `regime-tile.tsx` (T3.43b).

## Source of truth: `.claude/state/notes-T3.46.md` (read all) — facts to surface
- Radar born 2026-09-07 02:24Z; 1,8 days of history; max score ever 38,33 while the first page threshold is 40 → the Opportunities list never had an item; 100 % of 20 392 samples `NORMAL`.
- Coverage: 25 of 217 monitored markets have any anomaly — the pointer of an alphabetical sequential bootstrap, not a choice; `baselines_usable 9029 / under_construction 102597` (8,1 %); `detectors_disarmed` names 3 detectors with reasons; 3 others (`ORDERBOOK_IMBALANCE`, `OPEN_INTEREST_SPIKE`, `TRADE_VELOCITY_SPIKE`) produce zero **without** a reason (a defect, T3.46b will fix the scanner — the page must show "sem motivo declarado" honestly, never hide it).
- Verdict: no predictive value measurable yet; reopen when baselines ≥ 60 % passing the v2 gate AND anomalies covering ≥ 150 markets AND ≥ 14 days.

## Deliver
1. API (additive): a `radar_coverage` block on the existing radar summary endpoint (or a new small endpoint) with `markets_monitored`, `markets_with_anomaly`, `baselines_usable`, `baselines_under_construction`, `bootstrap_pointer` (the heartbeat `baselines_state` string), `detectors` = [{type, rows_31d, disarmed_reason|null}], `max_score_ever`, `first_anomaly_at`, `as_of`. Real reads (heartbeat via the existing scanner heartbeat reader; counts via repository SQL); no cache older than 60 s. `pnpm gen:types`. Unit tests + one integration test if a slot frees (otherwise written and marked to run by the orchestrator).
2. Web: a "Estado do Radar" strip at the top of Radar and Opportunities: "Radar em construção — cobre 25 de 217 mercados (bootstrap em ordem alfabética: 1000FLOKIUSDT 4/200) · baselines prontas 8 % · maior score já visto 38 (primeiro degrau 40) · nenhum mercado passou de NORMAL desde 07/09 23:24 Brasília"; a detectors table (12 types: produzindo / desarmado: motivo / silencioso sem motivo); the empty Opportunities state stops implying "nada interessante agora" and says the list never lit and why; link "quando o estudo reabre" with the three conditions and their current values (progress bars from the real numbers). D16/D17/D19 copy, Brasília time. Tests with the real shapes as fixtures.
3. Screen proof after the orchestrator rebuilds (`bash .claude/state/tmp/run-design-audit.sh -g "screens 1440 dark"` — radar, opportunities).

## Prove
`pnpm --filter web lint|typecheck|test`, api unit tests, `ruff`/`pyright`; report in Portuguese, extended format; `.claude/state/notes-T3.46c.md`.

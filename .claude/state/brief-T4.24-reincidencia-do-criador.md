# Brief T4.24 — a reincidência do criador: recusar quem já despejou uma moeda anterior (E2 v2, EXP-M6 braço 2) (15/09/2026 17:4x BRT)

**Pedido do Everton (15/09 17:4x BRT):** "as apostas, temos que tomar cuidado, pois tem gente que sobe moeda golpe e compramos e por isso perdemos."

**Medido no banco da VPS (176 apostas de papel medidas até 15/09 17:4x; SQL do orquestrador em `infra/scripts/sql/research/2026-09-15-t424-reincidencia.sql`):** 89 apostas tinham criador com moeda anterior no nosso banco; em **31** o criador **já tinha vendido** numa moeda anterior (`meme_features_1m.creator_sold = true` antes da criação da moeda apostada). Essas 31: R médio −0,168 (contra −0,140 nas outras 145), **26 das 85 saídas `creator_dump`** e só 4 ganhas (13 %, contra 19 %). Sinal real, modesto: excluir reincidentes teria evitado −5,2 R e 4 acertos. Não é bala de prata — a maioria dos golpes é de criador **novo** (carteira nova por moeda) — mas é o filtro mais barato que ainda não existe.

**Entregar (padrão da T4.16 `pedigree.py`/EXP-M6 e das migrações 0034–0038):**
1. Feature de pedigree nova, calculada na hora da proposta a partir do nosso banco: `creator_prior_dump_count` = moedas anteriores do mesmo criador (qualquer janela, até a criação da moeda julgada) em que (a) `meme_features_1m.creator_sold = true` **ou** (b) `meme_paper_bets.creator_sold_seen_at IS NOT NULL` (vigilância da cadeia, T4.2h) **ou** (c) uma aposta nossa saiu por `creator_dump`; e `creator_prior_dead_count` = moedas anteriores que morreram (< 20 % do topo em 30 min, se houver série; senão declarar não medido). Gravadas em `reasons` (`feature: pedigree`, junto de `creator_prior_mints_1h`/`symbol_dup_24h`).
2. Exclusão `creator_repeat_dumper` (parâmetro `pedigree_repeat_dumper: true`, desligado por padrão nos conjuntos congelados): recusa quando `creator_prior_dump_count ≥ 1`; desconhecido (criador nulo) já recusa por `creator_unknown`.
3. Braços pré-registrados como `descartar`: `flow_v2/5` = `flow_v2/2` + `pedigree_repeat_dumper` e `operator/5` = `operator/4` + o mesmo (a mesa passa a usar o filtro; `operator/4` aposentado como as anteriores) — migração `0039_meme_creator_repeat` (ids `…0010`/`…0011`), invariante de um `operator` ativo, downgrade recusa com referências; `HEAD_REVISION → 0039`; os `test_0038_*` estagiados; `test_0039_*`.
4. Um contador diário no fechamento (T4.15) por conjunto: apostas recusadas por `creator_repeat_dumper` no tique (via `meme_lab_ticks.refusals`) e R das apostas com `creator_prior_dump_count ≥ 1` nos conjuntos que não filtram — a comparação que julga o braço.
5. Obsidian: EXP-M6 "Braço 2 — reincidência (15/09/2026)" com os números acima e previsão `descartar` (+ a régua); docs `DATABASE.md` §51, plano §T4.24, `RISK_ENGINE_MEME.md` §10.14; testes unit + `test_migrations -k "0038 or 0039 or alembic_check or upgrade_head"` + persistência (`test_lab_persistence`); `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; `.claude/state/notes-T4.24.md`; `git status --porcelain` ao final.

**Regras:** primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real; não tocar `apps/web/**` (rótulo `creator_repeat_dumper` → "criador reincidente" fica listado nas notas para o front), `services/meme-executor/**`, `packages/exchange-adapters/**`, nem `obsidian/02-MARKET`/`00-INBOX`/`plantao-meme` (plantão em voo); `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 10 linhas em português, horários em Brasília.

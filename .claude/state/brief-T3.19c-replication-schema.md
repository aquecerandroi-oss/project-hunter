# Brief T3.19c — migration `0012_replication`: the replication cohort and `promising_at` exist in the schema

**Owner:** database-architect. **Reviewers afterwards:** risk-engine-guardian (cohort isolation), security-reviewer (grants). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time (other agents use Docker); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not touch `.env*`.** Base: `main` at `f7c76b8`. In flight elsewhere: T3.15e (`services/execution-worker/**`, `infra/scripts/activate_strategy_version.py`, `services/strategy-worker/tests/test_activate_paper_line.py`), T3.17b (`apps/web/components/lab/**`), Sexta-feira T3.21 (`obsidian/**`, `docs/OBSIDIAN.md`, `infra/scripts/obsidian_lint.py`) — do not edit those paths.

## Source
`.claude/state/notes-T3.19.md` (CONCERNS 1 and 2), `docs/plans/REPLICATION.md` §3–§6, `infra/migrations/ddl/shadow.py` (`ck_shadow_episodes_cohort_format`), `packages/core/hunter_core/strategies/envelope.py` or wherever `SHADOW_COHORT_PATTERN` lives (`git grep -n SHADOW_COHORT_PATTERN`), `services/strategy-worker/hunter_strategy_worker/{record,catalogue,replication}.py`, `docs/DATABASE.md` §16, §22, §23.

## Deliver
1. **`0012_replication`** (`infra/migrations/versions/0012_replication.py` + `ddl/replication.py`, frozen lists as the other revisions): (a) widen the cohort CHECK on `shadow_episodes` (and any sibling CHECK on `agent_signals`/`signal_outcomes` if one exists) to accept `replication:<uuid>:<k>` with `k` 1..99, keeping `prospective` and `replay:<uuid>` byte-identical; (b) `strategy_versions.promising_at timestamptz NULL` + `promising_by text NULL` (the scoreboard verdict that set it) — written only by the owner connection (same grant shape as `purpose`, 0010/0011: the worker cannot name it); (c) `strategy_versions.replication_parent_id uuid NULL REFERENCES strategy_versions(id)` and `replication_index smallint NULL` so a sibling's lineage is a column, not a `changelog` sentence — `replicate_strategy_version.py` (T3.19) is updated to write them (coordinate: that file is not in flight; `activate_strategy_version.py` is — do not touch it). CHECK: `replication_index IS NULL` ⇔ `replication_parent_id IS NULL`; unique `(replication_parent_id, replication_index)`. Downgrade refuses when any row carries a replication parent or a `promising_at` (§17.7 pattern). `docs/DATABASE.md` §24.
2. **`SHADOW_COHORT_PATTERN`** in the code accepts the same grammar; the strategy-worker stamps a sibling's cohort as `replication:<parent_id>:<k>` when the version row has `replication_parent_id` (read it in `catalogue.py`; `record.py` uses it) — so a sibling's signals carry the cohort the bridge already refuses (`cohort_not_live`, T3.15e). Tests in `services/strategy-worker/tests` (a sibling version emits with the replication cohort; a normal version keeps `prospective`).
3. **`promising_at` is set by one place**: a small function `mark_promising(session, version_id, verdict_source)` in `hunter_strategy_worker/replication.py` (owner connection), idempotent (never moves an existing timestamp), audited in `system_events`; the T3.18 scoreboard service reads the column for the out-of-sample block. Do **not** wire the API in this task; write the contract line in `notes-T3.19c.md`.
4. Tests: `test_migrations.py` (round trip, `alembic check`, downgrade guards), `test_schema_privileges.py` (worker cannot write the new columns), strategy-worker cohort tests. `ruff`/`pyright`/`check_file_size.py`.

## Prove
Report in Portuguese, extended format (STATUS · FILES · TESTS with real output · CONCERNS · role × column table); `.claude/state/notes-T3.19c.md`.

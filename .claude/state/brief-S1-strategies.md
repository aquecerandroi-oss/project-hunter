# Brief S1 — `hunter_core.strategies`: framework puro + `momentum_v1` + `volume_anomaly_v1`

**Owner:** quant-engineer (opus). **Revisão cruzada depois:** outro quant-engineer, code-reviewer, Astra (`bash infra/scripts/astra.sh ask S1-strategies "..."`). **Não commitar** — o orquestrador commita após revisar `git diff --stat`.

## Leia antes (nesta ordem)
1. `CLAUDE.md` (regras duras: Decimal, UTC, sem look-ahead, ≤ 350 linhas por módulo, TDD, comandos canônicos).
2. `docs/plans/SHADOW-LAB.md` — "Decisão conjunta" itens 1, 2, 3, 4, 7 e 10, "Desenho (consolidado)", tarefa **S1** e checklist "S1 — Estratégias e contratos puros". É o contrato; nada é opcional.
3. `docs/PIPELINE.md` (§ estratégias/agentes) e `docs/ARCHITECTURE.md` §6 (`hunter_core.strategies` previsto), `docs/plans/M2.md` T2.2 (ATR de Wilder, seed/âncora — a fórmula tem de ser compatível).
4. `packages/core/hunter_core/domain/market.py` (`NormalizedCandle`, `NormalizedFunding`, `NormalizedOpenInterest`), `packages/core/hunter_core/domain/enums.py`, `packages/core/hunter_core/db/models/agents.py` (`strategies`, `strategy_versions.default_parameters`, `agent_signals` campos: `direction`, `confidence`, `entry_zone`, `stop`, `targets`, `invalidations`, `expected_holding_s`, `reason`, `supporting_features`).
5. `.claude/rules/astra-second-opinion.md` — peça a opinião da Astra sobre o desenho do `StrategyContext` antes de implementar e sobre o diff antes de reportar.

## Entregar (arquivos permitidos: `packages/core/hunter_core/strategies/**`, `packages/core/tests/unit/test_strategies*.py`, `packages/core/tests/unit/strategies/**`)
- `hunter_core/strategies/__init__.py` (exports), `base.py`: `Strategy` (Protocol: `key`, `version`, `timeframe` (`"5m"|"15m"`), `parameters_schema` (JSON Schema dict), `default_parameters` (dict de `Decimal`/int/str), `evaluate(ctx, params) -> Decision | None`), `Decision` (frozen dataclass/pydantic: `direction` (só LONG no v0), `reference_price` (fechamento da barra de referência), `stop`, `target1` (+ `targets_informational` para alvos 2/3 só no snapshot), `invalidations`, `horizon_s`, `confidence`, `reason`, `supporting_features` — envelope tipado do item 2 da decisão: `observation_ts = source_bar_close`, valores calculados, disponibilidade/qualidade por entrada, `params_format`, `purpose = "research_only"`; **`decision_at` e `cohort` NÃO são da estratégia**, o worker acrescenta), `StrategyContext` (barras finais de 1 min **cortadas em `source_bar_close`** — nada com `open_time >= source_bar_close`, ordenadas, contíguas ou com lacunas declaradas; funding/OI disponíveis até `source_bar_close`; `eligible: bool` + `eligibility_reason`), `registry.py` (registro por `key`, `get(key, version)`), `canonical.py` (`canonical_json`, `params_hash`, `params_format = 1`: chaves ordenadas, decimais como string normalizada sem zeros à direita nem expoente, timestamps ISO-8601 UTC com `Z`, `None` → `null`; se a S0 já tiver criado este módulo, adote-o e apenas complete testes), `aggregate.py` (1 m → 5 m/15 m alinhados em UTC, só com barras contíguas, finais e inteiras; barra parcial no fim é descartada; lacuna interna ⇒ janela indisponível com motivo), `indicators.py` (ATR de Wilder(14) sobre 15 min com seed = média simples das 14 primeiras TR e âncora = `open_time` da barra de seed, ambos devolvidos no resultado para persistência; `return_n`, mediana de volume relativo, máxima de N fechamentos anteriores excluindo o atual), `momentum_v1.py`, `volume_anomaly_v1.py`.
- **Momentum v1** (timeframe 15 min): LONG quando `close_15m` > máxima dos 20 fechamentos anteriores (excluindo o atual), `return_15m > 0`, `relative_volume_15m` = volume da barra / mediana das 96 barras anteriores ≥ 1,5, ATR%(Wilder 14 × 15 min) entre 0,3% e 5%; `stop = close − 1,5·ATR`, `target1 = close + 1,5·ATR` ("1 R nominal na referência"), `targets_informational = [close + 3·ATR, close + 4,5·ATR]`, `horizon_s = 4 h`. Tudo em `default_parameters` (`lookback_closes = 20`, `rvol_window = 96`, `rvol_min = 1.5`, `atr_period = 14`, `atr_pct_min = 0.003`, `atr_pct_max = 0.05`, `stop_atr = 1.5`, `target_atr = 1.5`, `horizon_s = 14400`, custos assumidos `assumed_spread_bps = 2`, `slippage_bps = 5`, `fee_bps = 4`, `max_entry_delay_s = 120`). **Nenhum número hardcoded.**
- **Volume anomaly v1** (timeframe 5 min): LONG quando volume da barra de 5 min ≥ 4× mediana das 288 barras anteriores, `close > (high + low)/2`, `return_5m` entre 0 e 2·ATR% com **ATR de Wilder(14) de 15 min** (mesma calculadora); `stop = low` da barra de sinal, `target1 = close + 1,5·ATR`, `horizon_s = 2 h`; parâmetros idem em `default_parameters`.
- Geometria validada no snapshot (`stop < reference_price < target1`; senão `None` com motivo registrado em `supporting_features`?) — não: a estratégia devolve `None` e o motivo vai para o log de avaliação do worker; documente a escolha.
- Testes (pytest `unit`, séries sintéticas determinísticas, `Decimal`): gera sinal no cenário construído; não gera quando cada condição falha isoladamente; warm-up insuficiente ⇒ `None` com motivo `warmup`; minuto ausente na janela ⇒ agregação indisponível ⇒ `None` (nunca janela reduzida); alterar uma vela **posterior** a `source_bar_close` ou uma vela não-final não muda a decisão (invariância); bootstrap (contexto montado de uma vez) = execução contínua (contexto crescendo barra a barra) produz decisões idênticas; `params_hash` estável e vetores de equivalência; `default_parameters` validados contra `parameters_schema`; ATR de Wilder confere com um cálculo de referência independente (numérico manual em Decimal) e expõe seed/âncora; alvo/stop como "1 R nominal" (exemplo 100/97/103).
- `docs/PIPELINE.md`: nada. `obsidian/`: nada (S4 é da Sexta-feira). Se precisar registrar uma decisão de desenho, escreva em `.claude/state/notes-S1.md`.

## Regras
- Função pura: sem IO, sem relógio, sem Redis/Postgres. Só `Decimal`. Só UTC. ≤ 350 linhas por módulo (`uv run python infra/scripts/check_file_size.py`).
- Comandos: `uv run pytest packages/core/tests/unit -q -k strateg`, `uv run ruff check packages/core`, `uv run ruff format --check packages/core`, `uv run pyright packages/core`. Toolchain: prefixe o PATH com `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`.
- Não toque em `.env*`, não commite, não altere arquivos fora da lista (S0 mexe em `db/models/agents.py`, `domain/enums.py` e migrações; T1.7 em `tests/integration`).

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma) · TESTS · TEST RESULTS (saída real) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · Checklist S1 com `[x]`/`[ ]` honesto · NEXT STEP.

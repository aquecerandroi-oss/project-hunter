# Brief T4.27 — o mcap teórico das moedas Mayhem não é preço: separar o "bum" real do SOL virtual do agente (16/09/2026 02:2x BRT)

**Medido (banco da VPS, 3 dias, `infra/scripts/sql/research/2026-09-16-t427-picos-sao-mayhem.sql`):** 86 996 moedas com série de minuto; 2 126 com "pico" de mcap ≥ 300 SOL; 34 ≥ 1 000 SOL. **Todos os 95 picos ≥ 500 SOL são moedas Mayhem** (`mayhem_enabled = true`), 76 deles com progresso < 20 %. Exemplo KAT (`…pump`, 15/09 17:37 BRT): `virtual_sol_reserves` foi de 23,9 para 1 977 SOL em **60 s** com `real_token_reserves` caindo só 7 % — o SOL virtual da curva foi injetado pelo agente Mayhem, não por compradores; o nosso `mcap_sol` (preço marginal × supply) leu 1 981 SOL "de mcap" com 5 holders. As graduações reais dos 3 dias: 2 953, mediana **0 min** até graduar (1 599 no primeiro minuto — o mesmo slot), 815 entre 3 e 60 min.

**Consequências hoje:** (1) qualquer estudo de "bum" (o que o Everton quer capturar: tendência → moeda → lucro) que ordene por `mcap_sol` está olhando o agente Mayhem, não demanda; (2) o portão lê `mcap_sol`/`mcap_delta_60s`/`progress_rising` — em Mayhem, `mcap_delta_60s > 0` pode ser SOL virtual; (3) o placar de papel marca posição por `mcap_sol` (`mark_source = curve`) — uma aposta em Mayhem pode ser "vendida" a um preço que a curva não pagaria (a T4.2e já escreve o denominador certo, mas a marca não desconta o SOL virtual do agente); (4) os gráficos da T4.25 desenham esses picos como se fossem preço.

**Entregar:**
1. **Preço executável, não teórico, para Mayhem:** em `hunter_indicators/meme/curve.py` uma função `sell_all_value_sol(reserves, tokens_held)` (o que uma venda cheia renderia pela fórmula da curva, taxas incluídas — já existe para a marca? conferir `paper_engine`/`exits`) e, para moedas Mayhem, a marca e o `mcap` das features passam a usar **`real_sol_reserves`** como teto do que pode sair (nunca mais que o SOL real na curva) — documentar em `docs/RISK_ENGINE_MEME.md` §6 e `T4-MEME-RADAR.md` §4 (o mcap teórico continua gravado, com o rótulo; nasce `mcap_executable_sol` ao lado, nas duas séries — migração `0042_meme_executable_mcap`).
2. **Portão:** feature `is_mayhem` (já em `meme_tokens.mayhem_enabled`) exposta em `EntryFeatures`; parâmetro `exclude_mayhem: true` **ligado por padrão** em todos os conjuntos vivos via um `UPDATE` auditado dos `params` (script `meme_rule_set.py --set-param`, novo subcomando, `--apply`), e recusa `mayhem_curve`; os braços que quiserem Mayhem de propósito desligam explicitamente (nenhum hoje). Registrar na página de cada EXP-M* uma avaliação datada "Mayhem excluído desde 16/09 (T4.27)".
3. **Placar retroativo:** script auditado `meme_reclassify_mayhem.py --day … --apply --reason` que marca as apostas fechadas em moedas Mayhem com `outcome_quality = indeterminate` e motivo `mayhem_virtual_sol` (a marca era SOL virtual) — dry-run por padrão, ids no relatório; o fechamento diário já exclui indeterminadas.
4. **Estudo "bum real":** SQL de pesquisa `2026-09-16-t427-bum-real.sql` excluindo Mayhem: das moedas não-Mayhem dos últimos 3 dias, quantas passaram de 100/300/1 000 SOL de **`real_sol_reserves`** (SOL de verdade na curva), em quantos minutos, quantas graduaram, e quantas o Lab propôs/comprou — o número que responde "quantos bums reais existem por dia e a que horas". Resultado no relatório e em `obsidian/11-KNOWLEDGE/KB-0098-mayhem-nao-e-preco-e-quantos-bums-reais-ha-por-dia.md` (+ `Index.md`).
5. Rótulo web `mayhem_curve` → "curva Mayhem (SOL virtual do agente)" listado nas notas (fora da tarefa); testes unit + testcontainer (marca de uma aposta Mayhem limitada ao SOL real; recusa `mayhem_curve`); `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; `EXPLAIN` de qualquer consulta nova por tique (regra T4.24b); docs; `.claude/state/notes-T4.27.md`; `git status --porcelain` ao final.

**Regras:** primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real; não tocar `apps/web/**`, `services/meme-executor/**`, nem os arquivos da T4.26 em voo (`discovery.py`, `repo.py`, `repo_rows.py`, `curve_rows.py`, `boards*.py`, `0041_*`, `meme_events*`, `pedigree.py`); se precisar de `proposals.py`/`lab_models.py`/`rules*.py`, editar só hunks próprios e declarar; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

You are exchange-integration-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T4.0d-mapa-onchain-pumpfun.md`. Everton (2026-09-12 03:4x BRT): "o mais importante é mapear o pump.fun por completo — vamos operar nele". Task T4.0d — the COMPLETE on-chain map of pump.fun on Solana, from primary sources (IDLs and open-source decoders; every claim with the URL/commit and the read time in Brasília): (1) the bonding-curve program `6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P`: every instruction (create/create_v2, buy, sell, complete/withdraw, set_params, extend_account, collect creator fee…) with accounts and args; the events (`CreateEvent`, `TradeEvent`, `CompleteEvent`, …) with their fields — these are what a decoder must parse to reconstruct trades WITHOUT paying for a feed; the `BondingCurve` and `Global` account layouts (fields, sizes, the discriminator) and the exact price/cost formulas incl. fee application; the current `Global` parameters read LIVE from the public RPC (`getAccountInfo` on the global account, decoded: initial virtual reserves, fee bps, creator fee, migration/`complete` threshold in lamports) — 10 req/s public limit, ≤ 20 requests; (2) PumpSwap `pAMMBay6oceH9fJKBRHGP5D4bD4sWpmSwMn52FMfXEA`: pool account layout, swap instructions, fee config accounts, how migration creates the pool (which instruction, which program does it), how to compute price from reserves; (3) the Mayhem program `MAyhSmzXzV1pTf7LsNkrNwkWKTo4ougAJ1PPg47MD4e`: what its IDL exposes (instructions, accounts, whether `create_v2` sets the mayhem flag, how "auto" vs "manual" mode appears on-chain) and how to identify the agent's trades (wallet `BwWK17cbHxwWBKZkUYvzxLcNQ1YVyaFezduWbtm2de6s`); (4) the open-source decoders compared: `chainstacklabs/pumpfun-bonkfun-bot` (already chosen in T4.0), plus any Rust/TS parsers (e.g. `pump-fun-idl` repos, `solana-pumpfun-*`), with stars/last commit and which handles IDL version changes; (5) the reading paths and their costs: `getSignaturesForAddress` + `getTransaction` per signature (how many RPC calls per trade), `logsSubscribe`/`programSubscribe` on the public WS (allowed? limits), Geyser/gRPC (keyed), Helius `getTransactionsForAddress`/webhooks (keyed) — a table "what a free public RPC can sustain for N tokens" with the arithmetic; (6) what execution would need later (transaction shape for buy/sell on the curve and on PumpSwap, compute budget/priority fee, Jito tips, slippage param, ATA creation) — description only, NO signing code. Deliver `docs/PUMPFUN-ONCHAIN.md` (Portuguese, reference), `.claude/state/notes-T4.0d.md` (URLs, commands, decoded live `Global` values), and if the `chainstacklabs` layout in `packages/exchange-adapters/hunter_exchanges/pumpfun/decode.py` (being finished by Astra — READ ONLY, do not edit) disagrees with the IDL, say so precisely. Rules: no background shells; every command foreground with explicit `timeout 290`; do not commit; do not touch `.env*`; never paste a key; use only the public mainnet RPC (`https://api.mainnet-beta.solana.com`) read-only. Report in Portuguese ≤ 15 lines with the live `Global` parameters and the cost table.

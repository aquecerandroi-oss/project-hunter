# Adendo ao brief T3.24b — decisão D18 tomada (opção A) e os blocos novos da API (T3.18b)

**Base:** `main` em `9a56e37` (T3.18b: API do placar já entrega `replay` e `replication`; curva aceita `cohort=`). **Decisão:** Everton delegou (2026-09-08, "pode tomar a decisão"); D18 (`.claude/state/decisions-delegated-2026-09-08.md`) = **opção A, Placar-primeiro**. Implementar o brief `brief-T3.24-design-lab.md` inteiro **e** os itens abaixo. O mockup da §3 continua obrigatório (fica em `/_design`, com A e B lado a lado, para o product-designer validar e o Everton ver) — mas **não bloqueia** a implementação da opção A.

**Pré-condições agora satisfeitas:** T3.24a aplicado (`242a359`); sign-up de teste destravado (instância Clerk: e-mail obrigatório com código, sem usuário/senha; `bash .claude/state/tmp/run-design-audit.sh -g signup` passou às 13:22Z); a `ever` local tem carteira principal (19 435,59 USDT). Regra nova do spec de auditoria: `waitUntil: "load"` (nunca `networkidle` — a Carteira tem stream) e o OTP do Clerk é digitado com `page.keyboard.type`, não `fill`.

## Itens adicionais (dados reais da API T3.18b — `.claude/state/notes-T3.18.md` §T3.18b)

A1. **`lab-scoreboard-card.tsx`**: abaixo das métricas prospectivas, um bloco **"Replay"** compacto (só quando `row.replay !== null`): rótulo exatamente `row.replay.label` ("replay — não conta para o veredito") em 11px `fg-subtle`; 4 números em uma linha: "decisões simuladas" (`decisions_simulated`, massa — D14), "operações fechadas" (`operations_closed`, evidência — D14), "expectância" (`expectancy_r`, formato R com 2 casas, `reason` → `reasonLabel`), "PF" (`profit_factor` idem); "janela {window_from}–{window_to} (Brasília)" e "{distinct_days} dias · {distinct_markets} mercados" em 11px. Nunca misturar com o veredito/maturidade (que continuam só prospectivos — teste: versão com replay positivo e prospectivo negativo mostra veredito prospectivo).
A2. **Bloco "Replicação"** (só quando `row.replication !== null`): chip de status com rótulo pt (`none`→ não mostrar o bloco, `promissora`→"promissora", `replicando`→"replicando", `real`→"real", `refutada`→"refutada"), `promising_at` em Brasília, "irmãs {mature}/{n} maduras · {positive} positivas (esperado {expected}, exigido {required})", `evidence` de cada braço como texto pequeno ("prospectivo"/"replay"/"misto"); `out_of_sample.reason`, `siblings.reason`, `market_halves.reason`, `bootstrap.refused_reason` como uma lista de 11px com o rótulo pt (dicionário em `components/lab/labels.ts`; os valores crus do backend ficam em `title`). Sem gráfico novo.
A3. **`lab-curve-section.tsx` / `lab-curve-chart.tsx`**: seletor "Coorte da curva": `prospectiva (padrão)` | `replay` (chama `GET /lab/shadow/curve?cohort=replay`); a linha de replay desenhada **tracejada** ao lado da prospectiva quando as duas estão carregadas (legenda: "prospectiva" / "replay — não conta para o veredito"). Tipos: `packages/shared-types/src/generated/api.d.ts` já regenerado — usar os tipos gerados, não redeclarar.
A4. **`lib/api/lab.ts`**: parâmetro `cohort` na curva; nenhum outro endpoint muda.
A5. **Testes**: `apps/web/tests/lab-scoreboard.test.tsx` (blocos presentes/ausentes; rótulo exato do replay; veredito não muda), `lab-curve.test.tsx` (duas séries, tracejado), fixtures a partir do exemplo real em `notes-T3.18.md` §T3.18b.

## Prova (além do Aceite do brief)
`pnpm --filter web lint|typecheck|test` com saída real; tela real via `bash .claude/state/tmp/run-design-audit.sh -g "lab"` (spec `tests/e2e/design-audit.audit.ts`, grupos `lab interactions` e `screens 1440`) e as capturas em `.claude/state/design/2026-09-08/lab-*.png` **antes/depois**; relatório em português no formato estendido; `.claude/state/notes-T3.24b.md`. Não commitar. Nunca shell em background; comandos em primeiro plano ≤ 5 min; árvore compartilhada (nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`); não tocar `.env*`, `apps/api/**`, `services/**`, `obsidian/**`, `lib/format.ts`.

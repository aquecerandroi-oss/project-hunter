# Brief T3.26 — the two most promising backlog candidates enter the Lab as research versions, with 31-day replay evidence on day one

**Owner:** quant-engineer. **Reviewers afterwards:** risk-engine-guardian (nothing reaches the wallet), code-reviewer, Sexta-feira (EXP pages). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` at `c446323`. In flight elsewhere: T3.24a (`apps/web/**`), designer screenshots (reads), Sexta-feira plantão (`obsidian/**` — **do not edit `obsidian/**`**; write the EXP page drafts under `.claude/state/exp-drafts/` and she files them).

## Why (Everton, 2026-09-08: "bora"; the replay evidence of the same morning)
Both research strategies lose over 31 days of replay with real funding: momentum v2 expectancy −0.17 R (222 evaluable, PF 0.65), volume_anomaly v2 −0.60 R (337, PF 0.28). The Obsidian base already names the two most likely causes (EXP-0001/0002 "Hipóteses de falha", `obsidian/11-KNOWLEDGE/Strategy Backlog.md` items 1 and 2): the cost floor is too low for the 1.5 ATR target (KB-0008), and the invalidation kills 35 % of trades with 0 winners among them (KB-0006). More waiting on the same parameters is not research; the next hypotheses are.

## Read first
`obsidian/11-KNOWLEDGE/Strategy Backlog.md` (items 1 and 2 and their KB notes), `obsidian/05-EXPERIMENTS/EXP-0001-momentum-v1.md` §"Hipóteses de falha", `obsidian/05-EXPERIMENTS/_TEMPLATE-EXP.md`, `docs/plans/SHADOW-LAB.md`, `docs/plans/REPLICATION.md` (§1 vocabulary: a variant with different content is a **new hypothesis**, not a sibling), `packages/core/hunter_core/strategies/momentum_v1.py` (parameters schema: `atr_pct_min`, invalidation rule), `services/strategy-worker/hunter_strategy_worker/{activation_db,paper_line,replication,replication_derive}.py` and `infra/scripts/{activate_strategy_version,replicate_strategy_version}.py` (how a version is derived, frozen, audited — reuse), `services/strategy-worker/hunter_strategy_worker/replay/run.py` (the replay job), `.claude/state/notes-T3.19b.md` (what a replay proves and does not).

## Deliver
1. **`infra/scripts/derive_variant.py <key> <version> --set param=value [--set ...] --changelog "..." [--dry-run]`**: derives a new `research_only` version from a frozen one with explicit parameter overrides (validated against the schema; `code_ref` recomputed and checked like `--paper-line`; `status = draft`, then a separate audited `activate` — reuse `activation_db`/`activate_derived`), records `derived_from` in the changelog the way the catalogue exporter parses (`notes-T3.20.md`), audited in `system_events`. If the momentum code has no parameter for the invalidation rule, **do not** change the code in this task: deliver the cost-floor variant, and write the invalidation variant as a brief for a `momentum_v2` code version (new module, new `code_ref`) — a code change is a new strategy, reviewed by the quant protocol.
2. **Variant A — cost floor**: `momentum v2` → new version with `atr_pct_min = 0.0089` (KB-0008), everything else byte-identical. **Variant B — invalidation** if the parameter exists (e.g. a "two closes" or "buffer 0.25 ATR" option); otherwise the brief from item 1.
3. **Evidence on day one**: after activating each variant (research_only; audited; say the exact commands), run the 31-day replay on the local stack **or** the VPS (`docker exec hunter-strategy-worker-1 python -m hunter_strategy_worker.replay.run --version momentum:v<N> --from 2026-08-08 --to 2026-09-08 --markets ETHUSDT,SOLUSDT,XRPUSDT,DOGEUSDT --workers 3 --ledger /tmp/replay-<name>.jsonl` — the VPS has candles and funding for those four; ask the orchestrator to run it if the classifier refuses you) and paste the receipt and the cohort summary (`/tmp/q7.sql` on the VPS has the query shape: evaluable, expectancy, net profit rate, PF, days). Compare with the parent's replay in the same window (`replay:f8d8279c…`). Say what changed and what did not; never a verdict beyond `inconclusivo` (30-day rule is prospective).
4. **EXP drafts** `.claude/state/exp-drafts/EXP-0006-momentum-piso-de-custo.md` (and `EXP-0007-…` if variant B exists) in the template's shape (frozen Hypothesis/Protocol, first dated evaluation = the replay, labelled replay), for the Sexta-feira to file into `obsidian/05-EXPERIMENTS/` and link from the backlog.
5. **Tests**: `derive_variant.py` against Postgres (testcontainers, pattern of `test_activate_paper_line.py`): override validated, code_ref checked, draft not active, audit row; `ruff`/`pyright`/`check_file_size.py`.

## Prove
Report in Portuguese, extended format (STATUS · FILES · TESTS with real output · PROOF: the replay receipts and the parent/variant comparison table · CONCERNS); `.claude/state/notes-T3.26.md`. Nothing here touches the wallet: variants are `research_only`, the bridge refuses them by purpose and cohort.

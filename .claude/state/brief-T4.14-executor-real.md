# Brief T4.14 — o executor real: da aprovação na mesa à transação assinada (Everton, 12/09/2026 12:3x BRT: "bora tentar logo com dinheiro real; ele faz a operação, não consegue?")

**O que existe:** T4.8 (`packages/exchange-adapters/hunter_exchanges/pumpfun/{tx,quote,verify,tx_rpc,trade_event}.py`, `packages/core/hunter_core/execution/meme/{signer,gates,submit,journal}.py`): instruções byte a byte iguais às do site, cotação local, verificador adversarial, `simulateTransaction` obrigatório, chave lida uma vez, portões `meme_gates.json`, `submitted_unconfirmed/confirmed/failed`. T4.6/T4.7: a mesa aprova propostas (`meme_proposals.decision`) e o laço de papel preenche. T4.4 (`docs/RISK_ENGINE_MEME.md`): 25 checks (§4), sizing (§5), saídas sempre permitidas (§6), kill switch latched (§7), falhar fechado (§8), caminho de execução (§9), VM1–VM9 (§11), portões A/B/C (§12), o que muda no motor (§13, `hunter_risk_meme`), perguntas ao Everton (§14). **O que falta:** o serviço que liga a aprovação à cadeia, com o motor de admissão meme, e a venda real.

**Entregar (serviço `services/meme-executor/`, `HUNTER_ROLE=meme_executor`, perfil compose `meme-live`, imagem `hunter-api`):**
1. **`hunter_risk_meme`** (`packages/risk-core/hunter_risk_meme/` ou `packages/core/hunter_core/execution/meme/admission.py` — decidir e justificar): os checks da §4 com os nomes de recusa da doutrina, sizing §5 (mínimo entre tetos, limitante publicado), política da carteira §3 lida do `.env` (`MEME_WALLET_MAX_SOL`, `MEME_MAX_SOL_PER_TRADE`, `MEME_DAILY_LOSS_CAP_SOL`, `MEME_MAX_OPEN_POSITIONS`, `MEME_COOLDOWN_S`) — ausência de qualquer um = recusa de boot com a flag ligada; kill switch §7 (arquivo/Redis `meme:kill`, latched, só o Everton solta); VM1–VM3/VM7 do `infra/scripts/meme_vm.py` passam a PASS.
2. **Modo live nas propostas:** `meme_proposals.mode` (`paper` \| `live`, default `paper`) e `meme_live_orders` (migração nova, número livre após as de T4.11/T4.12): `proposal_id`, `side`, `intent` jsonb (mint, `size_sol`, `max_sol_cost`, slippage, prioridade), `admission` jsonb (checks e recusa nomeada), `tx_signature`, `status` (`admitted` \| `refused` \| `simulated` \| `submitted_unconfirmed` \| `confirmed` \| `failed`), `fill` jsonb (do `TradeEvent`: SOL, tokens, taxas, slot, blockTime), `received_at`s; `meme_live_positions` derivadas (FIFO, marca pela fotografia/fita, R com risco = SOL gasto). A mesa (T4.7) só cria proposta `live` quando `mode=live` está habilitado na API (flag lida da API: `ENABLE_MEME_LIVE_TRADING` também na API, só para **exibir** o botão "Aprovar (REAL)" com confirmação dupla e o rótulo REAL — a API nunca assina). O laço de papel continua paralelo (sombra).
3. **Executor:** laço de 1 s: propostas `live` aprovadas → admissão → cotação (`/global-params` + estado da curva por RPC) → `tx.build_buy` → verificador §9.1 → `simulateTransaction` → assinar → enviar (RPC; Jito opcional off) → confirmar por `TradeEvent` → gravar `confirmed` com o fill real; idempotência em duas chaves (proposta e assinatura; blockhash novo = ordem nova); recusas nomeadas gravadas; **saídas** (alvo, trailing, `max_hold_s`, `sell_now`, migração → venda na PumpSwap via o mesmo construtor se a doutrina/T4.8 cobrir, senão `sell` na curva antes da migração e recusa nomeada depois), kill switch fecha tudo que estiver aberto **se** `meme_auto_close_on_emergency` (default false, §14.4). Heartbeat `hb:meme:executor` (flag, portões, carteira pública, saldo lido por RPC, ordens por estado, última assinatura), `GET /meme/live` (VIEWER+) com o mesmo, rótulo "REAL".
4. **Prova antes de qualquer SOL:** (a) unit: admissão (todas as recusas), sizing, idempotência, máquina de estados; (b) testcontainer sobre a cadeia real; (c) **devnet**: com carteira descartável gerada em memória e faucet (se recusar como na T4.8, simulação mainnet `sigVerify=false` com carteira vazia) — compra e venda de ponta a ponta com o programa da curva na devnet se existir, colando assinaturas em `.claude/state/notes-T4.14.md`; (d) `meme_vm.py` na árvore local com saída real; (e) boot com flag ligada e sem chave/portões/tetos ⇒ recusa nomeada (teste); (f) `forbidden_patterns.sh --self-test`.
5. Docs: `docs/RISK_ENGINE_MEME.md` §13 → "implementado em …" e §12 → o "teste pequeno autorizado por escrito" como forma válida do Portão A/B **somente** com a decisão do Everton registrada em `obsidian/06-DECISIONS/` (o executor lê `meme_gates.json` com `authorized_by`, `scope`, `expires_at`); `docs/DEPLOYMENT.md` (perfil `meme-live`, env, "como ligar" passo a passo, "como desligar em 5 s"); `docs/ACTIVATION.md` §9 meme; `docs/plans/T4-MEME-RADAR.md` §T4.14; `.claude/state/notes-T4.14.md` incremental; `pnpm gen:types`; `git status --porcelain` ao final.

**Regras:** tudo em primeiro plano `timeout 290` (590 testcontainer); sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; sem commit; **nunca** ler/escrever `.env*`; **nunca** chave real (só chaves de teste em memória); **nunca** `sendTransaction` na mainnet; `ENABLE_MEME_LIVE_TRADING` nunca `true` em arquivo; não tocar `apps/web/**` (escrever nas notas o que a mesa precisa: botão "Aprovar (REAL)", confirmação dupla, rótulo), `lab*.py`/`paper_engine.py` (o papel continua intocado), `wallets.py` (T4.12), `meme_tests*` (T4.13); `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília: o que foi provado (devnet ou simulação), o que fica de fora, e a lista exata do que o Everton precisa digitar/decidir para ligar.

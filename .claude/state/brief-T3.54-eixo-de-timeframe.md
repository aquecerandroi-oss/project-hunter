# Brief T3.54 — o eixo de timeframe: a identidade do pedágio diz que ATR% cresce com o timeframe, logo decidir/parar em 1 h custa 3–4× menos por R do que em 15 m; testar (a) o ATR de referência em 1 h nas versões vivas (parâmetro, derivação barata) e (b) uma irmã que decide em barras de 1 h (módulo novo, fora do fecho vivo); mais a "abertura" (session_orb) revisitada com essa lente (Everton, 2026-09-08 23:25: "trade só na abertura, 5 min, 10 min, 1 h — várias estratégias testáveis")

**Owner:** quant-engineer. **Reviewer afterwards:** code-reviewer (only if a new module is written). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (`timeout 290` per replay slice — 1 h decisions need MORE markets, not more days: use 12–20 markets by liquidity, split in slices); no testcontainers (strategy unit tests are pure); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; VPS writes ONLY through audited scripts (`bash infra/vps/compose.sh ops python infra/scripts/derive_variant.py … --dry-run` first, then real; `activate … --purpose research_only`; replay CLI in `hunter-strategy-worker-1`); reads in `repeatable read read only`; never stop or recreate containers.** Base: `main` at HEAD. Write scope: `packages/core/hunter_core/strategies/mean_reversion_h1_v1.py` (+ test) only for part (b), `constraints_table.py` entry, `infra/scripts/seed_reference.py` row, `.claude/state/notes-T3.54.md`, `exp-drafts/EXP-0021-timeframe.md`, `infra/scripts/sql/research/2026-09-09-t354-*.sql`. **Never edit the closure modules nor `mean_reversion_v1.py`/`momentum_v1.py`.**

## Facts to start from
- `mean_reversion_v1.timeframe = M15` is a class attribute (not a parameter); `atr_timeframe` and `trend_timeframe` ARE parameters (`mean_reversion_v1.py:97-130`). `session_orb_v1` is already "trade at the open" (−0,19 R, 20 decisions, EXP-0010).
- KB-0076: `custo_R = 0,0020 / (stop_atr × ATR%)`. ATR%(1h) ≈ 2–4 × ATR%(15m) on the same market — measure it first (read-only SQL from 1m candles, 4 + 12 markets, 31 d).

## Deliver
1. **Measure** ATR%(15m) vs ATR%(1h) vs ATR%(4h) per market (p50) over 31 d and the implied toll per R at `stop_atr = 1` — the table that says whether the lever is worth 2× or 4×.
2. **(a) Parameter variants (cheap)**: derive `mean_reversion v10` from v6 with `atr_timeframe = 1h` (stop/targets keep their ATR multiples — the price distance grows with the 1 h ATR) and `momentum v10` from v8 with `atr_timeframe = 1h` if momentum exposes it (check its schema; if not, say so). Dry-run, digests unchanged. Activate research_only, replay 31 d × 4 markets, paired vs parent, stress, C5 check against the `paper_v1` stop band `[0,003; 0,03]` (a 1 h ATR stop may exceed 3 % — report the share).
3. **(b) Decision-timeframe sibling**: a flat module `mean_reversion_h1_v1` with `timeframe = H1` and the same rules (may import `mean_reversion_v1`? NO — importing it would put the new module in nobody's closure but the parent's digest stays; still, prefer a thin sibling that copies the class with `timeframe = H1` and cites the parent — write the closure test like T3.45b's). Unit tests: non-anticipation (`test_no_lookahead.py` pattern), 1 h alignment, digests of live versions untouched. Seed row + constraints entry. Report the exact files; the orchestrator commits/deploys; THEN seed `--only strategies`, activate, replay 31 d on 12–20 markets (decisions are ~4× rarer per market), cross-sectional day-block CI.
4. **Session/open lens**: with the ledger of `session_orb v1` and of (a)/(b), the expectancy by hour-of-day bucket (BRT and UTC) — does "the open" (which open: 00:00 UTC, 13:30 UTC NY, 09:00 BRT) carry anything; ≤ 5 lines.
5. Verdicts per the funnel; ≤ 10 lines for Everton; EXP-0021 draft.

## Prove
Receipts verbatim, SQL with output, tests per file, `ruff`/`pyright`/`check_file_size.py`; report in Portuguese, extended format; `.claude/state/notes-T3.54.md`. Times in Brasília (UTC−3) with UTC as detail.

# Brief T3.7c — funding-rate history backfill (the replay cannot price its outcomes without it)

**Owner:** exchange-integration-specialist (market-worker owns REST — joint M2 decision). **Reviewers afterwards:** code-reviewer, quant-engineer (the outcome resolver's use of it). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` at `4deef9d`. In flight elsewhere: T3.25 part A (`apps/api/**`), designer audit (`apps/web` reads, `docs/DESIGN.md`) — do not touch those paths.

## Measured on the VPS, 2026-09-08 11:15Z
Two 31-day replays (momentum v2, volume_anomaly v2; ETH/SOL/XRP/DOGE; 11 904 + 35 712 bars; 224 + 341 resolved outcomes) produced only **23 + 2 evaluable** outcomes: the rest carry `funding_schedule_unknown` because `funding_rates` holds only the live-collected period (since ~2026-09-05) while the candles now go back to 2026-08-08 (T3.7b `request_backfill.py`). The resolver refuses to invent a funding cadence (correct — `.claude/state/notes-T3.19b.md` concern 4, KB-0021/0022). Without funding history the replay engine (T3.19b) and the sibling block (D15) have no net R to show.

## Read first
`services/market-worker/hunter_market_worker/{backfill,backfill_plan,backfill_announce,backfill_consumer}.py` (the candle lane: request events, chunks, priority, idempotent identity), `hunter_market_worker/deriv*.py`/`funding*.py` (how live funding is polled and persisted: table `funding_rates`, columns, `received_at`), `packages/exchange-adapters/hunter_exchanges/binance/rest.py` (rate limiter, weight budget; Binance USDS-M `GET /fapi/v1/fundingRate` returns up to 1000 rows per call, `startTime/endTime/symbol`), `infra/scripts/request_backfill.py` (T3.7b: how a request is published; extend it, do not duplicate), `docs/PIPELINE.md` §1b and §2b, `docs/DATABASE.md` (funding_rates section), `services/strategy-worker/hunter_strategy_worker/outcome*/resolve*` (read only: what the resolver needs — the funding events between entry and exit with their timestamps).

## Deliver
1. **Funding history lane in the market-worker**: a request kind `funding` on the existing backfill contract (extend the event payload with `kind: candles|funding`, default `candles`, additive) that fetches the exchange's funding-rate history for a `(market, window)` under the REST budget, persists into `funding_rates` idempotently (same identity as the live poller: `(market_id, funding_time)`; `received_at = now`, source `backfill`), never overwrites a live row, and announces completion on the existing `market.candles.backfilled`-style event or a `market.funding.backfilled` one (pick the one consistent with PIPELINE §1b; additive). Chunking: one call covers ~333 days at 3 fundings/day, so one request per market is enough — keep the lane's priority below live work.
2. **`infra/scripts/request_backfill.py --kind funding --days 31 --markets ...`** publishes the requests (dry-run first); reuse the market list logic.
3. **Proof on the local stack** (do not stop containers): request 31 days of funding for 3 markets, show the rows land (`select market_id, count(*), min(funding_time), max(funding_time) from funding_rates ...`), rerun = no duplicates; then show that a replay outcome in that window now resolves with a net R (coordinate by reading `replay/run.py`; a tiny replay of one day is enough).
4. **Docs**: `docs/PIPELINE.md` §1b (funding kind), `docs/DEPLOYMENT.md` (the command to run on the VPS after deploy: `docker exec hunter-api-1 python infra/scripts/request_backfill.py --kind funding --days 31 --publish --markets <the 19 + BTC>`).

## Prove
`services/market-worker/tests` per file (backfill_* and funding tests, new ones), `infra/scripts/tests/test_request_backfill.py`, `ruff`/`pyright`/`check_file_size.py`. Report in Portuguese, extended format; `.claude/state/notes-T3.7c.md`.

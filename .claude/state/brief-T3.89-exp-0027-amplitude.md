# Brief T3.89 — the second candidate of 2026-09-11: EXP-0027 "amplitude como estado" on 90 days of history (the data became available 09:31 BRT)

**Facts:** `market_breadth` now holds `breadth_v2` (universe = the 16 markets with ≥ 90 d: ARB BNB BTC DASH DOGE ETH LINK NEAR PROM SAHARA SOL SUI TAO UNI XRP ZEC; 128 343 rows, 128 342 usable, 2026-06-14 00:00Z → live; backfill audited `market_breadth.backfill`, T3.88). The pre-registration is `.claude/state/exp-drafts/EXP-0027-amplitude.md` (T3.88 rewrote it as a historical test — read it fully; its rule is binding): parent `mean_reversion v10` (cohort `replay:c7d138eb-a633-4457-a4fa-aa82a5df95e3`, 798 decisions/89 d, ex-funding −0,0293 R, EXP-0025), arm A `breadth=0.10-0.60@breadth_v2` (inside the cell), arm B (falsification) `breadth=0.60-1.00@breadth_v2` (broad sell-off, expected worse per KB-0083); success = conditional × unconditional Δ with day-block 95 % CI above zero AND n ≥ 100 AND positive in ≥ 2 of 3 30-day windows AND leave-one-market-out never negative, plus the paired-by-bar Δ within ±0,02 R as the "gate is only a gate" proof; anything less = `descartar`. The policy field `version` is mandatory (`breadth_policy.py`, T3.88) — check the exact `--policy` syntax in `infra/scripts/derive_variant.py` and `docs/ACTIVATION.md` §7c (e.g. `breadth=0.10-0.60@breadth_v2`; if the CLI spells it differently, use the CLI's spelling and say so).

## Deliver
1. Copy the pre-registration into `obsidian/05-EXPERIMENTS/EXP-0027-amplitude.md` (template + `Experiments Index`) BEFORE any replay, with the frozen predictions (state numbers: expected share of eligible bars per arm from the breadth distribution over the 90 d — measure it read-only first: quantiles of `breadth_v2.value` and share of minutes in [0.10, 0.60) and [0.60, 1.00]).
2. Derive (dry-run first) and activate `research_only` via `bash infra/vps/compose.sh ops python infra/scripts/derive_variant.py …` / `activate_strategy_version.py …`: `mean_reversion v18` (A) and `v19` (B) from v10; record ids and digests.
3. Replay 90 d × 16 in slices ≤ 4 markets × ≤ 31 d, explicit cohorts (one per arm), `--explain-ledger`, ONLY via `STRATEGY_SHARDS=4 MARKET_SPOT=1 MARKET_SHARDS=4 bash infra/vps/compose.sh replay python -m hunter_strategy_worker.replay.run …`, one lane at a time, `timeout 290` per slice (split to 4 × 15 d if a slice exceeds it, as T3.84c did); if the gate refuses, report the reason and stop.
4. Stress per arm; analysis (`.claude/state/exp-drafts/t362b/blocos90.py`; pairing by (market, bar) against the parent's cohort on shared eligible bars — never by decision; three windows; LOMO; K1–K6; C5), verdicts (append-only), strategy page `mean_reversion.md` updated, retire the arms that fail (`--deprecate --changelog`) through ops the same day; `.claude/state/notes-T3.89.md` with every command/output and the `git status --porcelain` list of your files; SQL `infra/scripts/sql/research/2026-09-11-t389-*.sql`; `obsidian_lint.py` must end "base limpa". ≤ 12 lines in Portuguese at the end, times in Brasília (UTC−3, compute it).

Rules: never a background shell; every command foreground with explicit `timeout 290`; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no `git pull` on the VPS; VPS writes only through the audited scripts named above; never edit the closure modules.

# Brief T4.2h-b — a vigilância do criador nas posições reais, no heartbeat e na tela (15/09/2026 16:2x BRT)

**Entregue na T4.2h (`.claude/state/notes-T4.2h.md`, commit 75b8df5):** `creator_watch.py` lê as ATAs do criador a cada 15 s para os mints com aposta de papel aberta e marca `creator_sold_seen_at`/`creator_sold_fraction` em `meme_paper_bets`; 27 vendas vistas até 15/09 16:13 BRT, saída em média 149 s depois da venda (medir por que não 15 s: a saída acontece na fotografia seguinte do laço de papel — conferir se o `creator_dump` está sendo avaliado a cada tique de 15 s ou só no minuto; se for no minuto, mover a avaliação de saídas dos mints com venda vista para o tique de 15 s).

**Entregar:**
1. **Posições reais:** as mesmas três colunas em `meme_live_positions` (migração `0038_meme_creator_watch_live`), o laço passa a vigiar também os mints de `meme_live_positions` abertas; o executor (`services/meme-executor/hunter_meme_executor/exits.py`) lê `creator_sold_seen_at` no tique de 5 s e vende (`creator_dump`), respeitando o kill switch e as regras de saída existentes; teste unit do executor.
2. **Latência:** medir e publicar `creator_watch_sale_to_exit_s_p50/p95` (papel) e garantir que a saída por venda vista ocorra na fotografia seguinte de 15 s (`lab_bets.py`/`lab_fast.py`); teste que prova ≤ 30 s no testcontainer.
3. **Heartbeat** (`sources.py` — está no teto de 350: usar `source_stats.py` ou um módulo novo `creator_stats.py`): `creator_watch_mints`, `creator_watch_calls_60s`, `creator_watch_drops_1h`, `creator_watch_missing`, as latências; `GET /meme/sources` (API) e rótulos em `apps/web/components/meme/meme-sources-format.ts` + testes Vitest (padrão da T4.20); `pnpm gen:types`.
4. Teste de persistência do laço (`test_creator_watch_persistence.py`): aposta aberta + `FakeChain.call` devolvendo duas leituras (1000 → 400) → coluna marcada com fração 0,6 → `load_open_bets` mostra `creator_net_seller = true` → saída `creator_dump` no tique seguinte.
5. Docs `RISK_ENGINE_MEME.md` §10.12 (atualizar), `DATABASE.md` §48/§50, plano §T4.2h-b; `.claude/state/notes-T4.2h-b.md`; `git status --porcelain` ao final.

**Regras:** primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real (nunca `sendTransaction`; flags nunca `true` em arquivo); não tocar `rules*.py`, `lab_models.py`, `infra/migrations/ddl/meme_gate_*`, `0037` (outra agente, T4.23) nem `obsidian/02-MARKET`/`00-INBOX`/`plantao-meme` (plantão); a migração desta tarefa é a `0038` e senta sobre a `0037` da T4.23 — se a `0037` ainda não existir no disco quando você criar a sua, use `down_revision = "0037_meme_e1_arms_3_4"` mesmo assim e declare no relatório; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 10 linhas em português, horários em Brasília.

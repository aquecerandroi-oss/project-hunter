# Brief T3.91 — the candidate of 2026-09-12: EXP-0029 "a discordância BTC × alts como estado" on 90 days of history (series backfilled 04:33 BRT)

**Facts:** `market_dispersion` now holds `dispersion_24h_v1` (universe = the 16 markets with ≥ 90 d; reference `BTCUSDT`; **126 720 rows written** by `backfill_dispersion.py --days 90 --apply --reason "EXP-0029 …"` at 04:33 BRT on 12/09 — 88 foldable days, `audit_logs` line `market_dispersion.backfill`; columns: `end_time`, `dispersion_version`, `horizon_minutes`, `universe_size`, `covered`, `alts_covered`, `alts_below_btc`, `btc_r24h`, `median_alt_r24h`, `dispersion`, `share_below_btc`, `coverage`, `reason`, `computed_at`, `inputs`). Production: VPS `hunter-vps`, `/opt/project-hunter` at commit `24a1357` (T3.90 deployed 04:30 BRT: scanner job live, gate in the strategy shards, `--policy dispersion=` accepted by `derive_variant.py`; read `docs/ACTIVATION.md` "A dispersão BTC × alts (dispersion=, T3.90)" — six warnings, signed bounds, half-open band, `version` mandatory). The pre-registration is **already in the vault**: `obsidian/05-EXPERIMENTS/EXP-0029-dispersao-btc-alts.md` (written before the backfill; arms frozen: **A = v20 `dispersion=-0.10--0.03`**, **B = v21 `dispersion=0.00-0.10`**, parent `mean_reversion v10`; predictions frozen: A Δ > 0 but < +0,05 R → "A não aprova"; B Δ ≤ 0; population check §2: A between 10 % and 30 % of 15-min bars, B between 20 % and 40 %). Read it in full first, then `.claude/state/notes-T3.90.md`, `.claude/state/brief-T3.89-exp-0027-amplitude.md` + `.claude/state/notes-T3.89*.md` (the exact recipe you repeat: cohorts, slices, analysis script, verdict format), `.claude/state/exp-drafts/t362b/blocos90.py` (pairing by (market, bar), day-block bootstrap), `obsidian/05-EXPERIMENTS/EXP-0027-amplitude.md` (the previous verdict page — the shape of the append-only sections).

## Deliver, in this order
1. **Fill every `‹backfill›` in EXP-0029** with read-only SQL on the VPS (`ssh hunter-vps "docker exec hunter-postgres-1 psql -U hunter -d hunter -At -c '…'"` — SELECT only; never DDL/DML there): distribution of `dispersion` (rows/usable, window, days with coverage ≥ 80 %, `reason` counts, min/p05/p10/p25/p50/p75/p90/p95/max/mean), share of **15-min bars** (use the row at each 15-min close) per band A `[−0,10; −0,03)`, B `[0,00; 0,10)`, `[−0,03; 0,00)`, `< −0,10`, `≥ 0,10`, no usable row — over the whole window AND per 30-day window (three); calibration window **2026-06-13 → 2026-07-31** tercile cuts `t1`/`t2` (n); descriptive read per cell of the **parent v10's decisions** from 2026-08-01 on (join `decisions`/outcomes of v10 to `market_dispersion` by bar time; columns as the EXP table lists) and terciles T1/T2/T3 — everything stamped with the BRT time of the reading. Then the **population check §2**: if A or B is outside its band, STOP after writing the result in the EXP (status `inviavel-populacao` or what the page prescribes) and report — do not derive.
2. Derive (`--dry-run` first, then real) and activate as `research_only`: `bash infra/vps/compose.sh ops python infra/scripts/derive_variant.py mean_reversion v10 --policy dispersion=-0.10--0.03 --changelog 'EXP-0029 braço A (discordância)'` → v20; `… --policy dispersion=0.00-0.10 --changelog 'EXP-0029 braço B (falseamento)'` → v21; then `activate_strategy_version.py` exactly as T3.89 did for v18/v19 (research_only, never paper); record ids and digests in the EXP and in your notes. If `derive_variant.py` rejects the signed band, quote the refusal and stop.
3. Replay 90 d × 16 markets per arm in slices ≤ 4 markets × ≤ 31 d (windows from 2026-06-14 like T3.89: 06-14→07-14, 07-14→08-14, 08-14→09-11; groups ETH,SOL,XRP,DOGE / BTC,BNB,ZEC,SUI / NEAR,UNI,ARB,TAO / LINK,DASH,PROM,SAHARA — full symbols with USDT), one cohort per arm (`--cohort replay:<uuid4>`), `--explain-ledger /tmp/exp0029-<arm>-<slice>.jsonl`, ONLY via `STRATEGY_SHARDS=4 MARKET_SPOT=1 MARKET_SHARDS=4 bash infra/vps/compose.sh replay python -m hunter_strategy_worker.replay.run --version mean_reversion:v20 --from … --to … --markets … --explain-ledger … --cohort …`. Run the 24 slices sequentially from a loop script you write on the VPS (`/tmp/t391-replay.sh`, started with `nohup … > /tmp/t391.log 2>&1 &` **on the VPS**, which is allowed; locally every command stays foreground with `timeout 290`, polling `ssh hunter-vps 'tail -5 /tmp/t391.log'`), never two replays at once; if the pause gate refuses (`outbox_lag`/`heartbeat`/`consumer_lag`/`decision_lag`), report the reason and wait, do not force. Do not `pgrep -f` with a pattern that matches your own ssh session.
4. Stress per arm (`replay.stress`, as T3.89), then analysis with `blocos90.py`: conditional Δ (arm vs parent v10 on shared eligible bars, paired by (market, bar)) and unconditional Δ, day-block bootstrap CI 95 %, three 30-day windows, leave-one-market-out, K1–K6, C5, "gate is only a gate" proof (paired Δ within ±0,02 R when both decide). Verdict per the frozen ruler of EXP-0029 (append-only sections in the EXP; update `result`/`evaluable`/`days`/`status` in its frontmatter; `obsidian/03-TRADING/Strategies/mean_reversion.md` or wherever the strategy page lives; INBOX row H-P18 → `testada` with the verdict; diary entry `obsidian/09-OPERATIONS/Diario/2026-09-12.md` if that is today's file, else create per template; `obsidian_lint.py` → base limpa). Retire the arms that fail the same day: `activate_strategy_version.py … --deprecate --changelog 'EXP-0029: <verdict>'` through ops.
5. `.claude/state/notes-T3.91.md` with every command and its real output (BRT times), cohort uuids, slice durations, refusals.

**Rules:** never a background shell locally; every local command foreground with explicit `timeout 290`; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit (the orchestrator commits by pathspec); never touch `.env*`; never stop/recreate containers; **no `git pull` on the VPS** (the `ops`/`replay` guard needs the `hunter-api:$HEAD` image, which exists for `24a1357`); VPS writes only through `derive_variant.py`, `activate_strategy_version.py` and the replay worker; never edit the closure modules; never a paper/live activation. `SendMessage` is disabled; the Bash tool mangles backslashes inside heredocs — write files with the Write/Edit tools. Report in Portuguese, ≤ 15 lines, BRT times (UTC − 3; Git Bash `date` prints UTC): population numbers, verdicts A/B with Δ and CI, what was retired, what stayed open.

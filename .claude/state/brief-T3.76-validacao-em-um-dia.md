You are quant-engineer on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, `/opt/project-hunter`, HEAD there `223cbff`). First save this brief verbatim as `.claude/state/brief-T3.76-validacao-em-um-dia.md`.

# Brief T3.76 — the one-day validation batch (Everton, 2026-09-10 12:10 BRT: "90 dias é muita coisa, precisamos validar dentro de 1 dia")

**The rule from now on:** every candidate gets replay 90 d × 16 markets + stress + replication + pre-registration in ONE day; the prospective (paper, 30 d) starts the same day a candidate passes and runs in parallel — it is never skipped. Today's batch, verdict by 22:00 BRT.

**Facts:** `.claude/state/notes-T3.62b.md` + `obsidian/05-EXPERIMENTS/EXP-0025-mean-reversion-90-dias.md`: the family is negative over 90 d and positive only Aug–Sep (v10 J3 +0,12 R / PF 1,48 vs J1 −0,12 / J2 −0,09). `KB-0079` says momentum only wins in HIGH_VOLATILITY. Hypothesis to test today: **the family's edge is a regime, not a strategy — restrict eligibility to consolidation regimes** via the T3.52 policy (`derive_variant.py --policy regime=btc:LABEL[,LABEL]`, labels of `regime_hourly_v1`: BTC_BULL, BTC_BEAR, SIDEWAYS, HIGH_VOLATILITY, LOW_VOLATILITY, UNKNOWN=ineligible). **Blocker measured 12:05 BRT:** `market_regimes` (scope btc, hourly) has only ~784 rows in the last 90 d (UNKNOWN 206, SIDEWAYS 163, BTC_BULL 150, HIGH_VOL 148, BTC_BEAR 83, LOW_VOL 34) ≈ 33 days of coverage — the series does not exist for most of the window, and the gate treats missing as ineligible. So step 1 is the regime backfill.

## Deliver (in order; stop and report at any step that needs the orchestrator to deploy)
1. **Regime backfill 90 d**: find how `regime_hourly_v1` is produced (`services/scanner-worker/hunter_scanner_worker/regime_job.py`, `regime_window.py`, `packages/indicators/hunter_indicators/regime/*`) and whether an audited backfill over a range exists (`infra/scripts/*regime*`, `request_backfill.py --kind`). If it exists: dry-run locally on a testcontainer, then run on the VPS via `bash infra/vps/compose.sh ops python infra/scripts/…` (the only allowed write path; `timeout 290` per slice), prove idempotency (second run inserts 0) and re-count coverage per label per month. If it does not exist: implement it (same classifier code, same `end_time`-anchored non-anticipation, immutable rows, `ON CONFLICT DO NOTHING`, unit + one testcontainer test) and STOP — report so the orchestrator deploys, then continue when told.
2. **Pre-registration BEFORE any replay**: `obsidian/05-EXPERIMENTS/EXP-0026-regime-como-estrategia.md` (template + Index) with the arms and the success rule: `mean_reversion v15` = v10 + `regime=btc:SIDEWAYS,LOW_VOLATILITY`; `v16` = v10 + `regime=btc:SIDEWAYS`; falsification arm `v17` = v10 + `regime=btc:HIGH_VOLATILITY` (expected worse); and `momentum v11` (already gated) over the full 90 d × 16 markets. Success = paired-by-(market, bar) Δ vs parent over shared eligible bars ≥ +0,05 R with the day-block 95 % CI above zero AND n ≥ 100 AND positive in at least 2 of the 3 30-day windows AND leave-one-market-out never negative; anything less = `descartar`. Replication rule: the 12 markets that did not pick v10 (see T3.62) reported separately.
3. Derive (dry-run first) and activate `research_only` via ops; replay 90 d × 16 markets in slices ≤ 4 markets × ≤ 31 d, `--explain-ledger`, explicit cohorts, `timeout 290` each, one lane at a time (the replay lane pauses itself when the live lane degrades — `consumer_lag`, T3.74b; never fight it); stress on each survivor; analysis with `t362b/blocos90.py` (day-block bootstrap, pairing by bar); K1–K6; C5 share.
4. Verdicts per arm in EXP-0026 (append-only), `obsidian/03-TRADING/Estrategias/mean_reversion.md` and `momentum.md` updated, `.claude/state/notes-T3.76.md` with every command/output, research SQL `infra/scripts/sql/research/2026-09-10-t376-*.sql`; retire (`activate_strategy_version.py --deprecate`, via ops) every arm that fails — Everton's standing rule "as que estão dando ruim pode matar"; a survivor stays `research_only` and the orchestrator decides the paper line. ≤ 12 lines in Portuguese at the end.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never edit the seven closure modules; never stop/recreate containers; no `git pull` on the VPS; VPS writes only through the audited scripts above. Times in Brasília (UTC−3) with UTC as detail.

# Brief T4.8c — o programa da pump.fun foi implantado de novo em 15/09 (slot 447 228 373 ≈ 07:34 BRT): regravar a paridade do caminho real antes de qualquer SOL (15/09/2026 17:0x BRT)

**Achado do plantão run 19 (lane 3, 16:2x BRT; `obsidian/11-KNOWLEDGE/KB-0096-*.md`, `.claude/state/plantao-meme/2026-09-15-1616-lane3.md`):** `ProgramData.slot` do programa da curva passou de **446 462 760** (12/09 12:24 BRT, T4.8b) para **447 228 373** (≈ 15/09 07:34 BRT). Conteúdo não lido. Ao mesmo tempo, `is_holder_reward` apareceu na REST (`/coins` 7/70 `true`, boards com a chave `hr`), e o vocabulário Mayhem mudou (`complete_reason = buy_zero_amount`). O executor (T4.8b `program_identity.py`/`program_check.py`) recusa `program_upgraded` ao ver o slot novo — correto, e é por isso que ele não sobe até esta tarefa. **Nada disto toca o papel** (o laço chain lê a curva sem erros: 0 `meme_loop_failed`, fita 95 %).

**Entregar (mesmo método da T4.8b — `.claude/state/notes-T4.8b.md`, `docs/PUMPFUN-ONCHAIN.md` §6c):**
1. **Captura real (≤ 15 chamadas RPC públicas, só leitura):** `ProgramData` (slot e, se o `upgrade_authority`/hash mudarem, registar), a IDL on-chain (hash contra a fixture da T4.8b — igual ou diferente, documentar), **um buy e um sell de hoje** (`getSignaturesForAddress` + `getTransaction`, ≥ 08:00 BRT), o layout do `TradeEvent` (bytes; `holder_rewards` ainda 0?), `Global` (tamanho), a `BondingCurve` (tamanho; campos novos?) e — novo — como o `is_holder_reward` aparece na cadeia (conta, flag, fee split): comparar com a T4.8b campo a campo, documentar em `docs/PUMPFUN-ONCHAIN.md` §6d ("segundo deploy, 15/09/2026").
2. **`tx.py`/`verify.py`/`quote.py`/`trade_event.py`** atualizados só onde a captura mostrar diferença; **paridade byte a byte** com o buy e o sell de hoje (fixtures `t48c_*`); taxas por componente ao lamport (o piso de taxa era constante datada — decodificar `FeeConfig` se a captura permitir, senão declarar).
3. **Simulação mainnet** pelo caminho do executor (`sigVerify=false`, carteira vazia em memória): buy **ok** e sell **ok** com CU, num mint clássico, num mint `is_holder_reward = true` e (se existir) num Mayhem; colar em `.claude/state/notes-T4.8c.md`; `meme_vm.py` VM4/VM5/§9.1 PASS.
4. `program_identity.py`: o slot esperado passa a ser o novo (com o anterior na história), o boot do executor volta a aceitar; teste unit que falha alto se a IDL/slot capturados divergirem de novo ("programa mudou: regravar T4.8d").
5. Adendo em `KB-0094` e `KB-0096` (o que a cadeia mostrou); `docs/RISK_ENGINE_MEME.md` §9.x; `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; testes adapter + executor verdes; `git status --porcelain` ao final.

**Regras:** primeiro plano `timeout 290`; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; **nunca** chave real; **nunca** `sendTransaction` na mainnet; flag nunca `true` em arquivo; não tocar `services/meme-worker/**`, `apps/**`, migrações, nem `services/meme-executor/hunter_meme_executor/exits.py` (outra agente, T4.2h-b, está nele — se precisar de algo lá, relate); não tocar `obsidian/02-MARKET`/`00-INBOX`/`plantao-meme`; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

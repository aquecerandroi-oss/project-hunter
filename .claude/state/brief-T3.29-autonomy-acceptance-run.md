# Brief T3.29 — prova operacional do fluxo autônomo paper na VPS antes de qualquer `ENABLE_PAPER_AUTONOMY=true` (Astra, revisão 2026-09-08 §2)

**Owner:** risk-engine-guardian. **Reviewers afterwards:** code-reviewer, Sexta-feira (Obsidian). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers; on the VPS (`ssh hunter-vps`, `/opt/project-hunter`, `docker exec hunter-api-1 …` / `hunter-postgres-1 psql -U hunter -d hunter`) READ ONLY — no UPDATE/INSERT/DELETE, no flag changes, no restarts.** Base: `main` at `50932ec`. In flight elsewhere: T3.24b/T3.28b (`apps/web/**`), T3.28a (`apps/api/hunter_api/middleware/**`, `apps/api/hunter_api/settings.py`, `infra/**/docker-compose*.yml`), Astra follow-ups (`apps/api/hunter_api/{repositories,services}/lab_replication.py`, `services/strategy-worker/hunter_strategy_worker/replication*.py`) — do not edit those paths. Your write scope: `services/execution-worker/**`, `packages/risk-core/**`, `tests/integration/paper/**`, `docs/ACTIVATION.md`, `docs/RISK.md`.

## Why
Everton's directive: "só declare o modo autônomo pronto quando o fluxo completo estiver verificado". Astra's review (`.claude/state/astra-review-lab-pronto-2026-09-08.md` §2) lists concrete gaps with file:line. The last recorded state: 154 paper signals, 0 proposals, 0 positions, 0 trades — the autonomous path has never run end to end on the VPS.

## Deliver — for each row, a measured answer on the VPS (read-only SQL / logs / heartbeat), then the code fix where the finding is code
1. **Admissão**: prove the `agents` row binding `momentum v3` (paper) to wallet `ever`'s paper portfolio exists and is enabled (`bridge_repo.py:130`, `bridge_screen.py:215`); if it does not exist, write the exact audited command the operator must run (never run it) and add it to `docs/ACTIVATION.md` as step 8a before the flag.
2. **Ordem — `avgPrice`**: `market_data.py:149` returns `None`; `entry_inputs.py:62` defers markets whose MARKET filters need it. Measure on the VPS how many of the β-universe markets have such filters (`markets.filters` / `exchange_filters`) and what share of `momentum v3` signals in the last 24 h would defer. Fix: read `avgPrice` from the exchange (REST `GET /fapi/v1/ticker/price`? no — `/fapi/v1/premiumIndex`? pick the endpoint that carries the MARKET-filter reference price and say why) into the execution-worker's input path with a freshness bound; tests.
3. **Proteção**: `protection.py:183/296` — write the integration test that a `pending_degraded` intent recovers after restart with the remaining quantity and no duplicate sell (`tests/integration/paper/`), fix what fails.
4. **MTM**: `bridge_inputs.py:180`, `cycles.py:275`, `health.py:95` — `mtm_fresh` measures the write, not the mark quality. Add `mark_quality` (share of open positions marked with a live price younger than N s) to the heartbeat/health and make the kill-switch pre-check refuse admission when quality < 100 % (paper_v1 preset); tests.
5. **Integração**: `tests/integration/paper/test_v6_stale_data_reconnect_restart.py` declares WS gap + Redis loss during decision uncovered — cover both (SPOT integrated); fix what fails.
6. **Backup**: verify on the VPS that dumps exist and one restores into a scratch database (`pg_restore --list` at minimum; a real restore into `hunter_restore_test` only if it is a separate DB and dropped afterwards — say what you did).
7. Update `docs/ACTIVATION.md` §8 with the acceptance checklist as measured (dates, numbers) — the operator flips the flag only when every row is green.

## Prove
Per-file test runs with real output, `ruff`/`pyright`/`check_file_size.py`; the VPS measurements verbatim (SQL + result); report in Portuguese, extended format; `.claude/state/notes-T3.29.md`.

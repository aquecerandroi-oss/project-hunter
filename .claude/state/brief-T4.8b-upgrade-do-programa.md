# Brief T4.8b — o programa da pump.fun mudou hoje: bonding_curve_v2 e o TradeEvent de 16 bytes a mais (12/09/2026 14:5x BRT)

**Achado da T4.14 (simulação mainnet, `sigVerify=false`, 14:09–14:2x BRT; `.claude/state/notes-T4.14.md` §prova de rede):** (A) o `TradeEvent` ganhou 16 bytes (`holder_rewards`) — a T4.14 deixou o decoder tolerante (`fill_decode_failed` nomeado); (B) `buy`/`sell` passaram a exigir contas restantes novas — a PDA **`["bonding-curve-v2", mint]`** — e a conta observada `4CLQ…` (T4.8) sumiu; toda compra montada pela T4.8 morre na simulação com o erro **6074**. O executor falha fechado (grava `failed`, nada assinado), mas **não compra** até isto ser regravado. **Antes de qualquer SOL real.**

**Leia antes:** `.claude/state/notes-T4.14.md`, `.claude/state/notes-T4.8.md`, `packages/exchange-adapters/hunter_exchanges/pumpfun/{tx,quote,verify,trade_event,tx_rpc,global_state,decode}.py`, fixtures `packages/exchange-adapters/tests/fixtures/pumpfun/{idl_pump_onchain_raw,rpc_tx_buy_raw,rpc_tx_probe_raw,simulation_proof_mainnet_raw}.json`, `docs/PUMPFUN-ONCHAIN.md` §6b, `services/meme-executor/hunter_meme_executor/{entries,submit*}.py` (o que consome o construtor).

**Entregar:**
1. **Nova captura real** (≤ 12 chamadas RPC públicas, só leitura): a IDL on-chain atual do programa da curva (`getAccountInfo` da conta da IDL, descomprimir) e **duas transações de hoje** (uma compra do roteador do site e uma venda), buscadas por `getSignaturesForAddress` do programa + `getTransaction` (≥ 14:00 BRT); comparar a lista de contas/discriminadores com a T4.8 e documentar cada diferença em `docs/PUMPFUN-ONCHAIN.md` §6c ("upgrade de 12/09/2026": data/slot do upgrade se a conta `ProgramData` mostrar, contas novas, layout do evento).
2. **Construtor `tx.py`** atualizado: a conta `bonding_curve_v2` derivada por PDA (nunca constante), as demais contas restantes exatamente como a transação real; **paridade byte a byte** com a compra e a venda de hoje (fixtures novas `t48b_*`); `verify.py` (§9.1) sabendo das contas novas; `quote.py` inalterado se as taxas não mudaram (provar com o fill real de hoje: taxas por componente ao lamport).
3. **`trade_event.py`**: layout novo com `holder_rewards` como campo (não só tolerado), fixture real; o `sol_spent` do fill continua vindo do evento **e** confere com o delta de saldo.
4. **Simulação mainnet** pelo caminho do executor (`sigVerify=false`, carteira vazia gerada em memória): `buy` **ok** (sem 6074) e `sell` ok, com CU; colar em `.claude/state/notes-T4.8b.md`; `meme_vm.py` VM4/VM5/§9.1 PASS de novo com o construtor novo.
5. Teste de **detecção de upgrade**: um teste unit que falha alto se o hash da IDL on-chain (capturado) diferir do da fixture, com mensagem "programa mudou: regravar T4.8b"; e um check no boot do executor (`program_idl_hash` esperado vs lido; divergência ⇒ recusa nomeada `program_upgraded`, nunca assinar).
6. `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; testes adapter + executor verdes; docs; `git status --porcelain` ao final.

**Regras:** as de sempre — primeiro plano `timeout 290`; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; sem commit; **nunca** `.env*`; **nunca** chave real; **nunca** `sendTransaction` na mainnet; flag nunca `true` em arquivo; não tocar `services/meme-worker/**`, `apps/**`, migrações; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

# Brief S0 — migração `0002_shadow_lab` e contratos duráveis do Shadow Lab

**Owner:** database-architect (opus). **Revisores depois:** code-reviewer, Astra (`bash infra/scripts/astra.sh ask S0-schema "..."`). **Não commitar** — o orquestrador commita após revisar `git diff --stat`.

## Leia antes (nesta ordem)
1. `CLAUDE.md` (regras duras, comandos canônicos, ≤ 350 linhas, TDD, Decimal/UTC).
2. `docs/plans/SHADOW-LAB.md` — seção "Decisão conjunta" itens 1, 2, 4, 5, 6, 8 e a tarefa **S0** + checklist "S0 — Migração e contratos duráveis". Esse é o contrato; nada nele é opcional.
3. `docs/DATABASE.md` (RLS, partições, convenções), `docs/plans/M2.md` T2.1 e T2.9 (a outbox compartilhada do M2 absorverá a `shadow_outbox`; T2.1 passa a referenciar esta migração, não recriar seus objetos).
4. Modelos atuais: `packages/core/hunter_core/db/models/agents.py` (`strategies`, `strategy_versions`, `agent_signals`, `signal_outcomes`), `packages/core/hunter_core/domain/enums.py` (`SignalStatus`, `OutcomeResult`), migração `0001` em `infra/migrations/versions/` (estilo, RLS, testes existentes em `apps/api/tests/integration/` e `packages/core/tests/`).
5. `.claude/rules/astra-second-opinion.md` — peça a opinião da Astra sobre a migração ANTES de finalizar e inclua "Segunda opinião (Astra)" no relatório.

## Entregar (arquivos permitidos)
- `infra/migrations/versions/0002_shadow_lab.py` (nome/ID conforme convenção do repositório), com `upgrade` e `downgrade` completos.
- `packages/core/hunter_core/db/models/agents.py` (+ novo módulo se passar de 350 linhas, ex. `agents_shadow.py`): `signal_outcomes.meta JSONB NOT NULL DEFAULT '{}'`; `signal_outcomes.tracking_state` (enum Postgres `shadow_tracking_state`: `pending_entry|active|terminal|no_entry|censored`), `no_entry_reason`, `censored_reason` (texto curto, nulos quando não se aplicam; CHECK de coerência: `no_entry` ⇒ `no_entry_reason` não nulo, `censored` ⇒ `censored_reason` não nulo, `result <> 'open'` ⇒ `tracking_state IN ('terminal','censored')`, `result = 'open'` ⇒ `tracking_state IN ('pending_entry','active','no_entry')` — ajuste se a decisão exigir diferente e diga por quê); tabela `shadow_episodes` (`strategy_version_id`, `market_id`, `cohort` texto (`prospective` | `replay:<run_id>`), `episode_id` uuid, `last_bar_close` timestamptz, `armed` bool, `open_outcome_signal_id` uuid nulo FK → `agent_signals.id`, `updated_at`; `UNIQUE (strategy_version_id, market_id, cohort)`; índice parcial para `open_outcome_signal_id IS NOT NULL`); tabela `shadow_outbox` (`id` bigserial, `event_id` uuid único, `stream` texto, `payload` JSONB, `created_at`, `dispatched_at` nulo, `attempts` int, `last_error` texto nulo; índice parcial em `dispatched_at IS NULL`).
- **Trigger de congelamento** em `strategy_versions`: para linhas com `activated_at IS NOT NULL`, qualquer `UPDATE` que altere `strategy_id`, `version`, `code_ref`, `parameters_schema`, `default_parameters`, `params_format` (nova coluna int NOT NULL DEFAULT 1) ou `activated_at` (inclusive para NULL) levanta exceção com mensagem clara; `status`, `changelog` e colunas de auditoria continuam mutáveis. A primeira ativação (transição `activated_at NULL → valor`) é permitida uma única vez.
- `packages/core/hunter_core/strategies/canonical.py` (novo pacote `hunter_core.strategies` com `__init__.py` mínimo): `canonical_json(obj) -> bytes` e `params_hash(params) -> str` (`params_format = 1`: chaves ordenadas, `Decimal`/float como string normalizada sem zeros à direita nem expoente, `datetime` como ISO-8601 UTC com `Z`, `None` como `null`, listas na ordem dada), com vetores de teste de equivalência (`{"a": Decimal("1.50")}` ≡ `{"a": "1.5"}` ≡ `{"a": 1.5}`; ordem de chaves irrelevante; `2026-01-01T00:00:00+00:00` ≡ `...Z`). Se S1 já tiver criado esse módulo quando você começar, adote-o e só complete os testes.
- Enum de domínio em `packages/core/hunter_core/domain/enums.py`: `ShadowTrackingState`, `ShadowCohort` helpers (parse/format de `replay:<run_id>`).
- Testes (pytest, marcador `integration` para banco real via testcontainers como os existentes; `unit` para canônico): upgrade → downgrade → upgrade sem erro e `alembic check` limpo; cada proteção da trigger (ativa, deprecated, reativada, `SET activated_at = NULL`, alteração de cada campo congelado, alteração permitida de `status`); CHECKs de coerência de `tracking_state`; unicidade de `shadow_episodes`; FK do outcome; `shadow_outbox` índice parcial; RLS: essas tabelas são de sistema (sem `organization_id`) — confirme a política usada por `agent_signals`/`signal_outcomes` hoje e mantenha coerente.
- `docs/DATABASE.md`: seção curta "Shadow Lab (0002)" descrevendo as tabelas/colunas/trigger.

## Regras
- TDD: teste falhando primeiro. Nada de dados fictícios. Uma migração, um propósito. `Decimal`/`NUMERIC(28,10)` para preços. Tudo UTC.
- Comandos: `uv run pytest packages/core -q`, `uv run pytest apps/api/tests/integration -q -k shadow`, `uv run alembic -c infra/migrations/alembic.ini upgrade head` e `... check` (stack local: `docker compose -f infra/docker/docker-compose.yml up -d postgres redis`), `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`, `uv run python infra/scripts/check_file_size.py`. Toolchain: prefixe o PATH com `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`.
- Não toque em `.env*`, não commite, não altere arquivos fora da lista (a T1.7 e a T1.6b estão em voo em `tests/integration`, `services/market-worker`, `packages/exchange-adapters`).

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES · TESTS · TEST RESULTS (saída real) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · Checklist S0 com `[x]`/`[ ]` honesto · NEXT STEP.

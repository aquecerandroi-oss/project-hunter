# Brief T3.33 — quatro estratégias novas do mercado entram no Lab: descoberta, escolha e contrato (Everton, 2026-09-08: "acha mais 4 estratégias no mercado e valide")

**Owner:** quant-engineer (discovery). **Second opinion:** Astra (the orchestrator runs `astra.sh ask` in parallel and hands you the answer; merge, do not defer). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers; VPS read-only.** Base: `main` at `c699d48`. In flight: T3.18c, T3.29, T3.31, T3.32 (`.claude/state/notes-T3.32.md` — the loss diagnosis; read it if it exists by the time you finish, it changes what "validate" should stress). Your write scope: `.claude/state/notes-T3.33.md`, `.claude/state/brief-T3.33{a,b,c,d}-*.md` (the four implementation briefs, in English, one per strategy), `.claude/state/exp-drafts/EXP-000{8,9,10,11}-*.md` (Hypothesis/Protocol frozen, no evaluation yet).

## What "validate" means here (do not redefine)
D14/D15 (`.claude/state/decisions-delegated-2026-09-08.md`), `docs/plans/SHADOW-LAB.md` §19, `docs/plans/REPLICATION.md`: a strategy version enters the Lab as `research_only`, gets **31-day replay evidence on day one** (ETH/SOL/XRP/DOGE + the β universe where candles exist; funding history exists for 31 days), then runs **prospective** on every market; the verdict needs 100 evaluable outcomes and 30 days; `promissora` → replication protocol (siblings, market halves, bootstrap). Everton's directive: SPOT only for paper execution, long-only is the safe default (say explicitly if a candidate needs shorts and why it still fits — otherwise drop it).

## Constraints of our data and engine (read, do not assume)
`packages/core/hunter_core/strategies/{momentum_v1,volume_anomaly_v1}.py` + `schema.py` + `constraints.py` (T3.26c) — the contract a strategy module implements (decision per 15 m bar, `Invalidation`, `AssumedCosts`, target/stop in ATR, horizon, parameters schema with ranges, `code_ref` digest closure: **a new module must not import anything that changes the digest of the existing two**); `docs/PIPELINE.md` (what a strategy can read at decision time: 15 m/1 h candles, ATR, RVOL, funding rate + history, spot price via `market-worker-spot` (14 pairs), β vs BTC, market status; **no** order book depth history, **no** open interest yet — say it when a candidate needs it and drop it or propose the collector as a separate brief); `services/strategy-worker/hunter_strategy_worker/replay/**` (what the replay can evaluate); `packages/core/hunter_core/strategies/seed.py` (how a strategy key is registered).

## Deliver
1. **Shortlist of 8 candidates from the public literature / practitioner playbooks for crypto perpetual-spot markets on 15 m–1 h bars**, each with: source (paper, book, known practitioner write-up — cite), the edge claimed and why it might exist (behavioural/structural), data it needs (✔/✘ against our list), long-only feasibility, expected trade frequency per market-day, main failure mode, and cost sensitivity (KB-0008). Candidates you must at least consider: funding-rate extreme mean-reversion (we have funding), spot–perp basis/premium divergence (we have spot: unique to us), volatility-compression breakout (Donchian/Bollinger squeeze + RVOL), short-term mean-reversion pullback in a 1 h uptrend (RSI(2)/z-score of close vs VWAP), session/opening-range breakout (Asia/Europe/US in Brasília terms), cross-sectional relative momentum (top decile of 24 h return vs β-adjusted), overnight/weekend effect, liquidation-cascade rebound (needs data we may not have).
2. **Pick 4**, maximising diversity of edge type (trend / mean-reversion / carry-structural / cross-sectional) and data we already have; justify each rejection in one line.
3. **For each of the 4, a frozen contract** in the EXP draft: entry rule (exact), invalidation (exact, and **not** the momentum one that KB-0006 shows kills 35 % with 0 winners), stop and target in ATR with the geometry argument (MFE/MAE from T3.32 if available), horizon, costs, parameters with ranges (for `constraints.py`), markets/universe, what would falsify it. Plus the implementation brief `brief-T3.33{a..d}-<key>.md` (owner quant-engineer; module `packages/core/hunter_core/strategies/<key>_v1.py` ≤ 350 lines; unit tests with synthetic bars proving each rule fires and does not fire; `seed.py` registration; `constraints.py` ranges; a replay of 31 days on the four reference markets **and** the receipt pasted; activation only by the operator via `activate_strategy_version.py` on the VPS — the brief says the exact command; nothing reaches the wallet).
4. **Validation plan common to the four** (one section): replay day one → prospective → maturity → replication; what number on day one would already kill a candidate (e.g. gross expectancy < 0 on 4 markets over 31 days with ≥ 100 outcomes → do not keep it running, deprecate).

## Prove
Report in Portuguese, extended format (STATUS · SHORTLIST table · the 4 chosen with rationale · FILES · CONCERNS); `.claude/state/notes-T3.33.md`. No code in this task.

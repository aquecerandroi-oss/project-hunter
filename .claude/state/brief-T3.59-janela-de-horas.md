# Brief T3.59 — "só operar de manhã" testado direito: regra de elegibilidade por janela de horas (UTC) na política por versão (T3.52), aplicada à mean_reversion v10 e à momentum v11, com critério de sucesso pré-registrado e avaliação pareada por BARRA (Everton, 2026-09-09 13:25: "testar 5 min, 10 min, 1 h ou só no início do dia")

**Owner:** quant-engineer. **Reviewer afterwards:** code-reviewer (code + non-anticipation). **Do not commit.** **Operational rule: never a background shell; every command foreground with explicit `timeout 290`; testcontainers max 2 files, one invocation each; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; never edit the seven closure modules; VPS writes only through audited scripts and only after the orchestrator deploys; SQL reads inside `repeatable read read only`.** Base: `main` at HEAD (T3.52 committed: `regime_gate.py`, `variant.py`, `eligibility_policy` JSON with a `regime` rule). Scope: `services/strategy-worker/hunter_strategy_worker/{regime_gate.py or a new sibling hours_gate.py, variant.py, context.py (the gate call), catalogue.py (parser)}`, `infra/scripts/derive_variant.py` (`--policy hours=12-15` UTC, combinable with `regime=`), tests, `docs/ACTIVATION.md` §7c (one paragraph), `docs/PIPELINE.md` §4b item 10 (one sentence), `.claude/state/exp-drafts/EXP-0023-janela-de-horas.md`, `.claude/state/notes-T3.59.md`.

## Evidence to start from (T3.54 §5, `.claude/state/notes-T3.54.md`)
- `session_orb v1` negative in 3 of 4 four-hour buckets; NY open (13:00–14:59 UTC) negative in every cohort that touches it; the day turn (00:00 UTC) negative.
- The only hour positive in both momentum cohorts: **12:00–12:59 UTC (09:00 BRT)**: +0,69 R (n 6) parent, +0,70 R (n 16, sum +11,26 R) in the 1 h variant; in `mean_reversion v10` the 12–15 UTC bucket is the second best. Best-of-24 with n ≤ 16: a hypothesis, nothing more. T3.53: hour-of-day cells were not judgeable (1–3 days of calendar) — this brief is the pre-registered test.

## Deliver
1. **Rule `hours`** in the eligibility policy: `{"hours": {"utc": [[12, 15]]}}` (half-open, one or more windows, validated: 0–24, non-overlapping); a bar is eligible when `source_bar_close` hour ∈ window; refusal reason `hours_gate:HH`; combinable with `regime` (both must pass; record both); envelope records the window; replay applies the same rule. Non-anticipation: the hour of `source_bar_close` is a property of the closed bar, nothing else is read.
2. `derive_variant.py --policy hours=12-15` (and `--policy regime=…,hours=…`), refuses malformed windows; lineage prefix preserved (T3.47c).
3. Tests: unit (parser, boundary 11:59/12:00/14:59/15:00, wrap-around window 22-02, combination with regime), one testcontainer test on the worker path (a version gated to 12–15 skips a 10:00 bar with `hours_gate:10`), replay plan.
4. **EXP-0023 pre-registration (write BEFORE any replay)**: variants `mean_reversion v12` from v10 with `hours=12-15` UTC and `momentum v12` from v11 (keeps its regime gate) with `hours=12-15`; success = paired-by-bar Δ vs parent ≥ +0,05 R with the day-block 95 % CI above zero AND n ≥ 30 in 31 d; anything less = `descartar`; also the honest control: the SAME test with the window 13–16 UTC (NY open, expected negative) as the falsification arm.
5. After the orchestrator deploys: derive (dry-run first), activate research_only, replay 31 d × 4 markets (`--explain-ledger`), paired by (market, bar) over shared eligible bars (T3.52d lesson — never by decision), stress if K1 survives, verdicts, ≤ 10 lines in Portuguese.

## Prove
Tests per file, `ruff`/`pyright`/`check_file_size.py`; report in Portuguese, extended format; `.claude/state/notes-T3.59.md`. Times in Brasília (UTC−3) with UTC as detail.

# Brief T4.6 — o Lab meme contínuo: simular sem parar na curva do pump.fun com dado ao vivo (Everton, 2026-09-12 04:5x BRT: "o Lab vai ficar em cima das meme coins simulando sem parar")

**Pré-requisitos (esperar os commits):** T4.2 (migração 0021, `meme-worker`, `meme_tokens`/`meme_curve_snapshots`/`meme_features_1m`), T4.5 (`hunter_indicators.meme`: curva, `PaperCurveWallet`, regras, replay), T4.4 (`docs/RISK_ENGINE_MEME.md`: tetos, checks de admissão, V1–V9 meme). Leia as notas dos três antes.

**Entregar:**
1. No `meme-worker` (ou serviço irmão `meme-lab`, se o worker precisar ficar só coletor — decidir e justificar): um laço por minuto que, para cada conjunto de regras ATIVO (`meme_rule_sets`: nome, versão, parâmetros congelados, `code_ref`, status research_only/paper, pré-registro EXP-M*), avalia a porta de entrada (`hunter_indicators.meme.rules`) sobre a linha `meme_features_1m` de cada mint rastreado e, quando entra, abre uma aposta paper na `PaperCurveWallet` daquele conjunto (uma carteira paper por conjunto, saldo teto em SOL do `RISK_ENGINE_MEME.md`), aplica as regras de saída a cada minuto (alvo k×, trailing, time stop, saída forçada em migração/rug), e persiste `meme_paper_bets` (mint, conjunto, entrada: reservas/ts/SOL/fee/priority estimada; saída: idem + motivo; R em SOL com risco inicial = perda máxima permitida; `fill_delay_snapshots` usado; `observed_at` das fotografias usadas) — nada de fill no preço visto: reprecificar na fotografia seguinte (T4.5).
2. Não-antecipação: a porta lê só features com `end_time <= agora − 1 min` fechadas; a saída só usa a fotografia seguinte; nada de olhar o futuro. Teste de look-ahead obrigatório.
3. Régua e diário: por conjunto, por dia (Brasília): apostas, acertos, R em SOL, PnL em SOL e em USD (cotação SOL/USD observada com fonte e hora), drawdown, tokens que rugaram durante a aposta; `GET /api/v1/orgs/{org}/meme/lab` (VIEWER+) com o placar por conjunto e a distância à meta (US$ 7 M em 30 d: capital, retorno diário exigido, retorno diário medido); `obsidian/09-OPERATIONS/Diario-Meme/<dia>.md` gerado por script (`infra/scripts/meme_diary.py`, ops, dry-run/apply) com os números do dia e as apostas mais relevantes; o arquivista completa com o aprendizado.
4. Primeiro conjunto ativo: EXP-M1 (T4.5) como `research_only` — parâmetros congelados no pré-registro; nenhum outro sem pré-registro.
5. Testes: unit + 1 testcontainer no caminho de persistência e no laço (uma aposta abre, uma fecha por alvo, uma por time stop, uma por migração; look-ahead); `ruff`/`pyright`/`check_file_size.py` limpos; arquivos ≤ 350; docs `docs/plans/T4-MEME-RADAR.md` §T4.6, `docs/PIPELINE.md` seção meme.

**Regras:** nunca shell em background; comandos em primeiro plano com `timeout 290`; testcontainers 1 arquivo por vez; árvore compartilhada — nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; nunca `.env*`; nunca chave; nada de transação real (sem `ENABLE_MEME_LIVE_TRADING`, sem assinatura). Relatório ≤ 12 linhas em português, horários em Brasília.

You are a research agent for PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T4.0-pumpfun-fontes.md`. Task T4.0 — Everton (2026-09-12) declared that meme coins are his real target and asked for a pump.fun integration to MONITOR that market (a "Meme Radar"; no on-chain execution in this cut). Research, with web access, ONLY from pages you actually open (URL + date read; never invent limits or prices): (1) the pump.fun data surfaces available in 2026: the Solana program(s) (pump.fun bonding curve program id, PumpSwap AMM after migration), the official/unofficial HTTP API (`frontend-api.pump.fun` — is it still reachable, does it need Cloudflare/cookies, rate limits), the PumpPortal data websocket (`wss://pumpportal.fun/api/data`: `subscribeNewToken`, `subscribeTokenTrade`, `subscribeMigration`, `subscribeAccountTrade` — message schema, free vs paid, limits), Solana RPC options (public mainnet endpoint limits; Helius/QuickNode/Triton free tiers and what needs an API key — a paid or keyed service is Everton's decision, say which), Bitquery/Dune/Moralis pump.fun endpoints (keyed/paid), and open-source projects that already parse pump.fun events (GitHub, with stars and last commit date) — which one has the cleanest event decoding (create, buy, sell, complete/migrate) and the bonding-curve price/market-cap formula (virtual reserves; graduation threshold in SOL, current numbers); (2) the numbers that define the market: tokens created per day, share that graduates, typical lifetime, PumpSwap fee, what "market cap" means on the curve; (3) a data-model proposal for the project (Postgres): `meme_tokens` (mint, name, symbol, creator, created_at, uri, curve state, migrated_at, pool), `meme_trades` (signature, mint, side, sol, tokens, price, wallet, ts — partitioned by month), `meme_snapshots` (per minute: price, mcap, curve progress, unique buyers, buy/sell ratio, top-10 holder share, creator sold) — mirror the conventions in `docs/DATABASE.md` (read it) and the adapter conventions in `docs/EXCHANGE_INTEGRATION.md` and `packages/exchange-adapters/hunter_exchanges/` (read the Binance adapter's structure: normalized models, recorded fixtures, reconnection); (4) the honest risks: rugs/bundlers, wash trading, API instability, rate limits, the fact that price on the curve is not a CEX orderbook, and that the risk engine's SPOT-only doctrine (`docs/RISK_ENGINE.md`) does not cover on-chain execution — monitoring only. Deliver `docs/plans/T4-MEME-RADAR.md` (Portuguese: objective, sources table with cost/key/limits/reliability, event schema, data model, the first three tasks T4.1 adapter (PumpPortal WS + Solana RPC read-only), T4.2 storage + scanner features, T4.3 API + web "Meme Radar", each with acceptance checks; and the list of decisions for Everton: keyed/paid RPC provider yes/no, storage retention, which features first), `.claude/state/notes-T4.0.md` (every URL opened with the read time), and a Hipóteses row in `obsidian/00-INBOX/Hipoteses-do-plantao.md` only if a testable market hypothesis appears. Rules: no code changes; no background shells; every command foreground with explicit `timeout 290`; do not commit; do not touch `.env*`; never paste any API key. Report in Portuguese ≤ 15 lines, times in Brasília (UTC−3, compute it).

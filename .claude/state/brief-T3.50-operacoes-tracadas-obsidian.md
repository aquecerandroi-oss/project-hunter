# Brief T3.50 — toda operação do Lab ganha o seu gráfico traçado (velas, linhas de tendência vigentes na barra da decisão, entrada/stop/alvo/saída) salvo como PNG no vault e indexado numa nota do Obsidian (Everton, 2026-09-08 19:50: "já pode colocar para funcionar todas a traçar, tirando print e subindo no Obsidian, colocando linha e tudo")

**Owner:** quant-engineer (owns `tl_*` and the T3.34 renderer). **Do not commit** (list the exact files; the orchestrator commits by pathspec). **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (batch the rendering: ≤ 200 images per invocation, loop in the foreground); no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; VPS strictly read-only (SQL via `ssh hunter-vps "docker exec -i hunter-postgres-1 psql -U hunter -d hunter -v ON_ERROR_STOP=1 -f -"` inside `begin transaction isolation level repeatable read read only; … commit;` — the T3.34 export recipe in `.claude/state/notes-T3.34.md` §4); do not stop or recreate containers; do not add matplotlib to any `pyproject.toml` (`uv run --with matplotlib` as in T3.34).** Base: `main` at `2e39774`. Write scope: `infra/scripts/render_operations.py` (+ a small `infra/scripts/render_operations_*.py` sibling if the 350-line budget needs it), `infra/scripts/tests/test_render_operations.py`, `infra/scripts/sql/research/2026-09-09-t350-*.sql`, `obsidian/attachments/operacoes/**`, `obsidian/03-TRADING/Operacoes-tracadas/**`, `obsidian/09-OPERATIONS/Diario/2026-09-08.md` (append), `docs/PIPELINE.md` (one section), `.claude/state/notes-T3.50.md`.

## Start from what exists
`.claude/state/design/trendlines/plot_trendlines.py` (T3.34 renderer, matplotlib, Decimal in, float only at the plotting edge), `hunter_core.strategies.tl_scan.tl_scan` + `tl_setup` (the frozen line geometry; parameters = `trendline_breakout_v1.default_parameters` for the pattern part), the envelope fields of a trendline decision (`line_id`, `line_slope_per_bar`, `line_first_idx`, `line_last_idx`, `line_price_at_decision`, `pivot_low_*`, `pattern_params` — `tl_setup.py:236-270`), the Lab tables (`agent_signals`, outcomes, `positions`/orders where present; `apps/api/hunter_api/repositories/lab_*.py` for the exact joins and `identity_key`).

## Deliver
1. **Export (read-only)** `render_operations.py export --version <strategy> <vN> [--cohort …] [--since …]`: one JSONL line per closed operation (market, decision time, entry/stop/target/exit prices and times, exit reason, result R, cohort, version, the decision envelope) plus the 1m candles from 96 bars×15 m before the decision to the exit (folded to 15 m with the completeness rule). Count per version pasted (the VPS has ~4 086 concluded outcomes; versions under study: `trendline_breakout v1` (after T3.34c), `mean_reversion v2/v3`, `momentum v6`, `session_orb v1`, the T3.47 variants when they exist).
2. **Render** `render_operations.py render <jsonl> --out obsidian/attachments/operacoes/<strategy>-<vN>/`: candlesticks 15 m, the trend lines that were valid at the decision bar (`tl_scan` cut at the decision bar — non-anticipative, same frozen code; for trendline decisions also highlight the `line_id` the strategy used and the pivot of the stop), entry/stop/target as horizontal segments from entry time to exit time, the exit marker with reason, title `<market> · <strategy vN> · <decision time Brasília> · <result R>`; PNG ≤ 120 KB (dpi/size tuned, paste sizes); file name `<YYYYMMDD-HHMM>Z-<market>-<result>.png`; idempotent (skip if the PNG exists and `--force` absent).
3. **Obsidian**: one note per version `obsidian/03-TRADING/Operacoes-tracadas/<strategy>-<vN>.md` (frontmatter: version, code_ref digest, cohort, as_of, n, expectancy) with a table per operation (time Brasília, market, entry/stop/target, exit reason, R, lines valid at decision, `line_id` used if any) and the embedded image `![[...]]`; an index `obsidian/03-TRADING/Operacoes-tracadas/README.md` linking the versions and stating plainly which strategies read lines (only trendline_breakout) and which do not (the lines on their charts are context computed after the fact by the same frozen scanner, never an input of the decision). Wire links: `[[EXP-…]]` of the version, `[[KB-0076]]`.
4. **Run it for every version listed in item 1 that has closed operations now** (all of them — Everton asked for all; if the total image size would exceed 150 MB, render the newest 300 per version, say so, and propose the cap). Append the run to `obsidian/09-OPERATIONS/Diario/2026-09-08.md`.
5. Tests: fixture with a real exported operation (small), render to a temp dir, assert the file exists, the lines drawn equal the `tl_scan` output at the cut, the title/table content; no network in tests.
6. `docs/PIPELINE.md`: the section "Operações traçadas" (how to re-run, idempotency, what the lines mean).

## Prove
Counts per version, PNG sizes, `ruff`/`pyright`/`check_file_size.py`, test output, three sample images referenced by path; report in Portuguese, extended format; `.claude/state/notes-T3.50.md`.

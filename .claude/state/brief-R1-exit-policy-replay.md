# Brief R1 — Replay de políticas de saída sobre entradas congeladas (EXP-0004)

**Owner:** quant-engineer (opus). **Revisores depois:** code-reviewer, Astra; Sexta-feira escreve o experimento no Obsidian. **Não commitar.** **Regra operacional: nunca use Bash em background; cada comando em primeiro plano com timeout ≤ 5 min; suítes com testcontainers por arquivo.**

**Pedido do Everton (2026-09-06):** "quero que ache [estratégias] e iremos usar no virtual; assim que tiver pronto sairemos validando tudo". A rodada 6 de conhecimento concluiu que o teste mais barato e mais informativo é este: **a mesma entrada, oito políticas de saída, sete contrastes**, sobre os sinais que o Lab já registrou. Nada é ativado; é pesquisa (`purpose=research_only`).

## Leia antes (nesta ordem)
1. `CLAUDE.md`; `docs/plans/SHADOW-LAB.md` (Decisão conjunta inteira: preços sintéticos, stop primeiro, gap na abertura, horizonte exato, MFE/MAE honestos, métricas com nome, gate de maturidade, "inconclusivo" até 100 outcomes e 30 dias).
2. `obsidian/11-KNOWLEDGE/Strategy Backlog.md` — seções **"O bloco de políticas de saída — T-005, L1 e L2 são a mesma busca"** e **"O menor conjunto para o Everton começar a validar mais rápido"**; a linha #1 do backlog (`INV-A/B/C/E`); a "Fila para a sombra — livros" (L1, L2). `Registro de Tentativas.md` linha T-005 e a seção de família declarada.
3. As notas de origem: KB-0005, KB-0006 (invalidação), KB-0046, KB-0054 (alvo assimétrico), KB-0045 (saídas dos Turtles: sem alvo; canal oposto de 10 fechamentos), KB-0049 (validação, Holm, dependência), KB-0051 (concentração temporal), KB-0041/0042 (deslocamento referência→entrada e o `open`).
4. O acompanhamento real: `services/strategy-worker/hunter_strategy_worker/{walker,settle,funding,episodes,repo}.py` e `.claude/state/notes-S2.md` — **o replay tem de reproduzir a base com o mesmo código de acompanhamento**, não com uma reimplementação. (A correção de identificação de funding por slot está sendo feita agora em `funding.py`/`settle.py`; use a versão da árvore quando ela estiver commitada — o orquestrador avisa.)
5. `packages/core/hunter_core/db/models/agents_shadow.py` (`agent_signals`, `signal_outcomes`, `strategy_versions`, `tracking_state`) e `docs/DATABASE.md` §16.

## Entregar
Arquivos permitidos: `packages/indicators/hunter_indicators/replay/**` (novo, puro: políticas de saída e contrastes), `services/strategy-worker/hunter_strategy_worker/replay/**` (novo: leitura dos registros e reuso do walker/settle), `infra/scripts/replay_exits.py` (CLI, **só SELECT** no banco; nunca escreve em tabelas do Lab), testes nos dois pacotes, `.claude/state/notes-R1.md`, `.claude/state/r1-proof.md`.

1. **Passo 1 — reproduzir a base.** Para cada `agent_signal` com `tracking_state ∈ {terminal, no_entry}` das versões congeladas de `momentum_v1` e `volume_anomaly_v1`, reconstruir a partir das velas 1m persistidas (`candles`, `market_id` + janela `[source_bar_close, entrada + horizonte]`), com os mesmos custos assumidos e o mesmo `ATR₀` congelado, e comparar com `signal_outcomes`: **saída, preço de saída, `R_net`, `R_ex_funding` e motivo idênticos** (Decimal, não float). Relatar a taxa de reprodução por versão e **cada divergência com causa** (lacuna de ingestão, funding, vela reescrita por backfill). Sem ≥ 99% de reprodução ou divergências todas explicadas, os passos seguintes não valem nada — pare e reporte.
2. **Passo 2 — as oito políticas, declaradas antes de olhar resultado:** `base` (INV-A, atual), `INV-B` (sem invalidação, com alvo), `INV-C` (invalidação só em dois fechamentos consecutivos), `INV-E` (invalidação com buffer `L − 0,25·ATR₀`), `alvo 3,0 ATR₀`, `alvo 4,5 ATR₀` (os `target2/target3` já gravados), `EXIT-NOTGT` (sem alvo; stop + invalidação + horizonte), `EXIT-CHAN` (sem alvo; saída no fechamento abaixo da mínima de 10 fechamentos de 15 min). **Entradas pareadas**: a validação `stop < entrada < alvo` fica congelada na base; um braço nunca admite entrada que a base recusou, e nunca ocupa/rearma slots (`episodes.py`) — é replay, não estratégia independente. Stop primeiro, gap na abertura primeiro, horizonte exato, funding pelo mesmo `settle` (funding desconhecido continua `R_net = null` com motivo; **nunca zero**).
3. **Passo 3 — os sete contrastes**, pareados por sinal: `INV-B − base`, `INV-C − base`, `INV-E − base`, `alvo 3 − base`, `alvo 4,5 − base`, `NOTGT − base`, `CHAN − NOTGT`. Diferença média de `R_net` por sinal, intervalo por **bootstrap em blocos de tempo** (blocos de 1 dia; a dependência é transversal — muitas altcoins reagindo ao mesmo BTC — então os blocos são por tempo, não por mercado), **Holm a 5% sobre os sete**, efeito mínimo declarado de 0,05 R (da linha T-005). Sem combinar vencedores. Reportar também: taxa de alvo entre toques resolvidos, taxa de lucro líquido, expectancy líquida hipotética em R, PF com denominador — pelos nomes do Lab.
4. **CLI** `uv run python infra/scripts/replay_exits.py --database-url ... --versions momentum_v1,volume_anomaly_v1 --out .claude/state/r1-proof.md [--policies ...] [--reproduce-only]`, saída em Markdown com as tabelas e um JSON canônico ao lado (para o Obsidian). Idempotente e reproduzível byte a byte para o mesmo banco.
5. **Testes:** políticas puras com velas sintéticas rotuladas (cada política com um caso em que ela difere da base e um em que coincide); reprodução da base contra um `signal_outcomes` sintético gravado pelo próprio walker (testcontainers); pareamento (braço não admite entrada recusada pela base); bootstrap em blocos determinístico por semente; Holm com um caso de livro.
6. **Prova** contra o banco local e, se o orquestrador liberar, contra um dump da VPS (`infra/vps/backup_postgres.sh` produz um; **não** conecte o replay ao Postgres de produção da VPS por fora do dump). Registre em `r1-proof.md`: n por versão, reprodução, tabela dos sete contrastes com IC e p ajustado, e a frase honesta: com < 100 outcomes maduros ou < 30 dias o resultado é **inconclusivo** por contrato, e o que este piloto entrega é aprendizado operacional, não confirmação.

## Regras
TDD; Decimal/UTC; ≤ 350 linhas por módulo; nada fabricado; nada ordena; nada ativa; sem escrever em tabelas do Lab. Comandos: `uv sync --all-packages`, `uv run pytest packages/indicators services/strategy-worker -q`, `uv run ruff check .`, `uv run ruff format --check packages services`, `uv run pyright packages/indicators services/strategy-worker`, `uv run python infra/scripts/check_file_size.py`. PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Opinião da Astra sobre o desenho (pareamento, blocos, Holm) antes de implementar e sobre o diff depois (`bash infra/scripts/astra.sh ask R1-replay "..."`). Não toque em `.env*`, `services/scanner-worker/**`, `services/market-worker/**`, `apps/**`.

## Relatório final (em português)
COMPLETED · FILES · TESTS (saída real) · REPRODUÇÃO DA BASE (taxa e divergências explicadas) · CONTRASTES (tabela) · O QUE É INCONCLUSIVO E POR QUÊ · Segunda opinião (Astra) · NEXT STEP (o que a Sexta-feira escreve em `obsidian/05-EXPERIMENTS/EXP-0004-politicas-de-saida.md`).

You are risk-engine-guardian on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T4.4-doutrina-execucao-pumpfun.md`. Everton (2026-09-12 04:2x BRT): "vamos começar a operar ainda hoje nessa carteira [uma carteira Solana nova, só do Hunter]; use quantos agentes possível". The orchestrator's position, which this brief must make executable: the Hunter never signs a real transaction until the full flow is verified (Everton's own Risk Engine directive: "só declare o modo autônomo pronto quando o fluxo completo estiver verificado"), the key lives only in the VPS `.env` typed by Everton, and real money needs an explicit switch he alone flips. Task T4.4 — the execution doctrine for pump.fun (Solana), as a contract BEFORE code: deliver `docs/RISK_ENGINE_MEME.md` (Portuguese; the sibling of `docs/RISK_ENGINE.md` — read it, `docs/plans/T4-MEME-RADAR.md`, `.claude/state/astra-review-t40-pumpfun.md`, `docs/PUMPFUN.md`/`docs/PUMPFUN-ONCHAIN.md` if they already exist, and `.claude/state/notes-T4.0*.md`): (1) scope: bonding-curve buys/sells and PumpSwap sells only; never leverage, never other programs; the paper simulator first (the curve is a deterministic formula: paper fills = formula + 1,25 % fee + priority fee estimate + slippage from concurrent trades measured after the fact); (2) the wallet policy: a dedicated wallet, balance cap in SOL (`MEME_WALLET_MAX_SOL`), per-trade cap (`MEME_MAX_SOL_PER_TRADE`), per-day loss cap and kill switch (`MEME_DAILY_LOSS_CAP_SOL`, latched, resume only by OWNER), max open positions, max exposure per token, cooldown after a rug detection, no re-entry into a token that rugged; the flag `ENABLE_MEME_LIVE_TRADING` (default false; only Everton; documented next to `ENABLE_LIVE_TRADING`), and the env var name for the key (`SOLANA_WALLET_SECRET_KEY`, never logged, never in metrics, loaded only by the meme executor process); (3) the admission checks for a curve buy: token age ≥ X s and ≤ Y min, curve progress window, creator not net seller, bundled share ≤ threshold (null → refuse until measurable), top-10 share ≤ threshold, our size ≤ % of the last-minute curve volume (participation, like PAPER_V1), priority fee ≤ cap, slippage ≤ cap, RPC/WS freshness ≤ cap (clock skew rule as in §7.1), Mayhem coins: agent trades excluded from volume and a separate policy; for sells: target multiple, trailing stop, time stop, forced exit on creator dump / curve completion / rug signal; every refusal named; (4) the execution path options with the trust model: PumpPortal Local Trading API (builds the tx; we sign locally; what leaves our box), direct instruction building from the IDL (T4.0d), Jito bundles/tips (when, caps), retries/idempotency by client id, confirmation and finality rules (commitment level), reconciliation with on-chain state after each fill, and what "fill" means on a curve (the event, not the tx submission); (5) the nine-verification equivalent for memes (V1–V9: sizing, caps, kill switch, duplicate submission, RPC failure mid-send, partial fill/none, rug during hold, restart with open position, two sessions) — each with the test that will prove it and the demo command; (6) the definition of "fluxo completo verificado" that unlocks real money: N days of paper with real curve data, the V1–V9 green on the VPS, a manual paper order path, and Everton's explicit switch — and the honest statement that "hoje" (today) can only be paper + radar; (7) an ADR in `docs/decisions/` (or the repo's ADR path) recording the decision; (8) a §"o que muda no motor atual" (the SPOT-only doctrine stays; the meme engine is a separate package `packages/risk-core/hunter_risk_meme/` proposed, not written) and `.claude/state/notes-T4.4.md`. Docs only in this task: NO code, NO keys, NO `.env*`. Rules: no background shells; every command foreground with explicit `timeout 290`; do not commit; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`. Report in Portuguese ≤ 12 lines.

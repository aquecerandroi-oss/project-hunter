# Brief T3.90 — H-P18 "dispersão BTC × alts como estado": a série `dispersion_24h` no molde da amplitude (breadth_v2), para o lote diário de 12/09

**Facts:** H-P18 (plantão runs 6/10/11, `obsidian/00-INBOX/Hipoteses-do-plantao.md`; Astra's must-fix: `r24h(t)` measured at the decision cut, frozen per bet, four cells, mild contrast inside BTC, no look-ahead) — on 10/09 and 11/09 the median 24 h return of the 16 markets was −4,8 %/−3,85 % while BTC was −1,5 %/−1,3 % ("discordância"); T3.88 (`packages/core/hunter_core/universe.py`, `packages/indicators/hunter_indicators/breadth/{series,spec}.py`, `services/scanner-worker/hunter_scanner_worker/breadth_{job,repo}.py`, `services/strategy-worker/hunter_strategy_worker/{breadth_gate,breadth_policy}.py`, table `market_breadth` with `breadth_version`, `infra/scripts/backfill_breadth.py`, `breadth_windows.py`) is the pattern: a per-minute, `end_time`-anchored, immutable series over the shadow universe (the 16 markets with ≥ 90 d), a policy rule with explicit `version`, a backfill CLI (report first, `--apply --reason`), and the EXP-0027 protocol (T3.89b: conditional × unconditional Δ vs parent with day-block CI; paired-by-bar Δ within ±0,02 R as the gate proof; identity clause vs the hourly regime cut — EXP-0027 fell exactly on that clause: breadth was not a new state). Read `.claude/state/notes-T3.88.md`, `notes-T3.89.md`, `obsidian/05-EXPERIMENTS/EXP-0027-amplitude.md`.

## Deliver (code + pre-registration; STOP before any VPS write — the orchestrator deploys and runs the backfill)
1. Series `dispersion_24h_v1` at minute T over the shadow universe: for each market the 24 h close-to-close return `r24h = close(T)/close(T−24 h) − 1` from `candles_1m` (only complete data; a market lacking either close is excluded and counted as missing); `btc_r24h`; `median_alt_r24h` over the universe excluding BTC; `dispersion = median_alt_r24h − btc_r24h`; also `share_below_btc` (fraction of alts with r24h < btc_r24h); coverage ≥ 80 % or `insufficient_coverage`. Store in `market_breadth` if its shape fits (a `series` column exists? — check; if the table is breadth-specific, add a sibling table `market_dispersion` via migration 0020 with the same conventions: global, immutable, `end_time`-anchored, UNIQUE (exchange, end_time, version), privileges by subtraction, downgrade refuses to lose rows — database-architect conventions in `docs/DATABASE.md` §31) — say which and why.
2. Producer in the scanner-worker (per-minute job with the same lock pattern), sharing `hunter_core.universe`; backfill CLI `infra/scripts/backfill_dispersion.py` (report first; `--apply --reason`; batched inserts like T3.88's fix).
3. Policy rule `dispersion` (`{"dispersion": {"version": "dispersion_24h_v1", "min": "-0.05", "max": "0.00"}}` half-open; refusal `dispersion_gate:<value>` / `dispersion_unavailable`; version mandatory; order hours → regime → breadth → dispersion; `derive_variant.py --policy dispersion=-0.05-0.00@dispersion_24h_v1` — check the parser's handling of negative bounds and fix it if needed), replay applies the persisted series only.
4. Tests: unit (series math incl. the missing-close rule and non-anticipation with a candle one minute too late), policy/boundaries/negative bounds, one testcontainer file for the producer (idempotent, coverage floor, lock) and one for the gate (a bar with dispersion −0,08 skipped by name); `ruff`/`pyright`/`check_file_size.py` clean; files ≤ 350.
5. Pre-registration `obsidian/05-EXPERIMENTS/EXP-0029-dispersao-btc-alts.md` (+ Index) BEFORE any replay: parent `mean_reversion v10` (cohort `replay:c7d138eb-…`), arm A = the "discordância" cell (alts well below BTC: `dispersion` in [−0,10, −0,03)), arm B falsification = concordância/alts above BTC ([0,00, +0,10)), tercile cuts fixed on June–July for the descriptive reading, the EXP-0027 success rule + the identity clause vs the hourly regime (if the regime cut separates as much, `descartar`), expected shares of eligible bars measured from the backfill (leave placeholders the orchestrator fills after the backfill report).
6. Docs: `docs/PIPELINE.md` §4b item, `docs/ACTIVATION.md` §7c; `.claude/state/notes-T3.90.md` with commands/outputs and the full `git status --porcelain` list of your files.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared and Astra is editing `apps/**`, `services/execution-worker/**`, `packages/exchange-adapters/**` and `obsidian/11-KNOWLEDGE/**` right now — do not touch those; never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never edit the closure modules; no VPS writes. Report in Portuguese ≤ 12 lines, times in Brasília (UTC−3, compute it).

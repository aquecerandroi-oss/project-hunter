# Brief T3.32 — por que as estratégias perdem: diagnóstico com dado real, decomposto, e o que mudar primeiro (Everton, 2026-09-08: "tá tendo muito prejuízo, dê um jeito de melhorar, analisa as notas de estratégias e veja o motivo")

**Owner:** quant-engineer. **Reviewers afterwards:** Astra (`infra/scripts/astra.sh ask perdas-2026-09-08 "..."` — the orchestrator runs it), Sexta-feira (files the page). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers. VPS (`ssh hunter-vps`, `docker exec -i hunter-postgres-1 psql -U hunter -d hunter -At < file.sql`, `docker exec hunter-strategy-worker-1 python -m hunter_strategy_worker.replay.run …`) READ ONLY for the database — no UPDATE/INSERT/DELETE, no activation, no flags. A replay run writes its own cohort rows (that is allowed: it is the research tool) — say every cohort id you create.** Base: `main` at `970c20b`. In flight: T3.18c (`apps/api/**`, `services/strategy-worker/hunter_strategy_worker/replication*.py`, `packages/indicators/hunter_indicators/replication/**`), T3.29 (`services/execution-worker/**`), T3.31 (`apps/web/**`) — do not edit those. Your write scope: `.claude/state/exp-drafts/**`, `.claude/state/notes-T3.32.md`, `infra/scripts/sql/research/**` (the SQL you ran, verbatim), `packages/indicators/hunter_indicators/replay/**` and `infra/scripts/replay_exits.py` only if an exit-policy arm needs a parameter that does not exist yet (then tests).

## Money first (this is what Everton sees on the Lab, VPS, 2026-09-08 14:40Z, all simulated at 0,25 % risk of 19 333 USDT = 48,33 USDT per operation)
| version | purpose | evaluable | result | per op | expectancy (replay 31 d) | PF |
|---|---|---:|---:|---:|---:|---:|
| momentum v2 | research | 134 | −1 232,88 USDT | −9,20 | −0,22 R (248 closed) | 0,57 |
| momentum v3 | paper line | 115 | −1 279,87 USDT | −11,13 | — | — |
| momentum v4 (piso 0,0089) | research | 0 (27 emitted) | — | — | −0,15 R (30) | 0,72 |
| volume_anomaly v2 | research | 299 | −5 714,35 USDT | −19,11 | −0,60 R (337) | 0,28 |
| momentum v1 | superseded | 929 | −8 551,75 USDT | −9,21 | | |
| volume_anomaly v1 | superseded | 2 079 | −33 166,48 USDT | −15,95 | | |
Nothing here is real money (`ENABLE_LIVE_TRADING=false`, autonomy off); the Lab exists to find this out before any wallet. But "we lose in every version and every window" needs a cause, not more waiting.

## Read first (the base already has hypotheses — confirm or kill them with data, do not restate them)
`obsidian/11-KNOWLEDGE/Strategy Backlog.md`, `KB-0006` (invalidation kills 35 % with 0 winners), `KB-0008` (cost floor), `KB-0021/0022` (funding), `obsidian/05-EXPERIMENTS/EXP-0001…EXP-0006` (each "Hipóteses de falha"), `obsidian/06-DECISIONS/Revisoes-Astra/2026-09-08-shadow-lab-pronto.md` §"O que eu faria diferente" (the 5 measurements Astra asked for), `.claude/state/brief-T3.27-momentum-v2-invalidacao.md` (the INV-A/B/C/E exit arms already replayable via `hunter_indicators/replay/policies.py` + `replay_exits.py`), `packages/core/hunter_core/strategies/{momentum_v1,volume_anomaly_v1}.py` (entry, stop, target, invalidation, horizon, costs), `docs/plans/SHADOW-LAB.md` §19 (metric definitions), `services/strategy-worker/hunter_strategy_worker/outcome*` (how R_net is resolved).

## Deliver — every number from a query you paste, per version (momentum v2, volume_anomaly v2; v3 = same code as v2 with paper purpose; v4 as contrast)
1. **Where the money goes**: by exit reason (stop / target / invalidation / timeout-horizon / censored): count, share, mean R_net, sum R_net, mean gross R, mean cost in R (fees+slippage+spread), mean funding in R, mean duration. Then the same by market (top 10 losers, top 10 winners), by hour-of-day (Brasília), by day, by ATR%-decile at decision. Say which two cells explain most of the loss.
2. **Costs vs edge**: gross R before costs vs net — if the gross expectancy is ≈ 0 the strategy has no edge and costs decide; if gross is positive and net negative, costs/targets are the problem (KB-0008). State which.
3. **Invalidation**: reproduce KB-0006 on the current population (share of trades killed by invalidation, their MFE — how many would have hit target had they been left alone) and **run the INV-* exit arms as a replay over the frozen entries of momentum v2** (T3.27 item 0: `infra/scripts/replay_exits.py`, policies A/B/C/E) on the 31-day window ETH/SOL/XRP/DOGE (same as `replay:f8d8279c…`) — table: arm × (n, expectancy, PF, hit rate, mean duration). This is the one experiment that can change the sign; do it.
4. **Target/stop geometry**: distribution of MFE/MAE in R for winners and losers; what target multiple the MFE distribution supports (e.g. the 60th percentile of MFE) vs the 1,5 ATR used; what stop distance the MAE of eventual winners needs.
5. **Regime**: hit rate and expectancy split by BTC 1 h trend (up/down/flat) and by realised vol regime — is the strategy losing everywhere or only in one regime the entry filter could exclude?
6. **Verdict in plain Portuguese for Everton** (≤ 12 lines, no jargon without a gloss): the 2–3 causes ranked by money, what each proposed change is expected to do (with the replay number that supports it), what it cannot promise (30-day prospective rule), and the exact next variants to derive (parameters only via `derive_variant.py` — never change strategy code in this task).
7. **Drafts**: `.claude/state/exp-drafts/KB-00xx-por-que-perdemos-2026-09-08.md` (knowledge page: findings with SQL provenance and `as_of`/`read_at`) and `EXP-0007-momentum-invalidacao-braços-INV.md` (the exit-arm replay as an EXP in the template shape), for Sexta-feira to file.

## Prove
Every table with the SQL that produced it (in `infra/scripts/sql/research/2026-09-08-*.sql`), replay receipts verbatim (cohort ids), `ruff`/`pyright` if any code changed; report in Portuguese, extended format; `.claude/state/notes-T3.32.md`. Never a verdict beyond `inconclusivo` for any single variant; the diagnosis itself is what Everton asked for.

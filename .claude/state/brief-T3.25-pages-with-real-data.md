# Brief T3.25 — four "Planejado" pages that already have real data behind them: Strategies, Backtests (replay), Trades, Risk Center

**Owners:** part A (API) backend-specialist now; part B (web) frontend-specialist after the product-designer's T3.23 briefs land (the nav registry and page shells are theirs to shape). **Reviewers afterwards:** risk-engine-guardian (Risk Center numbers, Trades), code-reviewer, product-designer (screens). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate the local stack containers (the designer uses them).** Base: `main` at `1f91c79`.

## Everton's ask (2026-09-08, showing the nav): "bora que ainda falta muita coisa"
The sidebar still marks Trades (M3), Strategies (M6), Backtests (M6), Risk Center (M4), Agents (M4) as "Planejado". Since this morning: strategy versions carry purpose/verdict/lineage (0010–0012), the replay engine writes receipts (0013), the wallet writes orders/fills/trades, the Risk Engine has durable state. A page whose data exists is not "planned"; a page whose data does not exist stays planned and says which milestone brings it (CLAUDE.md hard rule: no fake data, no inert controls).

## Part A — API (this dispatch)
Read first: `docs/PRODUCT.md` (what each page promises), `apps/web/lib/nav-registry*` (labels and milestones), `apps/api/hunter_api/routers/{lab,lab_scoreboard,portfolio,risk,system}.py` and their schemas/services (patterns: `DecimalStr`, `resolve_as_of`, tenant scoping, honest nulls with reasons), `docs/DATABASE.md` §16, §18–§25, `docs/RISK_ENGINE.md` §3 (paper_v1 limits), `packages/risk-core/hunter_risk/**` (`RiskLimits`, `PAPER_V1`), `services/strategy-worker/hunter_strategy_worker/replay/ledger.py`, `docs/plans/REPLICATION.md`.
1. **Strategies**: `GET /lab/shadow/strategies` — one row per `strategies` key with its versions: version, purpose, status, activated_at/deprecated_at, code_ref, `default_parameters` **with the schema** (type/bounds/description per parameter, so the web can render "cada diâmetro"), lineage (`replication_parent_id`, `replication_index`), `promising_at`, the scoreboard verdict and maturity (reuse `lab_scoreboard` service, do not recompute), signal counts per cohort (prospective / replay / replication), links: EXP page name if known (`obsidian` is not a DB — expose nothing invented; the web links the Obsidian page by convention `03-TRADING/Estrategias/<key>-<version>.md`). Global like the other Lab routes.
2. **Backtests = replay**: `GET /lab/shadow/replays` (paged, newest first) from `replay_runs`: run id, version label, window, markets count, bars, signals, outcomes, bars/s, seconds, started/finished, cohort; `GET /lab/shadow/replays/{run_id}` with the slices and the population by state; and the cohort's outcomes reuse the existing signals endpoint with `cohort=replay:<uuid>` (already accepted since 0012 — confirm with a test). Honest empty state when the table is empty.
3. **Trades**: confirm `GET /orgs/{org}/portfolios/{id}/trades|orders|positions` (T3.8a) already carry what a Trades page needs: entry/exit prices and times, qty, fees in both currencies, realized PnL USDT/BRL, exit reason, protection intents; add what is missing (compare with `docs/PRODUCT.md`'s Trades promise) — no new tables.
4. **Risk Center**: `GET /orgs/{org}/portfolios/{id}/risk/limits` — the **numeric** preset (`paper_v1`: 0.25 %/1 %/1 %/0.5×/40 %/10 %/5 positions, warning 1 %/4 %, blocked 2 %/8 %, min volume 50 M) read from `RiskLimits`/the wallet's preset (single source, the one the engine uses — prove by test that the numbers equal `PAPER_V1`), plus the current usage against each cap (from `portfolio_risk_state`/`build_portfolio_state`: exposure, per-coin, beta-weighted, open positions, reserved), the kill-switch scopes and the last transitions (reuse `risk.py`), and the day reference. Everton read on the portfolio page that "a API ainda não expõe os valores numéricos do preset" — this closes it.
5. Types regenerated (`pnpm gen:types`; `openapi.json` not committed); unit + integration tests per endpoint (tenant scoping for the org-scoped ones; global for the Lab ones); `ruff`/`pyright`/`check_file_size.py`.

## Part B — web (after T3.23/T3.24 land; the frontend agent reads the designer's specs first)
Pages `/[org]/strategies`, `/[org]/backtests`, `/[org]/trades`, `/[org]/risk` become real: nav registry entries lose the "Planejado" badge only when the page renders real data; each page has the honest empty state; Strategies shows the parameter table (every parameter), verdict chip, lineage tree, links to Obsidian and to the Lab scoreboard; Backtests lists replay runs with receipts and opens a run (population by state, the outcomes table reused from the Lab with cohort filter); Trades reuses the wallet tables with the money formatting rules; Risk Center shows every cap as "usado / limite" with a bar, the kill-switch scopes, transitions and the day reference, in Brasília time. Tests + browser check with the Clerk test user.

## Prove
Report in Portuguese, extended format; `.claude/state/notes-T3.25.md` (section A now, B later). Nothing here activates anything.

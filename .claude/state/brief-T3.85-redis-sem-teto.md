You are devops-engineer on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, read-only unless stated). First save this brief verbatim as `.claude/state/brief-T3.85-redis-sem-teto.md`.

# Brief T3.85 — Redis at 12,57 GiB with 2 014 keys, no `maxmemory`, BGSAVE/AOF nearly continuous (found by T3.83, 2026-09-11)

**Facts:** `INFO memory`: `used_memory_human 12.57G`, `maxmemory 0`; keyspace `db0: keys=2014, expires=1315`; T3.83 saw `rdb_saves=2459`, a fork in progress, and the midnight-UTC burst causing read timeouts in the 4 strategy shards. Only ~2 000 keys → a handful of huge ones: almost certainly Redis Streams published without trimming (`market.candles.closed`, ticker/trade streams, outbox streams — find the names in `packages/core/hunter_core/redis/keys.py`, `hunter_core/events/*`, `services/market-worker/.../streaming.py|persist.py`, `coverage_publish.py`) and/or hot-state hashes. Postgres is the system of record; streams are transport (consumers ACK; T3.74d/T3.83 rely on `XAUTOCLAIM` for redelivery).

## Deliver
1. **Read-only inventory on the VPS** (foreground `timeout 290`): `redis-cli --bigkeys` (sampled) and, for every stream, `XLEN`, `MEMORY USAGE`, first/last entry ids (age of the oldest entry), `XINFO GROUPS` (lag/pending per group); `INFO persistence` (rdb_changes_since_last_save, aof_enabled, last save durations), `CONFIG GET save|appendonly|appendfsync|maxmemory|maxmemory-policy`; the container's memory limit (`docker inspect`) and host RAM (`free -h`). Write the table: key → bytes → entries → oldest age → consumer groups → is it needed beyond the consumers' positions?
2. **Producer-side bound** (code, with tests): every `XADD` gets `MAXLEN ~ N` or `MINID` retention sized from the consumers' worst-case lag (e.g. 24 h for candle streams, 1 h for tickers) — find the single publish helper (`hunter_core/events/publish.py` or similar) and put the bound there once; the outbox stream keeps whatever the T3.13 delivery contract needs (read `docs/PIPELINE.md`/`ARCHITECTURE.md` on the outbox before touching it); unit tests on the helper; a metric for trimmed entries if cheap.
3. **Server-side safety** (compose, no data loss): `maxmemory` bound with `noeviction` (never silently drop stream data — the bound is a circuit breaker that surfaces as an error, plus alerting via the existing heartbeats/`/system/latency` if a redis error is visible there), `save` cadence and `appendfsync everysec` sanity vs the burst, `--maxmemory` value justified from the inventory and the container limit; document in `docs/DEPLOYMENT.md`.
4. **One-time trim proposal — DO NOT RUN**: for each oversized stream, the exact `XTRIM <key> MINID <id>` (id = oldest position any consumer group still needs, minus a margin) and the bytes it frees; write it as a runbook block in `docs/DEPLOYMENT.md` and in the notes; the orchestrator presents it to Everton before running (it deletes transport data already consumed, never Postgres rows — say that plainly).
5. `.claude/state/notes-T3.85.md` with the inventory, commands/outputs and the full `git status --porcelain` list of your files; `obsidian/07-BUGS/Open Bugs.md` entry.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; NO Redis writes (no XTRIM, no CONFIG SET) on the VPS; no `git pull` on the VPS; keep files ≤ 350; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 12 lines with the inventory top-5, times in Brasília (UTC−3, compute it).

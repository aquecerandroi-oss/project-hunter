# Brief T3.18b — the scoreboard shows replay separately: two counters per version (D14) and the replay evidence block (D15), never mixed into the verdict

**Owner:** backend-specialist (API) — the web part belongs to T3.24b (Lab redesign). **Reviewers afterwards:** quant-engineer, code-reviewer. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` at `b6890c6`. In flight elsewhere: T3.24a (`apps/web/**`), T3.26 (`services/strategy-worker/**`, `infra/scripts/derive_variant.py`), Sexta-feira (`obsidian/**`) — do not edit those paths.

## Source
`.claude/state/decisions-delegated-2026-09-08.md` D14 (two measures: simulated decisions = mass, closed operations = evidence) and D15 (replay may mature the sibling block with a label; never block 1 nor the scoreboard verdict), `docs/plans/REPLICATION.md` §3.5/§6/§9, `apps/api/hunter_api/{repositories,services,schemas,routers}/lab_scoreboard.py` (today: `prospective` only — correct for the verdict), `lab_replays.py` (T3.25 A: replay receipts), `.claude/state/notes-T3.19b.md`, `notes-T3.19d.md` (what a receipt carries; `signals/outcomes` are per cohort, not per slice).

## Deliver
1. **`ScoreboardRowOut.replay`** (nullable block): `runs` (count of `replay_runs` for the version), `decisions_simulated` (sum of `bars_evaluated` — the D14 mass counter), `operations_closed` (evaluable outcomes across the version's `replay:` cohorts — the D14 evidence counter), `expectancy_r`, `net_profit_rate` (num/den), `profit_factor` (with reason), `distinct_days`, `distinct_markets`, `window_from/window_to` (min/max over runs), `label: "replay — não conta para o veredito"`. Computed with the same SQL definitions as the prospective block (reuse the metric functions; only the cohort filter differs). The verdict and maturity stay `prospective` only — assert it in a test with a version whose replay is positive and whose prospective is negative.
2. **`ScoreboardRowOut.replication`** (nullable): the T3.19 contract block from `notes-T3.19.md` §6 (`status none|promissora|replicando|real|refutada`, `promising_at`, siblings `{n, positive, mature}`, market halves, bootstrap) — read `promising_at`/lineage from the 0012 columns and the sibling cohorts `replication:<parent>:<k>`; siblings' maturity may count replay per D15 (label it: `evidence: prospective|replay|mixed`). If the protocol functions in `packages/indicators/hunter_indicators/replication/**` need a session adapter, write the adapter in the API service; never duplicate the statistics.
3. **`GET /lab/shadow/curve`** accepts `cohort=prospective|replay|<exact cohort>` (default `prospective`) so the web can draw the replay line dashed beside the live one.
4. **`GET /lab/shadow/scoreboard?as_of=`** unchanged otherwise; `pnpm gen:types` regenerated (`openapi.json` not committed).
5. Tests: unit for the block builders (every null reason), integration with a seeded version having prospective + replay + one sibling cohort; `ruff`/`pyright`/`check_file_size.py`.

## Prove
Report in Portuguese, extended format (STATUS · FILES · TESTS with real output · a real response example with the two blocks · CONCERNS); `.claude/state/notes-T3.18.md` (append section "T3.18b").

# WIP — o que está no disco e NÃO commitado (12/09/2026 13:2x BRT, limite semanal estourado)

Produção: `578ebce` (estável; laço de papel rodando; NÃO deployar nada até esta árvore ser separada).
Último commit: `140b8da` (plantão run 12). A árvore de trabalho mistura três tarefas; nenhuma delas pode ir para o `main` sozinha sem revisar os arquivos compartilhados.

## T4.12 — carteira observada (COMPLETA no disco; relatório em `.claude/state/notes-T4.12.md` §2.4 com a lista exata)
Migração `0027_meme_wallets` + `ddl/meme_wallets.py` + `ddl/meme_lab_views.py` (recreate_scoreboard_0022); modelo `meme_wallets.py` (+`__init__.py`); adaptador `wallet_fills.py`, `rpc_wallet.py`, `rpc.py` (`call()`); worker `wallets.py`, `wallets_state.py`, `wallets_repo.py`, `wallets_lab.py`, `wallet_positions.py`, `config.py` (`MEME_WATCH_WALLETS`), `context.py`, `main.py`, `wiring.py`; API `repositories/schemas/services/meme_wallets.py`, `schemas/meme_lab.py`, `schemas/meme_desk.py`, `routers/meme_lab.py`, `routers/meme_desk.py` (campo `real_observed`); diário `meme_diary_wallets.py`, `meme_diary.py`, `meme_diary_render.py`; docs DEPLOYMENT/T4-MEME-RADAR §T4.12/DATABASE §39; tipos gerados. Testes verdes (ver notas). Endereço: `6nAh8drzAYfFZuTFFRgwRdV8tNndFiX1E8NGRAGzSk5F`.

## T4.11 — moonshot + pós-migração (PARCIAL; cortada pelo limite; `.claude/state/notes-T4.11.md`)
Migração `0029_meme_moonshot` (+ ddl?), `hunter_indicators/meme/pool.py`, `exits.py`/`rules.py` (M), worker `lab_bets_pool.py`, `lab_params.py`, `lab_repo_pool.py`, `pool_mark.py`, `lab_bets.py`/`lab_models.py`/`lab_repo_bets.py`/`lab_rows.py`/`lab_values.py`/`paper_engine.py`/`repo_tape.py` (M), `meme_lab_views.py` (M), modelo `meme_lab.py` (M), testes `test_meme_exits_moonshot.py`, `test_meme_pool.py`, `test_lab_params_moonshot.py`, `test_pool_mark.py`; `obsidian/05-EXPERIMENTS/EXP-M4-moonshot.md` (+Index, Meme/README), `docs/RISK_ENGINE_MEME.md` (M); API: `schemas/meme_desk.py` (`MarkSource`, `mark_source`, `mark_stale_s`, `exit_on_migration`, `dead`), `services/meme_desk_out.py` (M). **Estado desconhecido**: verificar se `meme_desk_out.py` lê colunas que só existem na 0029 (quebraria a mesa sem a migração).

## T4.14 — executor real (PARCIAL; cortada; `.claude/state/notes-T4.14.md`)
`packages/risk-core/hunter_risk_meme/` + `packages/risk-core/tests/unit/meme/` + `packages/risk-core/pyproject.toml` (M); migração `0028_meme_live` + `ddl/meme_live.py`; modelo `meme_live.py`; API `routers/meme_live.py` (**import `hunter_core.errors` inexistente — quebra a coleta de testes da API e quebraria o boot da API se `app.py` for commitado**), `schemas/meme_desk.py` (`ProposalMode` paper|live, recusa `meme_live_disabled`), `app.py` (**registra `meme_live_router` — NÃO commitar app.py até o router existir e importar**); `infra/vps/compose.sh` (perfil `meme-live`?) e possivelmente compose files; `services/meme-executor/`? (conferir).

## Compartilhados com hunks de mais de uma tarefa
`apps/api/hunter_api/app.py` (T4.14), `schemas/meme_desk.py` (T4.11 + T4.12 + T4.14), `schemas/meme_lab.py` (T4.12), `services/meme_desk_out.py` (T4.11), `routers/meme_desk.py`/`meme_lab.py` (T4.12), `packages/core/tests/integration/test_migrations.py` (0027 + 0028 + 0029), `test_schema_privileges.py`, `packages/core/hunter_core/db/models/__init__.py` (meme_wallets + meme_live + …), `infra/migrations/ddl/meme_lab_views.py` (T4.11 + T4.12), `docs/plans/T4-MEME-RADAR.md`, `packages/shared-types/src/generated/api.d.ts`, `infra/vps/compose.sh`.

## Como retomar (quando houver crédito ou em 15/09)
1. Ler as três notas; NÃO rodar `git checkout --`/`restore`/`stash`.
2. Terminar T4.14 (executor) e T4.11 (moonshot) a partir do estado parcial (agentes com os briefs `brief-T4.14-executor-real.md` e `brief-T4.11-moonshot-e-pos-migracao.md`, instruídos a "continuar do disco").
3. Verificação combinada: `ruff`, `pyright`, unit (adapter/worker/API/indicators/risk-core), testcontainers (persistências + `test_migrations.py` cadeia inteira 0027→0028→0029), web `next build`.
4. Commit único (ou três por pathspec, na ordem 0027 → 0028 → 0029, cada um com os arquivos compartilhados no estado final) → deploy (migra as três) → prova na VPS (heartbeat, `/meme/live`, `/meme/desk.real_observed`).
5. Só depois: fechamento diário automático (lições do dia) e T4.2g (fita por lote).

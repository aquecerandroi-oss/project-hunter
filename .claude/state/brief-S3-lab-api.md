# Brief S3a — API do Shadow Lab (`/api/v1/lab/shadow/*`)

**Owner:** backend-specialist (sonnet). **Revisores depois:** security-reviewer, code-reviewer, Astra (`bash infra/scripts/astra.sh ask S3-lab-api "..."`). **Não commitar.** A tela (`S3b`, frontend-specialist) é despachada contra o contrato que você fixar aqui — escreva o contrato primeiro em `.claude/state/contract-S3-lab.md` (endpoints, query params, shapes JSON com exemplo real do banco local) e só depois implemente.

## Leia antes
1. `CLAUDE.md`. 2. `docs/plans/SHADOW-LAB.md` — Decisão conjunta itens 3, 4, 5, 9, 10, 11 e checklist **S3**. 3. O que a S2 gravou (commit 5d0153b): `services/strategy-worker/hunter_strategy_worker/{record,progress,excursions,settle,tracking_repo,catalogue}.py`, `.claude/state/notes-S2.md` (§ motivos de `no_entry` `late:delay|late:missed_open|late:unconfirmed|geometry`, `gap:*`, `blocked:*`; `r_ex_funding` separado; coortes v1 vs v2 após `--supersede`; populações reiniciam por `strategy_version_id`), `docs/DATABASE.md` §16 (a bicondicional: quem decide "aberto" é `tracking_state`, nunca `result`; `no_entry`/`censored` não são abertos; censura nunca vira `expired`). 4. Padrões da API (T1.4/T2.6): `apps/api/hunter_api/{routers,schemas,services,repositories}/{markets,radar}*.py` e testes. 5. `.claude/rules/astra-second-opinion.md`.

## Entregar (arquivos permitidos: `apps/api/hunter_api/routers/lab.py`, `apps/api/hunter_api/{schemas,services,repositories}/lab*.py`, `apps/api/hunter_api/app.py` (registro), `apps/api/tests/**` (lab), `packages/shared-types/**` gerado, `.claude/state/contract-S3-lab.md`, `.claude/state/notes-S3.md`)
- `GET /api/v1/lab/shadow/summary?window=7d|30d|all&as_of=&cohort=prospective` — por `strategy_version` (chave, versão, `code_ref`, `activated_at`, `deprecated_at`): contagens completas (`decisions` por `Evaluation.state`, sinais emitidos, `pending_entry`, entradas válidas, `no_entry` por motivo, `active`, `terminal` por `result` (target/stop/expired/invalidated), `censored` por motivo, funding indisponível); métricas **com os nomes da decisão**: `target_rate_among_resolved_touches` = target/(target+stop), `net_profit_rate` = `R_net > 0` / encerrados avaliáveis (expired/invalidated com resultado conhecido entram), `hypothetical_net_expectancy_r` = média de `R_net` nos encerrados avaliáveis, `profit_factor` = Σ R_net⁺ / |Σ R_net⁻| (nulo com motivo sem perdas ou sem amostra), `sum_of_hypothetical_r` (nome próprio, ordenado por `exit_ts`), `r_ex_funding_*` como bloco separado com cobertura; **`portfolio_pnl: null` e `portfolio_max_drawdown: null` com motivo `not_applicable`**; `maturity`: `evaluable_outcomes`, `distinct_days`, `inconclusive: true` enquanto < 100 outcomes avaliáveis OU < 30 dias distintos; `coverage`: mercados avaliados, dias distintos, `unavailable` por motivo (`gap`, `warmup`, `ineligible`), custos assumidos (`assumed_spread_bps`, `slippage_bps`, `fee_bps`, `max_entry_delay_s`) lidos de `default_parameters`; rótulo fixo `label: "SOMBRA — hipotético, sem capital, custos assumidos"`; `as_of` obrigatório na resposta.
- `GET /api/v1/lab/shadow/signals?strategy_version_id=&market=&tracking_state=&result=&cohort=&cursor=&limit=` — lista com snapshot: `decision_at`, `source_bar_close`, `reference_price`, `stop`, `target1`, `entry_plan`, `virtual_entry`, `entry_ts`, `exit_price`, `exit_ts`, `result`, `tracking_state`, `no_entry_reason`, `censored_reason`, `r_multiple` (nulo com `meta.r_net_reason`), `r_ex_funding`, excursões (`mfe`/`mae` nulos quando indeterminados + `meta.excursions` com `bounds`/`ambiguous`), `supporting_features` sob demanda (`?include=envelope`), `purpose`. Cursor estável por `(decision_at, id)`.
- `GET /api/v1/lab/shadow/versions` — catálogo com `status`, `code_ref`, `activated_at`, `deprecated_at`, `superseded_by`, `params_hash`, `default_parameters`.
- Autorização: qualquer usuário autenticado da organização lê (pesquisa é global, sem dado de tenant); sem escrita. Redis não é necessário; Postgres fora → 503. Estados vazios honestos (`0 sinais` é resultado válido; nunca 404 em lista vazia).
- Métricas calculadas em SQL/`Decimal` (nunca float), 4 casas; testes de integração com fixtures que seguem os modelos reais (`agent_signals`, `signal_outcomes`, `shadow_episodes`, `strategy_versions`): populações v1/v2 separadas, `no_entry` fora de "aberto", `censored` fora de `expired`, PF nulo sem perdas, `inconclusive` abaixo do limiar, cursor, 503.
- `pnpm gen:types` sem diff depois de gerado.

## Regras
TDD; Decimal; UTC; ≤ 350 linhas; sem `print`. Comandos: `uv run pytest apps/api -q -k lab`, `uv run pytest apps/api -q`, `uv run ruff check apps/api`, `uv run ruff format --check apps/api`, `uv run pyright apps/api`, `uv run python infra/scripts/check_file_size.py`, `pnpm gen:types` + `git diff --stat packages/shared-types`. PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Não toque em `.env*`, migrações, `services/**`, `packages/indicators`. T2.6 está em voo em `apps/api` (radar/opportunities/anomalies/regime): não edite os arquivos dela; em `app.py` acrescente só a linha do seu router.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma) · TESTS · TEST RESULTS (saída real) · REAL DATA CONNECTED (exemplo real do `summary` contra o banco local) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · Checklist S3 (parte API) · NEXT STEP.

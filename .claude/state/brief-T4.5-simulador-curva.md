You are quant-engineer on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T4.5-simulador-curva.md`. Everton (2026-09-12): "vamos começar a operar ainda hoje" — today's honest form of "operar" is a PAPER wallet on the pump.fun bonding curve with real live data. Task T4.5 — the pure paper simulator of the bonding curve + the first pre-registered hypothesis machinery. Inputs: `packages/exchange-adapters/hunter_exchanges/pumpfun/curve.py` (the formula with the live-confirmed constants 30 SOL / 1 073 000 000 virtual tokens; read `.claude/state/notes-T4.1.md`), `.claude/state/astra-review-t40-pumpfun.md` (fee 1,25 % per trade on the curve incl. creator fee; `Δ` cost for `q` tokens from reserves `k = x·y`), `docs/plans/T4-MEME-RADAR.md`, and — when T4.4 writes it — `docs/RISK_ENGINE_MEME.md` (read it if present; otherwise use the caps as parameters). Deliver `packages/indicators/hunter_indicators/meme/` (pure, Decimal, no I/O): (1) `curve.py` re-export/validation of the price/cost functions: `buy_cost(reserves, tokens)`, `tokens_for_sol(reserves, sol)`, `sell_proceeds(reserves, tokens)`, marginal price, mcap in SOL, curve progress %, fee application (1,25 % — parameter, never hardcoded in the math), and the reserves AFTER our own trade (our impact) — property tests: round-trip buy→sell loses exactly the fees + impact, monotonicity, no negative reserves; (2) `paper.py`: a `PaperCurveWallet` with a SOL balance cap, positions per mint (tokens, cost basis, entry reserves/time), `buy(mint, sol, reserves_at, ts, priority_fee)`, `sell(mint, tokens, reserves_at, ts, priority_fee)`, mark-to-curve valuation (what a full sell would yield NOW, fees included — the honest unrealized), realized PnL in SOL, a ledger of events; concurrency slippage is modelled by re-pricing against the reserves observed at the NEXT snapshot after our intent (parameter `fill_delay_snapshots`), never by assuming our trade fills at the last seen price; (3) `rules.py`: the exit rules as pure functions (target multiple, trailing from peak mark, time stop, forced exit on curve completion/migration event, rug signal placeholder) and the entry gate as a pure function over a features row (age window, progress window, creator-not-net-seller when known, participation cap vs last-minute curve volume when known; unknown → refuse by name); (4) `replay.py`: given a sequence of curve snapshots/events for one mint (from T4.2's tables or the T4.1 fixtures), run the paper wallet with a given rule set and produce the same outcome shape our Lab uses (entry, exit, reason, R in SOL terms with the initial risk = the stop distance or the max loss cap, holding time), so EXP-M1 can be evaluated with `.claude/state/exp-drafts/t362b/blocos90.py`-style day-block CIs; (5) tests: unit/property (hypothesis if available in the repo, else parametrised) for everything, with the T4.1 real fixtures as smoke inputs; `ruff`/`pyright`/`check_file_size.py` clean, files ≤ 350; (6) `obsidian/05-EXPERIMENTS/EXP-M1-comprar-cedo-na-curva.md` pre-registration draft (Portuguese; template): the hypothesis Everton stated ("comprar cedo na curva e vender em ROI alto") as a falsifiable rule set (entry: age ≤ A min and progress ≤ P %; exit: k× target / trailing / time stop T), the priors (KB-0086, T4.0's < 2 % graduation, base rates from T4.0e when it lands), success rule = positive expectancy in SOL after fees with day-block CI above zero AND positive in ≥ 2 of 3 windows AND not dependent on the top 1 % of tokens (leave-top-out) — retention without success selection; predictions frozen; `Experiments Index` row; `obsidian_lint.py` "base limpa". Rules: never a background shell; every command foreground with explicit `timeout 290`; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; do not edit `packages/exchange-adapters/**`, `services/**`, `apps/**`, `infra/**`, `packages/risk-core/**`. Report in Portuguese ≤ 12 lines.

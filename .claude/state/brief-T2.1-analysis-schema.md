# Brief T2.1 — migração `0003_analysis` (schema do M2: baselines, envelopes, estágio, outbox)

**Owner:** database-architect (opus). **Revisores depois:** code-reviewer, Astra (`bash infra/scripts/astra.sh ask T2.1-schema "..."`). **Não commitar** — o orquestrador commita após revisar `git diff --stat`.

## Leia antes (nesta ordem)
1. `CLAUDE.md` (regras duras, comandos canônicos, ≤ 350 linhas, TDD, Decimal/UTC).
2. `docs/plans/M2.md` — a linha **T2.1** da tabela de tarefas e, acima de tudo, a seção **"Decisão conjunta Claude ⇄ Astra (2026-09-05)"**, que prevalece sobre "Decisões deste plano": baselines imutáveis por minuto (420 por bucket, gate de 3 dias e 120 observações), envelope por amostra de histórico, `OpportunityStatus.EXTENDED`, `MarketRegime.UNKNOWN`, `anomalies.evaluation_state`, episódio de oportunidade (índice único parcial `WHERE expired_at IS NULL`), outbox (T2.9 — a tabela nasce aqui).
3. `docs/DATABASE.md` (§ existentes e **§16 Shadow Lab**: a `0002_shadow_lab` já existe; a sua migração é a `0003` e **referencia** os objetos dela — inclusive `INITIAL_ENUMS`/`SHADOW_ENUMS` em `infra/migrations/ddl/enums.py`; acrescente `ANALYSIS_ENUMS` e mantenha a partição exata de `ALL_ENUMS` testada).
4. `docs/PIPELINE.md` §2–§5 (features, baselines, anomalias, regime, score) e `docs/plans/SHADOW-LAB.md` item 6 (a `shadow_outbox` da S0 deve poder ser absorvida pela `outbox_events` genérica sem perder pendências: mesma forma de `event_id`, `stream`, `payload`, `dispatched_at`, `attempts`, `last_error`).
5. Modelos atuais: `packages/core/hunter_core/db/models/analysis.py` (`feature_definitions`, `feature_snapshots`, `anomalies`, `market_regimes`, `opportunities`, `opportunity_history`, `opportunity_weights`…), `packages/core/hunter_core/domain/enums.py`, `infra/migrations/versions/{0001,0002}*.py`, `infra/scripts/seed.py`, testes em `packages/core/tests/integration/{test_migrations,test_schema_*}.py`.
6. Pendência herdada da S0 (relatório dela, item 2): só os **nomes** dos tipos enum estão congelados por revisão; os **rótulos** ainda vêm de `ALL_ENUMS` em runtime, então acrescentar um valor a um enum existente (`EXTENDED`, `UNKNOWN`) alteraria a `0001` retroativamente. **Resolva isso aqui**: a `0003` acrescenta os valores com `ALTER TYPE ... ADD VALUE` explícito e a criação de enums por revisão passa a usar rótulos congelados por revisão (teste que prova que `0001` e `0002` criam exatamente os rótulos de então).
7. `.claude/rules/astra-second-opinion.md` — opinião da Astra sobre o desenho da migração ANTES de finalizar; "Segunda opinião (Astra)" no relatório.

## Entregar (arquivos permitidos)
- `infra/migrations/versions/0003_analysis.py` (+ `infra/migrations/ddl/analysis.py` para grants/índices/triggers, seguindo `ddl/shadow.py`), `infra/migrations/ddl/enums.py` (partição por revisão + rótulos congelados), `packages/core/hunter_core/db/models/analysis.py` (+ novo módulo se passar de 350 linhas), `packages/core/hunter_core/db/models/system.py` (`outbox_events`), `packages/core/hunter_core/db/models/__init__.py`, `packages/core/hunter_core/domain/enums.py`, `infra/scripts/seed.py`, `packages/core/tests/**`, `docs/DATABASE.md` (§17 "Análise (0003)").
- Conteúdo, conforme a decisão conjunta: `feature_baselines` imutáveis (chave `(market_id, feature, minute_of_day)` ou o que a decisão fixa — 420 por bucket; `computed_at`, `sample_size`, `median`, `mad`, `valid_from`; sem `UPDATE`, versão nova por recomputo), `opportunities.stage` (`EARLY|DEVELOPING|EXTENDED`), `opportunities.explanation` JSONB, `anomalies.evaluation_state`, episódio de oportunidade com índice único parcial `WHERE expired_at IS NULL`, enums `OpportunityStatus.EXTENDED` e `MarketRegime.UNKNOWN`, índices (`anomalies(status, detected_at)`, `opportunities(status, score desc)`, baseline único), `outbox_events` genérica, seeds `feature_definitions` v1 (todas as features da T2.2 com `inputs`) e `opportunity_weights` v2 ativo com v1 inativo.
- Testes: upgrade em banco limpo e em banco populado (0001 → 0002 → 0003), `downgrade -1` + `upgrade head`, `alembic check` limpo, partição de enums por revisão com rótulos, imutabilidade dos baselines, índice parcial do episódio, grants (`hunter_app` lê, `hunter_worker`/`scanner` escrevem conforme o padrão), seeds idempotentes.

## Regras
TDD; nada fabricado; `Decimal`/`NUMERIC(28,10)`; UTC. Comandos: `uv run pytest packages/core -q`, `uv run alembic -c infra/migrations/alembic.ini upgrade head` e `... check` (stack local: `docker compose -f infra/docker/docker-compose.yml up -d postgres redis`; ou testcontainers), `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`, `uv run python infra/scripts/check_file_size.py`. PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Não toque em `.env*`, não commite, não altere arquivos fora da lista (S2 está em voo em `services/strategy-worker`, `services/market-worker`, `packages/core/hunter_core/events/streams.py`, `infra/docker/docker-compose.yml`; T2.2 em `packages/indicators/**`).

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES · TESTS · TEST RESULTS (saída real) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · NEXT STEP.

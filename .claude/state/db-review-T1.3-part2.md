# database-architect — revisão T1.3, segunda passada (2026-09-05) — JÁ CONCLUÍDA, não re-despachar
Complementa `db-review-T1.3.md`. Só o que a primeira não cobriu. Decisões do orquestrador marcadas com **DECISÃO**.

## CRITICAL
- **D1 — convenção de porcentagem.** `docs/DATABASE.md:12` e `_common.py:23`: `NUMERIC(9,6)` em **fração** (0.012 = 1,2%). `sampling.py:57` grava `(ask-bid)/mid` = fração = **correto**. O divergente é `NormalizedTicker.spread_pct` (`domain/market.py:176`) e `NormalizedOrderBook.spread_pct`, que devolvem `×100`. O item M4 do fix-brief ("multiplicar por 100 / usar o helper") **está errado — NÃO aplicar**. **DECISÃO:** o banco continua em fração; `sampling.py:57` fica como está; abrir follow-up T1.1c (fora da T1.3): helpers de domínio passam a devolver fração (renomear para `spread` ou documentar "fraction"), API expõe fração e a UI multiplica por 100 só para exibir. Teste: bid 99 / ask 101 → `Decimal("0.02")` no snapshot.
- **D2 — `queues.py:134-138` `drop()` levanta `RuntimeError` quando `losses` chega a `max_items` (5000).** Com Postgres fora, `report_losses` nunca esvazia a fila → ~9 min depois o `RuntimeError` sai por `_enqueue` (só pega `QueueFull`) → `forever` fatal → processo morre. O fix H1 (proteger `report_losses` e manter `losses`) garante esse caminho. Fix: `losses` vira `deque(maxlen=...)`, evicção contada em métrica (`market_persistence_loss_reports_dropped_total`), **nunca** levanta. Teste: `PersistQueues(max_items=4)`, cinco drops não levantam, contador = 1.

## HIGH
- **D3 — sem `command_timeout`/`statement_timeout` em lugar nenhum** (`session.py:41-50`; servidor com `statement_timeout=0`, `lock_timeout=0`). `wait_for(flush_batch, 10)` cancela a task e depois espera `ROLLBACK`/`close` num socket morto → `drain_loop` trava para sempre; `/ready` fica falso e nada reinicia. Fix: `connect_args={"command_timeout": 30}` em `session.py:46-49` e `SET LOCAL statement_timeout = '15s'` em `_apply_context` para `hunter_worker` (packages/core — arquivo permitido da T1.3). `wait_for` vira só backstop.
- **D4 — `create_partitions.py` pede `ACCESS EXCLUSIVE` em `candles` e enfileira atrás do `check_gaps` (~40 s de `ACCESS SHARE`/min); todo INSERT de candle fica atrás do pedido pendente → timeout de 10 s, retry, descarte por idade aos 60 s → gaps.** Fix: `SET LOCAL lock_timeout = '3s'` por transação em `infra/scripts/create_partitions.py:167` (idempotente; tenta no próximo run) — **fora da T1.3, anotar em T1.6/plano**; na T1.3, o HIGH-2 (register_missing set-based) é o que encurta a janela.
- **D5 — `recovery.py:144` `end = align(now) - 1 min` disputa com a fila de persistência**: com o `drain_loop` 15 s atrasado, `check_gaps` registra 200 gaps falsos por ciclo e dispara 200 REST backfills contra o banco já lento (feedback positivo). Fix: `end = align(now) - 2 min` (graça ≥ `max_age` da fila). Teste: candle final enfileirado sem flush, `check_gaps` com `now = T + 70 s` → zero gaps, zero `fetch_candles`.

## MEDIUM
- **D6 — gaps `failed` nunca são retirados**: `register_missing` (`recovery.py:79-91`) subtrai `covered` de `open` **e `failed`** → minutos ficam suprimidos para sempre (perda permanente de dados quando a Binance volta a ter o histórico); e `expected_times` materializa 300k datetimes por ciclo com gaps de bootstrap de 1499 min. Fix: `covered` só de `open`; política de retry/retenção para `failed` (reabrir após cooldown; apagar além dos 90 dias). Teste: gap `failed` + adapter ok → próximo `check_gaps` recupera.
- **D7 — linhas cujo símbolo não tem `markets` row são descartadas em silêncio dentro de um flush "bem-sucedido"** (`persist_rows.py:73/97/114-116/151/158`), e `persist.py:131-133` publica no stream **todas** as liquidações do batch, inclusive as puladas. Fix: contador `market_persistence_drops_total{reason="unknown_market"}`; publicar só ids retornados por `.returning(Liquidation.id)`.
- **D8 — bucket de OI derivado do `r.ts` de cada mercado** (`sampling.py:182-184`, `persist_rows.py:159-161`): poll sequencial de 200 mercados atravessa o limite de 5 min → grade irregular por mercado. Fix: helper único `oi_bucket()` usando o timestamp do **ciclo**, não de cada leitura.
- **D9 — snapshot gravado com tudo NULL quando não há hot state** (`sampling.py:98-121`, teste `test_persist.py:89-102` afirma isso como intenção) e `ON CONFLICT DO NOTHING` torna a linha vazia permanente. **DECISÃO:** pular o mercado quando ticker e deriv estão vazios, contar em `market_snapshot_skipped_no_data_total`; ajustar o teste; anotar em `DATABASE.md` §4 que ausência de linha = não observado.
- **D10 — racional do H6 está errado**: `ON CONFLICT DO NOTHING` multi-row **não** levanta `CardinalityViolation` (só `DO UPDATE`); a **primeira** ocorrência vence. Fix: dedupe em Python mantendo a **última** ocorrência por chave (leitura mais nova vence); teste com dois OI do mesmo bucket e valores diferentes → persiste o segundo.

## LOW
- **D11 — `liquidations.id` uuid5 diverge do contrato "UUID v7" de `DATABASE.md:9`** (intencional, L4) e dedupe é exata só ao milissegundo (`ts` é microssegundo; PK `(ts,id)`). Fix: registrar a exceção em `DATABASE.md` §4 e `assert liq.ts.microsecond % 1000 == 0` em `persist_rows.py:119`.
- **D12 — bounds de partição são strings de data resolvidas pelo `TimeZone` da sessão** (`_partitions.py:73-79`, `102-109`): rodar `create_partitions.py` com sessão não-UTC deixa buraco de horas entre meses. Fix (fora da T1.3, migrations/scripts): literais explícitos `'2026-09-01 00:00:00+00'` ou `SET LOCAL TimeZone='UTC'`.

## Verificado correto (não mexer)
Conflict targets ↔ índices únicos (inclui `(id, ts)` inferindo `pk_liquidations (ts, id)`); partições cobrem tudo até 2027-01-01; buckets UTC estáveis (`snapshot_ts` calculado uma vez por rodada); `role_session(hunter_worker)` em todos os 11 entry points; RLS não se aplica (tabelas globais); grants exatos (nada em filhos de partição); recovery atômico com `begin_nested`; planos de gap servidos por índice; tipos `numeric(28,10)`/`timestamptz`; retenção real em `prune_partitions.py`; pool 5+5 suficiente.

You are database-architect on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T4.2-meme-storage-coletor.md`. Everton (2026-09-12 04:2x BRT): "vamos começar a operar ainda hoje nessa carteira; use quantos agentes possível" — the orchestrator's honest path: radar + paper today, real money only after the flow is verified. Task T4.2 — storage + collector + per-minute features for pump.fun, on top of the adapter committed in T4.1 (`packages/exchange-adapters/hunter_exchanges/pumpfun/` — models, PumpPortal WS client, frontend-api-v3 REST client with 60/60 s bucket, public RPC reader; read `.claude/state/notes-T4.1.md`, `docs/EXCHANGE_INTEGRATION.md` §pump.fun) and the plan `docs/plans/T4-MEME-RADAR.md` (+ Astra's corrections and the Mayhem addendum; endpoints in `.claude/state/notes-A4.1b-mayhem.md`). **Migration number: 0021** (another agent, T3.90, owns 0020 right now; do not touch it). Deliver: (1) migration `0021_meme_radar` following `docs/DATABASE.md` conventions (global reference data like `market_regimes`; privileges by subtraction; immutable event tables; downgrade refuses to lose rows): `meme_tokens` (mint PK, name, symbol, uri, creator, created_at, bonding_curve, initial virtual reserves, `mayhem_enabled`, `mayhem_mode`, `mayhem_state`, `completed_at`, `migrated_at`, `pool`, first_seen source/observed_at), `meme_curve_snapshots` (mint, observed_at, source, virtual/real reserves, complete, mcap_sol computed, RANGE-partitioned by month — ≥ 1 M rows/yr expected), `meme_trades` (signature+ix index PK, mint, slot, block_time, trader, side, sol_lamports, token_amount, price, `is_mayhem_agent`, source — partitioned by month; no producer today: the trade feed is paid and the on-chain decoder is T4.2b; create the table now so the schema is whole), `meme_features_1m` (mint, minute end_time, curve progress %, mcap_sol, unique_buyers_so_far (null with reason until trades exist), buy/sell ratio (idem), top10_share (null until holders reader exists), creator_sold (null until then), age_minutes, coverage, version — nulls carry a reason column); RLS-free, `hunter_worker` INSERT/SELECT, `hunter_app` SELECT; tests in `packages/core/tests/integration/test_migrations.py` + `test_schema_privileges.py` (immutability by privilege, downgrade-refuses-with-rows) — testcontainers one file at a time, `timeout 290`; (2) a new service `services/meme-worker/` (role `meme`, same skeleton as the scanner-worker: TaskGroup, heartbeat `hb:meme:radar`, readiness, metrics) that: consumes the PumpPortal WS (new tokens + migrations → `meme_tokens` upsert-once), keeps a tracked set of mints (created in the last 24 h or Mayhem-active, capped at N by config) and polls `frontend-api-v3` curve state within the 60/60 s budget (prioritising the youngest), reconciles reserves via the public RPC at 10 req/s for the top-K by mcap, writes `meme_curve_snapshots` and computes `meme_features_1m` per minute (only what the free sources allow; the rest null with reason), with a retention job (same window for graduated and non-graduated: `MEME_RETENTION_DAYS`, default 90) and gap accounting (`meme_ingest_gaps`); compose service in `infra/docker/docker-compose.yml` + `infra/vps/docker-compose.prod.yml` (profile `meme`, `SOLANA_RPC_URL` from env, no key required), `compose.sh` untouched unless a profile switch is needed (then minimal + documented); (3) unit tests on the tracker/prioritiser/feature math with the T4.1 fixtures, one testcontainer file on the persistence path (idempotent upserts, partition routing); `ruff`/`pyright`/`check_file_size.py` clean, files ≤ 350; (4) docs: `docs/DATABASE.md` §32 (meme tables), `docs/ARCHITECTURE.md` (meme-worker), `docs/DEPLOYMENT.md` (profile + env); freeze the read model for T4.3 in `.claude/state/notes-T4.2.md` §"contrato" FIRST (tables + the API-facing views) so the frontend/API agent can build in parallel — write that section within your first 20 minutes; the notes end with commands/outputs and the full `git status --porcelain` list of your files. Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never paste a key; do not edit `packages/exchange-adapters/**` (report needed changes), `apps/**` (T4.3 owns), `services/strategy-worker/**`/`services/scanner-worker/**` (T3.90 owns), `packages/risk-core/**` (T4.4 owns). Report in Portuguese ≤ 12 lines, times in Brasília (UTC−3, compute it).

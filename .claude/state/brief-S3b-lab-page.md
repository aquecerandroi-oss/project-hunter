# Brief S3b — tela `/lab` (aba "Sombra") do Shadow Lab

**Owner:** frontend-specialist (sonnet). **Revisores depois:** code-reviewer, Astra (`bash infra/scripts/astra.sh ask S3-lab-page "..."`). **Não commitar.**

## Leia antes
1. `CLAUDE.md` e `docs/DESIGN.md` (direção de design da T1.5b: hierarquia calma, estados honestos de dado, dourado raro, sem pulse, UTC+offset, densidade coerente, acessibilidade). 2. `docs/plans/SHADOW-LAB.md` — Decisão conjunta itens 9 e 11 e a checklist **S3**. 3. **O contrato da API, já implementado e testado com dado real:** `.claude/state/contract-S3-lab.md` e `.claude/state/notes-S3.md` (S3a; `GET /api/v1/lab/shadow/{versions,summary,signals}`, gate de maturação de horizonte, `decisions = null` com motivo, PF com denominador, `portfolio_pnl`/`portfolio_max_drawdown` sempre `null`/`not_applicable`, `maturity.inconclusive`, cursor por `(decision_at, id)`, `include=envelope`). 4. Tipos gerados: `packages/shared-types/src/generated/api.d.ts` (namespace `lab`). 5. Padrões do web (M1): `apps/web/app/(app)/[orgSlug]/markets/**`, `apps/web/components/markets/**`, `apps/web/lib/api/markets*.ts` (server actions, sessão, `API_URL`), `apps/web/lib/nav-registry.ts` + `nav-icons.ts` (itens são dados puros), `apps/web/hooks/**`, testes Vitest em `apps/web/tests/**`, `apps/web/lib/auto-refresh-interval.ts`. 6. `.claude/rules/astra-second-opinion.md`.

## Entregar (arquivos permitidos: `apps/web/app/(app)/[orgSlug]/lab/**`, `apps/web/components/lab/**`, `apps/web/lib/api/lab*.ts`, `apps/web/lib/nav-registry.ts` (só `lab` → `available`), `apps/web/tests/**` (lab), `.claude/state/notes-S3b.md`)
- Página `/lab` com a aba **Sombra** (única aba por enquanto; a estrutura de abas fica pronta para "Backtests" e "Paper" depois, mas sem itens inertes: só a aba que existe).
- **Rótulo fixo no topo**, sempre visível: "SOMBRA — hipotético, sem capital, custos assumidos: spread 2 bps, slippage 5 bps/lado, taxa 4 bps/lado" (custos lidos de `coverage.assumed_costs`, nunca hardcoded), com `as_of` em UTC+offset.
- **Resumo por versão** (cards ou tabela compacta, uma linha por `strategy_version`): chave/versão/status (`active`/`deprecated` com `superseded_by` como link interno), o funil de contagens (emitidos → entradas → sem entrada por motivo → ativos → encerrados por resultado → censurados por motivo), as cinco métricas com os **nomes em português da decisão** e tooltip com a definição exata ("taxa de alvo entre toques resolvidos = target/(target+stop)", "taxa de lucro líquido", "expectancy líquida hipotética em R", "profit factor" com `sum_positive/sum_negative_abs/sample_size` visíveis, "soma de R hipotéticos" com o `count` e "ordenada por exit_ts"), `r_ex_funding` como bloco separado, **"PnL de carteira: não aplicável" e "Drawdown de carteira: não aplicável"** escritos, nunca omitidos; valores `null` mostram o `reason` legível (`no_sample`, `no_losses`, `not_applicable`, `evaluation_state_not_persisted`) e nunca `0` ou traço mudo.
- **Selo de maturidade** por versão: "Inconclusivo · N outcomes avaliáveis / 100 · D dias distintos / 30" enquanto `inconclusive`, em tom neutro (não é erro, é a regra editorial); quando não for mais inconclusivo, continua rotulado "pesquisa".
- Filtros: `window` (7d/30d/all), `cohort` (prospective por padrão), versão.
- **Lista de sinais** (tabela virtualizada ≥ 200 linhas, como `/markets`): `decision_at` (UTC+offset), mercado (link para `/markets/[exchange]/[symbol]`), versão, referência/stop/alvo, entrada virtual, `tracking_state` e `result` como chips (paleta semântica separada do dourado; `no_entry`/`censored` com o motivo), `r_multiple` (com o motivo quando nulo) e `r_ex_funding`, excursões: `mfe`/`mae` quando conhecidos, senão "indeterminado" com os `bounds` e o selo `ambiguous`; paginação por cursor; painel lateral/expansível com o envelope (`include=envelope`) sob demanda em JSON legível.
- Estados honestos: vazio ("0 sinais nesta janela" é resultado), 503 (API/Postgres fora: "sem verificação" ≠ "0"), carregando; auto-refresh no mesmo padrão de `/markets` (`auto-refresh-interval.ts`), sem WS.
- Nav: `lab` → `available` **só agora**, porque a API existe; ícone existente em `nav-icons.ts`.
- Testes Vitest: renderização com fixture do contrato (copiada do exemplo real de `contract-S3-lab.md`), `null` com motivo nunca vira número, rótulo e "não aplicável" sempre presentes, chips por estado, cursor, estado vazio e 503, acessibilidade (grid com `role` correto, foco visível), nenhum `console.*` (usa `@/lib/logger`), `components/**` não importa `@/lib/server/**`.

## Regras
Server Components por padrão, `"use client"` só onde precisa; itens do nav são dados puros; ESLint `max-lines` 350; Prettier. Comandos: `pnpm --filter @hunter/web test`, `pnpm lint`, `pnpm typecheck`, e ao final `docker compose -f infra/docker/docker-compose.yml build web` (ou `pnpm --filter @hunter/web build` se o Docker estiver ocupado — diga qual rodou). Verifique no navegador do preview com o stack local (`pnpm dev --port 3000` em `apps/web` com `.env` carregado e `API_URL/NEXT_PUBLIC_API_URL=http://127.0.0.1:8000`; a API do Lab está no container `api`? — se a imagem `api` local não tiver o router S3a ainda, rode a API do host com `uv run uvicorn hunter_api.app:app --port 8010` e aponte `API_URL` para ela; registre o que fez). PATH: prefixe `/c/Program Files/nodejs:/c/Users/evert/AppData/Roaming/npm:/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin`. Não toque em `.env*`, `apps/api`, `packages/**` (exceto ler os tipos gerados). Peça a opinião da Astra sobre a hierarquia da página (o que aparece primeiro, como o "inconclusivo" e o "não aplicável" ficam visíveis sem gritar) antes de implementar e sobre o diff antes de reportar.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · TESTS · TEST RESULTS (saída real) · REAL DATA CONNECTED (screenshot/descrição da página com o dado real do banco local) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · Checklist S3 (parte tela) · NEXT STEP.

# Brief T4.20 — rótulos do front para a fita por lote (T4.2g) e o plano para executar à mão (T4.19) (12/09/2026 18:2x BRT)

**Contexto:** T4.2g adicionou ao `/meme/sources` a fonte `swap_api_activity` (fita por lote), o heartbeat `activity_mints`, `activity_live_1m`, `activity_dark_60s`, `activity_calls_60s` (se existir), `tape_activity_pct`, os motivos `no_sol_quote` (sem cotação SOL/USD no minuto) e `no_trade_feed`, e `tape_source` (`swap_api` | `activity_1m`) / `tape_window_s` / `tape_as_of` nas linhas de features. T4.19 adicionou `manual_plan` (texto em português, já pronto) à proposta (`ProposalOut.manual_plan`) — o Everton executa o teste real **à mão** no Terminal seguindo esse texto: "Comprar 0,05 SOL de X até HH:MM:SS. Vender até HH:MM — antes disso se…". Os tipos gerados já estão em `packages/shared-types/src/generated/api.d.ts` (**não** rodar `pnpm gen:types`).

**Ler antes:** `.claude/state/notes-T4.2g.md` §Preocupações item 7, `.claude/state/notes-T4.19.md` (se existir; senão `brief-T4.19-*.md`), `apps/web/components/meme/labels.ts` e `meme-sources-format.ts`/`meme-sources-panel.tsx`, `apps/web/components/meme-desk/proposal-card.tsx`, `labels.ts`, os testes `apps/web/tests/meme-*.test.ts(x)`, `docs/DESIGN.md`.

**Entregar (só `apps/web/**`):**
1. Rótulos exaustivos (com fallback nomeado): fonte `swap_api_activity` → "fita por lote (swap-api)"; heartbeat `activity_mints` → "moedas no lote", `activity_live_1m` → "janela de 1 min viva", `activity_dark_60s` → "lote sem janela de 1 min (60 s)", `activity_calls_60s` → "chamadas do lote por minuto", `tape_activity_pct` → "cobertura da fita por lote"; motivos `no_sol_quote` → "sem cotação SOL/USD no minuto", `no_trade_feed` → "sem fita"; `tape_source` `activity_1m` → "fita por lote", `swap_api` → "fita por mint"; `tape_as_of` → "fita de HH:MM:SS" (Brasília) e `tape_window_s` → "janela de N s" onde a linha de features for exibida (`/meme/{mint}`, mesa).
2. **Plano para executar à mão** no card da proposta do operador (`/meme/mesa`): quando `manual_plan` vier, um bloco destacado (rótulo "Plano para executar à mão", estado de atenção conforme `docs/DESIGN.md`), com o texto inteiro, um botão "Copiar mint" e a contagem regressiva até a expiração já existente; sem `manual_plan`, nada muda. Também na aba Testes, se a proposta for listada lá.
3. Testes Vitest atualizados (listas exaustivas), `pnpm vitest run tests/meme`, `pnpm turbo typecheck lint --filter=@hunter/web`, `pnpm --filter web build` (o trace do standalone falha no Windows por EPERM — limitação conhecida). `.claude/state/notes-T4.20.md`; `git status --porcelain -- apps/web` ao final.

**Regras:** primeiro plano `timeout 290`; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit`/`add`; sem `.env*`; nada real; só `apps/web/**` e as notas; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 8 linhas em português.

# Brief T3.34c — trendline_breakout_v1 sai do papel: fecha as ressalvas da revisão de c9691d0, entra no catálogo da VPS (`seed.py --only strategies`), ativa `research_only`, replay 31 d com o "dia um" pré-registrado (R pareado da invalidação, não só a taxa)

**Owner:** quant-engineer. **Do not commit** (report the exact files to commit; the orchestrator commits by pathspec). **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (`timeout 290` per replay slice, split markets if needed); no testcontainers in this task (strategy unit tests are pure); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; VPS writes ONLY through the audited scripts (`seed.py --dry-run` then `--only strategies --yes`, `activate_strategy_version.py --purpose research_only`, the replay CLI); reads in `repeatable read read only`; never stop or recreate containers.** Base: `main` at `2e39774` (VPS api/strategy-worker `2e39774`, which contains c9691d0). Write scope: `.claude/state/notes-T3.34b.md` (append "T3.34c"), `.claude/state/exp-drafts/EXP-0016-trendline-breakout.md`, `.claude/state/notes-T3.34c.md`, `infra/scripts/sql/research/2026-09-09-t334c-*.sql`. **No change to `packages/core/hunter_core/strategies/tl_*.py` or `trendline_breakout_v1.py`** (the digest is frozen by the test).

## Review findings to close first (code-reviewer on c9691d0, APROVAR COM RESSALVAS)
1. "37 parâmetros" is wrong: `default_parameters` has 36 keys. Fix `notes-T3.34b.md` and EXP-0016 (the commit message stays).
2. EXP-0016: add `KB-0006` / `EXP-0007` to "Relacionadas" and to C8, with one sentence acknowledging that the same rule shape ("fechar abaixo do nível rompido") was measured as no-improvement in `momentum_v1` (Δ −0,070 R … +0,032 R, sign flips between populations — "a invalidação adianta a perda, não a cria"). Pre-register the day-one metric: mean R of `geometry_invalidation` exits vs the counterfactual R until stop/target/expired on the same episodes (the EXP-0007 design), not only the percentage.
3. Note the two independent Wilder ATR(14) series (official 97-bar vs `tl_scan` 96-bar) as a known small divergence on real data (observation, not a bug).

## Then, on the VPS (receipts verbatim)
4. `seed.py --dry-run` → `--only strategies --yes` (only the new strategy row + v1 must appear; paste the diff of the catalogue before/after); `activate_strategy_version.py trendline_breakout v1 --purpose research_only` (dry-run first; digest `trendline_breakout_v1@sha256:7b83a1ff…`).
5. Replay 31 d, 4 markets, `--explain-ledger`, `--cohort` explicit. Day one, in priority order (from the review): (a) `pattern_pivots`/`pattern_lines`/`no_line` per evaluated bar — does the geometry exist on real data; (b) raw decision count (C3/K1); (c) `risk_too_wide` and `geometry_invalidation` rates separately, and the paired-R metric of item 2; (d) decomposition by `line_slope_per_bar` decile and by mode/market (60 % rule). Kill criteria K1–K5; stress pass only if K1 survives.
6. Verdict per the funnel (`descartar` / `manter em pesquisa` / `candidata`) and ≤ 10 lines in Portuguese for Everton, including the honest answer to "cada operação traça as linhas?": each decision of this version persists `line_id`, slope, first/last idx, price at decision, pivot low and the whole `pattern_params`, so the line of every operation can be redrawn exactly (that is T3.49, the Lab overlay); the other strategies do not draw lines.

## Prove
Receipts, SQL with output, report in Portuguese, extended format; `.claude/state/notes-T3.34c.md`.

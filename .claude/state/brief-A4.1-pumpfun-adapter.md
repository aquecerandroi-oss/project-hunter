# Brief A4.1 (para a Astra, modo run) — terminar o adapter do pump.fun (fontes gratuitas) que um agente nosso deixou pela metade

**Contexto:** o brief original é `.claude/state/brief-T4.1-pumpfun-adapter.md` (leia inteiro; ele manda). O agente que o executava caiu por limite de uso enquanto escrevia `decode.py` (layout da conta da bonding curve via RPC). O que ele deixou está em `packages/exchange-adapters/hunter_exchanges/pumpfun/` e `packages/exchange-adapters/tests/**/pumpfun*` (ver `git status --porcelain`) — **retome a partir do que existe**, sem apagar trabalho que esteja correto; se algo estiver quebrado, conserte e diga. Referências obrigatórias: `docs/plans/T4-MEME-RADAR.md` (com o adendo Mayhem e a revisão da Astra aplicada), `.claude/state/notes-T4.0.md`, `.claude/state/astra-review-t40-pumpfun.md` (o seu próprio parecer), `docs/EXCHANGE_INTEGRATION.md`, `docs/DATABASE.md`, e o adapter Binance como padrão de estrutura (`packages/exchange-adapters/hunter_exchanges/binance*`).

**Entregar (só fontes gratuitas; o RPC com chave é decisão pendente do Everton — a URL do RPC é injetável por `SOLANA_RPC_URL`, sem ler `.env*`):**
1. Modelos normalizados: token criado, migração, estado da curva (reservas virtuais/reais, `complete`, market cap em SOL pela fórmula confirmada ao vivo: 30 SOL / 1 073 000 000 tokens virtuais), e `NormalizedMemeTrade` definido sem produtor (feed de trades é pago) — cada leitura com `observed_at` e `source`.
2. Cliente websocket do PumpPortal (`wss://pumpportal.fun/api/data`, `subscribeNewToken` + `subscribeMigration`, reconexão com backoff, heartbeat, dedupe por mint+assinatura) com fixture gravada de mensagens REAIS (conectar ≤ 60 s, `timeout 120`; se não conectar desta máquina, dizer e derivar do esquema documentado, rotulando).
3. Cliente REST `frontend-api-v3.pump.fun` para estado da curva por mint, com token bucket de 60 req/60 s e fixture de uma resposta real (registrar o código HTTP se recusar). **Mayhem:** capturar os campos que indiquem `mayhem_mode`/estado do agente se existirem na resposta (ver adendo do plano).
4. Leitor RPC público da Solana (`getAccountInfo` da conta da curva, layout como em `chainstacklabs/pumpfun-bonkfun-bot` — citar arquivo/commit), 10 req/s, como verdade on-chain das reservas.
5. Testes unitários sobre fixtures (parse, normalização, fórmula da curva contra as constantes, dedupe, máquina de estados de reconexão) — sem testcontainers; arquivos ≤ 350 linhas; `uv run ruff check`, `uv run ruff format --check`, `uv run pyright` nos arquivos tocados e `uv run python infra/scripts/check_file_size.py` limpos.
6. Seção pump.fun em `docs/EXCHANGE_INTEGRATION.md` (fontes, limites, o que é grátis, o que espera a decisão do RPC), `.claude/state/notes-T4.1.md` com comandos/saídas e a lista `git status --porcelain` de TODOS os arquivos tocados.

**Fora do escopo:** migrações/tabelas (T4.2), wiring de serviço, execução. **Regras:** não commitar; não ler nem escrever `.env*`; nunca colar chave; nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; comandos em primeiro plano com `timeout 290`; não tocar `apps/**` (outra execução da Astra está lá), `services/execution-worker/**` (idem) nem `obsidian/**`. Relatório ≤ 12 linhas em português, horários em Brasília (UTC−3, calcular).

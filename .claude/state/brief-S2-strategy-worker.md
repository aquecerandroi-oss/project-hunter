# Brief S2 — `strategy-worker` em modo sombra (decisões, entradas por barras, outcomes, outbox)

**Owner:** backend-specialist (opus). **Revisores depois:** risk-engine-guardian (é o caminho que um dia vira execução; nada pode ordenar), code-reviewer, Astra (`bash infra/scripts/astra.sh ask S2-worker "..."`). **Não commitar** — o orquestrador commita após revisar `git diff --stat`.

## Leia antes (nesta ordem)
1. `CLAUDE.md` (regras duras, comandos canônicos, ≤ 350 linhas por módulo, TDD, Decimal/UTC, structlog).
2. `docs/plans/SHADOW-LAB.md` inteiro — a "Decisão conjunta" (itens 1–11) é o contrato; a checklist **S2** é o aceite. Nada é opcional.
3. `docs/DATABASE.md` §16 (o que a S0 entregou: trigger de congelamento, `tracking_state` + CHECK bicondicional `(result = 'open') = (tracking_state <> 'terminal')`, `shadow_episodes`, `shadow_outbox`, grants, consequência para `seed.py`/ativação: gravar o `code_ref` definitivo **antes** de ativar).
4. `.claude/state/notes-S1.md` (o que a S1 entregou e o que deixou para você: chame `explain()` uma vez por fechamento; `Evaluation.state` decide o rearme — só `not_triggered` comprova condição falsa; `decision_at`, `cohort`, `params_hash`, `uuid5` e o envelope escrito uma única vez são seus; proveniência em `decision_at`; retenção das velas da janela declarada; round-trip real dos níveis contra `NUMERIC(28,10)`).
5. Código: `packages/core/hunter_core/strategies/**` (`build_context`, `Strategy.explain`, `Decision`, `SupportingFeatures.to_jsonable`, `registry`, `canonical.params_hash`), `packages/core/hunter_core/db/models/{agents,agents_shadow}.py`, `packages/core/hunter_core/domain/enums.py` (`ShadowTrackingState`, `ShadowCohort`), `services/market-worker/hunter_market_worker/{main,supervision,health,persist,publication,recovery}.py` (padrões de supervisão, readiness, heartbeat, consumo de streams, transação, `role_session(..., db_role="hunter_worker")`), `packages/core/hunter_core/events/{envelope,streams}.py`, `packages/core/hunter_core/redis/keys.py`, `docs/plans/M2.md` T2.9 (outbox compartilhada que absorverá a sua) e §"scanner nunca chama REST" (backfill é pedido ao market-worker via `market.backfill.requested`).
6. `.claude/rules/astra-second-opinion.md` — peça a opinião da Astra sobre o desenho da máquina de estados (decisão → pending_entry → active → terminal/no_entry/censored; rearme) ANTES de implementar e sobre o diff antes de reportar.

## Entregar (arquivos permitidos)
- `services/strategy-worker/**` (novo pacote `hunter_strategy_worker`, `pyproject.toml` no workspace uv como o market-worker, `tests/**` com testcontainers como `services/market-worker/tests`), `infra/docker/docker-compose.yml` (serviço `strategy-worker`, `restart: unless-stopped`, healthcheck, mesmo padrão do market-worker), `infra/docker/Dockerfile.*` se o padrão exigir, `infra/scripts/activate_strategy_version.py` (ativação auditada: grava `code_ref` definitivo = hash de `hunter_core/strategies/**`, valida `default_parameters` contra `parameters_schema`, exige migração `0002` aplicada, registra `activated_at` + `changelog` + `system_events`; recusa se qualquer pré-requisito falhar), `packages/core/hunter_core/events/streams.py` (só acrescentar `SHADOW_SIGNALS_EMITTED = "shadow.signals.emitted"` e seu maxlen), `services/market-worker/hunter_market_worker/universe.py` + `universe_repo.py` **apenas** para honrar `tracking_hold` (mercado excluído do top N continua monitorado enquanto `shadow_episodes.open_outcome_signal_id IS NOT NULL` apontar para ele; teste no market-worker), `docs/PIPELINE.md` (seção curta "Shadow Lab: strategy-worker" — escritor único de outcomes; transferência futura ao analytics explícita).
- **Consumo:** grupo próprio em `market.candles.closed`; contexto por mercado montado do hot state `mkt:{ex}:{sym}:candles:1m` + Postgres para bootstrap, **cortado em `source_bar_close`** (`build_context`); avalia cada versão `active` só nos fechamentos alinhados do timeframe da versão (15 min momentum, 5 min volume); registra `Evaluation.state` por (versão, mercado, barra) para o rearme; dados `degraded/stale` ou mercado inelegível ⇒ `unavailable/ineligible` (não rearma, não decide).
- **Decisão → persistência (transação única, `hunter_worker`):** `agent_signals` (`id = uuid5(NAMESPACE_SHADOW, canonical(strategy_version_id, market_id, params_hash, source_bar_close, cohort))`, `emitted_at = decision_at`, `supporting_features` = envelope da S1 + `decision_at`, `cohort`, `params_format`, `purpose = research_only`, proveniência `available_through` = último `received_at`/`open_time` conhecido no instante; **escrito uma vez, nunca reescrito** — teste), `signal_outcomes` inicial (`tracking_state = pending_entry`, `virtual_stop/targets` congelados, `entry_bar_open` escolhido = primeira barra de 1 min com abertura estritamente posterior a `decision_at`, gravado em `meta.entry_plan`), `shadow_episodes` (lock `SELECT … FOR UPDATE` no slot; exclusividade `pending_entry|active` por (versão, mercado, coorte)), `shadow_outbox` (evento `shadow.signals.emitted`, `event_id = signal_id`); `INSERT … ON CONFLICT (id) DO NOTHING`; ACK do stream só após commit. Persistência deve acontecer **antes da abertura da barra de entrada**; se não, `no_entry: late`.
- **Despachante da outbox:** loop que publica `dispatched_at IS NULL` em ordem de `created_at`, marca `dispatched_at`, incrementa `attempts`, guarda `last_error`; reconciliação idempotente após restart (republicar não duplica: consumidor por `event_id`).
- **Outcomes por barras fechadas de 1 min** (job no mesmo worker, único escritor): `pending_entry` → ao chegar a barra de entrada: checar `entry_bar_open − source_bar_close ≤ max_entry_delay_s` (senão `no_entry: late`), `P_entry = open × (1 + (spread/2 + slippage)/10000)`, revalidar `stop < P_entry < target1` (senão `no_entry: geometry`), `active` com `entry_ts`, `virtual_entry`; `active` → por barra: gap na abertura primeiro, depois toques intrabar (stop e alvo na mesma barra ⇒ stop), expiração na abertura exata de `entry_bar_open + horizon_s`, invalidação observada no fechamento ⇒ saída na próxima abertura elegível, `P_exit = base × (1 − (spread/2 + slippage)/10000)`, `R_net` com taxas 4 bps por lado e funding assinado por unidade (funding não apurável ⇒ `r_multiple = NULL` + `meta.r_net_reason`, `meta.r_ex_funding`), `mfe/mae/mfe_ts/mae_ts` **nulos quando indeterminados** e `meta.excursions` conforme item 5 da decisão; barra necessária irrecuperável ⇒ `censored` com motivo; `terminal` nunca reabre. Ao encerrar: liberar o slot (`open_outcome_signal_id = NULL`, `armed = false` até uma barra `not_triggered` posterior ao término).
- **Supervisão:** `forever()`, readiness (`/ready` com migração `0002` presente, consumidor vivo, outbox sem atraso > N), heartbeat `hb:strategy:shadow`, métricas de contagem por estado; Postgres/Redis fora ⇒ backoff, nunca morre; restart não duplica nada.
- **Testes (obrigatórios, com cenário e saída real):** atraso desde a referência (12:00/12:05:02/12:06 → late; 12:00/12:00:02/12:01 → entra); commit que perde a abertura; geometria após gap; custos nos dois lados (número conferido à mão em Decimal); funding indisponível; stop e alvo na mesma barra; expiração e invalidação; excursões ambíguas (100/99/102 com low 98/high 103 ⇒ `mfe = NULL`, `bounds.mfe = [0, 3]`, `ambiguous = true`); envelope nunca reescrito; falhas injetadas antes do commit, entre commit e publicação, após publicação antes do ACK; dois consumidores concorrentes no mesmo episódio ⇒ um único acompanhamento; eventos fora de ordem; restart durante rearme; reentrega no mesmo run mantém um sinal e outro `run_id` tem identidade própria; recovery contíguo; gap irrecuperável ⇒ `censored`; `tracking_hold` com duas versões após exclusão do mercado e restart; `research_only` persistido no evento; round-trip dos níveis contra `NUMERIC(28,10)` (nível usado após restart = nível gravado); `activate_strategy_version.py` recusa sem migração/sem `code_ref`.
- **Prova operacional** (após os testes): subir o serviço no compose contra o stack local (market-worker já com dados reais), ativar `momentum_v1` e `volume_anomaly_v1` pelo script, observar ≥ 30 min: contagem de avaliações por estado, sinais emitidos (0 é resultado válido), outbox despachada, readiness verde, sem exceção no log. Registrar em `.claude/state/s2-proof.md` com saída real.

## Regras
- Só `Decimal`, só UTC, sem look-ahead (contexto cortado, barra de entrada escolhida antes de conhecer o resultado), nada ordena nem toca carteira, nada de dado inventado. ≤ 350 linhas por módulo. TDD.
- Comandos: `uv sync --all-packages`, `uv run pytest services/strategy-worker -q`, `uv run pytest services/market-worker -q -k universe`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`, `uv run python infra/scripts/check_file_size.py`, `docker compose -f infra/docker/docker-compose.yml build strategy-worker`. Toolchain: prefixe o PATH com `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`.
- Não toque em `.env*`, não commite, não altere arquivos fora da lista. Se precisar de algo da S0/S1 que não existe, registre em `.claude/state/notes-S2.md` e resolva no seu pacote sem reescrever o deles.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma nova; o que escreveu) · TESTS · TEST RESULTS (saída real) · REAL DATA CONNECTED (a prova de 30 min) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · Checklist S2 com `[x]`/`[ ]` honesto · NEXT STEP.

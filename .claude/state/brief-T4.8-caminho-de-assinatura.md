# Brief T4.8 — o caminho de execução real na curva do pump.fun, construído inteiro e mantido INERTE (12/09/2026 04:3x BRT; Everton: "preciso o trader total funcionando")

**Doutrina que manda:** `docs/RISK_ENGINE_MEME.md` (T4.4) — §9 caminho de execução e modelo de confiança, §3 política da carteira/chave/flag, §4 checks, §8 falhar fechado, §11 VM1–VM9, §12 os três portões, §13 o que muda no motor atual. `docs/PUMPFUN-ONCHAIN.md` (T4.0d: programas, IDL, contas, TradeEvent), `docs/PUMPFUN.md` §1 (`/global-params/{ts}`), §3.5 (PumpPortal trade-local — só fallback), §6. Adapter existente: `packages/exchange-adapters/hunter_exchanges/pumpfun/` (T4.1). Também `packages/core/hunter_core/execution/` e `services/execution-worker/` para o padrão de flag/recusa de boot (`config.py:107`) e o teste de não-vazamento da chave.

**Posição do orquestrador (não negociável):** nada aqui assina na mainnet. O código nasce completo e provado em **devnet** (ou em simulação local com `simulateTransaction`), atrás de `ENABLE_MEME_LIVE_TRADING` (default false, lido por UM processo) e dos portões do §12. Quando o Everton digitar a chave e ligar a flag, o mesmo código passa a ser real — por isso ele tem de estar certo agora.

**Entregar (código em `packages/exchange-adapters/hunter_exchanges/pumpfun/` e `packages/core/hunter_core/execution/meme/`; nada em `apps/**`, `services/meme-worker/**` além do que está listado):**
1. **`pumpfun/tx.py` — construtor de instruções** `buy` e `sell` do programa da curva (`6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P`) conforme a IDL oficial (contas na ordem exata, discriminadores, argumentos `amount`/`max_sol_cost` e `amount`/`min_sol_output`), com as ATAs derivadas, `Global` (`4wTV1YmiEkRvAtNtsSGPtUrqRYQMe5SKy2uB4Jjaxnjf`), fee recipient e o vault Mayhem quando aplicável (ler do estado da curva/`Global`, nunca constante mágica); `compute budget` e `priority fee` como parâmetros explícitos (sem default silencioso); tudo puro (sem rede), com fixtures reais (transações de buy/sell da mainnet decodificadas — pegue assinaturas públicas de `docs/PUMPFUN-ONCHAIN.md` ou de um explorer, via `getTransaction` no RPC público, ≤ 10 chamadas) provando que a nossa instrução é **byte a byte** igual à do site para as mesmas entradas.
2. **`pumpfun/quote.py`** — cotação local a partir de `/global-params/{ts}` + estado da curva (RPC) com as taxas por faixa do doc oficial; slippage máximo explícito; compara com a cotação do `swap-build` do site (só leitura, ≤ 5 chamadas) e registra a diferença em fixture.
3. **`hunter_core/execution/meme/signer.py`** — assinatura local: chave lida de `SOLANA_WALLET_SECRET_KEY` **uma vez**, por este módulo só, nunca logada, nunca em repr/exception/heartbeat; teste de não-vazamento (varre logs/exceções/dumps por prefixo da chave); recusa de boot se a flag estiver ligada e o §12 não estiver satisfeito (o verificador é um arquivo de estado assinado pelo operador — desenhe simples: `meme_gates.json` com A/B/C e data, lido e validado; sem ele, recusa).
4. **`hunter_core/execution/meme/submit.py`** — `simulateTransaction` obrigatório antes de enviar; envio por RPC (`sendTransaction` com `skipPreflight=false`) e, opcionalmente, Jito bundle (parâmetro, off por default); confirmação por `TradeEvent` decodificado da transação confirmada (fill = evento, taxas do evento); idempotência em duas chaves (`proposal_id` e assinatura; blockhash novo = compra nova, §9); estados `submitted_unconfirmed` / `confirmed` / `failed` com motivo nomeado.
5. **Verificador de transação (§9.1):** antes de assinar, reconstrói e compara programa/contas/argumentos com a intenção aprovada; transação adulterada é **recusada** (teste adversarial obrigatório).
6. **Devnet:** um teste marcado `live_devnet` (fora do CI) que cria um mint de teste no devnet OU, se o programa não existir no devnet, um teste de simulação local completa (`simulateTransaction` na mainnet com uma carteira sem saldo — só simula, nunca envia). Diga qual dos dois foi possível e por quê.
7. **VM1–VM9** (§11): scripts em `infra/scripts/meme_vm.py` com saída colada em `.claude/state/notes-T4.8.md`; `forbidden_patterns.sh` já cobre `ENABLE_MEME_LIVE_TRADING=true` (commit e45c280) — acrescente ao gate o padrão de chave Solana (base58 de 87–88 chars ou array JSON de 64 inteiros) se ainda não houver, com fixture no `--self-test`.
8. **Docs:** `docs/RISK_ENGINE_MEME.md` §9 → "implementado em …" (sem mudar a doutrina), `docs/PUMPFUN-ONCHAIN.md` §instruções (o que o construtor faz), `docs/DEPLOYMENT.md` (as variáveis já listadas por T4.4 passam a "lidas por `hunter_core.execution.meme.signer`"), `.claude/state/notes-T4.8.md`.

**Regras:** nunca shell em background; tudo em primeiro plano com `timeout 290`; ≤ 1 testcontainer por vez (provavelmente nenhum); árvore compartilhada — nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; **nunca** ler/escrever `.env*`; **nunca** gerar, colar ou logar uma chave real (chaves de teste geradas em memória e descartadas); **nunca** `sendTransaction` na mainnet; `ENABLE_MEME_LIVE_TRADING` nunca `true` em arquivo nenhum. Relatório ≤ 12 linhas em português com o que foi provado (devnet ou simulação) e o que ficou de fora, horários em Brasília (UTC − 3).

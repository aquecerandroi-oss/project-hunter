You are backend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd). First save this brief verbatim as `.claude/state/brief-T3.78-meta-diaria.md`.

# Brief T3.78 — "Meta diária" on the Placar: Everton set a daily goal of R$ 9 000 minimum profit (2026-09-10 13:2x BRT); the product must show, every day, with real data, how far the Lab is from it and why

**Facts:** T3.60 (`.claude/state/notes-T3.60.md`, `.claude/state/proposta-carteira-2026-09-09.md`, simulator in `.claude/state/exp-drafts/t360/`): the family as ONE portfolio under the real limits gives ~+2,4 R/day; the participation cap (`max_participation_pct` 1 % of the 1-min volume) binds in 164/176 markets so 1 R is worth R$ 7–34, not the R$ 250 label; R$ 9 000/day needs 1 R ≈ R$ 3 719. D-P9/KB-0083: pooled R ≈ 4,3× unique-bet R (8 versions of one idea). The Placar (`apps/api/hunter_api/routers/lab_scoreboard.py`, services/schemas alongside, web `apps/web/app/(app)/[orgSlug]/lab/...` scoreboard components) shows per-version cards with "RESULTADO ACUMULADO (SIMULADO)" in USDT/BRL at the label size.

## Deliver (API first; a frontend agent will build the panel against your schema — freeze it in `.claude/state/notes-T3.78.md` as soon as it exists)
1. `GET /api/v1/orgs/{org_id}/lab/daily-goal?day=YYYY-MM-DD` (default: today in America/Sao_Paulo; VIEWER+): per day, for the live family/families (all active versions, research + paper): `unique_bets` (distinct (market, bar) with an outcome closed that day — the exit day, in Brasília), `pooled_r`, `unique_r` (first version per bet wins, deterministic order by version activation — document it), `hit_rate`, `r_per_unique_bet`; `value_of_1r`: `label_brl` (0,25 % × R$ 100 000 = R$ 250), `real_brl_p50`/`p10`/`p90` (from the real 1-min volumes of the bet markets at the entry minute under `max_participation_pct`, converted with the FX observation of the day — reuse `hunter_risk` sizing helpers and the FX table; never reimplement thresholds); `goal_brl` = 9000 (a setting `DAILY_GOAL_BRL`, default 9000); `progress`: `unique_r × real_brl_p50` and `× label_brl`, `distance_to_goal_brl` for both, `required_1r_brl` = goal / unique_r (null when unique_r ≤ 0), `required_unique_r` = goal / real_brl_p50; plus a 30-day series of `unique_r` and `pooled_r` per day for the sparkline. Every number Decimal-as-string; `as_of`; `axis` (r_net vs r_ex_funding and how many outcomes lack net R — the funding-NULL problem, T3.75) — never silently mix axes.
2. Repository queries under RLS with the org session, read-only, indexed paths only (check `EXPLAIN` on a testcontainer with the T3.62b-size ledger); one testcontainer integration file (a day with 3 versions taking the same bet → unique_r counts once, pooled counts 3; value_of_1r from a market with 1-min volume 200 000 USDT → participation binds; FX missing → `real_brl` null with reason, never a default); unit tests for the arithmetic.
3. Docs: `docs/PRODUCT.md` (Placar section, one paragraph "Meta diária"), `docs/RISK_ENGINE.md` §4 one sentence pointing to the real-vs-label 1 R; `.claude/state/notes-T3.78.md` with the frozen JSON schema, commands/outputs and the full `git status --porcelain` list of your files.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; do not edit `apps/web/**`, `services/**`, `packages/risk-core/**`; keep files ≤ 350 lines; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 10 lines.

# Brief T3.79 — "quero tudo instantâneo, deixe nos trinques essa parte" (Everton, 2026-09-10 14:5x BRT): the end-to-end latency budget, measured per hop and published

**Facts:** T3.74c instrumented the strategy-worker hop (`hunter_shadow_stage_seconds{stage}`, `hunter_shadow_decision_lag_seconds`, heartbeat `decision_lag_p50_s/p95_s`; uncommitted, another agent T3.74d is finishing it — do NOT edit `services/strategy-worker/**`). The other hops are not measured as a chain: Binance trade/kline event time → market-worker ingest (`services/market-worker/hunter_market_worker/{streaming.py, coalesce.py, coverage.py}`) → 1-min candle flush to Postgres and to the Redis stream the strategy-worker consumes → execution-worker bridge/admission cycle (`services/execution-worker/hunter_execution_worker/{cycles.py, bridge.py, admission_cycle.py, entry.py}`) → paper fill against the book (`apply.py`, `booking.py`). Targets (SLO, to be written in `docs/PIPELINE.md` §6b and `docs/ARCHITECTURE.md`): exchange event → candle on our stream < 1 s; candle close → decision median < 5 s / p95 < 20 s; decision → admission < 2 s; admission → paper fill < 2 s; end to end < 10 s.

## Deliver
1. **Measurement first (read-only on the VPS, foreground `timeout 290`)**: for each hop, what timestamps already exist (exchange event time in the raw payload, `received_at` on candles, stream entry ids, `agent_signals.emitted_at`, `trade_proposals.created_at`/decided_at, `orders`/`fills` timestamps) and the current p50/p95 of each hop over the last 24 h from SQL/Redis (`infra/scripts/sql/research/2026-09-10-t379-*.sql`). Where a hop has no timestamp, say so — that is the gap to instrument. Note: the market-worker's 1-min candle by construction closes at the minute boundary; the "flush delay" is `received_at − (open_time + 1 min)`.
2. **Instrumentation** (Prometheus histograms + heartbeat fields, same style as T3.74c): market-worker `hunter_market_ingest_lag_seconds` (exchange event time → our receive), `hunter_candle_flush_lag_seconds` (bar close → stream publish); execution-worker `hunter_execution_admission_lag_seconds` (signal emitted → decision persisted), `hunter_execution_fill_lag_seconds` (decision → fill applied), and a derived `end_to_end_lag_s` p50/p95 in `hb:execution:paper`. Never change what any hop DOES — only measure; clocks: use the exchange's event time where present and our monotonic receive time; document the clock-skew tolerance (2 s, `market_status.py`) and never report negative lags as zero (name them `clock_skew`).
3. **Publish**: `GET /api/v1/system/latency` (VIEWER+) returning the five hops with p50/p95 and the SLO status (`ok`/`warn`/`critical` with the thresholds above) from the heartbeats/metrics already in Redis (no new DB tables); one paragraph in `docs/PIPELINE.md` §6b (the budget table) and `docs/ARCHITECTURE.md`. A frontend agent will add the row to the system status screen later — freeze the JSON in `.claude/state/notes-T3.79.md`.
4. Tests: unit for the lag math (skew, missing timestamps → null with reason, never 0) and the SLO classification; one testcontainer file per worker touched only if the path needs it (max 2 files, one invocation each, `timeout 290`). `.claude/state/notes-T3.79.md` with the measured table (today's numbers per hop), commands/outputs and the full `git status --porcelain` list of your files.

Rules: never a background shell; every command foreground with explicit `timeout 290`; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no VPS writes; do not edit `services/strategy-worker/**` (T3.74d/T3.77 own it), `apps/web/**`, `packages/risk-core/**`, nor `apps/api/hunter_api/routers/lab_*` (T3.78 owns them); keep files ≤ 350 lines; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 12 lines with today's per-hop numbers, times in Brasília (UTC−3).

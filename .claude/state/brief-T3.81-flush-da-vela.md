You are exchange-integration-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, read-only). First save this brief verbatim as `.claude/state/brief-T3.81-flush-da-vela.md`.

# Brief T3.81 — the candle flush is the second hop that breaks the budget (from T3.79, 2026-09-10)

**Facts (`.claude/state/notes-T3.79.md`, committed `ac03216`):** the hop "bar close → candle published on our stream/persisted" measured over 24 h on the VPS: p50 ≈ 1,03 s, p95 ≈ 3,5–4,1 s (proxy `candles_1m.received_at − (open_time + 1 min)`, `source='ws'`). SLO: p95 < 1 s. The market-worker runs 4 shards + spot (`MARKET_SHARDS=4`, `MARKET_SPOT=1`), ingests Binance klines/trades over WS (`services/market-worker/hunter_market_worker/{streaming.py, coalesce.py, coverage.py, coverage_publish.py, persist.py, ingest.py, latency.py}`), coalesces 1-min candles and flushes them in batches; T3.46 added the `observe_proof`/`refreshed_cut`/stamp-after-flush pipeline. New instrumentation from T3.79: `hunter_market_ingest_lag_seconds`, `hunter_candle_flush_lag_seconds`.

## Deliver
1. **Where the seconds are** (read-only on the VPS, foreground `timeout 290`): per shard, the flush-lag distribution by minute-of-hour and by market count; the coalescer's batch timer/size (`coalesce.py` — is there a fixed flush interval? a batch size threshold? a Postgres write that waits on the coverage stamp?); Binance kline `x=true` (final) event arrival time vs bar close (Binance emits the final kline ~0–2 s after close — measure with `received_at` vs event time now that ingest is stamped, or from the raw payload `E`/`T` fields); shard CPU. Write the budget: exchange → us (unavoidable), coalesce wait, persist, publish.
2. **The fix, biggest lever first**, with tests (unit + one testcontainer file, one invocation, `timeout 290`): e.g. flush the final kline immediately on `x=true` instead of on the next timer tick; publish to the Redis stream BEFORE the Postgres batch write (the strategy-worker consumes the stream; persistence can lag a little as long as the coverage proof stays correct — check T3.46 invariants: `covered_until` must never claim a minute not yet persisted); shorter batch timer; per-shard tuning. Never change candle semantics (`is_final`, `source`, coverage math) — prove with the existing coverage tests (`services/market-worker/tests/`) plus a new test that a final kline reaches the stream within X ms of its arrival in a fake-clock harness.
3. Docs: `docs/PIPELINE.md` §6b (flush budget line), `docs/EXCHANGE_INTEGRATION.md` (one paragraph), `.claude/state/notes-T3.81.md` with commands/outputs and the full `git status --porcelain` list of your files; research SQL `infra/scripts/sql/research/2026-09-10-t381-*.sql`.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no VPS writes; do not edit `services/strategy-worker/**`, `services/scanner-worker/**`, `apps/**`; keep files ≤ 350 lines; `ruff`/`pyright`/`check_file_size.py` clean. Report in Portuguese ≤ 10 lines with the measured budget, times in Brasília (UTC−3).

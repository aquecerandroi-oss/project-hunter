# Brief T4.1 — o adapter do pump.fun, SÓ fontes gratuitas (o provedor de RPC com chave/pago é decisão pendente do Everton; a interface deve aceitar um RPC com chave depois sem mudar os chamadores)

**Contexto:** `docs/plans/T4-MEME-RADAR.md` (T4.0 + revisão da Astra aplicada + adendo Mayhem), `.claude/state/notes-T4.0.md`, `.claude/state/notes-A4.1b-mayhem.md` (endpoints públicos `GET /coins/mayhem-mode?mayhemState=…` e `GET /mayhem/overview`; coin JSON com `mayhem_state` e `mayhem:{state,mode}`), `.claude/state/astra-review-t40-pumpfun.md` (feed de trades do PumpPortal é pago; taxa 1,25 %/trade; graduação = `real_token_reserves = 0` + `complete = true`), `docs/EXCHANGE_INTEGRATION.md`, `docs/DATABASE.md`, e a estrutura do adapter Binance em `packages/exchange-adapters/hunter_exchanges/` (modelos normalizados, fixtures gravadas, reconexão, rate limit). Um agente anterior deixou `packages/exchange-adapters/hunter_exchanges/pumpfun/{curve,decode,models,normalize,rest,ws}.py` sem testes nem notas — retomar a partir daí, consertando o que estiver errado.

**Entregar:**
1. Pacote `hunter_exchanges/pumpfun/`: `NormalizedMemeTokenCreated` (mint, name, symbol, uri, creator, created_at, conta da bonding curve, reservas virtuais iniciais, `mayhem_enabled`/`mayhem_mode` se vierem na mensagem), `NormalizedMemeMigration` (mint, pool, migrated_at), `NormalizedCurveState` (mint, reservas virtuais/reais de token e SOL, `complete`, market cap em SOL pela fórmula da curva com as constantes confirmadas ao vivo — 30 SOL / 1 073 000 000 tokens virtuais —, `mayhem_state`/`mayhem_mode` quando presentes, `observed_at`, `source`), e `NormalizedMemeTrade` definido SEM produtor (feed pago — documentar).
2. Cliente websocket do PumpPortal (`wss://pumpportal.fun/api/data`, `subscribeNewToken` + `subscribeMigration`; reconexão com backoff, heartbeat, dedupe por mint+assinatura) com fixture de mensagens REAIS capturadas ao vivo (conectar ≤ 60 s em primeiro plano, `timeout 120`; guardar o JSON cru em `packages/exchange-adapters/tests/fixtures/pumpfun/` com o carimbo da captura; se o socket não abrir desta máquina, dizer e derivar a fixture do esquema documentado nas notas do T4.0, rotulada como derivada de esquema).
3. Cliente REST `https://frontend-api-v3.pump.fun` para: estado da curva por mint (`/coins/{mint}`), listagem Mayhem (`/coins/mayhem-mode?limit=&mayhemState=`) e `/mayhem/overview`; token bucket de 60 req/60 s (medido); `observed_at`/`source` em toda leitura; fixtures de respostas reais (uma de cada), registrando o código HTTP se recusar.
4. Leitor RPC público da Solana (`getAccountInfo` da conta da bonding curve, decodificado com o layout usado por `chainstacklabs/pumpfun-bonkfun-bot` — citar arquivo/commit), como verdade on-chain das reservas; 10 req/s; URL injetável por `SOLANA_RPC_URL` (padrão: endpoint público da mainnet); nunca ler `.env*`, nunca colar chave.
5. Testes unitários sobre as fixtures (parse, normalização, fórmula da curva contra as constantes, dedupe, máquina de estados da reconexão) — sem testcontainers; arquivos ≤ 350 linhas; `uv run ruff check`, `uv run ruff format --check`, `uv run pyright` nos arquivos tocados e `uv run python infra/scripts/check_file_size.py` limpos.
6. Seção pump.fun em `docs/EXCHANGE_INTEGRATION.md` (fontes, limites, o que é grátis, o que espera a decisão do RPC), `.claude/state/notes-T4.1.md` com comandos/saídas e a lista `git status --porcelain` de TODOS os arquivos tocados.

**Fora do escopo:** tabelas/migrações (T4.2), wiring de serviço, execução. **Regras:** nunca shell em background; todo comando em primeiro plano com `timeout 290` explícito; árvore compartilhada — nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; nunca tocar `.env*`; nunca colar uma chave de API. Relatório em português ≤ 12 linhas, horários em Brasília (UTC−3, calcular).

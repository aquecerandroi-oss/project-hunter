# Brief T3.15e — fecha a revisão de segurança da T3.15d antes de commitar: cron de partições via `ops`, `cap_drop` em `ops`/`migrate`, preflight contra `DATABASE_URL_MIGRATIONS` no `.env`, `ops` construível, docs corrigidos

**Owner:** devops-engineer. **Reviewer afterwards:** security-reviewer (same reviewer). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*` (read names only); do not stop or recreate local stack containers; VPS read-only.** Base: `main` at `f6d222f` **plus the uncommitted T3.15d diff** in `infra/docker/docker-compose.yml`, `infra/vps/docker-compose.prod.yml`, `docs/{DEPLOYMENT,ACTIVATION,DATABASE}.md` (read `git diff` first; build on it).

## Findings to close (security review of the T3.15d diff)
- **HIGH-B (breaks production if deployed)**: the daily partitions cron (`infra/vps/README.md:174,195,218`, `docs/DEPLOYMENT.md:608-609,646`) runs `create_partitions.py` through the `api` service, which no longer has the owner DSN → the job would fail every day at 04:07 and month-end inserts would hit `no partition`. Fix: cron and runbook use `bash infra/vps/compose.sh run --rm ops python infra/scripts/create_partitions.py …` (and `prune_partitions.py`); update `infra/vps/README.md`, `DEPLOYMENT.md`, the crontab template in `infra/vps/**` if one exists; a test/lint that greps the docs and the cron template for `HUNTER_COMMAND=partitions` on `api`.
- **LOW-G**: `ops` and `migrate` get `cap_drop: [NET_RAW, NET_ADMIN]` like the workers.
- **MEDIUM-D**: `compose.sh` preflight refuses to run when the `.env` defines `DATABASE_URL_MIGRATIONS` (like the `CORS_ALLOWED_ORIGINS` preflight), with the message that the owner DSN belongs only to `migrate`/`ops` env anchors; `.env.example` loses that line or comments it as host/CI only.
- **MEDIUM-C**: `hunter_core/settings.py:71-73` — `database_url_migrations` default `localhost` → make it empty by default so its absence is a loud configuration error in every owner script (`migration_url()` already refuses empty); tests.
- **LOW**: `ops` in prod gets `build:` like `api` (or `compose.sh run` uses `--no-build` semantics consistently) so a fresh `GIT_SHA` never triggers a registry pull; fix the wrong comment "no counterpart in the base compose".
- **MEDIUM-E**: `DEPLOYMENT.md:207-210` sentence about who holds the credential rewritten truthfully (anyone with the Docker socket, via `ops`/`migrate`; the host `.env` does not carry it).
- **MEDIUM (stdin scripts)**: ACTIVATION §7 states that `python - < file` runs the working-tree file, not the image's; the canonical command is the image's script (`ops python infra/scripts/derive_variant.py …`) once the image ships it; stdin only as the documented fallback with the digest check.
- **HIGH-A** is out of this brief (a non-superuser login role for `DATABASE_URL`): it is T3.15f for database-architect; reference it in `DATABASE.md` §23.5 so the section stops claiming HIGH-1 is closed.

## Prove
`docker compose config` (fictional env, names only) for both files with all profiles showing `DATABASE_URL_MIGRATIONS` only on `migrate`/`ops` and `cap_drop` on `ops`/`migrate`; the preflight test; settings tests; report in Portuguese, extended format; append "T3.15e" to `.claude/state/notes-T3.15d.md`.

# Brief T3.21 — the Obsidian base gets Bases, callouts, a linter and canvases (what is worth taking from `AgriciDaniel/claude-obsidian`)

**Owner:** Sexta-feira on Hermes (she owns `obsidian/`), delegating the Python linter to `backend-specialist` skills if she prefers. **Reviewer afterwards:** documentation-writer (read). **Commits:** only `obsidian/**`, `infra/scripts/obsidian_lint.py`, its test, and `docs/OBSIDIAN.md` — per task, with her trailer. **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not touch `.env*`.** Base: `main` at `adc4cb9` or later. In flight elsewhere: T3.20 writes `obsidian/03-TRADING/Estrategias/**` and `infra/scripts/export_strategies_to_obsidian.py` — coordinate by reading its README when it lands; do not edit those files.

## Decision (orquestrador, 2026-09-08, on Everton's ask "pega o que for necessário e embelezamento")
Do **not** install the plugin: writes are unsupported on Windows/Git Bash (WSL only) and it imposes its own vault layout (`inbox/`, `.raw/`, `wiki/`). Take four ideas and adapt them to our base. Read the two reference skills of that repo **read-only** (clone into `.claude/state/tmp/claude-obsidian/` and delete afterwards): `skills/obsidian-markdown/SKILL.md` and `skills/obsidian-bases/SKILL.md`; also skim `skills/wiki-lint/SKILL.md` and `skills/canvas/SKILL.md` for the checks and the canvas format. Never run its scripts.

## Deliver
1. **Frontmatter standard** for every note in `obsidian/` (`docs/OBSIDIAN.md` §1): `status`, `owner`, `updated` (ISO date), `tags`, plus per folder: EXP pages `exp`, `strategy`, `version`, `result` (`inconclusivo|validada|reprovada`), `evaluable`, `days`; strategy pages (T3.20 already emits theirs — do not change them, list them); bugs `severity`, `opened`, `closed`; decisions `decided_on`, `by`. Backfill the existing notes (append missing keys; never rewrite bodies).
2. **Two Bases** (Obsidian native `.base` files, format from the reference skill): `obsidian/05-EXPERIMENTS/Experimentos.base` (table + cards: EXP, estratégia, versão, resultado, avaliáveis/dias vs 100/30, última avaliação) and `obsidian/03-TRADING/Estratégias.base` (versão, propósito, status, veredito, replicação). Link both from `00-HOME.md`.
3. **Callouts standard**: `> [!veredito]`, `> [!decisao]`, `> [!alerta]`, `> [!medido]` (numbers pasted from SQL) with a CSS snippet under `obsidian/.obsidian/snippets/hunter.css` if the folder is a real vault root (check `obsidian/.obsidian` — if it does not exist, put the snippet in `docs/OBSIDIAN.md` for Everton to install by hand and say so). Apply the callouts to the last three diary entries and the EXP-0005 page as examples.
4. **Linter** `infra/scripts/obsidian_lint.py` (+ unit test with fixtures): dead `[[links]]`, orphan notes (no inbound link), missing frontmatter keys per folder, EXP pages whose earlier `### Avaliação de` sections changed since the last commit (`git diff` on the section headings — append-only rule), notes in `11-KNOWLEDGE` without `source`/`lido_em`. Exit code 1 with a Portuguese report. Add the line "rodar `uv run python infra/scripts/obsidian_lint.py`" to the plantão skill? **No** (that file is T3.15c/T3.15e territory today) — write it in `docs/OBSIDIAN.md` §"Plantão" and in `.claude/state/plantao.md`; the skill line is a one-line follow-up.
5. **Two canvases**: `obsidian/01-ARCHITECTURE/Fluxo sinal → carteira.canvas` (market-worker → scanner → strategy-worker (Lab) → bridge → admission → execution-worker → wallet, each node linking its module page) and `obsidian/03-TRADING/Família momentum.canvas` (v1 deprecated → v2 research → v3 paper → siblings when they exist).
6. **Knowledge template**: `obsidian/11-KNOWLEDGE/_TEMPLATE-NOTE.md` gains `confiança` (`anedótico|backtest do autor|estudo revisado|replicado`) and `lido_em`; existing KB notes get the keys backfilled from their body where the value is explicit, else `confiança: ?` (never guessed).
7. Run the linter on the whole base and fix what it finds; paste the before/after counts in the notes.

## Prove
Linter output before/after; `uv run pytest infra/scripts/tests/test_obsidian_lint.py -q`; `git status -sb` before writing STATUS. Report in Portuguese, extended format; `.claude/state/notes-T3.21.md`; commit `obsidian/**` + linter + `docs/OBSIDIAN.md` with `docs(obsidian): T3.21 — Bases, callouts, linter e canvases` and the trailer `Co-Authored-By: Sexta-feira <sexta-feira@project-hunter.local>`.

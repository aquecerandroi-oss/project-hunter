# Brief T4.16 — a porta v2 (fluxo e holders) e o relógio de 15 s nos primeiros 5 minutos (Everton, 12/09/2026 14:0x BRT: "aparecendo bastante proposta mas estamos perdendo todas; não está analisando direito? estamos muito lentos? analisa o caso")

**Diagnóstico medido (21 apostas de papel de 12/09, `.claude/state/...` consulta `q_case.sql`; números no relatório do orquestrador às 14:0x BRT):**
1. **Artefato:** 5 saídas `rug_no_snapshot` (−1 R cada = −5 dos −7,67 R do dia) fecharam a zero por falta de fotografia por 3 min; a moeda não morreu (mcap 30 min depois = mcap da entrada). Desde 12:14 (curva pela cadeia) não ocorre mais. Sem o artefato: 16 apostas, −2,7 R (−0,17 R/aposta ≈ taxa).
2. **Entrada:** em 17/21 a moeda nunca subiu após a entrada (mcap_in = máx 30 min); entramos em moedas no valor inicial (~28 SOL de mcap) com 1–7 holders; as sondas de hype entraram com 44–53 compras/min **e** 32–48 vendas/min (giro), não demanda líquida.
3. **Latência:** decisão → fill 5–148 s (mediana 27 s) e a porta lê só minutos fechados (`end_time ≤ agora − 1 min`): idade na entrada 99–289 s; o dev despejou em 5/8 sondas antes de 2 min.

**Entregar (motor de papel; pré-requisito: T4.11 e T4.14 commitados — mesmo motor):**
1. **Placar honesto:** `rug_no_snapshot` deixa de contar como −1 R: vira `indeterminate` (coluna `outcome_quality` em `meme_paper_bets`: `measured` \| `indeterminate`), excluído das somas de R/PnL e contado à parte no placar, na aba Testes (rótulo "indeterminado (sem fotografia)") e no fechamento diário; apostas passadas reclassificadas por script auditado (`--apply --reason`), nunca reescritas à mão.
2. **Relógio de 15 s para moedas jovens:** o laço `meme-chain` lê a curva das moedas com idade < 5 min a cada 15 s (RPC público: 100 req/10 s; 130 mints = 2 chamadas por leitura → 8 chamadas/min, dentro do limite) e grava `meme_curve_snapshots` (`source='solana_rpc'`); as demais continuam a 60 s. A porta passa a rodar **em cada fotografia** para moedas < 5 min (features "instantâneas" calculadas sobre a série de fotografias dos últimos 60/120 s: `mcap_delta_60s`, `mcap_slope_60s`, e a fita quando existir), e o fill acontece na fotografia seguinte (15 s), não no minuto seguinte. Não-antecipação: a porta só usa fotografias com `received_at <= agora`; teste de look-ahead obrigatório. `meme_features_1m` continua sendo a série oficial por minuto; as instantâneas ficam em `meme_features_15s` (RANGE mensal, retenção 7 d) — declarado como série separada, versão `meme_features_15s_v1`.
3. **Porta v2 (conjuntos novos, `research_only`, pré-registrados como EXP-M5 "fluxo e holders"):** `flow_v2/1` — idade 30–300 s; `net_sol_flow_1m > 0` (ou `mcap_delta_60s > 0` nas instantâneas); `unique_buyers_1m ≥ 10`; `sells_1m / buys_1m ≤ 0,6`; holders crescendo em duas leituras seguidas (do board ou risco); `curve_progress_pct ≥ 5 %` e subindo; `snipers ≤ 2`; `dev_share ≤ 0,10`; criador não vendedor líquido (desconhecido recusa); participação ≤ 1 %; tamanho 0,05 SOL; saídas: alvo 3×, trailing 35 % após 1,5×, `max_hold_s` 1 800, `creator_dump`, `line_broken` quando houver linha, `max_loss` 50 %. `hype_probe_v0/2` — a sonda com as mesmas condições de fluxo (e `sells/buys ≤ 0,6`) no lugar do `hype_score` sozinho. **Aposentar** `meme_paper_v0/1` (veredito da EXP-M1: `descartar`, 9/9 negativas + artefato) e `hype_probe_v0/1` (EXP-M3: descartar), com `--deprecate` auditado e a avaliação append-only nas páginas EXP-M1/EXP-M3.
4. Heartbeat: `fast_lane_mints`, `fast_lane_reads_60s`, `decision_to_fill_s_p50/p95` (medido!), `lab_bets_indeterminate_total`; `/meme/sources` e a mesa mostram `decision_to_fill_s` por aposta (a aba Testes já tem `fill_delay`).
5. Testes unit + 1 testcontainer; `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; docs `docs/plans/T4-MEME-RADAR.md` §T4.16, `docs/DATABASE.md`, `docs/RISK_ENGINE_MEME.md` §10 (papel: fill na fotografia de 15 s); `pnpm gen:types`; `.claude/state/notes-T4.16.md` incremental; `git status --porcelain` ao final.

**Regras:** as de sempre (primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; sem commit; sem `.env*`; nada real; sem tocar `apps/web/**` além de rótulos listados nas notas; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit). Relatório ≤ 12 linhas em português, horários em Brasília.

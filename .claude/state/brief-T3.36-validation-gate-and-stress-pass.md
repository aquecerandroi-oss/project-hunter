# Brief T3.36 — o funil de validação ganha um portão antes do código (C1–C8) e uma passada de estresse depois do replay; toda estratégia declara o seu controle (método trazido de claude-trading-skills, 2026-09-08)

**Owner:** quant-engineer. **Reviewers afterwards:** code-reviewer, Sexta-feira (Obsidian). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` after the commit that adds `.claude/skills/{backtest-expert,edge-strategy-reviewer}` (reference only, MIT, TraderMonty). In flight: T3.18c (`packages/indicators/hunter_indicators/replication/**`, `apps/api/**`), T3.32/T3.33/T3.34 (`.claude/state/exp-drafts/**`, briefs), T3.29, T3.31. Your write scope: `docs/plans/SHADOW-LAB.md`, `docs/plans/REPLICATION.md`, `obsidian/05-EXPERIMENTS/_TEMPLATE-EXP.md` (**only** this file under `obsidian/**`), `services/strategy-worker/hunter_strategy_worker/replay/stress.py` (new) + `replay/run.py` (flag only), `packages/indicators/hunter_indicators/replay/**` if the exit policies need a cost multiplier, tests, `.claude/state/notes-T3.36.md`.

## Source (read; take the method, not the scripts — they are for US equities with FMP/Alpaca)
`.claude/skills/edge-strategy-reviewer/references/review_criteria.md` + `overfitting_checklist.md` (C1 edge plausibility, C2 overfitting via condition count and precise thresholds, C3 sample adequacy, C4 regime dependency, C5 exit calibration, C6 risk concentration, C7 execution realism, C8 invalidation quality; PASS/REVISE/REJECT), `.claude/skills/backtest-expert/references/methodology.md` ("break the least": execution-friction tests, parameter robustness ±25–50 %, time-based robustness, regime split, sample-size guidelines, pitfalls) and `failed_tests.md`. Our own rules stay authoritative where they conflict: SHADOW-LAB §19 metrics, 100 outcomes and 30 days, replay never counts for the verdict (D14/D15), Decimal, no look-ahead.

## Deliver
1. **Portão C1–C8 antes do código** — `_TEMPLATE-EXP.md` gains a frozen section "Portão de desenho (C1–C8)" with the eight criteria adapted to us (in Portuguese; C6 "risk concentration" becomes the paper_v1 preset limits; C7 "execution realism" = spot-executable, 50 M USDT floor, entry delay; C8 = invalidation rule is not the momentum one unless argued) and the rule: **REJECT if entry has > 8 conditions or any threshold with more than 2 significant digits without a stated reason; REVISE if expected frequency < 30 opportunities/year/market on the reference universe**. The verdict of the gate is written by the quant in the EXP before implementation, and code-reviewer checks it exists.
2. **Passada de estresse depois do replay** — `replay/stress.py`: given a replay cohort (its frozen entries), re-price outcomes under (a) costs ×2 (spread/slippage/fee), (b) stop ×0,75 and ×1,25, (c) target ×0,75 and ×1,25, (d) entry delay +1 bar, (e) each market left out (leave-one-out), (f) first half vs second half of the window; output a table (expectancy, PF, n) per scenario and the verdict `robusto | frágil a custos | frágil a parâmetros | dependente de um mercado | dependente de metade`. **No re-run of the strategy** (entries are frozen; only outcomes are re-priced — say where that is impossible, e.g. entry delay, and then re-walk only the outcome). Receipt in `replay_runs` with `kind=stress` (if the model allows a kind column; otherwise a JSON ledger next to the cohort and a note that the schema needs a column — brief for database-architect, do not edit migrations).
3. **Controle declarado** — REPLICATION.md §"Controle": every EXP names a predeclared baseline (default: "comprar os mesmos mercados nos mesmos horários com stop/alvo/horizonte iguais, sem o filtro de entrada" = same exits, random-timing entries at the same frequency, seeded) and the replay can run it as cohort `control:<uuid>`; the strategy's edge is reported **as difference vs control** (expectancy Δ with the bootstrap CI already in `hunter_indicators.replication.bootstrap`). Implement the control walker only if `replay/run.py` can accept an "entry override" cheaply; otherwise specify it precisely for T3.19f.
4. **Docs**: SHADOW-LAB §"Funil de validação" = portão C1–C8 → implementação → replay 31 d (mata no dia um) → estresse → prospectivo → replicação; one paragraph on what each stage can and cannot say.
5. Tests: `stress.py` unit tests on a synthetic ledger (costs ×2 flips a marginal strategy; leave-one-out finds the single-market dependence); the template lint (`obsidian_lint.py`) still green.

## Prove
Per-file tests with real output, `ruff`/`pyright`/`check_file_size.py`; run the stress pass once on the VPS over `replay:f8d8279c…` (momentum v2) and paste the table; report in Portuguese, extended format; `.claude/state/notes-T3.36.md`.

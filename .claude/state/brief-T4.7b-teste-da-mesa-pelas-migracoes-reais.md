# Brief T4.7b — `test_meme_desk_repository.py` deixa a DDL congelada e passa a usar as migrações reais (12/09/2026 16:0x BRT)

**Problema:** `apps/api/tests/integration/test_meme_desk_repository.py` (T4.7) carrega a sua própria DDL literal (contrato §Tabelas em `0022`) e ficou para trás das migrações `0024`–`0031` (colunas novas em `meme_paper_bets`/`meme_proposals`, vistas recriadas, `mode`, `mark_source`, `outcome_quality`…); falha hoje e falhará a cada migração. O `xfail(strict=False)` `test_http_approve_against_the_real_migration` já mostra o caminho certo: Alembic até `head` via `apps/api/tests/integration/conftest.py`.

**Entregar:**
1. Reescrever o módulo para obter o esquema **só** pelas migrações reais (a fixture do `conftest.py` que sobe Alembic até `head`; se não houver uma reutilizável para "banco próprio dentro do `postgres_container`", criar uma **aditiva** no `conftest.py`, sem mudar as existentes). Apagar toda DDL literal do arquivo; manter os cenários (aprovar/recusar/manual/sell-now/cancel, idempotência, `audit_logs`, `real_observed`, `mode`), atualizando as inserções de semente para as colunas atuais (`NOT NULL`/CHECKs das 0022–0031: ler `infra/migrations/ddl/meme_lab*.py`, `meme_boards_guards.py`, `meme_live.py`, `meme_moonshot`, `0030_meme_gate_v2.py`, `0031_meme_lab_ticks.py`). O `xfail` deixa de existir (o teste passa de verdade).
2. Rodar: `HUNTER_MIGRATIONS_DIR` como os outros testcontainers do repositório fazem; `timeout 590 uv run pytest apps/api/tests/integration/test_meme_desk_repository.py -q` verde; `ruff check`/`ruff format --check`/`pyright` no arquivo; `check_file_size.py` (≤ 350 linhas por arquivo — dividir em `test_meme_desk_repository_{a,b}.py` se preciso, com o helper de semente em `apps/api/tests/integration/meme_desk_seed.py`).
3. `.claude/state/notes-T4.7b.md` curto; `git status --porcelain` ao final.

**Regras:** só tocar `apps/api/tests/integration/test_meme_desk_repository*.py`, `apps/api/tests/integration/meme_desk_seed.py` e (aditivo) `apps/api/tests/integration/conftest.py`; **nada** em `apps/api/hunter_api/**`, `services/**`, `infra/migrations/**` (outra tarefa, T4.16, está editando a árvore ao mesmo tempo — se uma migração no disco parecer inconsistente, relate em vez de corrigir); primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; sem commit; sem `.env*`; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 8 linhas em português.

# Brief T3.20 — every strategy version has its page in Obsidian, with every parameter

**Owner:** backend-specialist (the exporter script) + documentation-writer skills (the page template). **Reviewer afterwards:** Sexta-feira on Hermes (she owns the base). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; no testcontainers in this task (four other agents are using Docker) — unit-test the renderer with fixtures and dry-run the exporter against the VPS read-only; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not touch `.env*`.** Base: `main` at `b52f23f`. In flight elsewhere: T3.0f, T3.15c, T3.18 (API lab), T3.19 (replication: `packages/indicators/hunter_indicators/replication/**`, `infra/scripts/replicate_strategy_version.py`, `docs/plans/REPLICATION.md`) — do not edit those paths; read `docs/plans/REPLICATION.md` if it already exists for the cohort naming of siblings.

## Everton's ask (2026-09-08, verbatim)
"lembra todas estrategias vao ficar no obsidian esse é o diferencial gravar cada detalhe cada diametro".

## Read first
`obsidian/00-HOME.md`, `obsidian/05-EXPERIMENTS/*.md` (EXP pages: frozen Hypothesis/Protocol + dated evaluations, append-only), `obsidian/11-KNOWLEDGE/Strategy Backlog.md` and `_TEMPLATE-NOTE.md`, `obsidian/03-TRADING/*.md`, `.claude/agents/sexta-feira.md` ("The Obsidian base is your memory graph", "The Shadow Lab shift"), `packages/core/hunter_core/db/models/agents.py` (`strategies`, `strategy_versions`: parameters_schema, default_parameters, params_format, code_ref, purpose, status, activated_at, deprecated_at, changelog), `services/strategy-worker/hunter_strategy_worker/{catalogue,record}.py` (cohorts, params_hash), `infra/scripts/activate_strategy_version.py` (read only), `.mcp.json` (the `vault/` MCP is a different vault; this task writes `obsidian/**` files directly, as the plantão does).

## Deliver
1. **Convention** (`obsidian/03-TRADING/Estrategias/README.md`): one page per `strategy_version` at `obsidian/03-TRADING/Estrategias/<key>-<version>.md` (e.g. `momentum-v2.md`, `momentum-v3-paper.md` when purpose = paper, `momentum-v2-irma-03.md` for replication siblings). Frontmatter: `strategy`, `version`, `purpose`, `status`, `code_ref`, `params_hash`, `activated_at`, `deprecated_at`, `derived_from` (parent version page link), `cohorts` (list), `exp` (links to EXP pages), `updated`. Body sections, in this order: **Parâmetros** (a table of *every* parameter: name, value, the schema's type/bounds/description — "cada diâmetro"), **Origem** (changelog, who/when: the `system_events` rows of the activation script for this version), **Coortes e sinais** (counts per cohort from `agent_signals`), **Avaliações** (links to the dated evaluations in the EXP page — the EXP page stays the record; this page never duplicates numbers, it points), **Replicação** (status block from the T3.19 contract when present, else "não iniciada"), **Ligações** (`[[...]]` to the strategy family page, the parent, the siblings, the EXP, the Risk Engine page when purpose = paper). Plus one family page per `strategies.key` (`momentum.md`) listing its versions with purpose/status/verdict and linking each.
2. **Exporter** `infra/scripts/export_strategies_to_obsidian.py`: reads the database read-only (`DATABASE_URL` app role is enough — SELECT only), renders the pages from a template, writes only the **generated block** of each page between `<!-- generated:start -->` and `<!-- generated:end -->` markers so the Sexta-feira's hand-written notes below the block survive; creates missing pages, never deletes; `--dry-run` prints the diff; `--vps` mode is just the same script run inside the API container on the VPS with the output copied back (`docker cp`) — document the two commands in the README. Deterministic output (sorted keys, no timestamps except `updated`), so two runs produce no diff.
3. **First real run**: generate the pages for the versions that exist today (local if the stack is up; otherwise on the VPS read-only: momentum v1/v2/v3-paper, volume_anomaly v1/v2, the six drafts). Paste the resulting `git status --short obsidian/` and one full page (momentum-v3-paper) in the notes.
4. **Plantão hook**: add one line to `infra/hermes/skills/project-hunter/sexta-feira-plantao/SKILL.md`? **No** — that path belongs to T3.15c right now; instead write the plantão step in `obsidian/03-TRADING/Estrategias/README.md` ("a cada plantão: rodar o exportador, conferir o diff, acrescentar links e avaliações") and note in `notes-T3.20.md` that the skill line is a one-line follow-up.
5. **Tests**: unit tests for the renderer (fixtures: a version with 14 parameters, a paper line, a sibling with cohort) and for the marker-preserving writer (hand-written text below the block survives; idempotent second run). `ruff`/`pyright`/`check_file_size.py`.

## Prove
Report in Portuguese, extended format (STATUS · FILES · TESTS with real output · the real generated page · CONCERNS); `.claude/state/notes-T3.20.md`. `obsidian/**` files generated are part of the deliverable (the orchestrator commits them with the script).

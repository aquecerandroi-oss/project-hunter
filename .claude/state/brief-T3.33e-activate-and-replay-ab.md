# Brief T3.33e — breakout_v1 e mean_reversion_v1 entram no Lab: ativação auditada (research_only), replay de 31 dias no dia um, avaliação contra os critérios de descarte congelados

**Owner:** quant-engineer. **Reviewers afterwards:** code-reviewer (receipts), Sexta-feira (EXP filing). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (slice replays into ≤ 4-min windows with an explicit `--cohort`, as T3.19b/T3.26 did); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not touch `.env*`; do not stop or recreate containers.** Base: `main` at `b5d4f9b`; the VPS runs image `b5d4f9b` for api/web/strategy-worker (modules `breakout_v1`/`mean_reversion_v1` verified importable in `hunter-strategy-worker-1`). Authorised by Everton (2026-09-08: "pode fazer o lab tudo autorizado"): activation of **research_only** versions only, via the audited script; never `paper`, never `agents`, never flags. Everything else on the VPS is read-only.

## Commands (exact; from `.claude/state/notes-T3.33a.md` / `notes-T3.33b.md` §"Sequência para o operador")
On the VPS (`ssh hunter-vps`, `cd /opt/project-hunter`), inside `hunter-api-1` (has `DATABASE_URL_MIGRATIONS`):
1. `docker exec hunter-api-1 python infra/scripts/seed.py --dry-run` then without dry-run only if it reports the `breakout`/`mean_reversion` rows need the new description (the keys already exist as `draft`).
2. `docker exec hunter-api-1 python infra/scripts/activate_strategy_version.py breakout v1 --changelog "T3.33e: EXP-0008 entra no Lab como pesquisa" --dry-run` — the dry-run **must** print `…breakout_v1@sha256:4c920b0cc412429c2c4a6a19ca389aca8a215a638f0ff750a8caf61b16264ff1`; if it prints anything else, stop and report (the image is not this code). Then the same without `--dry-run`.
3. Watch `hb:strategy` (heartbeat) and `shadow_evaluations_total` for 10 minutes after activation (T3.33 concern 6: capacity; the paper line `momentum v3` must keep being evaluated first — T3.26c ordering) — paste the numbers. Only then activate `mean_reversion v1` the same way (digest `…mean_reversion_v1@sha256:a970c9d98fface2d714abdce25468828ee07087f9287fdb1239dadc3f0bd395f`).
4. Replays, one cohort per strategy, two contiguous slices each, ETH/SOL/XRP/DOGE, `--workers 3`: `docker exec hunter-strategy-worker-1 python -m hunter_strategy_worker.replay.run --version breakout:v1 --from 2026-08-08 --to 2026-08-23 --markets ETHUSDT,SOLUSDT,XRPUSDT,DOGEUSDT --workers 3 --cohort replay:<uuid> --ledger /tmp/replay-breakout-v1-a.jsonl` and `--from 2026-08-23 --to 2026-09-08` with the **same** cohort; then the same for `mean_reversion:v1`. Paste receipts verbatim (`replay_runs` rows, bars, seconds, errors).
5. Evaluation on day one, per EXP (read-only SQL, `/tmp/q7.sql` shape + `infra/scripts/sql/research/2026-09-08-03-custo-vs-edge.sql`): evaluable, expectancy (gross and net), PF, hit rate, distinct days/markets, `unavailable`/`ineligible`/`rejected` shares, and the EXP-specific columns — breakout: decomposition by `squeeze_ratio` bucket and the band `0,0050 ≤ ATR% < 0,0059` (count + expectancy; reviewer's condition); mean_reversion: `atr_gap`/`trend_gap` shares and z-score depth buckets. Apply the frozen kill criteria K1–K5 of each EXP and say which fired. Verdict per version: `inconclusivo` (30-day prospective rule), plus **`descartar`** recommendation if a kill criterion fires.
6. Update `.claude/state/exp-drafts/EXP-0008-*.md` and `EXP-0009-*.md` with a dated "Avaliação (replay, dia um)" section (label REPLAY), and `Registro de Tentativas` start dates = activation timestamps (write them in the notes for Sexta-feira, do not edit `obsidian/**`).

## Prove
Every command with its real output, receipts, SQL and tables; report in Portuguese, extended format; `.claude/state/notes-T3.33e.md`.

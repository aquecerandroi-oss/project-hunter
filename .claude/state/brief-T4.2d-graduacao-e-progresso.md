# Brief T4.2d — o que "graduou" quer dizer, o denominador do progresso e a cegueira declarada do radar (12/09/2026 06:1x BRT)

**Pré-requisito:** T4.2c commitado (trenches + swap-api + heartbeat). Ler `.claude/state/notes-T4.2c.md` antes; depois `.claude/state/plantao-meme/2026-09-12-0547-lane1.md` (run 5) e `2026-09-12-0429-lane3.md` (run 3), `.claude/state/astra-review-plantao-meme-20260912-0547.md` (must-fixes: eventos separados; reserva zero não classifica), `.claude/state/notes-T4.2.md` §contrato, `docs/PUMPFUN.md` §4.2.

**Fatos medidos que motivam a tarefa:**
1. `complete = true` da REST **não é graduação**: das 140 "completas" de 02:18–05:48 BRT, 59 tinham `real_sol_reserves` 85,005 e 77 tinham **0** (47 Mayhem); 72/140 não estavam no board `graduated`; nas 68 comuns, `real_sol = 0` em 31 (inclusive graduações reais — pós-migração a reserva sai da curva). No run 1 eram 149/150 com 85,005. Hoje o radar grava um booleano e `meme_tokens.completed_at` a partir dele.
2. Em produção (06:04 BRT) o portão do Lab recusa `progress_unknown` em 117/123 linhas: `meme_tokens.initial_real_token_reserves` só é escrito quando a **primeira** observação tem `real_sol_reserves = 0` (curva virgem); um mint descoberto depois da primeira compra nunca ganha denominador → progresso nulo para sempre.
3. O radar só vê o programa `pump` (`subscribeNewToken` + `is_in_scope_pool`); 8/50 do board `new` às 05:51 eram `raydium_launchpad` (StonkFun) — 16 % invisíveis por construção. Isso tem de estar **declarado num painel**, não escondido.

**Entregar:**
1. **Quatro indicadores separados de conclusão** em `meme_tokens` (migração `0024_meme_graduation`, DDL literal como as anteriores, unions em `test_migrations.py`/`test_schema_privileges.py`): `rest_complete_seen_at` (1.ª fotografia REST com `complete = true`), `curve_filled_seen_at` (1.ª fotografia com `real_sol_reserves ≥ 85 SOL` — limiar derivado de `/global-params`, não constante mágica), `graduated_board_seen_at` (1.ª presença no board `graduated` do indexer, T4.2c), `pool_created_at` (o `gd` do indexer ou o evento `migrate` do PumpPortal — o que vier primeiro, com `source`). `completed_at` (existente) passa a ser **a mais antiga das quatro** e nunca é escrito só por `complete = true` com `real_sol = 0` (Astra: reserva zero não classifica). Vista `meme_graduation_matrix_v1`: por dia Brasília, a matriz de concordância dos quatro (quantos mints têm 1, 2, 3, 4 sinais; quais pares discordam) — é o diagnóstico M-D1/M-D2 virando painel.
2. **Denominador do progresso**: `initial_real_token_reserves` passa a vir de `/global-params/{ts}` (`initial_real_token_reserves` 793,1 M para o padrão; Mayhem tem 1 B extra — ler o campo certo do estado da curva/`Global` e documentar) quando a primeira observação já tem `real_sol > 0`; guardar `progress_denominator_source` (`observed_virgin` \| `global_params` \| `unknown`). Recalcular `curve_progress_pct` nas features novas (não reescrever as antigas: append-only; documentar a quebra de série com `features_version` novo).
3. **Cegueira declarada**: contador por hora de entradas do board `new` cujo `pl`/`pg` ≠ pump (T4.2c grava `meme_board_observations`) e a fração; expor em `GET /meme/sources` como `discovery_blind_share_1h` com a explicação "programa fora do escopo do adaptador"; heartbeat `blind_share_1h`. Nenhuma tentativa de rastrear StonkFun/raydium — fora de escopo até decisão do Everton.
4. **Tela**: em `/meme` (faixa de visão geral) e `/meme/{mint}` (marcas de eventos), usar os quatro sinais com rótulos em português ("REST diz completa", "curva cheia (≥ 85 SOL)", "no board graduated", "pool criada") e mostrar discordâncias em âmbar; o estado `completed` do filtro passa a ser `completed_at` (a mais antiga). `apps/web` só nessas duas telas; `pnpm gen:types`.
5. Testes (unit + 1 testcontainer sobre a migração real), `ruff`/`pyright`/`check_file_size.py`, docs (`docs/DATABASE.md` §36, `docs/PUMPFUN.md` §4.2 "o que graduar quer dizer para nós", `docs/plans/T4-MEME-RADAR.md` §T4.2d), `.claude/state/notes-T4.2d.md`.

**Regras:** as de sempre — tudo em primeiro plano com `timeout 290` (590 para testcontainer, um por vez); nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; nunca `.env*`; nunca chave; nada real; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Edit/Write. Relatório ≤ 12 linhas em português, horários em Brasília (UTC − 3).

# Brief T4.2f — cobertura total: a curva de TODOS os rastreados por RPC (getMultipleAccounts) e o limite real do swap-api (12/09/2026 10:05 BRT)

**Medido em produção depois da T4.2e** (VPS `8478eef`, 10:00 BRT, `.claude/state/notes-T4.2e.md` §"Medição do orquestrador"): `progress_coverage_pct` 43,3 e `tape_coverage_pct` 38,8. Razão dominante de progresso ausente: **`not_polled` 1 267 linhas/15 min** — o poll REST (`/coins/{mint}`, 60 req/min) não alcança 130 mints por minuto; o denominador já não é o gargalo (213). Razão dominante de fita ausente: **`rate_limited` 1 069 linhas/15 min** com ~238 req/min usadas — o limite real do `swap-api` é ≈ 240/min por IP, não os 1 000/60 s documentados, e as páginas extras dos mints quentes gastam o resto. Enquanto isso o portão da EXP-M1 recusa `progress_unknown` 74 e `curve_volume_1m_unknown` 70 por tick.

**Leia antes:** `.claude/state/notes-T4.2e.md`, `.claude/state/notes-T4.2c.md` (tape policy, boards), `services/meme-worker/hunter_meme_worker/{collect,tracker,trades,fold,sources,config,repo}.py`, `packages/exchange-adapters/hunter_exchanges/pumpfun/{rpc,decode,swap_api,rate_shared}.py` (T4.0d/T4.8: `getAccountInfo` + decodificador da curva pela IDL; T4.2c: swap_api), `docs/PUMPFUN.md` §2 e §5, `docs/PUMPFUN-ONCHAIN.md`.

**Entregar:**
1. **Fotografia da curva de todos os rastreados, uma vez por minuto, pelo RPC público em lote:** `getMultipleAccounts` (100 contas por chamada; 2 chamadas para 130 mints; o RPC público aguenta ~10 req/s e 40 chamadas/método/10 s — medir e respeitar), decodificando `BondingCurve` pela IDL (reservas virtuais/reais, `complete`, `creator`), gravando `meme_curve_snapshots` com `source='solana_rpc'`, `slot`, `commitment`, `observed_at` = blockTime do slot se disponível (senão `received_at` com a marca), e alimentando o mesmo caminho da T4.2d (sinais de conclusão, denominador). O poll REST fica só para identidade/mayhem/fallback (1 leitura por mint novo + leitura final), liberando os 60 req/min. Meta: `progress_coverage_pct ≥ 90` (o que faltar é `denominator_unknown` Mayhem histórico ou `unsupported_quote`, nomeado).
2. **Limite real do swap-api:** medir os cabeçalhos (`x-ratelimit-*`/`retry-after`) na primeira 429 e adaptar o bucket ao valor observado (config `MEME_SWAP_API_BUDGET_60S` com default = o medido, documentado com a captura); **1 página por mint por minuto** (as 100 negociações mais recentes bastam para o minuto; paginação só para apostas abertas); prioridade aposta aberta > `graduating` > jovem > resto; `tape_reason` honesto (`rate_limited` só quando houve 429; `not_polled` quando o orçamento não alcançou). Meta: `tape_coverage_pct ≥ 80` com 130 rastreados.
3. **Prova:** `infra/scripts/sql/research/2026-09-12-t42f-cobertura.sql` (pode reutilizar o da T4.2e) + os campos do heartbeat; o orquestrador mede antes × depois na VPS (o agente não acessa a VPS).
4. Testes unit (decodificação em lote com fixture real capturada — ≤ 10 chamadas RPC ao vivo, sem chave; bucket adaptativo; 1 página/min) + 1 testcontainer sobre a cadeia real; `ruff`/`ruff format --check`/`pyright`/`check_file_size.py` (≤ 350 linhas); docs `docs/PUMPFUN-ONCHAIN.md` (§leitura em lote), `docs/PUMPFUN.md` §2 (limite real medido), `docs/plans/T4-MEME-RADAR.md` §T4.2f, `docs/DEPLOYMENT.md` (env nova); `.claude/state/notes-T4.2f.md` com comandos e saída real; ao final `git status --porcelain` de tudo que tocou.

**Regras:** tudo em primeiro plano com `timeout 290` (590 para testcontainer, um por vez); nunca `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; não commitar; nunca `.env*`; nunca chave; nada real; não tocar `lab*.py`/`paper_engine.py`/`proposals.py` além de ler, nem `apps/web/**`; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília (UTC − 3).

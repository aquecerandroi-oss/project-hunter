# Brief T3.60 — a família mean_reversion como UMA carteira sob o motor de risco real: quantos R únicos por dia sobram depois de concorrência, risco agregado e kill switch; drawdown máximo; e a tabela que Everton pediu ("quero tentar ganhar R$9 mil no dia": quanto vale 1 R × quantos R por dia)

**Owner:** risk-engine-guardian (with quant help for the ledger). **Read-only research: no limit change, no code in packages/risk-core, no commit.** **Operational rule: never a background shell; every command foreground with explicit `timeout 290`; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; VPS strictly read-only (`repeatable read read only`).** Base: `main` at HEAD. Write scope: `.claude/state/exp-drafts/t360/` (scripts + CSV), `infra/scripts/sql/research/2026-09-09-t360-*.sql`, `.claude/state/proposta-carteira-2026-09-09.md` (the document for Everton), `.claude/state/notes-T3.60.md`.

## Facts
- Today (09/09, until 15:35 BRT) the mean_reversion family closed 164 operations, +39,0 R, 60 % hits, but only **38 distinct bets** (market × hour) and **100 open** at once — eight versions of the same idea entering the same trades (T3.53 measured the family pooling ratio 3,6–3,8×).
- `packages/risk-core/hunter_risk/limits.py` `PAPER_V1`: `risk_per_trade_pct 0,0025`, `max_aggregate_planned_risk_pct 0,01`, `max_concurrent_positions 5`, `max_stop_distance_pct 0,03`, `max_participation_pct 0,01`, daily loss / kill-switch limits (read them). T3.48: participation binds in 164/176 markets → real risk 0,0067 %/op (R$6,70 per R) vs the 0,25 % label (R$250).
- Ledger: `signal_outcomes` + `agent_signals` (prospective since 08/09; replay cohorts 31 d × 4 markets per version) — `r_multiple`, entry/exit ts, market, version.

## Deliver
1. **Portfolio simulator** (pure Python, Decimal, in `t360/`): replays the family's signals as ONE portfolio in time order: dedupe same market within the same bar/hour (first version wins, say which), apply `max_concurrent_positions` (5 / 10 / 20), `max_aggregate_planned_risk_pct` (1 % / 2 % / 4 %), `risk_per_trade_pct` (0,25 % / 0,5 % / 1 %), the daily loss stop and kill switch as coded in `hunter_risk` (import and call the real check functions where possible — `checks.py`, `exposure.py` — never reimplement thresholds), and the participation cap with the real 1-minute volume of each market (so the "real size" column exists next to the "label size" column). Entries refused by a limit are counted (`refused_by`).
2. **Two populations**: (a) the prospective days 08/09–09/09 (the honest one, short), (b) the 31-day replay of the family's versions on the 4 markets (mass, not verdict — say it), and (c) a third view: today alone, hour by hour.
3. **Outputs per configuration**: unique operations/day, R/day mean and distribution (p10/p50/p90), max drawdown in R and in R$ at each size, worst day, days to kill switch, share of entries refused by each limit, and the money table Everton asked: `R$/dia = R únicos × valor de 1 R` for 1 R = R$250 / R$500 / R$1 000 (0,25/0,5/1 % of R$100 k) and for 0,25 % of R$400 k.
4. **Correlation between versions**: which subset of the eight versions gives the most unique R/day (greedy, with the pooling ratio) — the roster recommendation.
5. **The document for Everton** (`proposta-carteira-2026-09-09.md`, Portuguese, ≤ 2 pages + tables): what R$9 000/day requires (which size, which capital, which concurrency), what a bad day costs at each choice, the exact limit fields that would change (file, field, value) — nothing applied; and the honest caveat that none of it exists before the 100/30 ruler (≈ 08/10) and before the universe/participation decision.
6. ≤ 10 lines in Portuguese.

## Prove
Scripts with tests for the dedupe/limits logic (pytest in `t360/`), SQL with output, report in Portuguese, extended format; `.claude/state/notes-T3.60.md`. Times in Brasília (UTC−3) with UTC as detail.

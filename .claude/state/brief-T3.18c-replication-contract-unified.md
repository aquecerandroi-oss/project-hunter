# Brief T3.18c — one contract for "avaliável", maturity and PF across placar/replicação; replay can never make a parent "promissora"; siblings never count the same evidence twice

**Owner:** quant-engineer. **Reviewers afterwards:** code-reviewer; Astra (`astra.sh ask`) on the statistics. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` at `3ca215e` or later. In flight elsewhere: T3.24b/T3.28b/T3.28c (`apps/web/**`), T3.7e (`services/market-worker/**`), T3.29 (`services/execution-worker/**`, `packages/risk-core/**`), T3.30 (`obsidian/**`), T3.28d (`apps/api/hunter_api/settings.py`, `middleware/**`). Your scope: `packages/indicators/hunter_indicators/replication/**`, `services/strategy-worker/hunter_strategy_worker/replication*.py`, `infra/scripts/replicate_strategy_version.py`, `apps/api/hunter_api/{repositories,services,schemas,routers}/lab_replication.py`, `lab_scoreboard*.py`, `lab_curve.py`, `apps/api/tests/**` for those, `docs/plans/REPLICATION.md`, `docs/plans/SHADOW-LAB.md` §19.

## Sources (read both; they agree and complement)
`.claude/state/astra-review-lab-pronto-2026-09-08.md` §1 (MUST-FIX) and `.claude/state/review-T3.18b-quant.md` (13 findings). D14/D15 in `.claude/state/decisions-delegated-2026-09-08.md`.

## Deliver (each item = a test that fails before and passes after)
1. **CLI cannot be fooled by replay** (Astra HIGH): `replication_stats.load_outcomes` (`services/strategy-worker/.../replication_stats.py:69`) filters `cohort = prospective` for the parent; `replicate()` (`replication.py:257/296`) records `promising_at` only from that population. Test: parent with negative prospective + positive 100/30 replay → CLI refuses without `--force-research`.
2. **One definition of evaluable** (Astra HIGH, quant #2): `repositories/lab_replication.py:104-107` applies the same horizon gate `is_evaluable` as the scoreboard (`lab_summary_metrics.py:68`); `PopulationStats.days` counts exit days like `maturity.days`; PF with no wins → value 0, reason null per SHADOW-LAB §19 (fix `PopulationStats.of` in `packages/indicators/.../stats.py`, not the scoreboard). Test: `replication.parent.evaluable == row.evaluable` and `parent.verdict == row.verdict` on a seeded version, including only-losses and no-losses.
3. **Sibling evidence counted once** (Astra HIGH, quant #7): `sibling_population()` (`repositories/lab_replication.py:162-181`) deduplicates by (market, decision_at) between live and replay and across replay runs; each arm carries `window_from/to` of its replay runs and the block carries the label "siblings: replay sobre <janela>" when any arm is replay/mixed (REPLICATION.md §3.5 item 4). Test: 25 outcomes replayed under two cohorts → 25, not 50.
4. **Incomplete round is immature, not refuted** (quant #5): `protocol.py:_siblings` uses `pool = max(total, expected)`; `passed=None` "rodada incompleta: {total} de {expected}" until `expected` arms exist; `refutada` only when `expected - negativas_maduras < required`. Test with n = 1, 6, 7, 10 all positive.
5. **Deterministic order** (quant #3): every outcome query orders by (emitted_at, id); bootstrap reproducibility test.
6. **Seed provenance** (quant #4): `BootstrapIntervalOut.seed_source: registrada | derivada_do_id`, seed read from the `strategy_version_replicated` event, not the changelog regex.
7. **Point-in-time** (quant #6, Astra): `replay_runs_summary` respects `as_of` (`finished_at <= as_of`).
8. **Cost** (quant #8): skip `replication_report` when `promising_at is None` (cheap null block); `?include=replay,replication` optional, default both — say what you chose.
9. **Mass honest** (quant #9): `decisions_simulated` = bars with a recorded decision; publish `evaluations_by_state` aggregated in the replay block so the 500 k/day target is measured against decisions, not bars.
10. **Curve** (quant #10): `CurveOut.cohort` echoed; `cohort=replay` wildcard refuses overlapping runs (reason `janelas_sobrepostas`) instead of double-summing.
11. **Tests strengthened** (quant #11): leak test uses a mature negative prospective (reprovada) vs large positive replay; `sibling_population` exercised directly; `ensure_utc` on outcome timestamps (#12); a sibling promoted to live keeps its prospective evidence (#13).
12. **Docs**: REPLICATION.md §46/§78 — halving the sample raises the standard error by sqrt(2), and the four blocks reuse data (not four independent repetitions); SHADOW-LAB §19 cross-referenced.
13. **For the web (T3.24b, not you)**: write in the notes the exact rule the card must show: `replication.status` may rest on replay (label it), `verdict` never.

## Prove
Per-file test runs with real output; `ruff`/`pyright`/`check_file_size.py`; `pnpm gen:types` regenerated; a real `GET /lab/shadow/scoreboard` example from the testcontainer showing items 2, 4, 6; report in Portuguese, extended format; `.claude/state/notes-T3.18.md` section "T3.18c".

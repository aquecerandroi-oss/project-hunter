# Brief T3.40 — as duas variantes que o diagnóstico do prejuízo pediu: momentum v2 com teto de pedágio (atr_pct_min 0,020) e com alvo 3 ATR — derivadas, ativadas como pesquisa, replayadas com o livro de motivos

**Owner:** quant-engineer. **Reviewers afterwards:** code-reviewer (receipts), Sexta-feira (EXP filing). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (slice replays ≤ 4 min with one explicit `--cohort` per variant); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; do not stop or recreate containers; VPS writes only through the audited scripts (`derive_variant.py`, `activate_strategy_version.py`) and the replay — authorised by Everton for research_only; never paper/agents/flags; if a dry-run prints an unexpected digest, STOP.** Base: `main` at `c78a416`; the VPS runs image `c78a416` (`--explain-ledger` available in `hunter-strategy-worker-1`).

## Source
`.claude/state/notes-T3.32.md` §"Variantes exatas a derivar": **V1** `momentum v2` with `atr_pct_min 0,003 → 0,020` (cost cap: at 1,5 ATR stop, toll ≤ 0,10 R); **V2** `momentum v2` with `target_atr 1,5 → 3,0` (the MFE distribution and the EXIT-NOTGT arm suggested the target caps winners). `notes-T3.26.md` (how v4 was derived; the changelog lineage; the paired comparison method — reuse it), `notes-T3.33f.md` (explain ledger usage), `obsidian/05-EXPERIMENTS/EXP-0006-momentum-piso-de-custo.md` (v4 = 0,0089 already exists: V1 is the next step of the same axis — say so in the lineage), `.claude/state/review-T3.26-risk.md` A2 (range validation now in `constraints_table.py`).

## Deliver
1. Dry-run then derive each variant from `momentum v2` on the VPS (`docker exec hunter-api-1 python infra/scripts/derive_variant.py momentum v2 --set atr_pct_min=0.020 --changelog "T3.40 V1: teto de pedágio 0,10 R (KB-0076)"`, and `--set target_atr=3.0 --changelog "T3.40 V2: alvo 3 ATR (KB-0076 MFE)"`), then `activate_strategy_version.py momentum v<N>` for each (dry-run first; digest must be `…momentum_v1@sha256:ab2e0398…`). 10-minute capacity watch after each (paper `momentum v3` still first, `outbox_lag_s 0`).
2. Replay both, 31 days, ETH/SOL/XRP/DOGE, two contiguous slices per variant with one `--cohort` each, `--explain-ledger /tmp/replay-<name>-<slice>.jsonl`; receipts verbatim.
3. Evaluation vs the parent (`replay:f8d8279c…`, momentum v2) **paired by (market, bar)** exactly as T3.26 did: decisions, evaluable, expectancy gross/net, cost R, PF, hit rate, days; the four paired groups (parent above/below the cap, variant paired/unpaired); for V2 additionally: share of outcomes by exit reason (target/stop/invalidation/horizon) vs parent, MFE captured. Kill criteria: the EXP-0006 rule ("a cut above 70 % of decisions is another strategy") for V1; for V2, `PF_net ≤ 0,80` or `expectancy_net ≤ parent` → recommend `descartar`. Verdict `inconclusivo` always (prospective rule); the recommendation is what matters.
4. EXP drafts: `.claude/state/exp-drafts/EXP-0012-momentum-teto-de-pedagio.md` and `EXP-0013-momentum-alvo-3-atr.md` (template shape; portão C1–C8 filled; frozen hypothesis; replay evaluation dated and labelled REPLAY); activation timestamps for `Registro de Tentativas` in the notes.

## Prove
Every command with real output, receipts, SQL; report in Portuguese, extended format; `.claude/state/notes-T3.40.md`.

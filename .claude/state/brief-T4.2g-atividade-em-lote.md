# Brief T4.2g — a fita por lote: `POST /v1/coins/market-activity/batch` no lugar da fita por mint (12/09/2026 11:2x BRT)

**Medido pela T4.2f:** o `swap-api` é limitado pelo Cloudflare em ≈ 20 req/60 s por IP (erro 1015, bloqueio de 60 s), qualquer rota; com 130 rastreados a fita por mint (`/v2/coins/{mint}/trades`) cobre no máximo ≈ 40 % das linhas do portão por minuto. O T4.0c mapeou `POST https://swap-api.pump.fun/v1/coins/market-activity/batch`: **vários mints numa chamada**, com compras/vendas/usuários por janela (5 m, 1 h, 24 h…). Uma chamada por minuto cobre todos os rastreados dentro do limite.

**Leia antes:** `docs/PUMPFUN.md` §2 (o corpo e a resposta do `market-activity/batch`, com a captura real de T4.0c em `.claude/state/notes-T4.0c.md`), `.claude/state/notes-T4.2f.md` (o bucket adaptativo em `rate_shared.py`/`tape_budget.py`, `trades.py`), `services/meme-worker/hunter_meme_worker/{trades,features_tape,fold,sources}.py`, `packages/exchange-adapters/hunter_exchanges/pumpfun/swap_api.py`.

**Entregar:**
1. Cliente `swap_api.market_activity_batch(mints, windows)` com fixture real (≤ 5 chamadas ao vivo; lotes de até N mints — medir o máximo aceito) e normalização (`NormalizedMarketActivity`: por mint e janela: `buys`, `sells`, `buy_volume_sol`, `sell_volume_sol`, `unique_buyers`, `unique_sellers`, `observed_at` = carimbo da resposta, `received_at`).
2. No worker, um laço `meme-activity` por minuto: um lote com todos os rastreados (quebrado em chamadas de N), gravado em `meme_market_activity_1m` (migração `0029`, RANGE mensal por `end_time`, PK `(end_time, mint, window)`), dentro do mesmo bucket do Cloudflare (as chamadas de lote contam no orçamento de 16/min — a fita por mint fica só para apostas abertas e `graduating`).
3. Features: `buys_1m`, `sells_1m`, `unique_buyers_1m`, `net_sol_flow_1m`, `curve_volume_1m_sol` passam a ser preenchidas **pelo lote** quando a fita por mint não cobriu o minuto (janela 5 m rateada? NÃO — só usar a janela cujo fim coincide com o minuto; se o endpoint só dá 5 m, gravar `tape_source = 'activity_5m'` e um `tape_window_s = 300`, com o portão lendo `curve_volume_5m_sol` explicitamente, nunca dividindo por 5 e chamando de 1 m). `creator_net_seller` continua exigindo a fita por mint (o lote não diz quem vendeu) → `creator_net_seller_reason = 'no_trade_feed'` quando só há lote — honesto.
4. Heartbeat `activity_coverage_pct`, `activity_batch_calls_60s`; `GET /meme/sources` expõe; SQL antes × depois em `infra/scripts/sql/research/2026-09-12-t42g-cobertura.sql`. Meta: `tape_coverage_pct` (fita OU lote) ≥ 90 %.
5. Testes unit + 1 testcontainer; `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; docs `docs/PUMPFUN.md` §2, `docs/plans/T4-MEME-RADAR.md` §T4.2g, `docs/DATABASE.md` §41, `docs/DEPLOYMENT.md`; `.claude/state/notes-T4.2g.md` incremental; `git status --porcelain` ao final.

**Regras:** as de sempre — primeiro plano `timeout 290` (590 testcontainer), sem background, sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`, sem commit, sem `.env*`, nada real, sem tocar `lab*.py`/`paper_engine.py`/`proposals.py` (além de ler) nem `apps/web/**`, `SendMessage` desabilitado, Bash mangla `\n` em heredoc → Write/Edit. Relatório ≤ 12 linhas em português, horários em Brasília.

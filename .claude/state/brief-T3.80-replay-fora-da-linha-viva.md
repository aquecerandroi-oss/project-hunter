# Brief T3.80 — o replay sai do container da linha viva (Everton, 2026-09-10: "quero tudo instantâneo, deixe nos trinques essa parte")

**Fato medido em 10/09 13:2x BRT (16:2xZ):** com o replay do T3.76 rodando dentro de `hunter-strategy-worker-1` (`docker exec … replay.run`), o atraso decisão−barra da linha viva subiu de 26 s (12Z) para 90 s de mediana / 171 s p95 (15Z). O portão de pausa do replay (T3.74/T3.74b: `outbox_lag_s`, heartbeat, `consumer_lag` por XINFO) não disparou — o XINFO fica em 0 enquanto o atraso real é de minutos (concern registrado no T3.74b). Duas consequências: a linha viva simula errado durante todo replay, e o replay compete por CPU/pool com quem tem de ser instantâneo.

## Entregar
1. **Motivo `decision_lag`** no portão de pausa (`replay/budget.py::live_lane_degraded`): lê `decision_lag_p50_s`/`p95_s` do heartbeat do worker (T3.74c) e pausa o dreno quando p50 > 10 s ou p95 > 30 s (limiares em `ShadowConfig`, documentados; não são parâmetro de estratégia); teste que simula heartbeat degradado → pausa; e a régua inversa: retoma só após N minutos sãos.
2. **Replay em processo próprio**: serviço `replay-worker` no compose (mesma imagem `hunter-api:$GIT_SHA`, perfil `replay`, `cpus`/`mem` limitados, pool de banco próprio e menor), invocado pela CLI atual (`compose.sh ops`-style: `bash infra/vps/compose.sh replay python -m hunter_strategy_worker.replay.run …`), nunca por `docker exec` no worker vivo; o `run.py` recusa rodar dentro do container da linha viva (variável `HUNTER_ROLE=strategy-worker` presente → recusa com mensagem), exceto em testes. `docs/DEPLOYMENT.md`, `docs/ACTIVATION.md` §7 (comandos de replay atualizados), `docs/PIPELINE.md` §6b.
3. Prova local: o benchmark 11×200 (T3.74c) rodando com um replay concorrente no mesmo host em outro processo mantém p95 do dreno vivo < 20 s.
Regras de sempre (foreground, timeout 290, sem commit, sem .env, árvore compartilhada, ≤ 350 linhas). Despachar DEPOIS do T3.74d terminar (edita `main.py`/`budget.py`).

# Brief T3.75 — funding history for the 90-day tape and a stress pass that is not blind outside August (from T3.62b concerns 1–2, 2026-09-10)

**Facts (`.claude/state/notes-T3.62b.md`, read it):** `funding_rates` only exists from 2026-08-08 16:00Z; the 90-day candle backfill (T3.62, `request_backfill.py`) brought candles, not funding; so `r_multiple` is NULL (`funding_schedule_unknown`) in 498/798 v10 decisions, K5 fires on every version, and `replay --stress` computes its `base` on n=300 and reports "dependente de metade" as an artefact. The measured funding drag where it exists is −0,0011 R/decision.

## Deliver
1. **Funding history**: find how funding is collected/backfilled (`infra/scripts/backfill_funding.py`, `services/market-worker/.../funding_announce.py`, `funding.py::_cadence` T3.65) and whether an audited backfill of `funding_rates` for the 16 markets (ARB BNB BTC DASH DOGE ETH LINK NEAR PROM SAHARA SOL SUI TAO UNI XRP ZEC, perpetual) from 2026-06-12 to 2026-08-08 is possible from Binance's public `fundingRate` history (limit 1000/request, 8 h cadence → ~170 rows per market). If `backfill_funding.py` supports it: dry-run locally against a testcontainer, then run it on the VPS through `bash infra/vps/compose.sh ops python infra/scripts/backfill_funding.py …` (the only allowed write path; `timeout 290` per slice; never `docker exec hunter-api-1 python`, never SQL writes). If it does not support the range, extend it with tests (unit + one testcontainer file) and STOP before running on the VPS — report so the orchestrator deploys first. Prove idempotency (second run inserts 0). After the backfill, re-count NULL `r_multiple` per version in the T3.62b cohorts (read-only) — the persisted outcomes will NOT recompute by themselves: say whether a re-settlement path exists (`settle.py`) or whether the replay must be re-run (do not re-run it in this task).
2. **Stress axis**: in `services/strategy-worker/hunter_strategy_worker/replay/stress.py`, when `r_multiple` is NULL for `funding_schedule_unknown`, the stress must fall back to `r_ex_funding` explicitly and say so in its output (`axis: r_ex_funding, funding_indeterminado: N`) rather than dropping the rows; tests (unit; one testcontainer if the path needs it). Never change thresholds or verdict rules; the `code_ref` closure modules are untouchable.
3. `docs/PIPELINE.md` §4b/§6b one paragraph each; `.claude/state/notes-T3.75.md` with commands/outputs and the full `git status --porcelain` list of your files; SQL as `infra/scripts/sql/research/2026-09-10-t375-*.sql`. ≤ 10 lines in Portuguese, times in Brasília (UTC−3).

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no `git pull` on the VPS. `ruff`, `pyright`, `check_file_size.py` clean on your files.

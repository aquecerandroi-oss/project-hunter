# Brief T4.16b — o rastreador não pode expulsar uma moeda com aposta aberta (12/09/2026 15:5x BRT)

**Fato medido (banco da VPS, 15:5x BRT):** cinco apostas `hype_probe_v0` fecharam `rug_no_snapshot` entre 15:01 e 15:45 BRT com `pending_reason = time_stop`. Nenhuma moeda morreu: a última fotografia de cada uma é **13 min antes** da saída (14:48 → 15:01, 14:53 → 15:06, 15:20 → 15:33, 15:27 → 15:40, 15:31 → 15:45) e não há fotografia depois. Causa no código: `MintTracker.prune()` (`services/meme-worker/hunter_meme_worker/tracker.py`) corta o conjunto em `MEME_TRACKED_MINTS_MAX` (120) pelas mais novas; com a descoberta a ~1 moeda por poucos segundos, uma moeda com aposta aberta é expulsa em ~10–15 min e o laço `meme-chain` (que lê `chain_mints(tracker)`) para de fotografá-la. A T4.16 já faz o placar tratar isso como `indeterminate`, mas a moeda continua sendo perdida — e uma posição **real** (T4.14) ficaria sem marca.

**Entregar (depois da T4.16 commitada — mesmo `main.py`/`lab_repo.py`):**
1. `MintTracker` ganha um conjunto de **fixadas** (`pin(mints)`/`unpin`): mints com aposta de papel aberta (`meme_paper_bets.status='open'`), posição real aberta (`meme_live_positions` aberta) ou proposta `operator` pendente (`proposed`, não expirada) **nunca** caem pelo teto nem pela janela; `prune()` devolve também `pinned_kept`. O conjunto é recarregado a cada tick do laço de papel (consulta única, `lab_repo`) e na partida (`load_tracked` + fixadas).
2. O teto vale para as **não fixadas**: `cap` = `MEME_TRACKED_MINTS_MAX` − |fixadas| (nunca < 20). Heartbeat: `tracked_pinned`, `tracked_capped_60s`.
3. Saída `time_stop` (e qualquer saída pendente) com a última fotografia > 3 min: antes de fechar `indeterminate`, o laço **pede uma leitura pontual** da curva pela cadeia (`getMultipleAccounts` de 1 PDA, orçamento do laço chain) e fecha pela leitura se ela vier (`mark_source='curve'`, `mark_stale_s` real); só sem resposta vira `indeterminate`.
4. Reclassificar as 5 apostas acima com o script auditado da T4.16 (`--apply --reason "tracker_evicted_open_bet"`), listando os ids no relatório; nada reescrito à mão.
5. Testes unit (prune com fixadas; teto reduzido; unpin ao fechar) + 1 testcontainer (aposta aberta → mint ainda fotografada após 200 moedas novas); `ruff`/`ruff format --check`/`pyright`/`check_file_size.py`; docs `docs/plans/T4-MEME-RADAR.md` §T4.16b, `docs/RISK_ENGINE_MEME.md` §10; `.claude/state/notes-T4.16b.md`; `git status --porcelain` ao final.

**Regras:** as de sempre (primeiro plano `timeout 290`/590; sem background; sem `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; sem commit; sem `.env*`; nada real; sem tocar `apps/web/**`; `SendMessage` desabilitado; Bash mangla `\n` em heredoc → Write/Edit). Relatório ≤ 10 linhas em português, horários em Brasília.

# Brief T3.73 — the Shadow Lab is deciding on SPOT rows with a perpetual cost model (found by D-P9, 2026-09-10)

**Facts (`.claude/state/notes-D-P9.md`, read it first):** `markets` has two monitored rows per symbol (`spot` and `perpetual`); on 2026-09-09 the `mean_reversion` family decided on BOTH rows in the same minute (e.g. NEARUSDT 21:30Z); the 4 spot decisions of the 21Z hour closed with `r_multiple = NULL` and `r_net_reason = funding_schedule_unknown` — funding being resolved on a spot market. This contaminates unique-bet counts, the Placar and the 100/30 ruler; and the spot rows are exactly the markets the paper wallet trades (SPOT only), so a paper line whose signals carry NULL R is a paper line without a scoreboard. Also: 512 of the day's signals were `no_entry` with `late:delay` — quantify it as the hidden denominator.

## Deliver (audit first, then the smallest correct fix; never a live-version parameter change)
1. **Measurement (VPS, read-only, `repeatable read read only`):** how many decisions/outcomes per version since 2026-09-01 are on spot rows vs perpetual rows; how many spot outcomes have `r_multiple NULL` and which `r_net_reason`; whether the shadow roster/universe (`services/strategy-worker/.../roster.py`, `catalogue.py`, scanner `universe`) intends spot rows for research versions at all — find the code that picks markets for shadow decisions and the code that resolves funding (`funding.py::_cadence`, T3.65) and write down what it does on a spot market.
2. **Decision + fix:** either (a) spot markets do not enter the shadow decision universe for perpetual-cost strategies (if the docs — `docs/plans/SHADOW-LAB.md`, `docs/PIPELINE.md` §4b — say the Lab studies perpetuals), or (b) spot outcomes resolve funding as exactly zero with a named reason (`funding_not_applicable:spot`) and the perpetual/spot pair is deduplicated in unique-bet counts. Pick what the documents already imply; if they are silent, choose (b) for the paper line (the wallet trades SPOT, so its signals must have R) and say why. Implement with tests (unit + one testcontainer file, `timeout 290`, one at a time). Keep non-anticipation and the frozen `code_ref` closure untouched (never edit the seven closure modules).
3. **The late:delay denominator:** a query that counts `no_entry` by reason per day since 09/01 and a paragraph in `.claude/state/notes-T3.73.md` on what `late:delay` means in `services/strategy-worker/.../decide.py`/`record.py` and whether it is a data lag or a rule; no fix in this task unless it is a one-line bug — report it.
4. Docs: `docs/PIPELINE.md` §4b one paragraph; Obsidian `obsidian/03-TRADING/Open Bugs.md` (or the existing open-bugs note — find it) one entry with the numbers; `.claude/state/notes-T3.73.md` with commands/outputs and the `git status --porcelain` list of EVERY file touched/created; research SQL as `infra/scripts/sql/research/2026-09-10-t373-*.sql`.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; no VPS writes; do not edit `apps/**` or `infra/scripts/link_*`. `ruff`, `pyright`, `check_file_size.py` clean. Report in Portuguese ≤ 12 lines, times in Brasília (UTC−3).

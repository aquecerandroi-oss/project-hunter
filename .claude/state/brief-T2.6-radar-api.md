# Brief T2.6 — API do Radar, Oportunidades, Anomalias e Regime

**Owner:** backend-specialist (sonnet). **Revisores depois:** security-reviewer, code-reviewer, Astra (`bash infra/scripts/astra.sh ask T2.6-api "..."`). **Não commitar.**

## Leia antes (nesta ordem)
1. `CLAUDE.md`. 2. `docs/plans/M2.md`: linha **T2.6** e a **"Decisão conjunta"** (episódios/status, `EXTENDED`, `IN_POSITION`/`RISK_BLOCKED` derivados por organização na leitura, envelope, anomalias com `evaluation_state`, regime `UNKNOWN`, `_live`). 3. `docs/ARCHITECTURE.md` §API e `docs/PIPELINE.md` §5–§6 (radar, explicação). 4. Schema real (T2.1, commit 88a3b0d): `packages/core/hunter_core/db/models/{analysis,analysis_baselines}.py`, `docs/DATABASE.md` §17 (`opportunities.stage/explanation/below_40_since`, `opportunity_history.envelope`, `anomalies.evaluation_state`, `feature_baselines`, `market_regimes`). 5. Padrões da API do M1 (T1.4): `apps/api/hunter_api/{routers,schemas,services,repositories}/{markets,system}*.py`, testes em `apps/api/tests/{unit,integration}` (auth, RLS, paginação, 503 honesto, `command_timeout`), `packages/shared-types` + `pnpm gen:types`. 6. `.claude/rules/astra-second-opinion.md`.

## Entregar (arquivos permitidos: `apps/api/hunter_api/routers/{radar,opportunities,anomalies,regime}.py`, `apps/api/hunter_api/{schemas,services,repositories}/{radar,opportunities,anomalies,regime}*.py`, `apps/api/hunter_api/main.py` (registro dos routers), `apps/api/hunter_api/routers/system.py` (só o `scanner` em `/system/workers`), `apps/api/tests/**`, `packages/shared-types/**` gerado, `.claude/state/notes-T2.6.md`)
- `GET /api/v1/radar`: paginado e **filtrado no servidor** (score mínimo, status, estágio, exchange, tipo de anomalia, regime, volatilidade, busca por símbolo), ordenação (score, mudança, volume, idade), cursor estável; `IN_POSITION`/`RISK_BLOCKED` derivados por organização na leitura (join com posições/risco da org do usuário — hoje vazios, mas o caminho existe e é testado com fixtures); nunca lista mercado sem oportunidade como se tivesse.
- `GET /api/v1/opportunities` e `/{id}`: decomposição (componentes, pesos, contribuições, `confidence`), `explanation` (JSON determinístico já gravado, em pt-BR), anomalias ativas ligadas, histórico (`opportunity_history` com `envelope` sob demanda), `feature_snapshot`, `baseline_ids`, `regime_id`, versões.
- `GET /api/v1/anomalies` (janela 24 h por padrão, filtros por tipo/status/mercado/severidade mínima; `evaluation_state` exposto; `unknown` nunca apresentado como resolvido), `GET /api/v1/regime` (atual por mercado + histórico; `UNKNOWN` com motivo; último regime só com carimbo `stale` quando for antigo), `/system/workers` passa a mostrar `scanner` (heartbeat `hb:scanner:*`, ausente = "sem verificação", distinto de indisponível).
- Ponte `rt:radar` → WS (`/ws` já existe; acrescente o canal e a autorização como nos canais de mercado; sem canal por organização aqui: radar é global).
- Estados honestos: sem dados = listas vazias com `as_of`; Redis fora = 503 no que depende dele; Postgres fora = 503; nada de valor padrão numérico.
- Tipos: `pnpm gen:types` sem diff depois de gerado; enums espelhados 1:1.
- Testes: unit (schemas, filtros, ordenação, cursor), integration com testcontainers (RLS: a derivação por organização nunca vaza posição de outra org; paginação; 24 h; `evaluation_state`; `UNKNOWN`; 503), contrato com o `feature_snapshot`/`envelope` gravados por fixtures que seguem os modelos da T2.1 (sem dado fabricado fora de teste).

## Regras
TDD; `Decimal` em tudo que é preço/score na borda (`Numeric(5,2)` do score, 4 casas em componentes); UTC; ≤ 350 linhas por módulo; sem `print`. Comandos: `uv run pytest apps/api -q -k "radar or opportunit or anomal or regime or system"`, `uv run pytest apps/api -q` (suíte inteira ao final), `uv run ruff check apps/api`, `uv run ruff format --check apps/api`, `uv run pyright apps/api`, `uv run python infra/scripts/check_file_size.py`, `pnpm gen:types` + `git diff --stat packages/shared-types`. PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Não toque em `.env*`, migrações, `packages/indicators`, `services/**` (T2.3 e T2.9 estão em voo).

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma) · TESTS · TEST RESULTS (saída real) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · NEXT STEP.

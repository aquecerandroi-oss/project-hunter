# Brief — Sexta-feira: turno de plantão (tarde de 2026-09-06)

**Regra operacional: nunca use Bash em background; cada comando em primeiro plano com timeout ≤ 5 min.** Leia `.claude/agents/sexta-feira.md`, `.claude/state/plantao.md`, `obsidian/09-OPERATIONS/Diario/2026-09-06.md`, `obsidian/05-EXPERIMENTS/EXP-0001-momentum-v1.md` e `EXP-0002-volume-anomaly-v1.md`.

## O que mudou desde o seu último turno (commits em `main`)
`72cfe72` T2.3 (baselines, anomalias, estágio) · `5bd17db` T2.6 (API radar/opportunities/anomalies/regime) + S3a (API do Lab) + auth 503 · `3654404` S3b (tela `/lab`) · `2587b9f` seed idempotente após ativação (o seu HIGH da VPS) · `98c15bc` code_ref normalizado (o seu HIGH de CRLF/LF) · `02693e2` statement_timeout por papel · `7e00f3b`/`88bac0b` HTTPS no IP da VPS com certificado interno + `default_sni` (o sign-in do Clerk entrava em loop em HTTP puro: cookies `Secure`); um `sk_test_` do Clerk vazou num prompt errado do `setup_env.sh` e o Everton **trocou a chave** (registre como incidente resolvido, sem o valor). A VPS está na imagem `88bac0b`, com `/ever/lab` no ar em `https://169.58.116.99` e o Everton **abriu e viu** a página. Em voo, não toque: T2.4 (`packages/indicators/{regime,opportunity}`, em revisão), T2.9 (`packages/core/hunter_core/events`, `services/market-worker`, `packages/exchange-adapters`), T2.7 (`apps/web` radar/opportunities).

## Faça, nesta ordem
1. **Avaliação datada nova nos dois EXP** com a coorte da VPS (SQL real contra o Postgres da VPS: `ssh hunter-vps` + `bash infra/vps/compose.sh exec -T postgres psql -U hunter -d hunter -c "..."`), com `as_of`, os mesmos nomes de métrica da S3a (`target_rate_among_resolved_touches`, `net_profit_rate`, `hypothetical_net_expectancy_r`, `profit_factor` com denominador, soma de R) e o gate de maturação de horizonte (só encerrados com `entry_bar_open + horizon_s <= as_of`). O que a tela mostrou hoje para momentum v1 na VPS: 208 sinais, 199 encerrados (81 target · 49 stop · 69 invalidated), taxa de alvo 0,5333, taxa de lucro 0,3956, expectancy **−0,2102 R**, PF **0,6084**, 91 avaliáveis / 1 dia → inconclusivo. Confirme com SQL. **Pesquisa (o que o Everton pediu à Sexta-feira):** abra em `EXP-0001` uma seção "Hipóteses de falha" com os dois fatos que saltam: (a) 69 de 199 encerram por **invalidação** (um terço) — verifique se a invalidação (fechamento de 15 min abaixo do nível de rompimento, `notes-S1.md` §6) está matando operações que depois teriam batido o alvo (SQL: dos invalidados, quantos tiveram MFE conhecido ≥ 1 R antes?), e (b) 14 outcomes com funding não apurável. Registre hipóteses, SQL e o que uma v2 testaria — **sem** ativar nada, sem mudar parâmetros.
2. **Obsidian:** changelog (commits acima), `07-BUGS` (fechar os dois HIGH seus; abrir: `test_quiet_socket_rotates_cleanly_at_the_rotation_deadline` intermitente no adaptador, apontado pela T2.9), `09-OPERATIONS/Deployment.md` (HTTPS no IP com certificado interno, `HUNTER_TLS_ARG`, `default_sni`, o aviso do navegador, o incidente do `sk_test_` e a regra "nunca cole chave no prompt do CLERK_ISSUER" — já bloqueada no script), `01-ARCHITECTURE/Workers.md`/`Data Flow` (outbox genérica da T2.9 e a decisão fail-closed do rate limit sem Redis), `02-MARKET/Features.md`/`Anomalies.md` (T2.2/T2.3 entregues; `status` conforme prova — features "implementado, sem scanner"; anomalias "implementado, sem scanner"), `06-DECISIONS/Architecture Decisions.md` (fail-closed do rate limit; HTTPS interno na VPS), `Revisoes-Astra/Index.md` (as revisões novas em `.claude/state/astra-review-*`), diário do dia, `plantao.md`.
3. **Saúde:** local (`docker ps`, market-worker unhealthy? o T2.9 recriou a imagem) e VPS (worker, strategy-worker, gaps, disco, backup de ontem em `/opt/backups`). Registre; conserte só o que tem receita conhecida e não colide com tarefas em voo.
4. **Commit e push** só dos seus arquivos (`obsidian/**`, `.claude/state/plantao.md`), mensagem convencional, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Peça à Astra opinião sobre as hipóteses de falha antes de commitar a etapa 1 (`bash infra/scripts/astra.sh ask S4-hipoteses "..."`).

Relatório final em português no formato estendido, com os números colados dos comandos e a "Segunda opinião (Astra)".

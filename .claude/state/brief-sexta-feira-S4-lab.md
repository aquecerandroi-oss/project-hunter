# Brief — Sexta-feira: S4 (experimentos do Shadow Lab no Obsidian) + Lab na VPS + memória

**Contexto (2026-09-06, madrugada).** Desde o seu último turno: M1 aprovado por você; commits `487bc4a` (T2.2 Feature Engine), `88a3b0d` (T2.1 migração 0003), `5d0153b` (S2 strategy-worker sombra, rodando no stack local com `momentum v2` e `volume_anomaly v2` após `--supersede`; v1 `deprecated` com sua população própria). Em voo, **não toque nos arquivos deles**: T2.3 (`packages/indicators/{anomalies,baselines}`, `stage.py`), T2.6 (`apps/api` radar/opportunities/anomalies/regime), T2.9 (`packages/core/hunter_core/events/{outbox,consume}*`, `services/market-worker/**` exceto universe*), S3a (`apps/api/**/lab*`). O market-worker local está `unhealthy` por contenção (T2.9 em prova + Postgres): registre, não conserte.

Leia `.claude/agents/sexta-feira.md` (inclusive a seção nova "Growing the roster"), `docs/plans/SHADOW-LAB.md` (Decisão conjunta itens 9 e 11; tarefa S4 e checklist), `.claude/state/notes-S2.md`, `.claude/state/s2-proof.md`, `obsidian/05-EXPERIMENTS/{Experiments Index,_TEMPLATE-EXP}.md`, `obsidian/04-AGENTS/{Momentum Agent,Volume Agent}.md`, `obsidian/10-PERFORMANCE/*`.

## Faça, nesta ordem
1. **S4 no Obsidian.** `05-EXPERIMENTS/EXP-0001-momentum-v1.md` e `EXP-0002-volume-anomaly-v1.md` com o template: Date, Hypothesis (a da estratégia, preservada), Strategy, Parameters (os `default_parameters` congelados, `params_hash`, `code_ref`), Protocol (entrada por barras, custos assumidos, stop-first, horizonte, coortes v1/v2 — v1 deprecated após o `--supersede`, com sua população própria), Markets, Sample Size, e **uma avaliação datada** com SQL reproduzível (a consulta fica na página) sobre o banco local: contagens por `tracking_state`/`result`/motivo, taxa de alvo entre toques resolvidos, taxa de lucro líquido, expectancy líquida hipotética em R, PF (nulo com motivo), soma de R hipotéticos, cobertura (mercados, dias, `unavailable` por motivo), `as_of`, versão da métrica; **"PnL de carteira / Max Drawdown de carteira: não aplicável"**; Result = "inconclusivo" (o limiar editorial é 100 outcomes avaliáveis E 30 dias distintos); Next Action. Números só de SQL rodado de verdade (`docker compose -f infra/docker/docker-compose.yml exec postgres psql -U hunter -d hunter -c "..."`), colados. Atualize `Experiments Index.md`, `04-AGENTS/{Momentum,Volume} Agent.md` (`status: sombra` agora é verdadeiro — a prova operacional existe: `s2-proof.md` e o worker no ar), `10-PERFORMANCE/Strategy Performance.md` (aponta para os EXP, métricas com os nomes certos), `02-MARKET/Workers.md`/`Data Flow` (strategy-worker, escritor único de outcomes, `shadow.signals.emitted`, isolamento do proposal builder), `08-CHANGELOG` (commits acima), `07-BUGS` (os achados corrigidos: código congelado por árvore, censura cega, `consume()` block_ms; abertos: market-worker unhealthy local), `09-OPERATIONS/Diario/2026-09-06.md`.
2. **Rotina do plantão** em `.claude/agents/sexta-feira.md` (seção "Plantão permanente"): a cada turno, uma avaliação datada acrescentada aos EXP ativos (SQL + números + `as_of`), nunca reescrevendo avaliações anteriores; nunca ativar automaticamente a variante vencedora; hipótese preservada. Ajuste `.claude/state/plantao.md`.
3. **Lab na VPS.** `ssh hunter-vps`, `cd /opt/project-hunter && bash infra/vps/compose.sh update` (git pull + rebuild + up: aplica a 0003, sobe o `strategy-worker`); depois ative as versões pelo script auditado dentro do container (`bash infra/vps/compose.sh exec strategy-worker python infra/scripts/activate_strategy_version.py momentum v1 ...` — leia o `--help`; com o `--supersede` só se houver linha ativada antiga, o que na VPS não há), confira `/ready` do strategy-worker (200 com as quatro checagens), heartbeat `hb:strategy:*`, primeiras avaliações no log, `shadow_episodes` avançando. Registre em `.claude/state/vps-lab-proof.md` com saída real. Se algo falhar na VPS, registre e não force.
4. **Commit e push** ao final de cada etapa, só dos seus arquivos (`obsidian/**`, `.claude/agents/sexta-feira.md`, `.claude/state/{plantao,vps-lab-proof}.md`), mensagem convencional, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; `git push origin main` (fetch e tente de novo se der lock; nunca rebase com árvore suja, nunca force). Peça a opinião da Astra sobre as avaliações datadas antes de commitar a etapa 1 (`bash infra/scripts/astra.sh ask S4-obsidian "..."`).

## Regras
Nada de `.env`; nenhum número sem comando colado; não altere arquivos das tarefas em voo; toolchain no PATH `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Relatório final em português no formato estendido, com "Segunda opinião (Astra)".

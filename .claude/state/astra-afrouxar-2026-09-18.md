# Astra — afrouxar a mesa real (18/09/2026 15:3x BRT) — só números, decisão do Everton tomada como dada

1. **Afrouxamento: (c′) só `max_sells_to_buys` "0.6" → "1.0" na `operator/5`, mantendo subindo/snipers 0–10/holders 20/buyers 10/`pedigree_repeat_dumper`.** Evidência: /8 = /6 + este número só (migração 0050) → apostas ×3,3 hoje (10 × 3) e ×2,5 na janela R56 (25 × 10); as 22 apostas marginais somam −0,038 SOL em 0,05 = **−0,0017/aposta (−0,035 R)**; R48: faixa 0,6–1,0 gradua 4× mais (2,15 % × 0,53 %), o cap 0,6 corta 24,6 % das graduadas; R51: `drop_ratio` +9 grads/+5 mortas (1,8 por perda extra, melhor razão com volume). (a) tirar `pedigree_repeat_dumper` (= /2) dá só +20 % (48 × 40 hoje; 62 × 51 R56) e as apostas marginais perdem **−0,011/aposta hoje** (8 apostas −0,086) e −0,0013 na R56 → pior. (b) /3 é aperto sobre /2 (snipers ≥ 3): −0,108 em 36 hoje, não gera volume. (d) holders 20→15 e buyers 10→5: R51 = 0 grads, 0 mortas a mais → nada; holders < 20 = custo puro; top-10 25→30 % = 11 recusas em 20 h (5 %) e é filtro de rug; `creator_unknown` no executor recuperaria o maior balde (77 = 34 % das recusas) mas 29 das 35 moedas morreram em 10 min (−0,48 SOL a 0,05 cada) → **não**.
   Hoje em papel (0,05): /5 −0,0010/aposta (1 000 apostas por SOL perdido), /2 −0,0026 (381), /3 −0,0030 (333), /8 +0,0063, op/5 +0,0026. Estimativa com ratio 1,0 na mesa: propostas 4–6/h no pico 15–22 BRT (flow_v2/5 em papel = 2,9/h × 2,5–3,3, limitado por 2 posições/TTL 180); passe do executor **30–50 %** se a hipótese do brief valer, mas a R56 mediu **7 fills em 171 propostas fora da janela de progresso (4 %; por moeda ≈ 9 %)** → **1–3 fills/h, 6–20 fills até 23 h**. `MEME_LIVE_AUTO_APPROVE_MAX_PER_HOUR` (padrão 5, só fills) só limita acima de 5/h — conferir no `.env`, subir para 10 se `hourly_cap` aparecer no heartbeat.
2. **Tamanho: `size_sol`/`max_sol_per_bet`/`max_exposure_per_mint_sol` = "0.07"** (≈ R$ 38; teto 0,28 do `.env` fica). Fato que muda a conta do brief: o sizing (`hunter_risk_meme/sizing.py:71-78`) tem o termo `daily_cap = 0,15 − perda_do_dia − Σ gasto das posições abertas`; com a YOU (−0,0452) o espaço é **0,105 SOL**: o "0,28 fixo" já sai clampado a 0,105 e a 2.ª posição a ~0, e cada perda encolhe o próximo ticket até `below_min_sol` — não "para depois de 2–4 perdas", **mingua**. Trades que cabem no espaço de 0,105 (perda típica −5 % = creator_dump/trailing; rug −80 %; 1 rug a cada ~8 na 1b): **0,05 → 15–25 (2 posições cabem: 0,10 ≤ 0,105; rug custa 0,04 = 40 % do espaço); 0,07 → 10–15 (1 posição cheia + 1 clampada a 0,035; rug 0,056); 0,10 → 6–8 (1 posição; 1 rug = espaço zerado).** 15–25 trades só cabem a 0,05 ou mexendo no cap (decisão dele, não recomendo hoje). Dado por trade, igual em qualquer tamanho ≤ 0,15 (impacto 0,07/30 SOL = 0,23 % < 0,5 %; 0,28 numa curva de 30 SOL = 0,9 %, já clampa): latência recebida→liquidada, cota→fill (Δ tokens), rota de saída (creator_dump/trailing/time_stop), taxa de 6003/`never_landed` sob o reenvio T4.55, tempo até a venda do criador, slippage de venda a 5 %/15 %, rent da ATA.
3. **Comandos (VPS, na ordem; decimais como TEXTO entre aspas; `compose.sh ops` como o Everton rodou às 14:28):**
   `bash infra/vps/compose.sh ops python infra/scripts/meme_rule_set.py --set-param 'size_sol="0.07"' --rule-set operator/5 --apply --reason "18/09 afrouxar: ticket 0.07 p/ caber 10-15 trades no espaco 0.105 do cap"` → idem `'max_sol_per_bet="0.07"'` → idem `'max_exposure_per_mint_sol="0.07"'` → **por último** `'max_sells_to_buys="1.0"'` (reason: "18/09 afrouxar: ratio 0.6->1.0, evidencia R48/R51/flow_v2-8"). Conferir: `--history operator/5` (4 linhas novas) e `--list`. **Nenhum `.env`/restart**: `MEME_MAX_SOL_PER_TRADE=0.28` fica como teto; `MEME_CURVE_PROGRESS_MAX_PCT=1.0` já está (T4.58); `meme_gates.json` não muda (assinatura dele). O radar relê o conjunto no tique seguinte.
   **Rollback (ordem inversa):** `--set-param 'max_sells_to_buys="0.6"'` → `'max_exposure_per_mint_sol="0.28"'` → `'max_sol_per_bet="0.28"'` → `'size_sol="0.28"'`, sempre `--rule-set operator/5 --apply --reason "rollback 18/09 ..."`. Parada imediata sem tocar parâmetro: `meme.kill` (kill switch) ou `MEME_LIVE_AUTO_APPROVE=false` + recriar executor.
4. **Duas paradas duras (Sexta-feira checa a cada 30 min em `meme_live_positions`/`meme_live_orders`):**
   (i) **Perda do dia ≥ 0,12 SOL** (80 % do cap; espaço < 0,03 = ticket inútil) **ou 3 perdas seguidas com ≥ 50 % do tamanho cada** (rugs) **ou 6 perdas seguidas de qualquer tamanho** → rollback do ratio para "0.6" e fim do dia (o cap 0,15 trava sozinho em `TRADING_DISABLED`, mas aí já não sobra dado útil).
   (ii) **Parcela de criador-vendeu nas primeiras 8 fills pós-mudança ≥ 5/8 (63 %) com saída em < 120 s** (base 1b: 4/6) **ou, em 2 h, `creator_net_seller`+`creator_flow_unknown` ≥ 80 % das propostas** (o afrouxamento gera recusa, não fill) → rollback do ratio; o resto fica.
   Ressalva: o passe de 50 % do brief não bate com a R56 (4 % por proposta, 9 % por moeda); se em 2 h houver < 2 fills, o gargalo é o executor (base NULL do criador, R56 §3.1 — conserto de código), não a porta — não afrouxar mais a porta por isso.

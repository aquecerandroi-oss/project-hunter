You are backend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, `/opt/project-hunter`, HEAD `baaf7ad` deployed 16:06 BRT / 19:06Z). First save this brief verbatim as `.claude/state/brief-T3.74f-sharding-do-worker.md`.

# Brief T3.74f — concurrency 32 was not enough; profile the burst and shard the strategy-worker (Everton: "quero tudo instantâneo")

**Facts (measured 19:31Z):** decision lag (emitted_at − observation_ts) by phase today: queue era 79 signals p50 88,8 s / p95 157,6 s; concurrency 8 (18:25–19:06Z) 42 signals p50 36,9 / p95 61,6; **concurrency 32 (since 19:06Z) 5 signals p50 27,1 / p95 56,5 / max 61,1** — the 19:30Z bar still took up to 61 s. Heartbeat `decision_lag_p50_s 27.1 / p95 37.8`. Target: p50 < 5 s, p95 < 20 s. T3.74e's model (325 s of work per burst / concurrency) predicted ~10 s; reality says the bottleneck is not the semaphore — likely CPU (one Python process, GIL: 32 coroutines do not give 32 cores; evaluate is CPU-bound Decimal math) and/or Postgres (`context_load` for ~200 markets × families at the same instant; Postgres was at 178 % CPU before). Read `.claude/state/notes-T3.74c.md`, `notes-T3.74e.md`, `services/strategy-worker/hunter_strategy_worker/{dispatch.py, consumer.py, context.py, context_cache.py, main.py, config.py}`, `packages/core/hunter_core/events/consume.py`, and how the market-worker shards (`services/market-worker/hunter_market_worker/main.py`, `MARKET_SHARDS`, `infra/vps/docker-compose.prod.yml` shard services, `compose.sh` profiles).

## Deliver
1. **Burst profile on the VPS (read-only, foreground `timeout 290`)**: at the 19:45Z and 20:00Z boundaries capture `docker stats --no-stream` for strategy-worker and postgres every ~5 s for 90 s (a foreground loop ≤ 290 s), the worker's stage histograms before/after (`hunter_shadow_stage_seconds{stage}` sums/counts → mean per stage in the burst), `queue_wait` p95, Postgres `pg_stat_activity` snapshot (active queries by state) at the peak, the Redis stream pending count. Conclude in one paragraph whether the limit is the Python CPU (GIL), Postgres, or I/O wait.
2. **Sharding**: `STRATEGY_SHARDS=N` (default 1 = today) with N processes/containers each consuming only the markets whose shard = `crc32(symbol) % N` (reuse the market-worker's exact shard function so the same symbol maps identically everywhere), each with its own consumer name in the same stream group (or a per-shard group — pick what keeps per-market ordering and no double processing; document why), its own heartbeat `hb:strategy:shadow:<i>` (the API's status/latency must aggregate — extend `apps/api/hunter_api/services/latency.py` and the workers view accordingly, minimal), shared nothing else; replay stays in `replay-worker` (T3.80). Compose: `strategy-worker-<i>` services under a `strategy-shards` profile like the market shards, `compose.sh` exports `STRATEGY_SHARDS`; the guard in `run.py`/`role_guard.py` still refuses replay inside any shard. Tests: unit for shard mapping/ownership (a bar for a market not owned is ACKed and skipped with a named metric, never evaluated), one testcontainer file proving two shards over the same stream produce the same set of decisions as one worker, byte-identical, with no duplicates; benchmark 11×200 with 4 shards in-process (or 4 subprocesses) vs 1: bars/s. Keep files ≤ 350; `ruff`/`pyright`/`check_file_size.py` clean.
3. If profiling shows Postgres is the wall (context loads), ALSO implement the rolling in-memory window per (family, market) fed by the stream (T3.74c option (a)) — but only after sharding, and say which of the two moved the number.
4. Docs: `docs/DEPLOYMENT.md` (the exact export the orchestrator must add: `STRATEGY_SHARDS=4` next to `MARKET_SHARDS=4`), `docs/PIPELINE.md` §6b, `docs/ARCHITECTURE.md` one paragraph, `obsidian/07-BUGS/Open Bugs.md` T3.73/T3.74 entry; `.claude/state/notes-T3.74f.md` with the burst profile numbers, commands/outputs and the full `git status --porcelain` list of your files; SQL `infra/scripts/sql/research/2026-09-10-t374f-*.sql`.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no VPS writes; no `git pull` on the VPS; do not edit `apps/web/**`; never change strategy parameters, versions, gates or the seven closure modules. Report in Portuguese ≤ 12 lines with the profile conclusion and the benchmark, times in Brasília (UTC−3, compute it).

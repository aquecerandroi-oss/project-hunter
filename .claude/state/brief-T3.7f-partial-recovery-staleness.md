# Brief T3.7f — fecha o buraco da recuperação parcial no hotfix T3.7e (re-revisão: HIGH) e restringe o SQL de reabertura ao incidente (MEDIUM)

**Owner:** exchange-integration-specialist. **Reviewer afterwards:** code-reviewer (same reviewer, items 1–2 of the re-review). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time (three other agents use testcontainers now — run only the files you touch plus `test_recovery.py`); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; add exact files only; do not touch `.env*`; do not stop or recreate local stack containers.** Base: `main` at `953d371` **plus the uncommitted T3.7e diff** in `services/market-worker/hunter_market_worker/{recovery,recovery_drain}.py`, `services/market-worker/tests/test_recovery_contracts.py`, `docs/PIPELINE.md`, `infra/scripts/sql/2026-09-08-reopen-false-unrecoverable.sql` (read `git diff` first; build on it, do not revert it). Scope: those files only, plus `tests/integration/test_market_recovery.py` (item 3).

## Findings (code-reviewer, re-review of T3.7e)
1. **HIGH — `recovery.py:245` / `recovery_drain.py:236-237`**: `earliest_stale_markets` is only set when `recovered=True` (100 % of expected minutes present). A partial recovery (Binance `rest.py:254` stops paginating at an empty page mid-window: a real trading pause) inserts candles older than `earliest_known` but leaves the gap `open` → the market is not marked stale → the next, older chunk of the same market in the same cycle with an empty response is finalised `unrecoverable/before_listing`. Fix: mark staleness from **any** persisted candle with `open_time < known` in this cycle (return the min inserted `open_time` from `recover_registered` even on partial recovery, or re-read `earliest()` for that market), not from `gap.status == "recovered"`. Test: newest chunk partially recovered (window not closed), older chunk empty → stays `open`, no `unrecoverable` event.
2. **MEDIUM — the reopen SQL** reopens `exhausted` gaps too (no `reason` column). Restrict to the incident: add `AND m.symbol IN ('BTCUSDT','UNIUSDT') AND g.updated_at >= '2026-09-08 13:40'` (use the real timestamp column; if none, `detected_at`), keep the candle-before-`gap_start` proof, print before/after, and a header comment explaining why the filter exists.
3. `tests/integration/test_market_recovery.py:226`: expectation predates T3.7d (`attempts` no longer resets on reopen) — update the assertion to `MAX_ATTEMPTS + 1` (import the constant) with a comment.

## Prove
Per-file test output (`test_recovery_contracts.py`, `test_recovery.py`, `tests/integration/test_market_recovery.py`), `ruff`/`pyright`/`check_file_size.py`; report in Portuguese, extended format; append section "T3.7f" to `.claude/state/notes-T3.7e.md`.

**Ainda há must-fix para aceitar F1–F10.** Revisão estática do diff e arquivos novos de `apps/web`; não executei testes/build, não modifiquei arquivos nem li `.env`.

1. **[P1] O refresh não substitui preços antigos recebidos por WebSocket.** Cenário: chega tick com preço 100; o socket cai; o refresh retorna preço 110. O tick armazenado continua prevalecendo, mantendo 100 indefinidamente. Acontece no [detalhe:40](C:/dev/project-hunter/apps/web/components/markets/market-detail-view.tsx:40) e na [tabela:70](C:/dev/project-hunter/apps/web/components/markets/markets-table.tsx:70); o hook conserva as mensagens em [useMarketChannels.ts:85](C:/dev/project-hunter/apps/web/hooks/useMarketChannels.ts:85). Comparar frescor antes de aplicar o patch. Exigir teste que receba tick, interrompa eventos e atualize as props com snapshot mais recente.

2. **[P1] Saúde operacional ignora snapshots renovados.** `LiveStatus` copia `initial.exchanges` apenas na inicialização do estado ([live-status.tsx:75](C:/dev/project-hunter/apps/web/components/system/live-status.tsx:75)); Readiness faz o mesmo ([readiness-panel.tsx:31](C:/dev/project-hunter/apps/web/components/system/readiness-panel.tsx:31)). Cenário: System abre saudável; Redis cai; novas props trazem indisponibilidade, mas o painel continua `Ready`. Reconciliar snapshots recebidos. O teste de refresh apenas conta chamadas ao mock, sem verificar mudança visível ([auto-refresh.test.tsx:28](C:/dev/project-hunter/apps/web/tests/auto-refresh.test.tsx:28)).

3. **[P2] Erros comuns perdem a causa inclusive no log.** Falta de configuração lança `Error("API_URL is not configured")` ([api.ts:40](C:/dev/project-hunter/apps/web/lib/server/api.ts:40)), mas os novos catches transformam qualquer erro não `ApiError` em `"erro desconhecido"` e registram somente isso ([system/page.tsx:38](C:/dev/project-hunter/apps/web/app/(app)/[orgSlug]/system/page.tsx:38), [dashboard/page.tsx:40](C:/dev/project-hunter/apps/web/app/(app)/[orgSlug]/dashboard/page.tsx:40)). Falhas de rede/JSON também perdem diagnóstico. Preservar mensagem e causa nos logs, mantendo mensagem apropriada na interface. Os testes usam apenas `ApiError` nos cenários de falha ([system-page.test.tsx:93](C:/dev/project-hunter/apps/web/tests/system-page.test.tsx:93)).

4. **[P2] O tratamento do gráfico ainda não cobre todo o ciclo de vida.** Um refresh com candles duplicados/desordenados pode fazer o segundo `setData` lançar fora do catch, escapando do fallback local ([candles-chart.tsx:149](C:/dev/project-hunter/apps/web/components/markets/candles-chart.tsx:149)). Além disso, se o `applyOptions` inicial lançar em `resize()`, a instância já existe e o efeito termina antes de registrar cleanup ([candles-chart.tsx:124](C:/dev/project-hunter/apps/web/components/markets/candles-chart.tsx:124)). Proteger inicialização completa e atualizações. O teste de erro lança antes de existir instância; portanto não comprova liberação após falha parcial ([candles-chart.test.tsx:85](C:/dev/project-hunter/apps/web/tests/candles-chart.test.tsx:85)).

5. **[P2] Status compacto continua inacessível e pode indicar verde indevidamente.** Primeira exchange conectada e segunda reconectando: o redutor ignora `warn` e mantém verde. O ponto tem `aria-hidden`, enquanto texto/title omitem estado e gaps ([live-status.tsx:123](C:/dev/project-hunter/apps/web/components/system/live-status.tsx:123)). Usuário de leitor de tela não distingue esses estados. Corrigir prioridade e expor informação textual; testar duas exchanges e o conteúdo acessível.

**Não identifiquei loop de render nem polling periódico em aba oculta no `AutoRefresh`:** há guarda de visibilidade e limpeza do intervalo ([auto-refresh.tsx:37](C:/dev/project-hunter/apps/web/components/auto-refresh.tsx:37)). Isso não resolve os problemas de atualização efetiva descritos acima.
# Brief T3.7b — the hourly β producer and the history it needs (so a β can exist before the paper flow is enabled)

**Owner:** quant-engineer (producer job in `services/scanner-worker/**`, the backfill request script in `infra/scripts/`). **Reviewers afterwards:** risk-engine-guardian (β is an admission input), code-reviewer. **Do not commit.** **Operational rule: never a background shell; every command in the foreground with a timeout <= 5 min; testcontainers suites one file per pytest invocation; do not touch `.env*`.** Base: `main` at `e26a4ee`. In flight elsewhere: T3.0f in `services/market-worker/**` and `infra/docker|vps/**` — **do not edit those paths**; publish backfill requests through the existing event contract instead.

## Measured on the VPS, 2026-09-08 03:40Z
`market_betas` has **0 rows**. `candles` has one timeframe (`1m`), 260 markets, first bar 2026-08-29 18:00Z, **11 distinct days**. `beta_v1` (`packages/indicators/hunter_indicators/beta/model.py`) needs `window_days = 30`, `min_contiguous_days = 20`, hourly bars, `valid_until = window_end + 1 h`, recomputed every closed hour. T3.7 (`da2fb49`) delivered the estimator and the schema (`0006`), and `docs/plans/M3.md` T3.7 says "cálculo horário no scanner-worker" — **that job was never written**, so no β can ever appear. Without a valid β the admission returns `unavailable` and refuses every entry (directive: "sem β validado só shadow"). Everton decided on 2026-09-08 to enable the paper flow; β is on the critical path.

## Read first
`packages/indicators/hunter_indicators/beta/{__init__,model}.py` and its tests (the frozen numerical contract: estimator, return kind, pairing, coverage, invalidity reasons), `.claude/state/notes-T3.7.md` if present, `docs/RISK_ENGINE.md` §6 (β), `docs/DATABASE.md` §18 (`market_betas`: immutable revisions, `window_start/end`, `available_at`, `valid_until`, reference identity), `packages/core/hunter_core/db/models/betas.py`, `services/execution-worker/hunter_execution_worker/bridge_universe.py` (how the consumer reads the current valid β), `services/scanner-worker/hunter_scanner_worker/{main,baseline*,consumers}.py` (how hourly jobs are scheduled today: the baseline refresh is the pattern), `packages/core/hunter_core/events/{streams,outbox}.py` and `services/market-worker/hunter_market_worker/backfill*.py` (the `market.backfill.requested` contract — read only), `.claude/state/decisions-M3-delegated-2026-09-06.md` D1 (perpetual decides: β is measured on the perpetual, the wallet executes spot).

## Deliver
1. **Hourly β job in the scanner-worker** (role `scanner`, leader/shard 0 only): every closed hour, for every monitored perpetual with BTCUSDT as reference, build hourly closes from `candles` 1m (aggregate in SQL, `is_final` only), run `beta_v1`, and insert **one immutable revision** per market per hour into `market_betas` as `hunter_worker` — valid ones with the numbers, invalid ones with the reason (`insufficient_contiguity`, `no_reference`, ...), never a fabricated β; BTC = 1 by identity as the model says. Idempotent per `(market_id, window_end, estimator)`; a rerun inserts nothing. Metrics `hunter_beta_revisions_total{outcome}`, `hunter_beta_valid_markets`, heartbeat field in `hb:scanner:*` (`beta_last_run`, `beta_valid`). Readiness: β job older than 2 h → degraded, not down.
2. **History depth**: a script `infra/scripts/request_backfill.py --days 31 --markets BTCUSDT,<the 19 perps with a spot pair>` (read the list from `packages/exchange-adapters/hunter_exchanges/testing/fixtures/spot_universe_ticker_24hr.json` or from `markets` where a spot pair exists) that publishes `market.backfill.requested` events through the outbox with the range the β needs (window 30 d + 1 bar, from now), respecting the existing contract and priorities (T2.9c lane). Dry-run prints what it would request. Do **not** change the market-worker.
3. **Proof on the local stack** (if Docker is up): request 31 days for 3 markets, wait for `market.candles.backfilled`, run the job once by hand (`uv run python -m hunter_scanner_worker.beta --once` or equivalent), show the revisions in `market_betas` with `valid_until > now()` for the markets with ≥ 20 contiguous days and the reason for the others. Paste real SQL output. If Docker is down, prove with testcontainers and say so.
4. **Docs**: `docs/PIPELINE.md` (β job: cadence, inputs, outputs, invalidity), `docs/DEPLOYMENT.md` (how to run the backfill request on the VPS and how to read `hb:scanner` β fields), `docs/RISK_ENGINE.md` §6 only if the contract text needs the producer named.

## Prove
`services/scanner-worker/tests` per file (new `test_beta_job.py` with testcontainers: idempotency, invalid reasons, BTC identity, no fabricated β, hourly boundary), `packages/indicators` (unchanged, run once), `ruff check`/`format --check`/`pyright services/scanner-worker infra/scripts/request_backfill.py`, `check_file_size.py`. Report in Portuguese, extended format (STATUS · FILES · TESTS with real output · PROOF · CONCERNS); `.claude/state/notes-T3.7b.md`. Nothing here activates anything: β existing is a precondition, not a trade.

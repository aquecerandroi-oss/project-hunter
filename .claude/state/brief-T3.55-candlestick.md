# Brief T3.55 — padrões de candlestick: (a) o que a evidência publicada diz, com fontes verificáveis; (b) o que o NOSSO dado diz (retorno futuro após cada padrão, 217 mercados, 31 dias, 15 m e 1 h, com IC por blocos) — antes de qualquer estratégia (Everton, 2026-09-08 23:40: "já estudou sobre o candlestick?")

Two agents, one brief. **Do not commit.** **Operational rule for both: never a background shell; foreground commands with a timeout <= 5 min; no testcontainers; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); do not touch `.env*`; VPS strictly read-only (`repeatable read read only`).**

## (a) Literature — owner: general-purpose agent with web access. Write scope: `.claude/state/exp-drafts/KB-0080-candlestick-evidencia.md`, `.claude/state/notes-T3.55a.md`.
- Template shape of `obsidian/11-KNOWLEDGE/_TEMPLATE-NOTE.md` (read it and `KB-0003`/`KB-0077` for tone): `fonte_url` REAL and opened (never from memory; if a paper cannot be opened, cite the abstract page and say so), `lido_em`, `evidencia`, `hipotese_testavel`, `confiança`.
- Cover at least: Marshall, Young & Rose (2006, Journal of Banking & Finance) on DJIA candlesticks; Caginalp & Laurent (1998, Applied Mathematical Finance) on S&P 500 stocks; at least two crypto-specific studies (2019–2025) on candlestick patterns in BTC/ETH/altcoins with intraday data; one survey/meta-analysis of technical-analysis profitability (Park & Irwin 2007 or newer). For each: sample, timeframe, patterns tested, holding period, result net of costs, and whether it survives multiple testing.
- Verdict in ≤ 10 lines of Portuguese: which patterns have any evidence, on which timeframe, and what the honest prior is for crypto perps at 15 m / 1 h. Then the list of the 8–12 patterns worth measuring in (b), with exact definitions (body/wick ratios in ATR or in range percentages, trend context) — the definitions must be deterministic and Decimal-friendly.

## (b) Our data — owner: quant-engineer (research only). Write scope: `packages/core/hunter_core/strategies/candle_patterns.py`? NO — no production code in this task. Only `.claude/state/exp-drafts/t355/` (pure-Python detector prototype + scripts, Decimal, tested with a small pytest file there), `infra/scripts/sql/research/2026-09-09-t355-*.sql`, `.claude/state/exp-drafts/KB-0081-candlestick-no-nosso-dado.md`, `.claude/state/notes-T3.55b.md`.
- Export (read-only) 15 m and 1 h bars folded from 1 m with completeness (`count = 15`/`60`) for 31 d on the 20 most liquid perps + the 4 replay markets; implement the pattern definitions from (a) — if (a) is not finished when you start, use the standard set (engolfo de alta/baixa, martelo, enforcado, estrela cadente, doji, marubozu, harami, estrela da manhã/noite, três soldados/corvos) with explicit numeric definitions written in the notes; detect at bar close only (non-anticipation: a pattern is known at the close of its last bar).
- Forward returns 1/4/16 bars after each pattern vs matched baseline (same market, same hour-of-day, bars without the pattern), day-block bootstrap CI (`t342-blocos/blocos.py` unmodified), Holm across patterns × timeframes; also the conditional version: pattern at support/resistance lines (`tl_scan` frozen) and pattern by BTC regime (previous closed hour). Toll lens: what stop distance in ATR would each pattern's natural invalidation imply and the resulting `custo_R` (KB-0076).
- Verdict ≤ 10 lines: does any pattern carry information on our data; conditional on lines/regime; is there a strategy worth a contract (C1–C8 gate draft) or is this "painel".

## Prove
SQL with output, bootstrap outputs, prototype tests; reports in Portuguese, extended format. Times in Brasília (UTC−3) with UTC as detail.

You are backend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, read-only). First save this brief verbatim as `.claude/state/brief-T3.82-universo-de-pesquisa.md`.

# Brief T3.82 — the Lab's shadow universe becomes "markets with ≥ 90 days of 1-min candles" (Everton approved 2026-09-10 19:1x BRT: "boaaa perfeito")

**Facts:** 200 monitored perpetuals; only 16 have ≥ 90 days of `candles_1m` (ARB BNB BTC DASH DOGE ETH LINK NEAR PROM SAHARA SOL SUI TAO UNI XRP ZEC); all 200 have the minimum context (26 h). Live shadow versions (10) decide on all 200 at every 15-min boundary; T3.74f/g measured the CPU wall (~80 ms per (market, version) evaluation on the VPS, ~500 per shard per boundary → 37–43 s lag with 4 shards). The 184 markets without 90 d can never pass the funnel (90-d replay + replication), so their live decisions are unvalidatable research cost. Decision: the shadow universe = markets whose 1-min history covers ≥ `SHADOW_UNIVERSE_MIN_HISTORY_DAYS` (default 90) — applied to every shadow version (research_only and paper alike; the paper wallet's executable universe is a separate concept in the execution-worker and is untouched). This is a worker-level universe policy, NOT an eligibility rule on versions (no `code_ref` change, no version derivation) and NOT a risk limit.

## Deliver
1. **Rule** in the strategy-worker (`services/strategy-worker/hunter_strategy_worker/` — a new `universe.py` + wiring in `consumer.py`/`dispatch.py` before any context load): a market is in the shadow universe when `min(open_time)` of its `candles_1m` (timeframe 1m, perpetual) is ≤ `source_bar_close − N days` AND the market is monitored/active (the T3.73 spot exclusion stays). Cache the membership per market with a TTL of 1 h (Redis or in-process, say which and why; a market crossing the 90-day line joins within an hour — that is fine). Bars of markets outside the universe are ACKed and skipped BEFORE the dispatcher's semaphore with a named counter `hunter_shadow_bars_skipped_total{reason="universe_history"}` and a heartbeat field `universe_size` (per shard: how many of the shard's markets are in). Never evaluate, never persist a decision for them. Replay (`replay/*`) already works on explicit market lists — untouched, but say so.
2. **Setting** `SHADOW_UNIVERSE_MIN_HISTORY_DAYS` (default 90; `0` = disabled) in `ShadowConfig` + compose docs; the value must appear in the heartbeat and in `GET /api/v1/system/latency` or the workers view (minimal, so the screen shows "universo: 16 de 200 (≥ 90 d)").
3. **Tests**: unit (boundary at exactly 90 d; a market that gained history joins after the cache TTL; disabled = today's behaviour), one testcontainer file: a stream with 3 markets (2 with 90 d, 1 with 30 d) → decisions only for the 2, the third counted under `universe_history`, ACKed, no row; the byte-identical-decisions proof for the 2 (reuse `test_dispatch_benchmark.py` harness). Keep files ≤ 350; `ruff`/`pyright`/`check_file_size.py` clean.
4. **Measurement plan** for the orchestrator (in the notes): expected evaluations per boundary after (16 × 10 = 160 vs ~2 000) and the SQL to verify the day's signals now come only from the 16.
5. Docs: `docs/PIPELINE.md` §6b (universe policy paragraph, with the decision's provenance and date), `docs/plans/SHADOW-LAB.md` one paragraph, `obsidian/06-DECISIONS/2026-09-10-universo-de-pesquisa-90-dias.md` (decision note: the numbers table, Everton's approval, what it does NOT change — risk limits, versions, the paper wallet's universe), `.claude/state/notes-T3.82.md` with commands/outputs and the full `git status --porcelain` list of your files.

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; never stop/recreate containers; no VPS writes; no `git pull` on the VPS; do not edit `apps/web/**`, `packages/risk-core/**`, `services/execution-worker/**`; never change strategy parameters, versions, gates or the seven closure modules. Report in Portuguese ≤ 10 lines, times in Brasília (UTC−3, compute it).

# Brief T2.5 — `scanner-worker` (features → baselines → anomalias → estágio → regime → oportunidade → radar)

**Owner:** backend-specialist (opus — integra tudo do M2; é o worker mais crítico do milestone). **Revisores depois:** database-architect (escrita em lote, partições, baselines), code-reviewer, Astra. **Não commitar.** **Regra operacional: nunca use Bash em background; cada comando em primeiro plano com timeout ≤ 5 min; suítes com testcontainers por arquivo.**

## Leia antes (nesta ordem)
1. `CLAUDE.md`. 2. `docs/plans/M2.md`: linha **T2.5** e a **"Decisão conjunta"** inteira (Custo: contexto incremental, mercados "sujos", coalescência, baselines em cache, sem histórico no scorer, escrita em lote, history por delta ≥ 3 / 5 min / mudança; alvo p99 ≤ 3 s tick→opportunity com 200 mercados; REST: o scanner **não chama a exchange**, backfill via `market.backfill.requested`; universo: consome `market.universe.changed`; retenção). 3. `docs/PIPELINE.md` §2–§6 (cadências: features tick 1 s / bar no fechamento; anomalias em `features.updated`; regime 1 min; oportunidade 2 s). 4. Os motores puros, todos commitados, com as **notas de handoff para a T2.5** em cada uma: T2.2 `.claude/state/notes-T2.2.md` (§13 `covered_until` do tape — sem ele `trade_velocity`/`buy_pressure` ficam indisponíveis e nenhum EARLY publica; `deriv_history`; §16 custo ~50 ms/vetor, `_epoch_minutes` 17×/vetor → otimize aqui com cache por contexto), T2.3 `.claude/state/notes-T2.3.md` (§10: watchdog chamando `advance(no_data)`, `covered_until`, mapeamento `market_id`↔`exchange/symbol`, `StageInputs`, persistir `AnomalyState`/`StageState`, lock de retenção, índice em `input_fingerprint`, teste Postgres do `SqlBaselineStore`), T2.4 `.claude/state/notes-T2.4.md` (§8: watchdog alimentando `advance_status` com amostra inelegível; resolução incremental de `RegimeObservation`/amostras horárias — não recalcular 43.200 candles por minuto; `BreadthObservation` com tamanho do universo; persistir `RegimeState`/`EpisodeState`; `regime_stale` antes do scorer; `regime.changed` publicado pelo par com `label_changed`; nota de deploy do `components_frozen`). 5. Schema T2.1: `packages/core/hunter_core/db/models/{analysis,analysis_baselines,system}.py`, `docs/DATABASE.md` §17 (`feature_snapshots` por minuto, `feature_baselines` imutável com retenção protegida, `anomalies` + `evaluation_state`, `market_regimes`, `opportunities` + `opportunity_history.envelope`, `outbox_events`). 6. Infra de eventos (T2.9, na árvore): `packages/core/hunter_core/events/{outbox,outbox_store,outbox_recovery,consume}.py` e o padrão do market-worker (`services/market-worker/hunter_market_worker/{main,supervision,health,heartbeat,persist,durable,outbox}.py`) e do strategy-worker (`services/strategy-worker/hunter_strategy_worker/{main,consumer,health}.py`). 7. `.claude/rules/astra-second-opinion.md` — opinião da Astra sobre a arquitetura dos runners/cadências e o plano de persistência antes de implementar.

## Entregar (arquivos permitidos: `services/scanner-worker/**` (novo pacote `hunter_scanner_worker`, membro do workspace uv), `infra/docker/docker-compose.yml` (serviço `scanner-worker`, mesmo padrão), `infra/docker/entrypoint.sh` (role `scanner`), `packages/core/hunter_core/settings.py` (só se o `Literal` de roles precisar), `packages/core/hunter_core/events/streams.py` (só nomes/maxlen novos), `packages/core/hunter_core/redis/keys.py` (`radar:scores`, `hb:scanner:*`, cache de baseline), `packages/indicators/**` **apenas** para adaptadores finos de IO se um motor precisar (sem mudar fórmulas; registre), `services/market-worker/hunter_market_worker/**` só para atender `market.backfill.requested` e `covered_until` se ainda não existirem, `docs/PIPELINE.md`, `.claude/state/notes-T2.5.md`, `.claude/state/t25-proof.md`)
- **Consumidores** (grupos próprios, idempotentes por `event_id`, ACK após efeito): `market.ticks`, `market.candles.closed`, `market.derivatives`, `market.liquidations`, `market.universe.changed` (warm-up nos novos, encerramento honesto nos excluídos).
- **Runners com as cadências da decisão:** features por tick com throttle de 1 s por mercado (só mercados "sujos") e por barra no fechamento; anomalias em `features.updated`; regime a cada minuto; oportunidade a cada 2 s por mercado sujo; refresh horário de baselines (só o bucket da hora fechada) + bootstrap na partida por candles persistidas; watchdog que alimenta `advance(no_data)`/`advance_status` para mercados mudos.
- **Contexto incremental:** `MarketContext` mantido por mercado a partir do hot state + eventos (sem reler 1500 candles por tick), `FeatureState`/ATR checkpoint persistidos e recuperados, `covered_until` do tape preenchido a partir da saúde da coleta (`hb:market:*`, `ws_state`), `deriv_history` das tabelas duráveis.
- **Persistência em lote** (transação única por lote, escrita como `hunter_worker`): `feature_snapshots` por minuto, `anomalies` (ciclo + `evaluation_state`), `market_regimes`, `opportunities` (episódio: índice parcial `WHERE expired_at IS NULL`, `status`/`expired_at` coerentes), `opportunity_history` (regra `should_record_history`, `envelope` completo por amostra), `feature_baselines` só INSERT; estados (`AnomalyState`, `StageState`, `EpisodeState`, `RegimeState`) persistidos e reidratados após restart.
- **Publicação via outbox genérica (T2.9)** para o durável (`anomalies.detected`, `regime.changed` pelo par com `label_changed`, `opportunities.updated`); `features.updated` e `rt:radar` efêmeros; `radar:scores` no Redis (projeção para a API).
- **Backfill:** falta de histórico → `market.backfill.requested` ao market-worker; nunca REST daqui.
- **Supervisão:** `forever()`, readiness (`/ready`: Postgres, Redis, consumidores vivos, outbox sem atraso, baselines carregadas ou "em construção" declarado), heartbeat `hb:scanner:<instance>`, métricas (latência tick→opportunity p95/p99, fila, mercados sujos, baselines por estado, custo por vetor), `RoleRegistry["scanner"]`, serviço no compose com `restart: unless-stopped` e healthcheck.
- **Nota de deploy** do `components_frozen` (T2.4) executada no stack local antes da prova, registrada.
- **Testes** (testcontainers como os outros workers): pipeline ponta a ponta com fakes (candle sintético → features → baseline → anomalia → estágio → regime → score → radar), idempotência por `event_id`, restart sem perda nem duplicação (estados reidratados), universo mudando, backfill pedido e nunca REST, `covered_until` liberando EARLY, `no_data` expirando anomalias/episódios, escrita em lote e as regras de history, latência medida no teste de carga sintético (200 mercados × 1 tick/s por 60 s) com p99 reportado.
- **Prova operacional** (após os testes): serviço no compose contra o stack local (market-worker real), 30 min: mercados avaliados, baselines por estado (utilizável / em construção com motivo), anomalias abertas/resolvidas, regimes, oportunidades por status/estágio, p95/p99 tick→opportunity, CPU, `/ready` verde, 0 exceções; o Radar da API (`GET /api/v1/radar`) devolvendo linhas reais. Registre em `.claude/state/t25-proof.md`.

## Regras
TDD; Decimal/UTC; ≤ 350 linhas por módulo; nada fabricado; nada ordena. Comandos: `uv sync --all-packages`, `uv run pytest services/scanner-worker -q`, `uv run ruff check .`, `uv run ruff format --check services packages`, `uv run pyright services/scanner-worker packages/core`, `uv run python infra/scripts/check_file_size.py`, `docker compose -f infra/docker/docker-compose.yml build api` (o scanner usa a imagem `api`, como os outros workers). PATH: prefixe `/c/Users/evert/AppData/Local/Microsoft/WinGet/Packages/astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe:/c/Users/evert/.local/bin:/c/Program Files/nodejs`. Não toque em `.env*`, `apps/**` (T2.7 em voo), `services/strategy-worker/**`.

## Relatório final (em português)
COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES (nenhuma migração; o que escreveu) · TESTS · TEST RESULTS (saída real) · REAL DATA CONNECTED (a prova de 30 min com p99) · MOCKS REMAINING · BUGS · SECURITY ISSUES · Segunda opinião (Astra) · NEXT STEP.

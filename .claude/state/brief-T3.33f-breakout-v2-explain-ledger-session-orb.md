# Brief T3.33f — breakout v2 por parâmetro (a geometria que não deixou a hipótese ser testada), `--explain-ledger` no replay, e session_orb v1 entra no Lab com replay

**Owner:** quant-engineer. **Reviewers afterwards:** code-reviewer. **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min (slice replays ≤ 4 min with an explicit `--cohort`); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (git-guard blocks); add exact files only; do not touch `.env*`; do not stop or recreate containers; VPS writes only through the audited scripts (`derive_variant.py`, `activate_strategy_version.py --supersede`, replay) — authorised by Everton for research_only versions; never paper/agents/flags.** Base: `main` at `cf51c7d`; the orchestrator is deploying `strategy-worker` to that image now — before any VPS step, check `docker ps --format '{{.Image}}' | grep strategy` shows `hunter-api:cf51c7d` or later, and that `docker exec hunter-strategy-worker-1 python -c "import hunter_core.strategies.session_orb_v1"` works. Scope: `services/strategy-worker/hunter_strategy_worker/replay/**` (explain ledger), `infra/scripts/derive_variant.py` only if a new capability is missing, tests, `.claude/state/exp-drafts/**`, `.claude/state/notes-T3.33f.md`.

## Sources
`.claude/state/notes-T3.33e.md` (breakout v1: 0 decisions, 14/14 `geometry_invalidation` — the 8-bar base is wider than a 1,25 ATR stop; the replay persists state but not reason; `unavailable` identical across versions; seed.py has no dry-run; mean_reversion +0,094 R net on 37), `notes-T3.33a.md` (breakout contract; K1–K5), `notes-T3.33c.md` (session_orb; digest `…session_orb_v1@sha256:a4d514ad…` — the dry-run must print it), `docs/ACTIVATION.md` §7 (derive + supersede path), `.claude/state/notes-T3.32.md` (cost identity).

## Deliver
1. **`--explain-ledger <path>` in `replay.run`**: one JSONL line per evaluated bar `(bar_close, market, state, reason, detail)` from the strategy's decision object (the reason strings already exist: `geometry_invalidation`, `not_compressed`, `rvol_low`, `atr_gap`…); no new table; tests. This is what makes every "why zero decisions" measurable (EXP-0008 C2 obligation, EXP-0009 trend/atr gap split).
2. **breakout v2 by parameter** (never code): from the frozen v1 parameters, measure first — with the explain ledger over the same 31-day window, the distribution of `base_low` distance in ATR at the 14 rejected bars — then derive **one** variant that makes the guard `stop < base_low < close` satisfiable for the typical base (either `stop_atr` up to the value the base needs, or `squeeze_window_bars` down; pick by the measurement and by the cost identity: a wider stop lowers the toll in R but lowers R per target; say the trade-off). `derive_variant.py breakout v1 --set … --changelog "T3.33f: geometria …"` → activate with `--supersede` so v1 is deprecated in the same audited step (v1 costs 216 evaluations per 15 min for nothing). Dry-run first; paste the digest/params_hash.
3. **Replay breakout v2**, same cohort discipline (two slices, one `--cohort`), same four markets; evaluate against K1–K5 + the ATR band `0,0050 ≤ ATR% < 0,0059` count/expectancy (reviewer's condition) + decomposition by `squeeze_ratio` bucket from the explain ledger.
4. **session_orb v1**: `seed.py` is the operator's (no dry-run; report that the `session_orb` row exists or not: if the key is missing in `strategies`, STOP and report — the operator runs `docker exec hunter-api-1 python infra/scripts/seed.py`); if the draft row exists, `activate_strategy_version.py session_orb v1 --dry-run` (digest check) → activate → 10-min capacity watch (`versions_active` 7, paper first, `outbox_lag_s`) → replay in two slices → evaluation with the session decomposition and the 70 % rule of EXP-0010 (one session carrying the sample) and the share of exits spilling into the next session.
5. EXP drafts: dated "Avaliação (replay)" sections for EXP-0008 (v2) and EXP-0010; the v1→v2 lineage in EXP-0008; activation timestamps for `Registro de Tentativas` in the notes.

## Prove
Per-file tests with real output for the ledger, `ruff`/`pyright`/`check_file_size.py`; every VPS command with output; receipts; tables; verdicts (`inconclusivo` + `descartar` recommendation when a kill criterion fires); report in Portuguese, extended format; `.claude/state/notes-T3.33f.md`.

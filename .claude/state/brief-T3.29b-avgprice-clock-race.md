# Brief T3.29b — fecha a revisão da T3.29 (REQUEST_CHANGES): corrida de relógios no avgPrice, teste da pré-checagem `marks_incomplete` na ponte, regras escritas no documento-fonte

**Owner:** risk-engine-guardian. **Reviewer afterwards:** code-reviewer (same reviewer, items 1–5). **Do not commit.** **Operational rule: never a background shell; foreground commands with a timeout <= 5 min; testcontainers one file per pytest invocation, one at a time — run only the files you touch (`test_avg_price.py`, `test_mark_quality.py`, `test_bridge_cycle.py`, `test_entry_guards.py`, `test_manual_request_decided.py`); the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a` (the git-guard hook blocks them); add exact files only; do not touch `.env*`; do not stop or recreate local stack containers; VPS read-only.** Base: `main` at `56038a8` (T3.29 = `5ff19ac`). Scope: `services/execution-worker/hunter_execution_worker/{avg_price,entry_inputs,cycles,market_data,bridge,main}.py`, `services/execution-worker/tests/**`, `docs/ACTIVATION.md`, `docs/RISK_ENGINE.md`.

## Findings (code-reviewer on 5ff19ac)
1. **HIGH — clock race**: `Cycles.entries()` captures `now` (`cycles.py:206`) before `execute_approved_entries` → `data.snapshot` → `ExchangeAvgPrice.read()` does the HTTP round-trip and only then stamps `observed_at = self._clock()` (`avg_price.py:133`, default `utcnow`, never overridden in `main.py:77`). On every cache miss `observed_at > now`, and `entry_inputs.stale_average` (`entry_inputs.py:95-97`) treats negative age as `avg_price_stale` — a fresh price is refused as expired once per refresh. Fix: stamp `observed_at` **before** the request from the same clock the cycle uses (inject the cycle clock into the reader; pass `now` through), and tolerate `age < 0` up to a small skew as fresh (say 2 s) with a metric. Test: `ExchangeAvgPrice` with a real advancing clock inside a full cycle (two `utcnow()` reads) → the entry is not deferred.
2. **HIGH — untested bridge pre-check**: `bridge.py:168-172` `marks_incomplete` has no test in `test_bridge_cycle.py`/`test_bridge_consumer.py`. Add: coverage < 1 → deferred with reason; coverage back to 1 → admitted (the recovery side, finding 5).
3. **MEDIUM — docs**: `docs/RISK_ENGINE.md` must contain the rules the code cites ("§7, R-OPS-2", "rule 3"): avgPrice as the NOTIONAL reference with its two deadlines, `mark_quality` as an admission pre-check (and the note that one illiquid open position defers all new admissions of that wallet until marks return), `hot_state_unreachable`. `docs/ACTIVATION.md` §8: same operational note.
4. **LOW — `StaticSpotMarketData._dated`** silently stamps an undated `avg_price`: add an end-to-end test proving `avg_price_undated` through the static double when the fixture explicitly asks for no stamp (an opt-out flag), so a future test cannot be fooled.

## Prove
Per-file tests with real output, `ruff`/`pyright`/`check_file_size.py`; report in Portuguese, extended format; append section "T3.29b" to `.claude/state/notes-T3.29.md`.

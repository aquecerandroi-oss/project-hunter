You are backend-specialist on PROJECT HUNTER (repo C:\dev\project-hunter; absolute paths; `git -C C:/dev/project-hunter`; never rely on cwd; VPS alias `hunter-vps`, `/opt/project-hunter`). First save this brief verbatim as `.claude/state/brief-T3.74-atraso-de-decisao.md`.

# Brief T3.74 — the strategy-worker's decision lag went from 2 s to 125 s and half of the signals now die as `no_entry:late:delay` (found by T3.73, 2026-09-10)

**Facts (`.claude/state/notes-T3.73.md` §3, read it first):** median `decision − source_bar_close` was 2,0 s on 01–05/09, 107,9 s on 09/09, 125,1 s on 10/09; `plan_entry` marks `late:delay` when `entry_bar_open − source_bar_close > 120 s` (no price term); `no_entry:late:delay` went from 10–18/day to 1 637 on 09/09 (47,2 % of signals) and 53,7 % on 10/09; above 300 s the bar does not even become a signal, so the real denominator is not in the database. Roster today: 11 active versions (8 `mean_reversion` incl. v14 paper, `mean_reversion_h1 v1`, `momentum v3` paper, `momentum v11`) over the shadow universe; per-version context budgets landed 09/09 (T3.54b: `required_context_minutes`, floor 1560, ceiling 6000), eligibility gates (T3.52/T3.59), the 90-day backfill of 16 markets (T3.62, until ~23:00 BRT 09/09), and replays run on the same container (`hunter-strategy-worker-1`, image 572d3b6) — a replay of v10/v1/v2 over 90 days × 16 markets is running RIGHT NOW (T3.62b), so part of the lag may be replay contention. **Never stop, restart or recreate any container; no VPS writes; no `git pull` on the VPS.**

## Deliver
1. **Diagnosis with numbers** (read-only): (a) the lag series per hour since 01/09 (`agent_signals`/`shadow_decisions` timestamps vs `source_bar_close`) aligned with the deploy times (git log of `572d3b6`, `d21a11d`), the replay windows (`replay_runs.started_at/finished_at`), and the backfill; (b) the worker's own metrics: `curl` the strategy-worker metrics endpoint from inside the VPS (`docker exec hunter-strategy-worker-1 …` or the exposed 8000/8001 — find it in `health.py`/compose) — cycle durations, bars consumed per minute, context loads, `hunter_shadow_*` histograms; Redis stream lag (`XINFO GROUPS`/`XPENDING` on the candle stream the worker consumes — find the stream and group names in `consumer.py`/`main.py`); container CPU/memory (`docker stats --no-stream`); (c) the per-bar cost model: how many (version × market) evaluations per closed 1-min/15-min/1-h bar, how many context windows are re-read per evaluation (`context.py`, `context_budget.py`), whether the context is cached across versions of the same strategy or reloaded per version; (d) whether the replay engine shares the worker's event loop/DB pool (`replay/*`, `main.py`).
2. **Root cause statement**: replay contention, N×M evaluation cost, context reloads, DB pool exhaustion, or a lock — with the measurement that proves it, and what "2 s" looked like structurally before 09/09.
3. **Fix proposal, smallest first**, with the expected lag after each: e.g. separate replay into its own service/container (`ops`-style one-shot), cache context per (strategy, market) across versions, bound `required_context_minutes` reads, increase pool, shard versions. If a fix is code-only and ≤ 1 file + tests, implement it (unit + one testcontainer file, `timeout 290`, one at a time), else write the brief for the next task as `.claude/state/brief-T3.74b-*.md`. Never change a strategy parameter or a live version.
4. `.claude/state/notes-T3.74.md` with every command/output; research SQL `infra/scripts/sql/research/2026-09-10-t374-*.sql`; Obsidian `obsidian/07-BUGS/Open Bugs.md` entry updated with the root cause (the T3.73 entry exists — extend it); `git status --porcelain` list of every file touched/created. ≤ 12 lines in Portuguese, times in Brasília (UTC−3).

Rules: never a background shell; every command foreground with explicit `timeout 290`; testcontainers at most 1 file at a time; the tree is shared — never `git stash`/`checkout --`/`restore`/`reset`/`clean`/`commit -a`; do not commit; never touch `.env*`; do not edit `apps/**`. `ruff`, `pyright`, `check_file_size.py` clean on your files.

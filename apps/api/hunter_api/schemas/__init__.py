"""Pydantic v2 request/response models — the API's published contract.

These are what ``pnpm gen:types`` turns into TypeScript for the web app, so a
field renamed here renames it there; treat them as an interface, not as a
convenience.
"""

# T4.74 — Mesa `spot/1`: sinal do Lab (Binance) → Solana/Jupiter — plano por ondas

Desenho: `docs/design/spot1-lab-solana.md`. Regra de despacho: `.claude/rules/parallel-subagent-driven-development.md`
(conjuntos de arquivos disjuntos por onda; implementadores não commitam; o controlador commita por tarefa com
`git add <arquivos exatos>`). Todo arquivo ≤ 350 linhas (`infra/scripts/check_file_size.py`). Não tocar em
`infra/scripts/meme_spot_swap*.py` nem `services/meme-executor/hunter_meme_executor/spot_verify.py` (T4.73b em curso).
Nada liga: `SPOT1_ENABLED` nasce `false`; a ligação é do Everton depois da revisão do guardião e do Codex.

## Ondas
| Onda | Tarefas | Por quê |
|---|---|---|
| 1 | T4.74-1, T4.74-2 | schema e perfil de risco puro não se tocam |
| 2 | T4.74-3, T4.74-6 | fundações do executor; scripts/painel/docs dependem só das tabelas e do contrato do heartbeat (fixo no desenho §7) |
| 3 | T4.74-4 | caminho do dinheiro (precisa do perfil e das fundações) |
| 4 | T4.74-5 | saídas + fiação em `main.py`/`heartbeat.py` (precisa de `spot_leg`) |
| 5 | revisão | risk-engine-guardian (checklist §9 do desenho) → Codex → `.claude/state/notes-T4.74.md` → deploy inerte |

## T4.74-1 — Schema `0057_spot_desk` + ORM
Migração `0057` (`revises 0056_meme_spot_swaps`): `spot_desk_markets` (semente de 50 linhas do R63 §2a, sem `SOLUSDT`
e `ENAUSDT`; `enabled = tier ≠ 'C' AND custo ≤ 0,4 %`), `spot_orders`, `spot_positions` — colunas, CHECKs e grants
exatamente como o desenho §5; downgrade §17.7 recusa com assinatura presente. ORM espelhado byte a byte
(`alembic check` limpo). `HEAD_REVISION` → `0057_spot_desk`. Teste de migração por analogia ao `test_migration_0055.py`
(upgrade, contagem da semente, downgrade limpo, downgrade recusado com uma `spot_orders` assinada).
- Files: `infra/migrations/ddl/spot_desk.py`, `infra/migrations/ddl/spot_desk_seed.py`,
  `infra/migrations/versions/0057_spot_desk.py`, `packages/core/hunter_core/db/models/spot_desk.py`,
  `packages/core/hunter_core/db/models/__init__.py` (1 linha de export),
  `packages/core/tests/integration/test_migration_0057.py`,
  `packages/core/tests/integration/test_migrations.py` (só `HEAD_REVISION`)
- Depends-on: none
- Especialista: db (migração) · Aceite: `uv run alembic check` limpo; 0057 sobe/desce em testcontainers; 50 linhas semeadas.

## T4.74-2 — Perfil `spot` no motor de risco (puro)
`hunter_risk_meme/spot_profile.py`: `SPOT_LANE = "spot"`, `MemeSpotProfile` (`ticket_sol`, `max_open`, `max_parity_pct`,
`max_impact_pct`, `max_cost_r`, `max_signal_age_s`), `SpotSignalInputs` (ref, stop, target1, emitted_at, expires_at,
paridade, impacto, custo estimado), `evaluate_spot_entry(wallet: MemeWalletState, limits, profile, signal, kill) ->
MemeDecision` compondo `checks_wallet` (kill switch, `daily_loss`, `wallet_cap`, `concurrent_positions`) + checks
próprios nomeados (§2–§3 do desenho: `spot1_open_cap`, `duplicate_market`, `signal_stale`, `signal_expired`,
`geometry_invalid`, `parity`, `impact`, `cost_r`, sizing "ficha ou nada" com `binding_constraint`). Tabela de casos: cada
recusa aparece uma vez; posição `lane = spot` conta na vaga global e no cap diário; indisponível ⇒ `unavailable`, nunca zero.
Doc: `docs/RISK_ENGINE_MEME.md` nova §19 "Perfil spot/1".
- Files: `packages/risk-core/hunter_risk_meme/spot_profile.py`, `packages/risk-core/hunter_risk_meme/__init__.py`,
  `packages/risk-core/tests/test_spot_profile.py`, `docs/RISK_ENGINE_MEME.md`
- Depends-on: none
- Especialista: risk-engine · Aceite: 100 % das recusas cobertas; `pyright` limpo; nenhum `float`.

## T4.74-3 — Fundações da pista no executor
`spot_config.py` (`SpotConfig.from_env`, tabela §6, faixa/aviso como `launch_config.py`; `enabled` só com live + signer);
`spot_repo.py` (SELECT/INSERT/UPDATE em `spot_orders`/`spot_positions`/`spot_desk_markets`: candidatos, `insert_order`,
`mark_*`, `insert_position`, `open_spot_positions`, `set_mark`, `close_position`, `closed_stats(since)`, `decimals`
write-back — SQL limitado, `WORKER_ROLE`); `spot_signals.py` (a consulta §2 + leitura de `candles` para `bin_usd`/`sol_usd`
+ cálculo de paridade e geometria, puro no meio); `spot_brake.py` (`brake_positions(session) -> list[OpenPosition]` =
memes + spot mapeadas com `params.lane = "spot"`, e `spot_pending_intents`). Testes com fakes de sessão (padrão
`_fake_db` de `test_treasury_tick.py`).
- Files: `services/meme-executor/hunter_meme_executor/spot_config.py`,
  `services/meme-executor/hunter_meme_executor/spot_repo.py`,
  `services/meme-executor/hunter_meme_executor/spot_signals.py`,
  `services/meme-executor/hunter_meme_executor/spot_brake.py`,
  `services/meme-executor/tests/test_spot_foundations.py`
- Depends-on: T4.74-1
- Especialista: meme-executor · Aceite: `SPOT1_ENABLED=false` ⇒ nenhuma consulta; paridade indisponível ⇒ `None` com motivo.

## T4.74-4 — Caminho do dinheiro: `spot_leg`, entradas, regras de saída puras
`spot_send.py` (`spot_leg`, assinatura fixa do desenho §5.1; espelho de `treasury_send.attempt_swap` generalizado ao par
com SOL num dos lados; recusa `priority_fee_above_cap`; invariante lido de `simulation.accounts`; `pending` ⇒
`submitted_unconfirmed`); `spot_entries.py` (`spot_entries_once`: candidatos → inputs do perfil → `evaluate_spot_entry`
→ kill switch relido → `spot_leg` compra → `insert_position` com a geometria; 1 compra por tique; refutação/cooldown
lidos de `closed_stats`); `spot_exit_rules.py` (puro: `decide_exit(position, mark, now, kill) -> reason | None`,
slippage por tentativa/motivo, backoff); `JupiterClient.swap(max_priority_fee_lamports=None)`.
- Files: `services/meme-executor/hunter_meme_executor/spot_send.py`,
  `services/meme-executor/hunter_meme_executor/spot_entries.py`,
  `services/meme-executor/hunter_meme_executor/spot_exit_rules.py`,
  `packages/exchange-adapters/hunter_exchanges/jupiter/client.py`,
  `services/meme-executor/tests/test_spot_money_path.py`
- Depends-on: T4.74-2, T4.74-3
- Especialista: meme-executor (execução) · Aceite: `signer.sign` aparece uma única vez no diff; `client.swap()` sem o
  parâmetro continua enviando `"auto"` (teste da tesouraria intacto).

## T4.74-5 — Saídas, reconcile, fiação e heartbeat
`spot_exits.py` (`spot_exits_once`: marca por cotação, `decide_exit`, lock por posição, `spot_leg` venda, `close_position`
com PnL ao lamport, `blocked_exits`, refutação recalculada ao fechar; `spot_reconcile_once` cavalga no `reconcile`);
`spot_heartbeat.py` (campo `spot1`, §7). Fiação: `config.py` (`spot: SpotConfig`), `context.py` (`spot: SpotStats`),
`main.py` (tarefas `spot-entries`/`spot-exits` só com `spot.enabled`; log de boot), `heartbeat.py` (`fields["spot1"]`,
equity com `brake_positions`), `admission_context.py` e `launch_entries.py` (uma linha: `positions = await
brake_positions(session)`). Edits nos seis arquivos existentes são de 1–15 linhas cada; ficam nesta tarefa porque são o
ponto de encaixe e são disjuntos das outras ondas.
- Files: `services/meme-executor/hunter_meme_executor/spot_exits.py`,
  `services/meme-executor/hunter_meme_executor/spot_heartbeat.py`,
  `services/meme-executor/hunter_meme_executor/config.py`, `services/meme-executor/hunter_meme_executor/context.py`,
  `services/meme-executor/hunter_meme_executor/main.py`, `services/meme-executor/hunter_meme_executor/heartbeat.py`,
  `services/meme-executor/hunter_meme_executor/admission_context.py`,
  `services/meme-executor/hunter_meme_executor/launch_entries.py`,
  `services/meme-executor/tests/test_spot_exits.py`
- Depends-on: T4.74-4
- Especialista: meme-executor · Aceite: suíte inteira do executor verde; `heartbeat.py` ≤ 350 linhas (por isso
  `spot_heartbeat.py`); uma posição spot aberta muda `equity_sol` e `daily_loss_sol` no heartbeat.

## T4.74-6 — Scripts auditados, painel mínimo, docs
`infra/scripts/spot_desk_markets.py` (`--list`, `--enable/--disable <símbolo>`, `--set-mint`, `--sell-now <position_id>`,
`--reason`, dry-run padrão, `--apply` grava e registra em `system_events` componente `spot_desk`);
`infra/scripts/spot_ficha.py` (§7, dry-run, `--write`, idempotente); API `LiveExecutorOut.spot1` + parse no serviço;
`spot1-panel.tsx` dentro de "Carteira real" em `/meme/mesa`; `docs/ACTIVATION.md` §9j (o que o Everton digita, ordem da
prova ao vivo §9 do desenho); `.env.example` com as `SPOT1_*`; `obsidian/03-TRADING/Spot/README.md` + `Mesa-spot-1.md`
(placar vazio). Pode ser despachada como 6a (Python + docs) e 6b (API + web) na mesma onda — conjuntos disjuntos.
- Files: `infra/scripts/spot_desk_markets.py`, `infra/scripts/spot_ficha.py`, `infra/scripts/tests/test_spot_scripts.py`,
  `apps/api/hunter_api/schemas/meme_live.py`, `apps/api/hunter_api/services/meme_live.py`,
  `apps/web/components/meme-live/spot1-panel.tsx`, `apps/web/app/(app)/[orgSlug]/meme/mesa/page.tsx`,
  `docs/ACTIVATION.md`, `.env.example`, `obsidian/03-TRADING/Spot/README.md`, `obsidian/03-TRADING/Spot/Mesa-spot-1.md`
- Depends-on: T4.74-1 (tabelas para os scripts; o contrato do heartbeat vem do desenho §7, não de código)
- Especialista: ops/web · Aceite: scripts sem `--apply` não escrevem; `obsidian_lint.py` limpo; painel renderiza com
  `spot1 = null` (executor antigo) sem quebrar.

## Depois das ondas
1. `uv run pytest packages/risk-core services/meme-executor infra/scripts/tests packages/core/tests/unit -q -m "not live"`;
   `ruff`, `pyright`, `check_file_size.py`; suíte de migração com Docker onde houver.
2. Revisão do risk-engine-guardian (checklist §9 do desenho) → correções → Codex → `.claude/state/review-T4.74.md`.
3. Deploy com `SPOT1_ENABLED=false` (inerte). A prova ao vivo (0,02 SOL → 1 operação → 20) é do Everton, §9 do desenho.

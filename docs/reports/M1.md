# Relatório — Milestone 1 (Market data real: Binance)

**Status:** EM ELABORAÇÃO (2026-09-05). Faltam T1.6b (performance/sharding) e T1.7 (testes de integração e ponta a ponta); a Sexta-feira completa as seções marcadas ⏳ e aprova em nome do Everton quando os critérios objetivos forem cumpridos.

Formato da diretiva de 2026-09-05: COMPLETED · FILES CREATED · FILES MODIFIED · DATABASE CHANGES · TESTS · TEST RESULTS · REAL DATA CONNECTED · MOCKS REMAINING · BUGS · SECURITY ISSUES · OBSIDIAN UPDATED · NEXT STEP.

## COMPLETED
- **Decisão conjunta Claude ⇄ Astra** antes de codar (4 rodadas, `.claude/state/dialogue-M1.md`): rotas WS `/public` e `/market`, `@depth20`, recovery sem regressão, identidade determinística de liquidações, supervisão por conexão, staleness por componente.
- **T1.1 / T1.1b** tipos normalizados de mercado (`hunter_core.domain.market`), `event_ts`, `funding_kind` — `415cc83`, `3139fb4`.
- **T1.2 / T1.2b** adaptador Binance USDS-M público (`hunter_exchanges.binance`): REST (`exchangeInfo`, `klines`, `ticker/24hr`, `depth`, `premiumIndex`, `fundingRate`, `openInterest`, `time`), WS em duas rotas com ≤ 200 símbolos por conexão, assinaturas incrementais (SUBSCRIBE/UNSUBSCRIBE com ACK), funding realizado paginado, fila limitada que nunca descarta vela final, rate limit por peso e por tentativa com gate por IP, rotação antes das 24 h, fixtures gravadas ao vivo — `f71059e`, `97c36ff`.
- **T1.3** market-worker (`hunter_market_worker`, implementado pela Astra, revisado por 4 revisores + Astra adversarial, 3 partes de correção): universo dinâmico (top N por volume, allow/blocklist, diff-only), ingestão com hot state no Redis (contratos exatos), coalescência 250 ms, persistência em lote idempotente (velas finais, snapshots por minuto, funding realizado, OI por ciclo, liquidações `ON CONFLICT (id, ts)`), recovery de gaps em transação única (bootstrap sem watermark, buracos internos, `failed` com cooldown), supervisão (`forever`, watchdog por conexão, readiness com timeout), heartbeat por exchange — `b8c4766`.
- **T1.4** API: `GET /api/v1/markets`, `/markets/{exchange}/{symbol}`, `/candles`, `/system/workers`, `/system/market-status`, qualidade por componente (ticker/book/mark, `degraded` com gap, `unavailable`), 503 honesto quando o Redis cai — `1df4b87`, tipos regenerados `00c7996`.
- **T1.5 / T1.5b** telas: `/markets` (tabela virtualizada, busca, badges de qualidade que envelhecem sozinhos, linha inteira clicável, Ctrl/⌘K), `/markets/[exchange]/[symbol]` (velas 1 min, book top-20 e trades como snapshot com idade, derivativos), Live Market Status no dashboard e topbar (três estados: sem verificação ≠ indisponível ≠ atrasado), tabela de workers no System; polimento com direção de design conjunta (hierarquia calma, dourado raro, velas verdes/vermelhas, densidade 40/32 px coerente, UTC+offset, acessibilidade) — `9b6106f`, `25012ff`.
- **T1.6** prova operacional contra a Binance real (1h50): 6 defeitos encontrados e corrigidos com teste; `restart: unless-stopped` provado com corte de rede; velas conferidas contra o REST — `493c4ce`. Serviço `market-worker` no compose (Astra) — `d76a0cf`.
- **Ops:** VPS Contabo (Ubuntu 24.04, 12 vCPU, 47 GB) bootstrapada e endurecida (Docker, firewall 22/80/443, fail2ban, swap, backup diário do Postgres, Caddy) — `bd7bedc`, `9690369`, `e5bc12c`.
- ⏳ **T1.6b** performance/sharding do worker (meta: 200 mercados com `markets_ok` ≥ 95%).
- ⏳ **T1.7** testes de integração e ponta a ponta.

## FILES CREATED / FILES MODIFIED
`git diff --stat f153315..HEAD` → **402 arquivos, +46.466 / −188 linhas** (desde a auditoria). Principais áreas: `packages/exchange-adapters/hunter_exchanges/**` (adaptador), `services/market-worker/hunter_market_worker/**` (worker, 19 módulos), `packages/core/hunter_core/domain/market.py`, `packages/core/hunter_core/db/session.py` (timeouts), `apps/api/hunter_api/{routers,schemas,services,repositories}/{markets,system}*.py`, `apps/web/app/(app)/[orgSlug]/markets/**`, `apps/web/components/{markets,system,layout}/**`, `apps/web/hooks/**`, `infra/docker/{docker-compose.yml,docker-compose.override.yml,entrypoint.sh}`, `infra/vps/**`, `infra/scripts/{bootstrap_vps.sh,setup_env.sh,astra.sh}`, `docs/plans/{M1,M2}.md`, `docs/decisions/0003–0004`, `obsidian/**`. ⏳ Lista final por tarefa após T1.6b/T1.7.

## DATABASE CHANGES
Nenhuma migração nova no M1 (schema do M0 já cobria mercados). Tabelas que passaram a receber escrita real: `assets`, `markets`, `candles`, `market_snapshots`, `funding_rates`, `open_interest_history`, `liquidations`, `ingestion_gaps`, `system_events`. Reparo do banco local de desenvolvimento (`processed_events.claimed_at/completed_at`, migrado com imagem antiga). Pendências de ops registradas: `lock_timeout` no `create_partitions.py`, bounds de partição em UTC explícito, agendamento de partições (fechados na T1.6: ⏳ confirmar no commit).

## TESTS
Funções de teste por área (contagem no repositório): exchange-adapters 212 · market-worker 159 · api 351 · core 216 · web 311 (`it(`) · tests/integration 33 (⏳ T1.7 em curso).

## TEST RESULTS (saída real)
- `uv run pytest packages/exchange-adapters -q` → **189 passed, 3 skipped**; `HUNTER_LIVE_TESTS=1 … -m live` → **3 passed** (dado real nas duas rotas).
- `uv run pytest services/market-worker -q` → **139 passed** (2 rodadas, sem instabilidade); `uv run pytest packages/core -q` → **254 passed**.
- `uv run pytest apps/api -q` → **445 passed, 1 failed** (o vermelho era o `command_timeout` da T1.3 sobre um teste de webhook; ⏳ confirmar verde após o ajuste).
- `pnpm --filter @hunter/web test` → **45 arquivos / 323 testes**; lint zero avisos; `docker compose build web` → `Image hunter-web:dev Built`.
- ruff / pyright limpos em todos os pacotes tocados; `check_file_size.py` → 0 acima de 350 linhas.
- ⏳ T1.7: `tests/integration`, `-m live`, `pnpm --filter @hunter/e2e e2e`.

## REAL DATA CONNECTED
- Binance USDS-M perpétuos USDT, API pública, sem chave: universo dinâmico (top por volume 24 h), 6 streams por mercado (`aggTrade`, `bookTicker`, `depth20`, `kline_1m`, `markPrice@1s`, `forceOrder`) em duas rotas.
- Prova T1.6 (1h50 ao vivo): 200 mercados, 1.200 assinaturas, **324.300 velas de 1 min persistidas, 800 conferidas contra o REST da Binance com zero divergência**, 3.860 snapshots, 2.706 gaps recuperados (`open → recovered`).
- Hoje no ambiente local: **50 mercados** (override `MARKET_UNIVERSE_SIZE=50`) com hot state completo (50/50 tickers, 49/50 books, preço vivo do BTCUSDT); ⏳ 200 após T1.6b.
- O dono viu o mercado real em `/ever/markets` e no detalhe em 2026-09-05.

## MOCKS REMAINING
Nenhum dado fabricado. O que ainda é honesto-vazio: Radar, Opportunities, Portfolio, Trades, Agents, Risk, Lab, Journal (itens da nav desabilitados com o milestone); métricas Prometheus de proposals/fills (sem produtor até M3/M4).

## BUGS
Encontrados e corrigidos no M1 (com teste): script Lua do rate limiter passava `float` ao `EXPIRE` (pipeline morto contra Redis real; suíte verde com dublê) · inserts linha a linha em snapshots/OI (20 s por flush) · `check_gaps` com 1.005 statements por passada · fila de perdas que derrubava o processo · Postgres em queda matando o worker · refresh de universo falhado dormindo 900 s · Redis reiniciado deixando o worker zumbi · healthcheck com teto de 3 s vs latência de 24 s · funding rotulado errado (36% de diferença) · header de peso da Binance devolvendo orçamento gasto (risco de 418) · nível de book malformado virando preço inventado · chave Redis corrompida derrubando a lista · `hostname:pid` exposto a qualquer usuário · export não-async em arquivo `"use server"` · helper de módulo cliente chamado pelo servidor · linha da tabela sem clique · `role="presentation"` anulando a grade para leitor de tela · `localStorage` sem proteção.
Abertos (registrados em `obsidian/07-BUGS/Open Bugs.md` e `docs/plans/M1.md` "Limitações conhecidas"): worker satura um core a 200 mercados (⏳ T1.6b); `command_timeout` global afeta a API (follow-up); `last_price` com carimbo do `bookTicker` (corrigido na T1.3 ⏳ confirmar); cooldown do rate limit não persistido entre processos (T1.6b/T2.9); idade própria de liquidações (M2); paginação SQL da lista de mercados (M2); tooltip por componente e modelo de scroll do `thead` (M2); `spread_pct` ×100 nos helpers de domínio (T1.1c).

## SECURITY ISSUES
Corrigidos: `hostname:pid` de processos exposto via `/system/workers`; ação de servidor sem sessão (agora falha fechada); tamanho máximo de busca; encoding de símbolos em URLs. Aceitos e registrados: JWT sem `azp` aceito (herdado do M0, `azp` obrigatório no M2); Astra roda sem sandbox por autorização explícita do dono com controles compensatórios (`infra/scripts/astra.sh`); VPS com senha de SSH desligada, só chave; segredos só no `.env` local/VPS.

## OBSIDIAN UPDATED
`08-CHANGELOG/Changelog.md` (um item por commit), `07-BUGS/{Open,Resolved} Bugs.md`, `06-DECISIONS/Architecture Decisions.md` (+ `Dialogos/`, `Revisoes-Astra/`), `02-MARKET/{Market Collector,Exchange Adapters,WebSockets}.md` (status real com caminhos), `01-ARCHITECTURE/{System Overview,Mente da Sexta-feira}.md`, `09-OPERATIONS/{Deployment,Monitoring}.md` e `Diario/2026-09-05.md`. ⏳ `Market Collector` → `implementado` só com 200 mercados sustentados.

## NEXT STEP
Fechar T1.6b e T1.7 → aprovação do M1 pela Sexta-feira → onda 1 do M2 (`docs/plans/M2.md`, decisão conjunta fechada): T2.1 schema (baselines imutáveis, envelopes, `EXTENDED`, `UNKNOWN`, `evaluation_state`, outbox) e T2.2 Feature Engine, em paralelo; depois anomalias/estágio, regime v0 e Opportunity Score, scanner-worker, API do Radar e telas.

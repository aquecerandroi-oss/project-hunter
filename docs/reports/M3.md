# M3 — Carteira virtual SPOT e Risk Engine paper_v1 — relatório (RASCUNHO)

**Estado em 2026-09-07 (tarde): código completo, NÃO APROVADO.** Este rascunho foi escrito pelo orquestrador para o parecer da Sexta-feira (delegação do Everton de 2026-09-05). Faltam antes do parecer: as revisões obrigatórias da T3.15 (`0010`) e da T3.0e, a consolidação de docs da T3.10 e o segundo deploy da VPS. Nenhum número abaixo foi estimado: cada um vem de um commit, de uma prova em `.claude/state/*-proof.md` ou de uma nota `notes-T3.*.md` com a saída colada.

Diretiva de origem: `.claude/state/directive-risk-engine-2026-09-06.md` (verbatim do Everton). Decisão conjunta Claude ⇄ Astra: `.claude/state/dialogue-M3.md`. Plano: `docs/plans/M3.md` (15 tarefas, 5 ondas, D1–D13). Contrato: `docs/RISK_ENGINE.md` v2.2.1. ADR: `docs/decisions/0005-carteira-virtual-e-risk-engine-paper-v1.md`.

## COMPLETED

| Objetivo (definition of done) | Evidência |
|---|---|
| Carteira principal única e permanente por organização, aberta com R$ 100.000 convertidos a USDT com cotação e fonte registradas, sem aporte nem reset | `0006_paper_wallet` (`11faba8`, `296f3c1`): `portfolio_currency_anchor`, `fx_observations`, índice `uq_portfolios_principal_paper`; ledger T3.3 (`8a6a69f`, `d23b7bd`); carteira `ever` **aberta na VPS em 2026-09-07 04:27Z** com 19.333,0111164813 USDT a 5,1725 (fonte: coletor USDTBRL, `09eb6de`) |
| Resultado em BRL e USDT separando operacional de cambial | `PortfolioState` com `operacional_brl = (E − E0)·F0`, `cambial_brl = E·(Ft − F0)`; página `/[org]/portfolio` (`817f129`, `096d8c5`), BRL na convenção brasileira |
| Risco 0,25 % por operação com custos, soma ≤ 1 %, participação ≤ 1 % do minuto, β-BTC ≤ 0,5×, total ≤ 40 %, por moeda ≤ 10 %, máx. 5 posições, pendentes contam | `packages/risk-core` T3.2 (`bf4924b`, `5f86028`): nove tetos, `binding_constraint`, dois contrafactuais, fail-closed; 204 testes; `RiskLimits` = perfil `paper_v1` (fonte única provada, `8c53b30`) |
| Kill switch AVISO (1 % dia / 4 % DD → metade) e BLOQUEADO (2 % / 8 % → bloqueia entradas, cancela pendentes, mantém proteções, nunca liquida tudo, retoma só com o Everton), America/Sao_Paulo, pico histórico | T3.6 (`9a0ac45`) durável em `portfolio_risk_state` + `kill_switch_transitions` com prova de mesma transação (`xmin`); retomada só OWNER (`0007`, `2688ef1`); V2 e V3 provadas pelo caminho persistido (`eccb648`) |
| Somente SPOT, sem alavancagem, short ou futuros; piso 50 M USDT 24 h | Adaptador `binance_spot` (`078d6ef`), `market_type` em toda identidade fora do banco (`cefad8c`), ingestão spot com universo próprio ≥ 50 M (`8822b31`), banda de saída D12 (`abf8e80`); `LiveExecutionAdapter` levanta `LiveTradingDisabled` |
| Entradas só a mercado, fill pelo livro elegível após latência declarada, sem fill fabricado, proteções duráveis na mesma transação da posição | Simulador T3.4 (`edd5d7e`, `ec78727`); execution-worker T3.5 (`7ecafd2`, `12edda3`, `90f1862`); V9 "sem livro, sem fill" e §10 crash em fronteiras (`ae25e32`) |
| Ponte sinal → proposta existe mas **não** admite nada sem decisão do Everton | T3.14 (`12edda3`) atrás de `ENABLE_PAPER_AUTONOMY=false`; todo sinal do Lab é `research_only`; rótulo `paper` só nasce por `activate_strategy_version.py --paper-line` (`6b837ac`) e o portão da ponte só aceita `paper` (`56d2dea`); `live` recusado por nome |
| Nove verificações antes do modo autônomo | V1–V3 + §11 (`eccb648`), V4–V9 + §10 (`ae25e32`): 55 testes no caminho persistido, 4 `xfail(strict)` com a divergência medida registrada (`spec-T3.9-verificacoes.md` §13/§13b) |
| **Modo autônomo declarado pronto?** | **Não.** A diretiva diz "só quando o fluxo completo estiver verificado". O fluxo está verificado em teste; falta a ativação auditada de uma versão `paper` (D10, sete condições, ato do Everton) e a observação real na VPS |

## FILES CREATED (principais)
`packages/risk-core/hunter_risk/**` · `packages/core/hunter_core/{ledger,execution,admission,risk}/**` · `services/execution-worker/**` · `services/market-worker/hunter_market_worker/{spot,spot_universe,spot_band,coverage_limits,heartbeat_events,fx}.py` · `packages/exchange-adapters/hunter_exchanges/binance_spot/**` · `services/strategy-worker/hunter_strategy_worker/{activation_db,paper_line}.py` · `infra/migrations/versions/0006..0010_*.py` + `ddl/{paper,paper_roles,paper_roles_2,paper_geometry,strategy_purpose}.py` · `apps/web/app/(app)/[orgSlug]/portfolio/**` · `tests/integration/paper/**` (12 arquivos) · `docs/RISK_ENGINE.md`, `docs/plans/M3.md`, `docs/decisions/0005-*.md`, `docs/HERMES.md` · `.claude/state/{brief,notes,review}-T3.*.md`, `t30-proof.md`, `t35-proof.md`, `spec-T3.9-verificacoes.md`, `decisions-M3-delegated-2026-09-06.md`, `decisions-delegated-2026-09-07.md`.

## FILES MODIFIED (principais)
`docs/DATABASE.md` §18–§22 · `docs/PIPELINE.md` (§1d spot, §8 ponte e kill switch, tabela de streams) · `docs/DEPLOYMENT.md` (execution-worker, métricas, rollback `DEL mkt:*:candles:1m`, `ip_range` do Caddy) · `docs/SECURITY.md` §2 (retomada = OWNER) · `packages/core/hunter_core/{settings,observability}.py` · `services/market-worker/**` (identidade `market_type`, ids de evento, cobertura multi-shard) · `services/scanner-worker/hunter_scanner_worker/main.py` (descarta spot) · `apps/api/hunter_api/{routers,services,repositories}/**` (portfolio, risk, markets, system) · `apps/web` (System card, painel "Por que estamos olhando isso?") · `infra/docker/docker-compose.yml`, `infra/vps/docker-compose.prod.yml`, `infra/vps/compose.sh` · `infra/scripts/{activate_strategy_version,open_paper_wallet}.py`.

## DATABASE CHANGES
| Revisão | O quê | Estado |
|---|---|---|
| `0006_paper_wallet` | carteira, reservas, ordens/fills/posições/trades com FKs compostas, âncora de câmbio, `fx_observations`, `market_betas`, `portfolio_risk_state`, `kill_switch_transitions` (mesma transação por `xmin`), intenções de saída, consumos de participação, principal única por organização | VPS ✔ |
| `0007_paper_roles` | worker decide e grava risco; API pede, lê e autoriza; retomada OWNER; `request_digest`; `brl_unavailable_reason`/`marks_stale` | VPS ✔ |
| `0008_paper_roles_2` | API sem DML em ordens/fills/posições/trades; carteira nasce auditada; motivo do kill switch só com transição | VPS ✔ (deploy de 2026-09-07 ~16:26Z) |
| `0009_paper_geometry` | `trade_proposals.request_payload`, `positions.is_residual` (pó não é posição) | VPS ✔ |
| `0010_strategy_purpose` | `strategy_versions.purpose` (`research_only`/`paper`/`live`), congelado após ativação, escrita só pelo dono | **local ✔, VPS pendente** (segundo deploy) |
Todas com round trip, `alembic check` (`test_alembic_check_reports_no_drift`) e testes de privilégio por papel.

## TESTS CREATED
`packages/risk-core/tests` (204) · `packages/core/tests/{unit,integration}` para ledger, simulador, admissão, kill switch, β, schema 0006–0010 (`test_schema_paper` 96, `test_schema_privileges` 23, `test_migrations` 49) · `services/execution-worker/tests` (132 + T3.15b) · `tests/integration/paper/**` (55 + 4 xfail) · `services/market-worker/tests/test_spot_*` (46) · `services/strategy-worker/tests/test_activate_paper_line.py` (6) · `apps/web` (534 testes, System e portfolio).

## TEST RESULTS (saída real, 2026-09-07, uma invocação por arquivo)
```
packages/core/tests/integration: test_migrations 49 · test_schema_privileges 23 · test_schema_paper 96 · test_schema_shadow 53 · test_schema_rls 17 · test_schema_constraints 13 · test_admission 17 · test_admission_reservation 10 · test_portfolio_state 22
packages/core/tests/unit 706 passed · apps/api/tests/unit 382 passed
tests/integration/paper: 53 passed, 4 xfailed (suite inteira, 398 s; 1 erro WinError 64 = flake do testcontainers no Windows, verde isolado)
services/execution-worker/tests: 132 passed (17 arquivos) + T3.15b: 14+7+5+3+11
services/market-worker/tests: 364 passed (41 arquivos) + T3.0d/T3.0e (test_spot_band 14, test_spot_universe 17, test_market_ids 3)
services/strategy-worker/tests: unit 159 · test_activate_paper_line 6 · test_activation 12 · test_version_roster 9 · test_shadow_decisions 15 · test_code_ref 23
packages/exchange-adapters: 385 passed, 7 skipped (live)
ruff check/format --check: limpos · pyright: 0 erros nos pacotes de produção · check_file_size: 465 arquivos, 0 acima de 350 linhas
```

## REAL DATA CONNECTED
- Carteira `ever` aberta na VPS com câmbio real do coletor USDTBRL (uma observação por minuto, idempotente, nunca fabricada).
- Execution-worker na VPS desde 2026-09-07 ~16:26Z: `hb:execution:paper` com `equity 19333.0111164813`, `kill_switch ACTIVE`, MTM no minuto, `/ready` 200, `paper_autonomy false`; seis pontos de curva gravados nos primeiros minutos.
- Ingestão spot provada 15 min ao vivo contra a Binance pública (`t30-proof.md`): 19 fitas, 16 livros, +122 velas spot; **desligada por padrão** (`MARKET_SPOT_ENABLED=false`) porque, com 200 perpétuos no mesmo event loop, ligar o spot derrubou o socket perpétuo (1011, 8 reconexões em 6 min).
- Prova do worker no stack local por 30 min (`t35-proof.md`): pedido manual → fill → posição com proteções → stop → saída → PnL reconciliado, 0 exceções.

## MOCKS REMAINING
- O adaptador de execução é um **simulador** contra o livro spot real (é o produto do M3: paper). Nenhuma ordem vai à exchange; `LiveExecutionAdapter` recusa por construção.
- V6 itens 3/4 (queda de WS com gap, perda de Redis no caminho da carteira) esperam o spot ligado no hot state.
- `avgPrice` do ticker spot não é coletado (recusa conservadora no simulador).

## BUGS (abertos, com dono)
- `obsidian/07-BUGS/Open Bugs.md` (plantão de 2026-09-07): 12 entradas novas — 4 da `review-T3.14.md` (fechadas na T3.14b), 5 da `review-T3.0b.md` (fechadas na T3.0d), pó sem assentamento (sweep), `SPOT_VOLUME_FLOOR_USDT` duplicado, flaky `WinError 64`, `agents` obrigatório para a ponte.
- 160 erros de pyright pré-existentes em `services/execution-worker/tests/test_restart_recovery.py`.
- `AssumedCosts` aceita `float` (sensível ao `code_ref`).
- `queue_oldest_pending_ts` a 11 % de CPU por shard (T2.5h, shard por tráfego).

## SECURITY ISSUES
- Fechados: os dois bloqueantes da revisão da 0006 (`c35fd40` → `296f3c1`); S1–S3 da revisão da 0007 (`d6dbbca` → `c1f8c5f`, `cdb4bb5`); `find_pending` com `FOR UPDATE` sob `hunter_app` (`90f1862`).
- Abertos: revisão de segurança da `0010` (escrita de `purpose` só pelo dono — provado por teste, falta o parecer); rotação do certificado interno do Caddy quando o domínio (D13) existir.

## OBSIDIAN UPDATED
Plantão de 2026-09-07 (`4806649`): changelog com 19 entradas, bugs, diário, `03-TRADING/{Risk Engine,Portfolio}.md`, `00-HOME.md`; D10–D13 (`fa09b5d`). **Pendente:** entradas de changelog para os commits da tarde (`8822b31` … `56d2dea`), página da Sexta-feira no Hermes em `04-AGENTS/`, diário da tarde.

## NEXT STEP
1. Revisões da T3.0e (em voo, Sexta-feira no Hermes) e da T3.15/`0010` (database-architect + guardian + security), depois T3.10 (docs) e o parecer da Sexta-feira sobre este relatório.
2. Segundo deploy da VPS (`0010` + T3.15b), comando do Everton.
3. Decisão do Everton: `activate_strategy_version.py momentum v1 --paper-line` e a ativação auditada da linha `paper` quando as sete condições da D10 estiverem provadas (β válido exige ~20 dias contíguos: não antes de 2026-09-18/25).
4. M2: condições 2 (leitura datada 2026-09-09/10), 3 (p99 pós T2.5h) e 4 (e2e com `CLERK_E2E_*`).

## NEXT MILESTONE
M4 — agentes e a ponte sinal → proposta em modo autônomo paper (só depois de D10 cumprida), ordens limit passivas com série de capacidade (T3.M), analytics/journal (M5). Dinheiro real continua na Fase 4, fora de qualquer plano atual.

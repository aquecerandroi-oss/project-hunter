# M3 — Carteira virtual SPOT e Risk Engine paper_v1 — relatório

**RASCUNHO — parecer da Sexta-feira pendente.** Estado em 2026-09-08 (HEAD `d6b8b37`): código
completo, incluindo o que passou da onda 5 original (T3.M, T3.10) para tarefas novas que a própria
onda 5 abriu (T3.11–T3.25 — replicação, replay, catálogo Obsidian, Hermes, Lab em dinheiro, horário
de Brasília). **Nada foi ativado.** Faltam antes do fechamento: o segundo deploy da VPS com `0010`
e `0011` já está feito, mas `0012`/`0013` (replicação/replay) ainda **não**; a Parte B da T3.25
(telas web de Strategies/Backtests/Trades/Risk Center) só foi despachada, não executada; a segunda
opinião da Astra sobre D10–D13 (ela volta em 2026-09-12); e o parecer desta própria Sexta-feira.
Nenhum número abaixo foi estimado: cada um vem de um commit, de uma prova em `.claude/state/*-proof.md`
ou de uma nota `notes-T3.*.md`/`review-T3.*.md` com a saída colada.

Diretiva de origem: `.claude/state/directive-risk-engine-2026-09-06.md` (verbatim do Everton).
Decisão conjunta Claude ⇄ Astra: `.claude/state/dialogue-M3.md`. Plano: `docs/plans/M3.md` (15
tarefas originais, 5 ondas, D1–D13; D14/D15 em `.claude/state/decisions-delegated-2026-09-08.md`
tratam do protocolo de replicação, fora do plano original). Contrato: `docs/RISK_ENGINE.md` v2.2.
ADR: `docs/decisions/0005-carteira-virtual-e-risk-engine-paper-v1.md`. Runbook de ativação:
`docs/ACTIVATION.md`. Protocolo de replicação: `docs/plans/REPLICATION.md`.

**Nota de escopo.** A onda 5 do plano ("prova integrada e fechamento") ganhou, em voo, um conjunto
de tarefas que o plano de 2026-09-06 não previa: replicação de versões promissoras (T3.19),
replay histórico como motor de validação em massa (T3.19b–d), catálogo de estratégias no Obsidian
(T3.20), Bases/canvases/linter da base de conhecimento (T3.21), horário de Brasília em toda a UI
(T3.22), primeira auditoria de design com dado real (T3.23) e o despacho para tirar quatro páginas
de "Planejado" (T3.25). Nenhuma delas muda o **objetivo do M3** (carteira virtual + Risk Engine com
entradas manuais); são trabalho que apareceu porque o Everton pediu "bora ativar tudo" e "repetir
uma estratégia promissora várias vezes" no meio da execução. Estão registradas aqui porque tocam
os mesmos arquivos e o mesmo schema, e porque a próxima decisão de ativação (D10) depende de dado
que elas produzem (β, spot como serviço próprio).

## COMPLETED

| Objetivo (definition of done) | Evidência |
|---|---|
| Carteira principal única e permanente por organização, aberta com R$ 100.000 convertidos a USDT com cotação e fonte registradas, sem aporte nem reset | `0006_paper_wallet` (`11faba8`, `296f3c1`): `portfolio_currency_anchor`, `fx_observations`, índice `uq_portfolios_principal_paper`; ledger T3.3 (`8a6a69f`, `d23b7bd`); carteira `ever` **aberta na VPS em 2026-09-07 04:27Z** com 19.333,0111164813 USDT a 5,1725 (fonte: coletor USDTBRL, `09eb6de`/T3.11a) |
| Resultado em BRL e USDT separando operacional de cambial | `PortfolioState` com `operacional_brl = (E − E0)·F0`, `cambial_brl = E·(Ft − F0)`; página `/[org]/portfolio` (`817f129`, `096d8c5`), BRL na convenção brasileira; horário de exibição em Brasília com UTC no tooltip (T3.22, `6d142fe`) |
| Risco 0,25 % por operação com custos, soma ≤ 1 %, participação ≤ 1 % do minuto, β-BTC ≤ 0,5×, total ≤ 40 %, por moeda ≤ 10 %, máx. 5 posições, pendentes contam | `packages/risk-core` T3.2 (`bf4924b`, `5f86028`): nove tetos, `binding_constraint`, dois contrafactuais, fail-closed; 204 testes; `RiskLimits` = perfil `paper_v1` (fonte única provada, `8c53b30`) |
| Kill switch AVISO (1 % dia / 4 % DD → metade) e BLOQUEADO (2 % / 8 % → bloqueia entradas, cancela pendentes, mantém proteções, nunca liquida tudo, retoma só com o Everton), America/Sao_Paulo, pico histórico | T3.6 (`9a0ac45`) durável em `portfolio_risk_state` + `kill_switch_transitions` com prova de mesma transação (`xmin`); retomada só OWNER (`0007`, `2688ef1`); V2 e V3 provadas pelo caminho persistido (`eccb648`) |
| Somente SPOT, sem alavancagem, short ou futuros; piso 50 M USDT 24 h, com histerese só na saída (D12) | Adaptador `binance_spot` (`078d6ef`), `market_type` em toda identidade fora do banco (`cefad8c`), ingestão spot com universo próprio ≥ 50 M (`8822b31`), banda de saída D12 (`abf8e80`); **spot virou serviço próprio** (`market-worker-spot`, T3.0f, `0451066`) — provado na VPS: 14 pares, 0 reconexões, sem afetar o perpétuo; `LiveExecutionAdapter` levanta `LiveTradingDisabled` |
| Entradas só a mercado, fill pelo livro elegível após latência declarada, sem fill fabricado, proteções duráveis na mesma transação da posição | Simulador T3.4 (`edd5d7e`, `ec78727`); execution-worker T3.5 (`7ecafd2`, `12edda3`, `90f1862`); V9 "sem livro, sem fill" e §10 crash em fronteiras (`ae25e32`) |
| Câmbio BRL: coletor e uso na abertura | `services/market-worker/.../fx.py` (T3.11a, `09eb6de`): ticker `USDTBRL` a cada ~60 s no shard 0, idempotente, banda de plausibilidade grava e sinaliza mas não recusa; 9 testes com testcontainers |
| β versionado contra o BTC | `packages/indicators` (T3.7, `da2fb49`) + produtor horário no scanner-worker (T3.7b, `8424ec9`) escrevendo `market_betas`; **na VPS o backfill de 31 dias foi pedido em 2026-09-08 ~04:35Z e ainda não terminou** — sem ele, todo β é `insufficient_history` e a admissão continua recusando por `beta_unavailable` (ver MOCKS REMAINING) |
| Ponte sinal → proposta existe mas **não** admite nada sem decisão do Everton | T3.14 (`12edda3`) atrás de `ENABLE_PAPER_AUTONOMY=false`; todo sinal do Lab é `research_only`; rótulo `paper` só nasce por `activate_strategy_version.py --paper-line` (`6b837ac`) e o portão da ponte só aceita `paper` (`56d2dea`, `9a291d3`); `live` recusado por nome; a ponte agora julga a **coluna** `purpose`, não o envelope (T3.15c) |
| Nove verificações antes do modo autônomo | V1–V3 + §11 (`eccb648`), V4–V9 + §10 (`ae25e32`): 55 testes no caminho persistido, 4 `xfail(strict)` com a divergência medida registrada (`spec-T3.9-verificacoes.md` §13/§13b) |
| Protocolo de replicação (fora do plano original, pedido do Everton em 2026-09-08) congelado, e nenhum passo dele toca a carteira | `docs/plans/REPLICATION.md` (T3.19, `b378d57`): quatro blocos (fora da amostra, 10 irmãs, metades de mercado, bootstrap), veredito `real`/`refutada`/`replicando`, três barreiras de isolamento provadas por teste; migração `0012_replication` (T3.19c, `5c97b18`) grava `promising_at`/linhagem em coluna |
| Motor de replay histórico como caminho de validação em massa (fora do plano original) | T3.19b (`79379ef`): reusa `evaluate_slot` (a mesma função do caminho vivo), nunca escreve `shadow_outbox`, prova de não-antecipação por mutação; medido em 2026-09-08: 3,93 barras/s (3 processos, Docker Desktop Windows), projeção de ~75 barras/s/processo na VPS (não medida lá ainda); recibo durável por fatia em `0013_replay_runs` (T3.19d, `04f949d`) |
| Catálogo de estratégias e higiene da base de conhecimento (fora do plano original) | `infra/scripts/export_strategies_to_obsidian.py` (T3.20, `f7c76b8`): 19 páginas geradas das linhas reais da VPS; Bases/canvases/linter (T3.21, `ee2b472`/`82218c9`): `obsidian_lint.py`, 26 testes |
| **Modo autônomo declarado pronto?** | **Não.** A diretiva diz "só quando o fluxo completo estiver verificado". O fluxo está verificado em teste; faltam β válido na VPS, a ativação auditada de uma versão `paper` (D10, sete condições, ato do Everton) e a observação real |

## FILES CREATED (principais)

`packages/risk-core/hunter_risk/**` · `packages/core/hunter_core/{ledger,execution,admission,risk}/**` ·
`services/execution-worker/**` ·
`services/market-worker/hunter_market_worker/{spot,spot_universe,spot_band,coverage_limits,heartbeat_events,fx}.py` ·
`packages/exchange-adapters/hunter_exchanges/binance_spot/**` ·
`services/strategy-worker/hunter_strategy_worker/{activation_db,paper_line,replication,replication_stats,replay/*}.py` ·
`infra/migrations/versions/0006..0013_*.py` +
`ddl/{paper,paper_roles,paper_roles_2,paper_geometry,strategy_purpose,strategy_activation_owner,replication,replay_runs}.py` ·
`apps/web/app/(app)/[orgSlug]/portfolio/**` · `apps/web/components/lab/**` (Placar, curva, tabela em dinheiro) ·
`apps/web/lib/time.ts` (horário de Brasília único) ·
`tests/integration/paper/**` (12 arquivos) ·
`infra/scripts/{replicate_strategy_version,export_strategies_to_obsidian,obsidian_lint,obsidian_lint_rules,obsidian_lint_links}.py` ·
`infra/hermes/**` (SOUL, skills, memórias, installer) ·
`docs/{RISK_ENGINE.md,plans/M3.md,plans/REPLICATION.md,decisions/0005-*.md,HERMES.md,OBSIDIAN.md,ACTIVATION.md}` ·
`.claude/state/{brief,notes,review}-T3.*.md`, `t30-proof.md`, `t30f-proof.md`, `t35-proof.md`,
`spec-T3.9-verificacoes.md`, `decisions-M3-delegated-2026-09-06.md`,
`decisions-delegated-2026-09-0{7,8}.md` ·
`obsidian/03-TRADING/Estrategias/**` (19 páginas geradas), `obsidian/03-TRADING/Estratégias.base`,
`obsidian/05-EXPERIMENTS/Experimentos.base`, `obsidian/01-ARCHITECTURE/Fluxo sinal → carteira.canvas`,
`obsidian/03-TRADING/Família momentum.canvas`, `obsidian/05-EXPERIMENTS/EXP-0005-momentum-paper.md`,
`obsidian/04-AGENTS/{Sexta-feira no Hermes,Product Designer}.md`.

## FILES MODIFIED (principais)

`docs/DATABASE.md` §18–§25 · `docs/PIPELINE.md` (§1c FX, §1d spot, §2b β, §6b/§6c Lab e replay, §8
ponte e kill switch) · `docs/DEPLOYMENT.md` (execution-worker, coletor spot dedicado §3.3, replay
§5.2, VPS §9) · `docs/SECURITY.md` §2 (retomada = OWNER) ·
`packages/core/hunter_core/{settings,observability}.py` ·
`services/market-worker/**` (identidade `market_type`, `MARKET_ROLE`, ids de evento, cobertura
multi-shard) · `services/scanner-worker/hunter_scanner_worker/main.py` (β horário, descarta spot) ·
`apps/api/hunter_api/{routers,services,repositories}/**` (portfolio, risk, markets, system, lab,
lab_scoreboard) · `apps/web` (System card, painel "Por que estamos olhando isso?", Lab em dinheiro,
horário de Brasília em todo call site) · `infra/docker/docker-compose.yml`,
`infra/vps/docker-compose.prod.yml`, `infra/vps/compose.sh` (perfil `spot`, `MARKET_SPOT=1`) ·
`infra/scripts/{activate_strategy_version,open_paper_wallet}.py`.

## DATABASE CHANGES

| Revisão | O quê | Estado |
|---|---|---|
| `0006_paper_wallet` | carteira, reservas, ordens/fills/posições/trades com FKs compostas, âncora de câmbio, `fx_observations`, `market_betas`, `portfolio_risk_state`, `kill_switch_transitions` (mesma transação por `xmin`), intenções de saída, consumos de participação, principal única por organização | VPS ✔ |
| `0007_paper_roles` | worker decide e grava risco; API pede, lê e autoriza; retomada OWNER; `request_digest`; `brl_unavailable_reason`/`marks_stale` | VPS ✔ |
| `0008_paper_roles_2` | API sem DML em ordens/fills/posições/trades; carteira nasce auditada; motivo do kill switch só com transição | VPS ✔ |
| `0009_paper_geometry` | `trade_proposals.request_payload`, `positions.is_residual` (pó não é posição) | VPS ✔ |
| `0010_strategy_purpose` | `strategy_versions.purpose` (`research_only`/`paper`/`live`), congelado após ativação, escrita só pelo dono | VPS ✔ (segundo deploy) |
| `0011_strategy_activation_owner` | revoga de `hunter_worker`: `UPDATE` em `status`/`activated_at`/`deprecated_at`/`code_ref`/`parameters_schema`/`default_parameters`/`params_format`, `DELETE` e `INSERT` inteiros em `strategy_versions` — só o script de ativação (conexão de dono) escreve o catálogo | VPS ✔ (segundo deploy) |
| `0012_replication` | coorte `replication:<parent>:<k>` no CHECK e no padrão de domínio, `strategy_versions.promising_at`/`promising_by`, linhagem (`replication_parent_id`/`replication_index`, `UNIQUE(parent, index)`), tudo owner-only por subtração de ACL | **local ✔ (parcial — ver Concerns), VPS pendente** |
| `0013_replay_runs` | recibo durável por fatia de replay (`run_id`, janela, mercados, barras, sinais, outcomes, `bars_per_second`, `seconds`), `INSERT`-only para o worker (nunca `UPDATE`/`DELETE`), leitura para a API, downgrade recusa apagar recibo | **commitada, não aplicada em nenhum stack ainda (nem local nem VPS)** |

Todas com round trip, `alembic check` (`test_alembic_check_reports_no_drift`) e testes de privilégio
por papel. **Divergência entre stacks, registrada e não escondida:** o stack local ficou em `0012`
quando a `0013` foi escrita (`notes-T3.19d.md` §7.1 — "não apliquei, é banco compartilhado"); a VPS
está em `0011` (segundo deploy trouxe `0010`+`0011`, não `0012`/`0013`, que vieram depois). Efeito
real: um replay rodado hoje contra qualquer um dos dois stacks grava evidência em `system_events` e
no JSONL, mas **não** no recibo durável — loga `replay_run_table_missing` em `error` e continua sem
quebrar a corrida. Nenhuma linha de produto foi perdida; a série de recibos duráveis começa quando
alguém rodar `alembic upgrade head` no stack certo.

## TESTS CREATED

`packages/risk-core/tests` (204) ·
`packages/core/tests/{unit,integration}` para ledger, simulador, admissão, kill switch, β, schema
0006–0013 (`test_schema_paper` 96, `test_schema_privileges` 23, `test_migrations` 49 → 63 com `0012`/`0013`) ·
`services/execution-worker/tests` (132 + T3.15b/e + T3.5f, ver TEST RESULTS) ·
`tests/integration/paper/**` (55 + 4 xfail) ·
`services/market-worker/tests/test_spot_*` (46) + `test_fx.py` (9, T3.11a) + testes do `MARKET_ROLE`
dedicado (T3.0f) ·
`services/strategy-worker/tests/{test_activate_paper_line,test_replication_cohort,
test_replicate_strategy_version,test_replay_engine,test_replay_lookahead,test_replay_contract}.py` ·
`services/scanner-worker/tests/test_beta_job.py` (377 linhas, 8 casos com testcontainers) ·
`infra/scripts/tests/{test_request_backfill,test_obsidian_lint}.py` (4 + 26) ·
`apps/web` (687 testes, 79 arquivos — T3.22, ver TEST RESULTS; inclui Lab em dinheiro, portfolio,
System, `use-age-ticker`, `markets-quality-badge`).

## TEST RESULTS (saída real, citada por nota)

```
# T3.9 (2026-09-07, .claude/state/notes-T3.9a.md / notes-T3.9b.md)
packages/core/tests/integration: test_migrations 49 · test_schema_privileges 23 · test_schema_paper 96 ·
  test_schema_shadow 53 · test_schema_rls 17 · test_schema_constraints 13 · test_admission 17 ·
  test_admission_reservation 10 · test_portfolio_state 22
packages/core/tests/unit 706 passed · apps/api/tests/unit 382 passed
tests/integration/paper: 53 passed, 4 xfailed (398 s; 1 erro WinError 64 = flake do testcontainers
  no Windows, verde isolado)
  test_v4_concurrent_orders_and_duplicate_fills.py    → 3 passed in 35.82s
  test_v5_reconciliation.py                            → 3 passed in 113.26s
  test_v6_stale_data_reconnect_restart.py              → 3 passed in 34.81s
  test_v7_exchange_minimums.py                         → 9 passed, 1 xfailed in 1.32s
  test_v8_adverse_gap.py                               → 2 passed in 34.13s
  test_v9_no_fabricated_fill.py                        → 5 passed in 37.31s
  test_s10_crash_boundaries.py                         → 4 passed in 36.57s
  test_s11_concurrent_sessions.py                      → 5 passed, 1 xfailed in 54.59s
services/execution-worker/tests: 132 passed (17 arquivos) + T3.15b: 14+7+5+3+11
services/market-worker/tests: 364 passed (41 arquivos) + T3.0d/T3.0e (test_spot_band 14,
  test_spot_universe 17, test_market_ids 3)
services/strategy-worker/tests: unit 159 · test_activate_paper_line 6 · test_activation 12 ·
  test_version_roster 9 · test_shadow_decisions 15 · test_code_ref 23
packages/exchange-adapters: 385 passed, 7 skipped (live)
ruff check/format --check: limpos · pyright: 0 erros nos pacotes de produção ·
  check_file_size: 465 arquivos, 0 acima de 350 linhas

# T3.11a (2026-09-07, notes-T3.11a.md)
$ uv run pytest services/market-worker/tests/test_fx.py -q
9 passed in 53.89s
$ uv run pytest services/market-worker/tests/test_supervision.py services/market-worker/tests/test_readiness_naming.py -q
14 passed in 9.51s

# T3.5f (2026-09-08, notes-T3.5f.md) — a dívida de pyright do execution-worker foi paga
uv run pyright services/execution-worker  → ... (179 errors, 0 warnings, 0 informations)   # ANTES, HEAD 47cff11
uv run pyright services/execution-worker  → 0 errors, 0 warnings, 0 informations           # DEPOIS
uv run pyright  (repo inteiro)             → 0 errors, 0 warnings, 0 informations           # 902 arquivos

# T3.19c (2026-09-08, notes-T3.19c.md §5.1) — CONCERN, não corrigido por esta tarefa
services/strategy-worker/tests/test_shadow_decisions.py está VERMELHO no head (15 testes,
  "permission denied for table strategy_versions") — fixture chama isolate_catalogue() como
  hunter_worker, e a 0011 revogou UPDATE(status) desse papel. Dono declarado: T3.15d (adiada).

# T3.19d (2026-09-08, notes-T3.19d.md §5) — 0013_replay_runs
test_schema_seed_and_partitions.py 24 · test_schema_shadow.py + test_schema_constraints.py 66 ·
  test_replay_engine.py 13 · test_replay_lookahead.py 6 · test_replay_contract.py 32 ·
  packages/core unit 714 · services/strategy-worker unit 191 — tudo verde

# T3.20 (2026-09-08, notes-T3.20.md)
infra/scripts/tests/test_export_strategies_to_obsidian.py → 20 passed in 2.01s
ruff check → All checks passed! · pyright → 0 errors, 0 warnings, 0 informations

# T3.21 (2026-09-08, notes-T3.21.md)
uv run pytest infra/scripts/tests/test_obsidian_lint.py -q  → 26 passed in 0.97s
ruff check (4 arquivos) → All checks passed! · pyright (4 arquivos) → 0 errors, 0 warnings, 0 informations

# T3.22 (2026-09-08, notes-T3.22.md) — suíte web inteira, mais recente
Test Files  79 passed (79)
     Tests  687 passed (687)

# T3.7b (2026-09-08, notes-T3.7b.md)
services/scanner-worker/tests/test_beta_job.py: 8 testes com testcontainers, verdes
infra/scripts/tests/test_request_backfill.py: 4 testes puros, verdes
market_betas no stack local após backfill parcial: BTCUSDT definition β=1 válido;
  os outros 199 mercados "insufficient_history" — nenhum β validado ainda além do BTC por identidade
```

**Não rodada nesta consolidação:** uma execução única, datada, do repositório inteiro
(`pnpm test && uv run pytest` de ponta a ponta) depois de T3.19–T3.22. Os números acima são por
tarefa, cada um com a saída colada na própria nota; não há uma prova de que todos passam **juntos**
no mesmo commit. Isso é uma lacuna real do fechamento e fica em NEXT STEP.

## REAL DATA CONNECTED

- **Carteira `ever`** aberta na VPS com câmbio real do coletor USDTBRL (uma observação por minuto,
  idempotente, nunca fabricada) — 19.333,0111164813 USDT a 5,1725, 2026-09-07 04:27Z.
- **Execution-worker na VPS** desde 2026-09-07 ~16:26Z: `hb:execution:paper` com
  `equity 19333.0111164813`, `kill_switch ACTIVE`, MTM no minuto, `/ready` 200, `paper_autonomy false`.
- **Ingestão SPOT como serviço próprio na VPS** (T3.0f, `0451066`): 14 pares, 0 reconexões — fecha a
  ressalva medida em `t30-proof.md` (ligar spot no mesmo processo do perpétuo derrubava o socket
  perpétuo, 1011, 8 reconexões em 6 min). É a condição 4 de ativação da D10, **cumprida**.
- **β**: produtor horário rodando (T3.7b), mas **sem β validado na VPS ainda** — o backfill de 31
  dias foi pedido em 2026-09-08 ~04:35Z (90 janelas) e não há confirmação datada de conclusão nesta
  consolidação. Condição 5 da D10, **não cumprida**.
- **Prova do worker no stack local por 30 min** (`t35-proof.md`): pedido manual → fill → posição com
  proteções → stop → saída → PnL reconciliado, 0 exceções.
- **Motor de replay medido**, não estimado: 3,93 barras/s com 3 processos no Docker Desktop Windows
  (44,88 ms de round trip ao Postgres, 11,25 statements/barra); projeção de ~75 barras/s/processo na
  VPS **não verificada lá** — a própria nota (`notes-T3.19b.md` §8) recomenda rodar `--dry-run` e uma
  fatia de um dia na VPS antes de qualquer meta de throughput.
- **Catálogo de estratégias no Obsidian** gerado das linhas reais da VPS (T3.20): 19 páginas —
  `momentum` v1/v2/v3-paper, `volume_anomaly` v1/v2, seis rascunhos, duas páginas de família.

## MOCKS REMAINING

- O adaptador de execução é um **simulador** contra o livro spot real (é o produto do M3: paper).
  Nenhuma ordem vai à exchange; `LiveExecutionAdapter` recusa por construção.
- V6 itens 3/4 (queda de WS com gap, perda de Redis no caminho da carteira) esperam o spot ligado no
  hot state compartilhado — hoje o spot roda isolado num processo próprio, o que resolve o problema
  de saturação mas ainda não foi combinado com o cenário de queda de WS especificamente do spot.
- `avgPrice` do ticker spot não é coletado (recusa conservadora no simulador).
- **β validado**: existe só para BTCUSDT (`definition`, β=1 por identidade); os demais 199 mercados
  seguem `insufficient_history` enquanto o backfill de 31 dias não fechar 20 dias contíguos.
- **Recibo de replay (`0013`) não é durável em nenhum stack ainda** — nem local, nem VPS, aplicaram
  a migração; toda evidência de replay produzida até aqui vive só em `system_events` (expira em 30
  dias) e em JSONL solto no disco de quem rodou.
- **Linha `paper` do momentum é rascunho, não ativa** — `momentum v3` existe em `strategy_versions`
  com `purpose=paper` desde 2026-09-08 04:34Z, mas `activated_at IS NULL`; nenhum sinal `paper` foi
  emitido ainda.
- **Parte B da T3.25** (telas web de Strategies/Backtests/Trades/Risk Center) é só brief — a Parte A
  (API) também não foi executada nesta janela, apenas despachada (`d6b8b37`).

## BUGS (abertos, com dono)

- `obsidian/07-BUGS/Open Bugs.md`: entradas herdadas do M3 (pó sem assentamento — sweep;
  `SPOT_VOLUME_FLOOR_USDT` duplicado entre `market-worker` e `execution-worker`; flaky `WinError 64`
  em testcontainers no Windows; `agents` obrigatório para a ponte, sem linha nenhuma hoje) mais três
  novas do plantão de 2026-09-08 madrugada: `market_betas` vazia (dono T3.7b, **parcialmente
  fechado** — o produtor existe, falta o backfill terminar), candles com só 11 dias na VPS contra os
  20 contíguos que o β exige (mesmo dono), e 160 erros de pyright pré-existentes em
  `services/execution-worker/tests/test_restart_recovery.py` (**fechados** pela T3.5f, `19983dd` —
  179 → 0 erros no repo inteiro, mas o item específico deste arquivo não foi conferido isoladamente
  nesta consolidação).
- **HIGH, latente, ainda aberto — o teto do pico do kill switch conta snapshot de qualquer
  resolução.** `infra/migrations/ddl/paper.py` (função que calcula `_OBSERVED_EQUITY`) faz
  `max(equity)` sobre `portfolio_equity_snapshots` **sem filtrar `resolution`**; hoje só existe a
  resolução de 1 min, então o defeito é latente. No dia em que existir um roll-up de 1 h/1 d
  guardando o máximo do período, o pico sobe artificialmente, o drawdown contra ele estoura o
  limite de 8 % e a carteira fica `TRADING_DISABLED` **permanentemente**, sem perda real — e o pico
  é monotônico, não desce sozinho. Correção declarada e não aplicada: `resolution = '1m'` no
  subselect. Dono reconfirmado: T3.0d/T3.1f (nenhuma das migrações `0007`–`0013` tocou nisso).
- **Novo, achado pela T3.19c** — `services/strategy-worker/tests/test_shadow_decisions.py` está
  vermelho no HEAD (15 casos, "permission denied for table strategy_versions"): a `0011`
  (T3.15c) revogou `UPDATE(status)` de `hunter_worker`, e o fixture `isolate_catalogue()` desse
  arquivo ainda escreve como esse papel. Correção de uma linha (rodar na conexão de dono), dono
  declarado T3.15d (que também deve fechar o item de segurança HIGH abaixo) e ainda não despachada.
- **HIGH, operacional, VPS — o backup diário do Postgres nunca rodou.** `/opt/backups/backup.log`
  só tem `Permission denied` desde a instalação: `infra/vps/backup_postgres.sh` está rastreado
  `100644` (sem bit de execução) e o cron o invoca direto em vez de com `bash <script>`
  (`infra/scripts/bootstrap_vps.sh:346`). Não há um único dump da VPS. Consequência: `signal_outcomes`
  e todo o histórico do Shadow Lab são **irrecuperáveis** se o volume do Postgres se perder — é o
  único dado do projeto que não se refaz coletando de novo. Dono: `devops-engineer`; bloqueado até
  agora por permissão de sessão (precisa de `sudo`/execução na VPS).
- 11 itens abertos da revisão da 0006, todos com dono declarado e a maioria fechada (ver SECURITY
  ISSUES).

## SECURITY ISSUES

**Fechados:**
- Os dois bloqueantes da revisão da `0006` (`c35fd40` → `296f3c1`): transição do kill switch forjável
  e segunda carteira principal via workspace novo.
- Cinco "deve corrigir" da mesma revisão: `organizations.kill_switch_state` sem transição própria,
  FK simples em vez de composta em `orders`/`trades`, `UPDATE` do `hunter_app` em
  `portfolio_risk_state`, âncora aceitando par de câmbio errado, duas fontes divergentes do
  `paper_v1` — todos em `d6dbbca` → `c1f8c5f`/`cdb4bb5`.
- `find_pending` com `FOR UPDATE` sob `hunter_app` (`90f1862`).
- Da revisão de segurança da T3.15 (`.claude/state/review-T3.15-security.md`, 2026-09-08): item 2
  (a ponte lia `purpose` do envelope, não da coluna) **fechado** em T3.15c
  (`bridge_repo.py`/`bridge_screen.py` passam a comparar coluna × envelope e recusam
  `purpose_mismatch`); item 3 (`obsidian-mcp@2` sem pin) **fechado** em `docs/HERMES.md` (correção
  de 2026-09-08, fixado em `2.0.1`).

**Abertos:**
- **HIGH, não fechado — `infra/vps/docker-compose.prod.yml:30-33`.** `DATABASE_URL_MIGRATIONS` (a
  conexão de **dono** do schema) está no ambiente de `api` e de **todos** os workers, o que anula o
  estreitamento de grant que a `0010`/`0011` acabaram de fazer: qualquer execução de código na `api`
  (RCE de dependência, vazamento de env) pode conectar como dono e ativar uma versão `paper` ou
  desligar RLS de qualquer tabela. Correção declarada e não aplicada: separar `x-prod-db-env`
  (runtime) de um `x-prod-owner-env` usado só pelo serviço `migrate` e pelo shell do operador —
  registrada em `.claude/state/brief-T3.15d-owner-dsn.md`, **ainda não despachada**.
- **MEDIUM, aberto — `strategy_versions` em rascunho pode ser ativada/apagada pelo worker.**
  A trigger de congelamento da `0010` só dispara em linhas já `activated_at IS NOT NULL`; uma linha
  `draft` (como `momentum v3` está hoje) ainda pode ser `UPDATE`/`DELETE` pelo papel `hunter_worker`
  sem passar pelo script auditado. Correção proposta: mover `status`/`activated_at` para a lista
  revogada numa `0011b`/nova migração.
- **MEDIUM, aberto — `activate_strategy_version.py` só audita `Refused`.** Qualquer outra exceção
  (erro de banco, conexão) sobe sem deixar `system_events`, contrariando o que a §22.4 do
  `DATABASE.md` promete. Correção: `except Exception` com rollback explícito e evento em transação
  própria.
- Itens LOW da mesma revisão (changelog sem limite de tamanho, clone do `.env` do Hermes, mapa de
  infra versionado, rotina do Hermes lendo conteúdo externo sem marcá-lo como dado) — sem prazo,
  registrados em `.claude/state/review-T3.15-security.md`.
- Pendências abertas do lado da `0006`: `released` maior que a reserva aceito sem trigger,
  `evidence='{}'` aceito em transição automática, teardown apagando `kill_switch_transitions` em
  cascata, ciclo de reserva reabrível — sugestões, não bloqueantes, sem dono ainda.

## OBSIDIAN UPDATED

Plantão de 2026-09-07 (`4806649`, `fa09b5d`): changelog, bugs, diário,
`03-TRADING/{Risk Engine,Portfolio}.md`, `00-HOME.md`, D10–D13. Plantão de 2026-09-08 madrugada
(`3c55add`): `EXP-0005-momentum-paper.md` aberto (Hipótese e Protocolo congelados,
`result: nao-iniciado`), changelog dos 12 commits da tarde/manhã, `04-AGENTS/Sexta-feira no
Hermes.md`, três bugs novos. Depois disso (T3.20/T3.21, `f7c76b8`/`ee2b472`): catálogo de 19 páginas
de estratégia gerado do banco (`03-TRADING/Estrategias/**`), duas Bases (`Experimentos.base`,
`Estratégias.base`), dois canvases (fluxo sinal→carteira, família momentum), linter próprio (26
testes) e o snippet de CSS dos quatro callouts documentado em `docs/OBSIDIAN.md`.

**Pendente:** entradas de changelog e diário para os commits de 2026-09-08 depois do plantão da
madrugada (T3.0e review, T3.0f, T3.7b, T3.11a docs, T3.15c–e, replicação T3.19/T3.19b–d, Lab em
dinheiro T3.16–T3.18, horário de Brasília T3.22, auditoria de design T3.23, brief T3.25) — nenhum
plantão cobriu esse intervalo ainda; esta consolidação (T3.10) é texto normativo em `docs/`, não
substitui o diário da Sexta-feira em `obsidian/`.

## NEXT STEP

1. **T3.15d** (owner DSN separado nos dois composes + fixture de `test_shadow_decisions.py` na
   conexão de dono) — fecha o HIGH de segurança aberto e o vermelho conhecido antes de qualquer
   ativação.
2. **Aplicar `0012`/`0013` no stack local e na VPS** (`alembic upgrade head`) — hoje nenhum ambiente
   tem o recibo durável de replay nem a coorte de replicação de verdade.
3. **Backfill de β na VPS até fechar 20 dias contíguos** e medir `beta_valid` real — condição 5 da
   D10.
4. **Uma execução única e datada de tudo** (`pnpm lint && uv run ruff check . && ... && pnpm test &&
   uv run pytest`) no mesmo commit, depois dos itens 1–3 — hoje os números do TEST RESULTS são por
   tarefa, não uma prova conjunta.
5. **Parte A da T3.25** (endpoints de Strategies/Backtests/Trades/Risk Center) e, depois, a Parte B
   (telas), condicionadas aos specs do product-designer (T3.24, que sai da auditoria T3.23).
6. Decisão do Everton: `activate_strategy_version.py momentum v3 --changelog ...` (ativação
   auditada) quando as sete condições da D10 estiverem provadas — hoje faltam β válido e a segunda
   opinião da Astra (volta em 2026-09-12).
7. M2: condições 2 (leitura datada 2026-09-09/10), 3 (p99 pós T2.5h, universo redefinido pela D11) e
   4 (e2e com `CLERK_E2E_*`) continuam abertas, em caminho disjunto do M3.

## NEXT MILESTONE

M4 — agentes e a ponte sinal → proposta em modo autônomo paper (só depois de D10 cumprida), ordens
limit passivas com série de capacidade (T3.M, ainda não despachada), Agents page. Parte do que o
M4 previa (Risk Center com dado numérico, Trades reaproveitando a carteira) já está sendo antecipado
pela T3.25 porque o dado passou a existir antes do agente que o geraria de forma autônoma — ver
`docs/ROADMAP.md` M4/M5/M6 revisados. Dinheiro real continua na Fase 4, fora de qualquer plano atual.
